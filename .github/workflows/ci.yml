name: CI

on:
  push:
    branches: [main]
    paths:
      - "client/**"
      - "server/**"
      - "shared/**"
      - "scripts/map/**"
  pull_request:
    paths:
      - "client/**"
      - "server/**"
      - "shared/**"
      - "scripts/map/**"

jobs:
  client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: client/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit -p tsconfig.app.json
      - run: npm test

  server:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit -p tsconfig.json
      - run: npm test

  data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Валидация данных сценария 1946 (инварианты + калибровка)
        run: python scripts/map/validate_region_economy_1946.py
      - name: Structural-инварианты (unit-тест на синтетической фикстуре)
        run: python scripts/map/test_validate_region_economy_1946.py

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Secret scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha || '' }}
          head: HEAD
          extra_args: --only-verified

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: npm audit (client)
        working-directory: client
        run: npm audit --audit-level=high
        continue-on-error: true

      - name: npm audit (server)
        working-directory: server
        run: npm audit --audit-level=high
        continue-on-error: true
