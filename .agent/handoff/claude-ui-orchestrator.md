# Передача сессии — 2026-08-09

## Состояние

Дерево: `D:\Pax Historia LOCAL\.claude\worktrees\ui-orchestrator` |
Ветка: `claude/ui-orchestrator` | Последний коммит: `de16de3`
«docs(agent): Т-15 принято с оговорками; Т-16 понижена как латентная; Т-17»
`git status`: чисто.

**`main` за сессию ушёл с `92c1aa9` на `ea421ef`** — не меньше десяти чужих
влитий. Любое число, снятое на моей базе, к текущему `main` не относится, пока
не перемерено (об этом ниже, и это не формальность: ровно так я один раз соврал
в `main`).

**Стопка веток, не влитых в `main`, вливать в этом порядке:**

1. `claude/ui-localize-screen` (`c878c40`, 3 коммита от `e4179ca`)
2. `claude/ui-frame-fixes` (`7292710`, 3 коммита от `c878c40`)
3. `claude/ui-orchestrator` (`de16de3`) — последней, чтобы реестр отражал итог

`claude/ui-mode-vocabulary` уже влита (`ce74d66`), её дерево снято.

**Конфликт, о котором обязан знать интегратор:** `docs/IDEAS.md` дописан в конец
и этой веткой, и `ui-mode-vocabulary`. Разрешение содержательного выбора не
требует — **оставить оба блока**.

## Что сделано и чем доказано

Пять заданий приняты. Числа — мои прогоны, не из отчётов исполнителей.

- **Т-1 + Т-2** (`ui-mode-vocabulary`, влита): один словарь режимов карты,
  легенда из источника цвета, render-тест игрового экрана.
  Доказано: `tsc` 0, `npm test` 91→93 в 4 файлах, `lint` 0, `build` 0
  (cwd `client/`). Негативный контроль воспроизведён `report-auditor`:
  сломанный идентификатор даёт `tsc` exit 2 с TS2353 на исчерпывающем
  `Record<MapMode, …>` и TS2366 на `switch` легенды — **инвариант держит
  компилятор, а не внимательность**.
- **Т-1б** (там же): контраст образцов легенды. Пересчитал сам формулу WCAG —
  `OK_DIM` 1.78:1, контур `--text-faint` 5.30:1; требование ≥3:1 закрыто
  внешним контуром, а не заливкой (заливка несёт данные). Ряд из семи кнопок в
  один ряд, `aria-pressed`, `blocs` → `relations`.
- **Т-3** (`ui-localize-screen`): игровой слой говорит из словаря.
  `tsc` 0, `npm test` **102 в 6 файлах**, `lint` 0, `build` 0, public eval
  240/0. Все четыре негативных контроля воспроизведены аудитором.
- **Т-15** (`ui-frame-fixes`): ШАПКА вернулась на экран, попап старого слоя
  удалён, панель РЕЖИМОВ не дышит. `tsc` 0, `npm test` **112 в 7 файлах**,
  `lint` 0, `build` 0. `App.css` **1648 → 454** строки; аудитор проверил все
  101 снятое имя класса в обе стороны.
- **Триаж `docs/TODO.md`**: кумулятивная дельта ветки **−16 строк**
  (проверять `git diff --numstat 92c1aa9 -- docs/TODO.md`).
- **Два документа сняты с нормативности** (после хендоффа, по запросу `lead`,
  который нашёл их своей проверкой «план не ссылается на ветку» и отказался
  формулировать заявление о чужой области — правильно отказался):
  `docs/plans/12_UI_REDESIGN.md` → SUPERSEDED, `docs/design/`
  `interface-from-scratch/README.md` → ИСТОРИЯ. Оба объявляли UI-трек
  замороженным и называли рабочими ветки, из которых одна удалена.
  **Поправка к постановке `lead`:** «обе ссылки на ветки дохлые» неверно —
  `codex/interface-from-scratch` действительно удалена, а
  `codex/1946-country-borders` жива (есть локально и на `origin`, проверено
  `git branch -a`). Устарели утверждения, не только адреса.
  Устав роли (`.agent/roles/ui.md`) дополнен четырьмя ловушками сессии.

## Что открыто (в порядке приоритета)

Полные критерии приёмки — в `.agent/orchestration/ui.md`, они записаны ДО
выдачи и переписывать их нельзя.

1. **Т-14 Незарегистрированный namespace никем не ловится.** Снятие строки
   `adapter: ruAdapter` из `client/src/i18n/index.ts` оставляет `tsc` и 112
   тестов зелёными, а игрок видит сырые идентификаторы вместо 36 подписей.
   Дыра доказана прогоном аудитора. Первая, потому что ветка Т-3 добавила три
   namespace и целиком опирается на это звено.
2. **Т-17 Хвосты Т-15.** `--frame-h` (выравнивание ШАПКИ и ХОДА) не держит ни
   один тест — доказано: снятие `ref={shapkaRef}` в `client/src/game/Screen.tsx`
   проходит 112/112. Плюс убрать числа-без-окна из `docs/DECISIONS.md` и
   `UI_DESIGN.md` §11, плюс расширить запрет инлайнового `position` на все
   панели рамы.
3. **Т-11 Подсветка игрока прибита к «SUN».** `client/src/game/panels.tsx:660`
   — `country.id === "SUN"` вместо `model.playerId`. За любую другую державу
   интерфейс врёт о том, кто игрок. Сюда же `№` вместо `#` в EN
   (`adapter.ts:449`, `panels.tsx:493`) — тест на литералы этого не ловит,
   U+2116 не кириллица.
4. **Флаг в ШАПКЕ прибит к `<FlagSU />`** (`client/src/game/Screen.tsx`) —
   играя за США, увидишь советский флаг. Дёшево, видно сразу.
5. **Т-13 Вкладка «Блоки» РЕЕСТРА показывает выдумку.** `panels.tsx:737` —
   две захардкоженные строки, придуманные лидеры, числа участников 1 и 3.
   Нарушает `UI_DESIGN.md` §2. Т-3 непреднамеренно перевёл заглушку на
   английский. Корень общий со снятыми «Армиями»: сущности «блок» в модели нет.
6. **Т-16 `RegionDetail` упадёт на регионе без `demographics`** —
   `panels.tsx:471`, `region.groups[0].name` без проверки. **ЛАТЕНТНЫЙ:** в
   сценарии 1946 все 1389 регионов размечены, из стартового состояния не
   достигается.
7. **Т-9 Порог недовольства в трёх копиях:** карта `stability < 0.6`
   (`mapModeColors.ts:88`), панель `> 0.45` (`panels.tsx:274`), список очагов
   `> 0.3` (`panels.tsx:266`). Регион с 0.35 попадает в проблемные и остаётся
   зелёным на карте.
8. **Т-10 У карты нет состояний загрузки и ошибки** — `MapView.tsx:503` на
   провал только `console.error`. Пустая карта от заглушки неотличима.
9. **Т-12** расширить защиту от литералов на `client/src/ui/**`;
   **Т-8** глифы и палитра (три пары глифов сливаются при 28px; `IconEconomy`
   служит и томом, и режимом; три режима красятся одной зелёной тройкой);
   **Т-4** ветки `LlmCycleOutcome` и склонение 11 шаблонов; **Т-5** правило
   ПРИБОРОВ; **Т-6** приёмочный проход; **Т-7** accessibility.

## Роль

**Мандат на старте:** оркестратор интерфейса и арт-директор, область
`client/**`. Устав — `.agent/roles/ui.md`, цикл — скилл `orchestrate`, реестр —
`.agent/orchestration/ui.md`.

**Запрещено было:** править компоненты руками (держит
`scripts/hooks/role-guard.mjs`: правки вне `.agent/**` и `docs/**` блокируются);
мержить в `main` — сдача в очередь `integrator`; трогать геометрию карты
(`geometry`), правила механики (`logic`), содержание исторических текстов
(`world`); писать в `docs/TODO.md`; перекладывать вкусовые решения на
пользователя.

**Фактически к концу мандат РАСШИРЕН дважды, оба раза законно:**

1. **Гейт на `docs/TODO.md` снят ролью `lead`** в этой сессии: параллельный
   триаж разрешён **по карте разделов**, адресация — по ТЕКСТУ ЗАГОЛОВКА, а не
   по номеру строки (номера сдвигаются каждым влитием). Мои разделы:
   `## UI-редизайн`, `## Интерфейс — контракты стыка закрыты 2026-08-08`,
   `## Интерфейс`. Правила: чужие разделы и шапку не трогать; порог 1400 не
   поднимать; **итоговая дельта ветки ≤ 0**. Плюс уточнение `lead`: снять
   ЗАКРЫТЫЙ пункт — факт, проверяемый прогоном, законен для любого домена;
   перенести ОТКРЫТЫЙ в `IDEAS`, привязать к милстоуну или удалить как
   наблюдение — решение его домена. **Рассуждение доказательством закрытия не
   является.**
2. **Задание про карту** пришло от пользователя через `lead` (проверить, что
   карта работает). Закрыто: дефекта нет, пользователь смотрел песочницу.

**Новая сессия должна получить промт с этими двумя поправками, а не исходный.**

**Состояние роли (оркестратор), чего нет в git:**

- Очередь, критерии и вердикты — `.agent/orchestration/ui.md`. Строка
  `Статус:` (`выдано | на аудите | принято | возвращено`) читается хуком, её
  формат менять нельзя.
- Деревья исполнителей `ui-localize-screen` и `ui-frame-fixes` **живы и не
  влиты** — сносить только через `scripts/worktree-drop.ps1` после влития.
- **Субагент `ui-reviewer` НЕ поднимается** (`Agent type not found`), хотя
  `.claude/agents/ui-reviewer.md` существует и валиден, а `report-auditor` из
  того же каталога доступен. Гипотеза (не факт): в его `tools:` есть
  `WebSearch`, а он deferred. Обход: вкладывать устав дословно в
  `general-purpose`. Устав требует его вердикт при приёмке — механизм сломан,
  починка не в домене `ui`.
- Единственный стык — `shared/src/types`: очередь ведёт `logic`. **Интерфейс
  потребности не заявляет** — проверено, склонение имён решается русскими
  шаблонами без падежных форм.
- Активный чужой ExecPlan: `intercountry-ai.md`. По интерфейсу активных нет.

**Правила, действующие независимо от роли:** `AGENTS.md`, `client/AGENTS.md`,
`docs/UI_DESIGN.md`, `docs/LOCALIZATION.md`.

## Числа, на которые нельзя ссылаться без перезамера

- **Все проверки** — перемерять в дереве задачи, cwd `client/`:
  `npx tsc --noEmit -p tsconfig.app.json`, `npm test`, `npm run lint`,
  `npm run build`; из корня `python .agent/evals/public/run_public_evals.py`.
  Baseline `ui-frame-fixes`: tsc 0, **112 тестов в 7 файлах**, lint 0, build 0.
- **Размеры `docs/TODO.md` и `DECISIONS.md`** — порог считает
  `.agent/evals/public/run_public_evals.py:107` (`count("\n") + 1`, лимиты 1400
  и 2000). `wc -l` даёт на единицу меньше. `Measure-Object -Line` **пропускает
  пустые строки** — им не мерить.
- **Все пиксельные числа «после починки» Т-15** (четыре угла на 1280 и 1920,
  99 px во всех семи режимах) — на ОДНОМ источнике, исполнителе. Аудитор их не
  переснимал, я мерил до починки. Формально `static-only`.
- **41,7 МБ / 1591 фича / 942 мс** на `/world_1946.geojson` — мой замер из
  страницы на основном checkout, до десятка чужих влитий.

## Грабли этой сессии

- **Проверка на своей базе не есть проверка на `main`.** Я записал раздел про
  бюджет двумя формулами как ОТКРЫТЫЙ, проверив код на `92c1aa9`, — а
  `claude/budget-one-base` уже завела `budgetBase.ts` и свела обе формулы. Моя
  запись попала в `main` и утверждала неправду. Перед сдачей триажа факты
  переснимать против свежего `main`.
- **`preview_start` поднимает dev-сервер из ОСНОВНОГО checkout, а не из дерева
  ветки.** Проверять так: `fetch('/src/<файл>')` и смотреть, чей код приехал.
  Иначе меряешь чужую версию и «подтверждаешь» починку, которой в дереве нет.
- **Замер переполнения ВНУТРИ панели не отвечает на вопрос, на экране ли
  панель.** Так ШАПКА, уехавшая под нижний край, прошла две приёмки: мерили
  `scrollWidth`, честно докладывали «переполнения нет». Критерий «четыре угла»
  у меня был записан — в Т-6, которое я не выдавал.
- **Правильный вопрос дешевле правильной догадки.** На «карта — заглушка» я
  потратил прогон, замер 942 мс и попытку поднять второй браузер, прежде чем
  спросить пользователя, ЧТО он видит. Ответ («смотрел песочницу») различал
  варианты, а мои замеры — нет. Слово «заглушка» было ТЕРМИНОМ (так в проекте
  зовут гекс-сетку), а я читал его как метафору.
- **«Тип допускает» не равно «данные содержат».** Цепочку типов до краша ПОЛОСЫ
  я принял за активный дефект; аудитор проверил данные — 1389/1389 размечены,
  дефект латентный.
- **Утверждение сильнее гарантии — пять случаев за сессию:** `localeKeys.test.ts`
  считался защитой от строк в JSX (не был); урок про ключевание записан в док
  без теста; «семь ошибок `tsc` в шести файлах» не воспроизводилось; §10
  перечисляет защиты как полный список, а регистрация namespace вне него;
  число замера без названного окна. Evidence повторяемая, передана `lead` как
  кандидат в `.agent/EVOLUTION.md`.
- **Browser pane страницу НЕ отображает** (`document.hidden = true`,
  `innerWidth/innerHeight` бывают 0, скриншот не снимается, MapLibre не рисует
  кадр). Настоящий Chrome не подключён. Но **DOM и `getComputedStyle`
  работают** — все три дефекта Т-15 найдены именно так. Не тратить время на
  скриншоты; помечать `static-only`.
- **Песочница трижды скрыла поломку игры**: иконки режимов, легенда, ШАПКА.
  Причина одна — два источника модели и разные слоты карты. Проверять игровой
  путь, а не песочницу.
