# Передача сессии — 2026-08-09

## Состояние

Дерево: `D:\Pax Historia LOCAL\.claude\worktrees\logic-orchestrator`
Ветка: `claude/logic-orchestrator` | Последний коммит: `f8034ca` docs(agent):
матрица закрытия прохода — оценка домена упирается в отсутствие читателя
`git status`: чисто.

**Ветка отстаёт от `main` на 72 коммита** — за сессию интегратор влил очень
много. Ветка несёт ТОЛЬКО `.agent/orchestration/logic.md`, `docs/IDEAS.md` и
`docs/DECISIONS.md`; пересечение с `main` по коду — ноль. Обновление базы
механическое, конфликт возможен только в `DECISIONS.md` (дописка в начало) и
разрешается сохранением всех записей.

## Что сделано и чем доказано

Числа — мои прогоны в деревьях исполнителей, не из их отчётов. **У каждого
названа база**, потому что на разности с неизмеренной базой эта сессия
спотыкалась.

- **T-1 единая база бюджетной росписи** — влито. `budgetBase.ts` стал
  единственным местом формулы; три пути записи (`EconomyTick`,
  `CountryService`, команды ИИ) переведены. Проверка: 1450 passed / 1 skipped
  от базы `92c1aa9` (1445/1), tsc 0, evals 240/240.
- **T-2 `damageExtraction` снят** — влито. `rg` вхождений ноль; 1442/1 от той
  же базы (минус три удалённых теста).
- **T-5 мёртвая `calculateDiplomaticTension` + попарный порог союзника** —
  влито. 1444/1 от `92c1aa9`. Порог взят из `allianceThreshold` через экспорт
  из `DiplomacyTick`, второго определения формулы не заведено.
- **T-6 кап длины записи об отказе** — влито. 1450/1 от `92c1aa9`.
  **Задание исходило из неверной предпосылки**: путь поля в причине отказа
  лежит с `76bf6c0` и доезжает до модели дословно (`rejections.ts:534`), то
  есть `TODO.md:119` неверен. Взамен закрыт `MAX_REJECTION_FACT_LENGTH = 512`.
- **T-8 `aiTraits` получили потребителя** — влито. 1453/1 от базы `ce74d66`
  (1447/1) — **не от 1445**, это моя исправленная ошибка. Характер сдвигает
  пороги Правил A/B/C, нейтральный даёт ровно 1, поэтому старые тесты не
  потребовали перекалибровки.
- **T-3 канал «предложение игроку»** — работы нет: отложен пользователем после
  v1, дизайн целиком сохранён в `docs/IDEAS.md` (2026-08-09).
- **Граница домена расширена**: `server/src/llm/**` — область `logic` с
  2026-08-09 (решение `lead`, устав правится веткой `claude/logic-charter`).

## Что открыто (в порядке приоритета)

1. **T-9 — замер 24×2 для `claude/prompt-prefix-cache`.** Условие приёмки
   T-7; ветка стоит «на аудите». Исполнитель — та же сессия, работает. Пока
   числа не сойдутся, ветку в очередь интеграции НЕ ставить. Критерии —
   `.agent/orchestration/logic.md`, T-9.
2. **T-10 — деление ветки T-7.** Идёт: `claude/gemini-schema-fix`, два коммита
   из трёх, **docs-коммита ещё нет**. Без него влитие воссоздаёт висячие
   ссылки на `docs/LLM_RULES.md` — та самая причина первого возврата.
3. **T-4 — триаж `docs/TODO.md`.** Ветка и дерево УДАЛЕНЫ (были пустыми,
   снял интегратор при уборке) — завести заново
   (`scripts/worktree-new.ps1 todo-verdicts-logic`). Задание с картой разделов,
   правилами гейта и правкой «искать по заголовку, а не по номеру строки» —
   в реестре. Номера строк в карте снова устарели: `main` ушёл далеко.
4. **Матрица закрытия прохода.** Форму пользователь выбрал (HOI4:
   отношение × вид движения). Оценка домена: **читателя у неё нет** — в
   `shared/src/types` ни одного водного узла, морского pathfinding нет,
   прохода как сущности не существует. Рекомендация: вместе с Милстоуном 4.
   Сама форма матрицы не спроектирована — сознательно, работа требует свежего
   контекста.

## Роль

**Мандат на старте:** оркестратор игровой логики (`!роль logic`, устав
`.agent/roles/logic.md`). Изучает, проектирует, выдаёт задания, принимает после
аудита.

**Запрещено было:** править код (держится хуком `role-guard.mjs`); мержить в
`main`; менять продуктовую семантику без решения пользователя; писать в
`docs/TODO.md` (гейт `lead` — правит одна ветка).

**Фактически к концу:** мандат РАСШИРЕН законно — `server/src/llm/**` добавлен
в область решением `lead` 2026-08-09 (сессия проверила основание сама: слой не
входил ни в один устав, и промтовая задача уже правила `shared/src/types`).
Вместе с областью принято правило приёмки: **промтовые изменения доказываются
статистикой на живой модели, а не `npm test`** — стандарт домена 72 хода × 3
прогона. Новая сессия должна получить промт с этим расширением, исходный
стартовый промт его не содержит.

**Состояние роли (оркестратор):**
- очередь интеграции ПУСТА: `schema-rejection-field`, `diplomacy-debts`,
  `ai-traits-consumer` влиты; ждут только две ветки в работе (см. открытое);
- очередь `shared/src/types` пуста — ни одно живое задание их не трогает;
- пересечения: домен `ui` может пересечься только через `shared/src/types`;
  с `geometry` и `world` по файлам пересечений нет;
- координация с `lead` идёт сообщениями между сессиями; за день домен трижды
  поймал организатора на методе — это рабочая часть роли, а не конфликт.

**Правила, действующие независимо от роли:** `AGENTS.md`, устав
`.agent/roles/logic.md`, цикл — скилл `orchestrate`.

## Числа, на которые нельзя ссылаться без перезамера

- **Любой счёт тестов из этого пакета** — снят от баз `92c1aa9`/`ce74d66`, а
  `main` ушёл на 72 коммита вперёд. Мерить заново: `npx vitest run` в `server/`.
- **`docs/TODO.md` = 1284 строки** — число `lead` от середины дня, до
  последних влитий. Мерить: `(git show main:docs/TODO.md).Count` в PowerShell
  — **не** `Measure-Object -Line`, он молча не считает пустые строки.
- **Расход токенов из `logs/token-usage.jsonl` до 2026-08-09** — завышен на
  части вызовов: посредник до `v7.2.125` записывал токены размышления во вход.
  Перцентили по старым записям бюджет промта не описывают.
- **«+3,0 % промта» у датированных событий** (`DECISIONS`) — снято сломанным
  прибором. Направление, вероятно, держится (разность одинаково искажённых
  чисел), но само число непригодно.
- **Номера строк разделов `docs/TODO.md`** в задании T-4 — устарели дважды за
  день. Искать по тексту заголовка.

## Грабли этой сессии

- **Разность верна, только если сняты ОБА числа.** Я написал «база 1445 плюс
  восемь тестов» при базе 1447 и шести новых: измеренное число было верным,
  ошибкой была неизмеренная база.
- **Отсутствие текста в файле — не отсутствие свойства.** `exclusiveMinimum`
  не ищется в `responseSchemas.ts`, потому что приезжает туда композицией из
  `primitiveSchemas.ts:68`. На этом ошибся `lead`, а на зеркальной версии —
  я, приняв `--stat` за проверку содержания.
- **Адрес без базы — такой же не-факт, как число.** Одна и та же конвертация:
  строка 200 в `main`, 235 в ветке. Правы были оба, мерили в разных деревьях.
- **Исправность прибора — часть замера.** Посредник врал про расход; поймано
  тем, что три побайтово одинаковых запроса дали три разных числа. Самая
  дешёвая проверка прибора — повторяемость входа, квоты не требует.
- **Формулировку пункта TODO я принял за проверенный факт** и понёс дальше
  как «подтверждено открытым» (`TODO.md:119`). Правило про чужие числа не
  применил к чужому тексту.
- **Таблицу состояния собрал из замера начала хода и не перепроверил** —
  к моменту публикации две строки устарели. Состояние без отметки времени
  живёт меньше, чем кажется.
