# Нарезка океанских монолитов на шельфовые зоны

Status: active
Owner: claude (worktree `.claude/worktrees/sea-shelf-zones`, ветка `claude/sea-shelf-zones`)
Starting commit: 5fe941147289891b93a5792b24f3ce2610e5e1e6

## Objective and observable outcome

Водный слой `out/seas_iho_coastline.geojson` — 101 зона, из которых шесть
монолитов держат 81,4% воды. Зона такого размера непригодна как игровая
клетка: в «Южной части Тихого океана» 47 прибрежных регионов 25 стран, и любое
морское действие в ней бессмысленно по масштабу.

Наблюдаемый результат: новый слой `out/seas_iho_shelves.geojson`, где из пяти
монолитов (Southern Ocean исключён) вырезаны именованные шельфовые зоны, а
абиссальные ядра остались целыми; каждая зона несёт `type` и `ice`; граф зон
связен, узкие места помечены атрибутом ребра; сумма `area_km2` сохранена
побайтно-проверяемым числом.

## Scope and constraints

### Что меняю

| Файл | Что |
|---|---|
| `scripts/map/build/build_neighbor_graph.py` | особый случай ±180 |
| `scripts/map/build/sea_graph.py` | новый: построение графа зон (используется двумя скриптами) |
| `scripts/map/build/diagnose_sea_graph.py` | новый: постоянная диагностика |
| `scripts/map/build/split_ocean_shelves.py` | новый: нарезка |
| `scripts/map/config/shelf_zones.json` | новый: курируемая таблица имён и якорей |
| `scripts/map/config/sea_chokepoints.json` | новый: узкие места как рёбра |
| `scripts/map/test_*.py` | новые тесты |
| `scripts/map/out/seas_iho_shelves.geojson`, `out/sea_graph.json` | новые выходы (вне git) |
| `docs/DECISIONS.md` | датированная запись по итогу |

### Что не трогаю

`out/seas_1946.geojson` · `client/public/world_1946.geojson` ·
`master/world_1946.master.geojson` · идентификаторы `SEA-` ·
`out/ownership_1946.json` · `out/names_ru.json` · `MASTER_REBUILD_STEPS` ·
озёра (только чтение) · `build_seas_from_iho.py` (его докстрока прямо
запрещает нарезку внутри) · внедрение слоя в мир · `server/` · `client/`.

Каскада `region_id` нет: состав регионов и стран не меняется, позиционные
файлы не затрагиваются.

### Пересечение с параллельными задачами

Ветка заведена от `claude/1946-map-coastline-fixes` (5fe9411), а не от `main`:
на `main` нет `build_seas_from_iho.py`, `master/world_1946.master.geojson` и
половины пайплайна — работать было бы не над чем. Ветка-источник на момент
старта чистая (`git status` пуст). Её я не мержу, не ребейжу и не удаляю.

Риск пересечения: если сессия `map-coastline-fixes` продолжит править
`build_seas_from_iho.py` или сам слой, мой вход разойдётся с её выходом.
Смягчение: свой скрипт читает выход как данные и в него не лезет; конфликт
будет в одном файле — входном geojson, который вне git и пересобирается.

## Assumptions and unknowns

- **Батиметрии в проекте нет.** Настоящая граница шельфа — изобата 200 м
  (GEBCO/ETOPO). Внешний датасет тянуть не буду: provenance, лицензия и вес
  против одной задачи не окупаются (правило внешних данных в `AGENTS.md`).
  Замена — курируемая таблица именованных зон с якорями и собственным вылетом
  от берега. Это осознанное упрощение, оно названо в отчёте.
- Площадь: своя сферическая формула на кольце вокруг полюса возвращает
  дополнение (Арктика считается как 505 млн км² вместо 5,2). Из пяти
  разрезаемых монолитов ни один не окружает полюс, но перед делением проверю
  на каждом, что `Σ geod(части) ≈ area_km2` источника; остаток разнесу
  пропорционально, чтобы сумма сохранилась точно.
- UNKNOWN до замера: сколько зон получится и сколько якорей нужно, чтобы
  порог «не больше ~25 прибрежных регионов» выполнялся везде.

## Alternatives and selected decision

| Вариант нарезки | Почему не он |
|---|---|
| Изобата 200 м из внешней батиметрии | внешние данные без провенанса и лицензии; вес против одной задачи |
| Voronoi по всему монолиту (включая абиссаль) | режет океанское ядро — прямо против правила «резать берег, а не океан» |
| Постоянный буфер от берега без якорей | куски получаются безымянными, а безымянный кусок — признак неверного разреза |

**Выбрано:** курируемая таблица зон (имя + якорь + вылет от берега + `why`),
прибрежная полоса `монолит ∩ буфер(суша, вылет_зоны)`, распределение полосы по
ближайшему якорю (диаграмма Вороного, расстояние учитывает ±180). Абиссальное
ядро = монолит − все свои шельфы, имя океана остаётся за ядром — выдуманных
имён не появляется вовсе.

## Progress

- [x] 1. `build_neighbor_graph.py`: особый случай ±180 + негативный контроль
      + синтетический тест. **На водном слое 0 новых рёбер, как и ожидалось;
      на мировом слое — 1**, см. Discoveries.
- [x] 2. `sea_graph.py` + `diagnose_sea_graph.py`: базовая линия ДО разреза,
      перемеренная своим кодом. Все восемь чисел задания воспроизведены.
- [x] 3. Коммит + промежуточный отчёт (контрольная точка)
- [ ] 4. `shelf_zones.json` + `split_ocean_shelves.py`: первый монолит
      (North Atlantic — 112 прибрежных регионов, худший)
- [ ] 5. Коммит + промежуточный отчёт (контрольная точка)
- [ ] 6. Остальные четыре монолита
- [ ] 7. Атрибуты `type` / `ice` на все зоны
- [ ] 8. `sea_chokepoints.json` + пометка узких мест; проверка, что помеченное
      ребро действительно мост
- [ ] 9. Полный прогон проверок, отчёт, запись в `docs/DECISIONS.md`

## Discoveries

**2026-08-02, шов ±180 на МИРОВОМ слое: ровно одно настоящее ребро, которого
не было.** Задание ожидало 0 новых рёбер — и это верно для водного слоя
(101 зона, `diagnose_sea_graph.py` печатает `из них только через ±180: 0`).
Но `build_neighbor_graph.py` работает не над ним, а над
`out/world_1946.geojson` (1451 суша + 113 старых `SEA-` + 13 озёр), и там
находится пара:

    SEA-0061 East Siberian Sea              [138.96 .. 180.00]
    SEA-0112 Arctic — American-Pacific Sector [-180.00 .. 0.00]

Они сходятся НА линии дат севернее Врангеля и соседями не считались. Ребро
настоящее, не артефакт сдвига: `edge_count` 4435 → 4436, диффом ровно две
строки в `out/neighbor_graph.json`.

Влияния на `server/data` нет: `import_to_game.py` берёт в соседи только
land-регионы, а обе фичи — вода. То есть дефект жил в графе, но до сценария
не доходил.

**2026-08-02, зон свыше 25 прибрежных регионов — девять, а монолитов пять.**
Кроме разрезаемых, порог превышают `Caribbean Sea` (73), `South China Sea`
(54), `Philippine Sea` (43), `Mediterranean Sea - Eastern Basin` (39). Это
названные моря, а не монолиты, и в scope задачи они не входят. После разреза
останутся выше порога — назвать вслух в отчёте.

**2026-08-02, мостов в исходном графе 27.** Кандидаты в узкие места считаются,
а не выбираются на глаз.

## Decision log

- 2026-08-02: ветка от `claude/1946-map-coastline-fixes`, не от `main` —
  на `main` нет входов задачи.
- 2026-08-02: диагностика пишется ДО нарезки, а не после (в задании она
  четвёртым пунктом). Причина: базовая линия, снятая собственным кодом, —
  единственный способ сравнить «до/после» одной и той же метрикой.

## Validation

Базовая линия из задания (замер 2026-08-02, подлежит перепроверке шагом 2):
зон 101 · сумма `area_km2` 362 846 524 · рёбер 168 · компонент 1 ·
изолированных 0 · медианная степень 3 · максимум прибрежных регионов 112 ·
островов-одиночек 174, морской сосед у 174 из 174.

Проверено самостоятельно на входном файле до начала работы: 101 фича, ровно
три свойства (`name`, `area_km2`, `source`), сумма `area_km2` = 362 846 524.

После работы обязаны выполняться все:

- изолированных зон 0, компонента 1;
- 174 из 174 островов-одиночек сохраняют морского соседа;
- путь существует между любыми двумя океанами;
- максимум прибрежных регионов на зону ≤ ~25, кроме названных вслух
  архипелажных исключений;
- сумма `area_km2` сохранена;
- каждое помеченное узкое место — мост в графе (снятие ребра рвёт связность),
  показано прогоном.

Команды (cwd = корень дерева
`D:/Pax Historia LOCAL/.claude/worktrees/sea-shelf-zones`):

```text
python scripts/map/build/diagnose_sea_graph.py
python scripts/map/test_split_ocean_shelves.py
python scripts/map/test_build_neighbor_graph.py
python scripts/map/validate_region_economy_1946.py
python scripts/map/test_validate_region_economy_1946.py
python .agent/evals/public/run_public_evals.py
```

Негативный контроль: каждый новый тест прогоняется против восстановленного
старого поведения и обязан упасть; вывод падения приводится в отчёте.

## Rollback / containment

Всё новое — отдельные файлы; единственная правка существующего кода —
`build_neighbor_graph.py` (одна функция, обратима `git checkout`). Выходные
`.geojson` вне git и пересобираются. Ветка изолирована в своём дереве.

## Final outcome

(заполняется по завершении)
