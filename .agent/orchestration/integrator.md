# Реестр интегратора — очередь веток в `main`

Формат и правила — `.agent/orchestration/README.md`. Очередь собрана из
`git worktree list` и `git branch --no-merged main`, а не из текста планов.

Состояние пересобрано 2026-08-08 (второй замер), `main` = `7f16c46` Merge branch
'claude/orchestrator-lead'. Все деревья чисты, незакоммиченного нет нигде.
Конфликты и размеры документов предсказаны `git merge-tree --write-tree` — рабочие
деревья не трогались. Ни одна ветка не влита, ни одна её проверка не
перезапускалась на объединённой базе: все критерии открыты.

Последовательность согласована с ролью `lead` (`.agent/orchestration/lead.md`).
Расхождения с её замерами вынесены в раздел «Расхождения с реестром lead» —
приняты те числа, которые эта роль сняла сама.

## Порядок влития

1. Q-1 `claude/world-assembly-merge` — карта (fast-forward, конфликтов нет)
2. Q-2 `claude/extraction-resources` — владелец `shared/src/types`
3. Q-3 `claude/sea-shelf-zones`, Q-4 `claude/handoff` — документы, требуют решения
4. Q-5 `claude/ui-port-panels` — последней, после ответов на К-2 и К-3
5. Реестры `claude/lead-registry`, `claude/integrator-queue` — в конце, итогом

## Смысловые пересечения, названные ДО влития

1. **К-3. `claude/ui-port-panels` × `main` (`8407ee5`) — бюджет.** Ветка удаляет
   `client/src/components/BudgetPanel.tsx`, а `main` переписал в нём базу долей
   на `income - importSpending` и добавил `BudgetPanel.test.tsx`. `merge-tree`
   даёт `CONFLICT (modify/delete)`. Вопрос UI-домену: считает ли бюджет нового
   слоя от той же базы. Принять удаление молча — вернуть игроку баланс, которого
   в движке нет.
2. **К-2. `claude/ui-port-panels` × `claude/extraction-resources` — лента
   событий.** `extraction-resources` правит
   `client/src/components/EventTimelinePanel.tsx` (+81/−45), `ui-port-panels`
   его удаляет; новый дом ленты — `client/src/ui/EventItem/EventItem.tsx`
   (в ветке присутствует). Вопрос «получает ли `EventItem` дату и в каком виде»
   — решение UI-домена, не интегратора.
3. **`.agent/handoff.md` — не текстовый конфликт, а смена поколения.** Файл
   одноместный: каждая сессия перезаписывает его своим пакетом. Сейчас в `main`
   лежит пара пакетов от 2026-08-07 (оркестратор геометрии + исполнитель
   очереди, 328 строк). `claude/handoff` несёт поколение 2026-08-03
   (оркестратор + промт-инженер, 308 строк), `claude/sea-shelf-zones` — свой
   одиночный пакет 2026-08-07 (168 строк), `ui-port-panels` — свой (195 строк).
   Механическое «разрешить конфликт» здесь означает выбрать, какое поколение
   останется. См. Q-3 и Q-4.
4. **Обязательство перезамера после карты (`8407ee5`).** Все бюджетные доли
   сняты на данных БЕЗ карты; после Q-1 переснять `probeBudgetShares.ts`.
   Не блокер влития, обязательный шаг после него.
5. **`shared/src/types/`** — трогает только линия `dated-events` →
   `extraction-resources`. Параллельной правки типов нет.

## Q-1 `claude/world-assembly-merge` — карта 1946 целиком
Статус: выдано
Ветка: `claude/world-assembly-merge` (+19 / −0), дерево
`.claude/worktrees/world-assembly-merge`, чисто
Выдано: 2026-08-08

**Содержание.** 59 файлов, +75607/−73230. Проверено `merge-base --is-ancestor`:
содержит целиком `claude/world-assembly`, `claude/map-orchestrator` и
`claude/map-china-restore` — отдельного влития те не требуют. `main` — её
предок, поэтому мерж будет fast-forward: конфликтов нет ни одного.

**Критерии приёмки** (перезапускает интегратор ПОСЛЕ объединения):
- [ ] `python scripts/map/validate_region_economy_1946.py` из корня
- [ ] `python scripts/map/test_validate_region_economy_1946.py` из корня
- [ ] `npx tsc --noEmit -p tsconfig.json` и `npm test` в `server/`
- [ ] `python .agent/evals/public/run_public_evals.py` — в частности пороги
      `docs/DECISIONS.md` ≤ 2000 (замер ветки: 1909) и `docs/TODO.md` ≤ 1400
      (1277)
- [ ] шаг-сторож курации Китая падает на восстановленном старом поведении —
      негативный контроль показан, иначе покрытием не считается
- [ ] ExecPlan'ы закрытых карточных задач удалены интеграционным коммитом

**После влития.** Переснять `probeBudgetShares.ts` (см. пересечение №4).
Три отработавших дерева (`world-assembly`, `map-orchestrator`,
`map-china-restore`) сносятся `worktree-drop.ps1` — командой пользователю.

**Аудит.** —

## Q-2 `claude/extraction-resources` — добыча ресурсов + датированные события
Статус: выдано
Ветка: `claude/extraction-resources` (+5 / −10), дерево
`.claude/worktrees/extraction-resources`, чисто
Выдано: 2026-08-08

**Содержание.** Проверено `merge-base --is-ancestor`: содержит целиком
`claude/dated-events` — отдельного влития та не требует. Правит
`shared/src/types/Event.ts`, `SaveFile.ts`, LLM-контракт, `ChronicleTick`,
`client/src/components/EventTimelinePanel.tsx`.

**Замер объединения с Q-1** (`merge-tree`, до влития): конфликт один —
`docs/DECISIONS.md`, один хунк. Итоговый файл 1941 строка при пороге 2000,
`docs/TODO.md` 1296 при пороге 1400. Порог НЕ пробивается.

**Критерии приёмки:**
- [ ] `npx tsc --noEmit -p tsconfig.json` и `npm test` в `server/`
- [ ] `npx tsc --noEmit -p tsconfig.app.json` и `npm test` в `client/`
      (`SaveFile` — общий тип, клиент его читает)
- [ ] изменение `SAVE_VERSION` названо явно: менялся или нет, что со старым сейвом
- [ ] негативный контроль: тест на датированные события падает, если вернуть
      старую форму ответа
- [ ] public eval зелёный — считанный после мержа, а не спроецированный

**Аудит.** —

## Q-3 `claude/sea-shelf-zones` — порядок восстановления после потери дерева
Статус: выдано
Ветка: `claude/sea-shelf-zones` (+1 / −139), дерево
`.claude/worktrees/sea-shelf-zones`, чисто
Выдано: 2026-08-08

**Содержание.** Один коммит: `.agent/runs/sea-zones-2026-08-06/README.md`
(+37/−1) — как восстановить водный слой, если дерево удалено; и `.agent/handoff.md`
(+7). Ценное здесь — README: в нём заявлена пройденная проверка
воспроизводимости (101 зона, 362 846 524 км², пять океанов 21/32/13/8/11).

**Решение к принятию.** Её `handoff.md` — снимок 168 строк из поколения,
которое `main` уже перерос (328 строк). Рекомендация интегратора: README
принять, `handoff.md` разрешить в пользу `main`. Это выбор содержания, а не
механика — вынесено пользователю.

**Критерии приёмки:**
- [ ] в `.agent/handoff.md` после мержа нет маркеров конфликта и не потеряно
      ничего из текущего поколения пакетов
- [ ] числа воспроизводимости остаются замером ветки: интегратор НЕ переписывает
      их в `DECISIONS` как свои — они сняты не им
- [ ] public eval зелёный

**Аудит.** —

## Q-4 `claude/handoff` — пакет передачи промт-инженера (2026-08-03)
Статус: выдано
Ветка: `claude/handoff` (+1 / −98), дерево `.claude/worktrees/handoff`, чисто
Выдано: 2026-08-08

**Содержание.** Один коммит, только `.agent/handoff.md`: к пакету оркестратора
от 2026-08-03 добавлен второй — промт-инженера режиссёра.

**Решение к принятию — ветка под вопросом целиком.** Вся её ценность лежит в
файле, который `main` с тех пор перезаписал двумя более свежими пакетами
(2026-08-07). Влить её как есть = воскресить поколение 2026-08-03 поверх
текущего. Варианты: (а) списать ветку, если пакет промт-инженера отработан;
(б) извлечь из неё раздел промт-инженера отдельной правкой поверх текущего
`handoff.md`. Что из этого — решение пользователя, не интегратора.

**Критерии приёмки** (если принимается вариант «б»):
- [ ] в `handoff.md` после правки живут и пакеты 2026-08-07, и извлечённый
      раздел промт-инженера
- [ ] public eval зелёный

**Аудит.** —

## Q-5 `claude/ui-port-panels` — перенос панелей и снос старого слоя UI
Статус: выдано
Ветка: `claude/ui-port-panels` (+11 / −123), дерево
`.claude/worktrees/ui-port-panels`, чисто
Выдано: 2026-08-08

**Содержание.** Перенос бюджета, преемника, фокуса исследований в новый слой;
приказ примитивами и текстом; ручной цикл ИИ-режиссёра окном из МЕНЮ; снос
старого слоя (`client/src/components/`, `client/src/hud/`, `useWindows`).
Вклад в журналы относительно своей базы: `DECISIONS` +38/−8, `TODO` +19/−11.
`.agent/handoff.md` переписан (+176/−144, итог 195 строк).

**Влитие последним и не раньше ответов на К-2 и К-3.**

**Критерии приёмки:**
- [ ] К-3: считает ли бюджет нового слоя от `income - importSpending`; если нет
      — ветка возвращается автору, а не чинится интегратором
- [ ] К-2: `EventItem` получает дату события — ответ UI-домена получен
- [ ] `BudgetPanel.test.tsx` из `main` либо перенесён на новый слой, либо его
      удаление обосновано тем, что свойство держит другой тест
- [ ] `npx tsc --noEmit -p tsconfig.app.json`, `npm test`, `npm run lint`,
      `npm run build` в `client/` — после объединения
- [ ] предсуществующие 20 ошибок lint в `client/src/map/` отделены от регрессии
- [ ] перед разрешением конфликта в `.agent/handoff.md` проверено, что перенос
      незакрытых пунктов в `docs/TODO.md` состоялся (требование `lead`)
- [ ] public eval зелёный: после этого шага `DECISIONS` проектируется ≈1968 при
      пороге 2000 — запас порядка 30 строк, считать по факту

**Аудит.** —

## Расхождения с реестром lead (замерено этой ролью)

- **Порог `docs/DECISIONS.md` не пробивается.** `lead` спроецировал 1909 + 152 =
  2061 сложением. Фактическое объединение (`merge-tree --write-tree`
  `world-assembly-merge` + `extraction-resources`) даёт **1941 строку** — вклады
  веток пересекаются, а не складываются. После Q-5 проектируется ≈1968 при
  пороге 2000. Архивация августа в `docs/decisions/2026-08.md` (сейчас 198
  строк) остаётся гигиеной и делается после влитий, но блокером очереди не
  является. Считать по факту после каждого мержа, не по проекции — включая эту.
- **`.agent/handoff.md` — не текстовый конфликт.** `lead` описал его как
  конфликт разных разделов одного файла. Это верно для поколения 2026-08-03;
  сейчас `main` держит поколение 2026-08-07, и вопрос стоит иначе: какое
  поколение остаётся (см. Q-3, Q-4).
- **`EventTimelinePanel.tsx`: +81/−45**, а не «+126 строк» — 126 это сумма
  изменённых строк в `--stat`. На суть К-2 не влияет.

## Известное ограничение инфраструктуры

`scripts/hooks/guard.mjs` блокирует правку любого файла в основном checkout, в
том числе этого реестра, хотя `role-guard.mjs` роли `integrator` его разрешает.
Поэтому реестр ведётся в дереве `.claude/worktrees/integrator-queue` и попадает
в `main` мержем, как обычная задача. Сами мержи хук не трогает — он проверяет
файловые инструменты и разрушительные команды.
