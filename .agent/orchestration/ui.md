# Реестр ui

## ДОМЕН НА ПАУЗЕ с 2026-08-09 — как его поднимать

Пауза — решение пользователя об организации, не оценка работы: одновременно
активны два домена вместо четырёх (`logic` несёт милстоун, `geometry` кормит
его входами), потому что узкое место — внимание пользователя, а не домены.
Поднимает домен роль `lead`.

**Пакет передачи `.agent/handoff/claude-ui-orchestrator.md` НЕ ищи — он удалён**
тем же интеграционным коммитом, которым влилась ветка, по своему жизненному
циклу (`.agent/PLANS.md`, скилл `session-handoff`). Пакет, переживший свою
ветку, — мусор: он описывает базу, которой больше нет. Всё, что обязано было
пережить влитие, лежит здесь и в уставе роли.

**Мандат новой сессии домена отличается от исходного промта — два расширения,
оба законные:**

1. **Гейт на `docs/TODO.md` снят** решением `lead`: домен правит СВОИ разделы,
   адресуя их по ТЕКСТУ ЗАГОЛОВКА, а не по номеру строки (номера сдвигает
   каждое влитие). Разделы домена: `## Интерфейс` и то, что от него отпочковано.
   Правила: чужие разделы и шапку файла не трогать, порог 1400 не поднимать,
   **итоговая дельта ветки ≤ 0**. Уточнение `lead`: снять ЗАКРЫТЫЙ пункт — факт,
   проверяемый прогоном, законен для любого домена; перенести ОТКРЫТЫЙ в
   `IDEAS`, привязать к милстоуну или удалить как наблюдение — решение его
   домена. **Рассуждение доказательством закрытия не является.**
2. **Задание «карта — заглушка» закрыто без дефекта**: пользователь смотрел
   песочницу.

**Числа, на которые нельзя ссылаться без перезамера.** Все проверки —
перемерять в дереве задачи, cwd `client/`: `npx tsc --noEmit -p
tsconfig.app.json`, `npm test`, `npm run lint`, `npm run build`; из корня
`python .agent/evals/public/run_public_evals.py`. Пиксельные замеры Т-15
(«четыре угла», 99 px во всех режимах) остались на ОДНОМ источнике —
исполнителе; аудитор их не переснимал, оркестратор мерил до починки. Формально
`static-only`. Пороги живых доков считает
`.agent/evals/public/run_public_evals.py` как `count("\n") + 1`; `wc -l` даёт на
единицу меньше, а `Measure-Object -Line` пропускает пустые строки — им не мерить.

**Инструмент.** Browser pane страницу не отображает (`document.hidden = true`,
`innerWidth/innerHeight` = 0, MapLibre не рисует кадр, скриншот не снимается);
настоящий Chrome не подключён. Но DOM и `getComputedStyle` работают — все три
дефекта рамы найдены так. `preview_start` поднимает dev-сервер из ОСНОВНОГО
checkout, а не из дерева ветки: проверять `fetch('/src/<файл>')`, иначе
подтвердишь починку, которой в дереве нет.

**Сломанный механизм, не в домене `ui`:** субагент `ui-reviewer` не поднимается
(`Agent type not found`), хотя файл валиден. Обход — вкладывать его устав
дословно в `general-purpose`. Устав домена объявляет его вердикт обязательным
при приёмке.

Ловушки домена, оплаченные сессией 2026-08-09, — в `.agent/roles/ui.md`
(«Типовые ловушки»).

Формат — `.agent/orchestration/README.md`. Устав — `.agent/roles/ui.md`.
Критерии приёмки записаны ДО выдачи заданий. Числа ниже сняты оркестратором в
этой сессии; числа чужих отчётов помечены как заявки.

## Состояние — 2026-08-09, `main` = `92c1aa9`, дерево `claude/ui-orchestrator`

**Стартовый промт сессии называл `main` = `9b71ead`.** Проверено: `9b71ead` —
предок текущего `main`, с тех пор влиты `claude/lead-registry` и
`claude/integrator-queue`. Работа велась от `92c1aa9`.

Baseline (мой прогон, cwd `client/`):

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit -p tsconfig.app.json` | exit 0 |
| `npm test` | 3 файла, 74 теста, все зелёные |
| `npm run lint` | exit 0 |

Все три чистые. Значит любое падение в работе исполнителя — регрессия, а не
наследство; отделять нечего.

### Что показал замер сверх заявок

- Игровой слой — 3490 строк: `Screen.tsx` 1367, `panels.tsx` 727,
  `adapter.ts` 520, `model.ts` 335, `GameShell.tsx` 301, `mapModeColors.ts` 143.
- **Тестов игрового слоя ноль.** Три тестовых файла: `i18n/localeKeys`,
  `map/engine/GeometryEngine`, `map/engine/buildCountryLabels.e2e`. Ни один не
  монтирует `Screen`. Это и есть корень Д-1: дефект дожил не из-за опечатки, а
  из-за отсутствия поверхности, на которой опечатка была бы видна.
- `@testing-library/react` 16.3.2 и `happy-dom` 20.10.6 **уже в
  devDependencies и не используются**. Новая зависимость на render-тест не
  нужна. Блока `test` в `vite.config.ts` нет — окружение берётся пофайловым
  `// @vitest-environment happy-dom`, а не сменой окружения для остальных 74
  тестов.

### Д-1 Иконки и ЛЕГЕНДА режимов карты — подтверждено, но описано неполно

Заявка подтвердилась: `MODE_ICONS` (`Screen.tsx:166`) ключеван восемью
идентификаторами песочницы (`proto/data.ts:275`), игра передаёт восемь
идентификаторов движка (`mapModeColors.ts:6`), **пересечение множеств пусто** —
все восемь кнопок в игре рисуются пустыми, `LEGENDS[mapMode]`
(`Screen.tsx:535`) в игре не находит ничего никогда.

Три факта, которых в заявке не было и которые меняют решение:

1. **`legendForMode` (`mapModeColors.ts:100`) — мёртвый код.** Никто её не
   импортирует (проверено `grep` по всему `client/src`). Это вторая, уже
   написанная реализация легенды: ключевана идентификаторами ДВИЖКА, отдаёт
   дискретные образцы цвета и `labelKey` под i18n, покрывает 7 режимов из 8.
2. **Градиентная `LEGENDS` в `Screen.tsx` описывает карту, которой нет.**
   `computeMapModeColors` красит тремя-четырьмя плоскими ведрами, а не
   непрерывной шкалой. Починка одних ключей включила бы легенду-градиент над
   ведёрной картой — то есть заменила бы «легенды нет» на «легенда врёт», а
   четыре ведёрных режима (`res`, `dip`, `mil`, `inf`) остались бы без легенды
   вовсе: в `LEGENDS` их аналогов нет.
3. **Переименование бесплатно.** Тип `MapMode` живёт только в клиенте: в
   `server/src` и `shared/src` его нет, `localStorage`/`sessionStorage` в
   клиенте не используется вообще, режим хранится единственным
   `useState<MapMode>("pol")` (`GameShell.tsx:48`). Ни миграции, ни сейвов, ни
   стыка с `logic`.

Дыру в типах, через которую дефект прошёл сквозь `tsc`, держат два места:
`ScreenProps.mapMode: string` (`Screen.tsx:86`) и приведение
`onMapMode={(mode) => setMapMode(mode as MapMode)}` (`GameShell.tsx:279`).
Пока `mapMode` — `string`, компилятор о расхождении словарей знать не может.

### Д-2 Локализация игрового слоя — подтверждено, заявленной защиты НЕТ

Русские строки в коде: `Screen.tsx` ~56 пользовательских (названия ТОМОВ,
`aria-label` всех кнопок ШАПКИ, вкладки ЛЕНТЫ, тумблеры МЕНЮ, весь текст окна
ручного цикла), `panels.tsx` ~50 (заголовки всех разделов, шапки таблиц
РЕЕСТРА), `adapter.ts` ~30 (тиры держав, подписи ПРИБОРОВ, статьи бюджета, оси
идеологии, «выполнена»/«в работе»), `model.ts` ~15 (`TOMES`, `LEDGER_TABS`,
`REGION_TABS`). Английской версии у них нет.

**Заявка «класс дефекта уже держит `localeKeys.test.ts`» неверна.** Этот тест
проверяет обратное свойство: что каждый ключ, который код У СЛОВАРЯ ПРОСИТ,
в словаре есть. Строка, зашитая в JSX, у словаря не просит ничего и для теста
невидима. Класс «строка в коде вместо ключа» не держится ничем — ни тестом, ни
линтом, ни `tsc`. Поэтому задание Т-3 обязано принести СВОЙ тест, иначе долг
вернётся следующей правкой экрана.

Попутно найдено: `TOME_NAMES` (`Screen.tsx:157`) и `TOMES` (`model.ts:139`) —
два словаря с одинаковым содержимым. Тот же класс, что Д-1: одно оформление,
два источника имён.

### Д-3 Правило «ПРИБОРОВ ровно пять» — подтверждено, но сформулировано неверно

Игра отдаёт пять (`adapter.ts:393` — `gdp`, `treasury`, `rank`, `stab`,
`legit`), песочница шесть (`Prototype.tsx:52`). Правило объявлено в
`model.ts:195` и в `docs/UI_DESIGN.md` §6.

Развилка «либо тест на пять, либо снять правило» — ложная. Пять — не свойство,
а прикидка; настоящее свойство: **ряд ПРИБОРОВ не переполняет ШАПКУ на
поддерживаемой ширине**. Тест на счётчик разрешил бы шесть узких приборов и
запретил бы пять широких — то есть охранял бы не то, что ломается. Решение —
в Т-5.

### Д-4 Склонение имён в `primitiveOutcome` — подтверждено, измерено

В `ru/primitiveOutcome.json` и `ru/primitiveRejection.json` 101 строка
подставляет имя сущности. Из них **11 ставят имя после предлога или в роли
притяжательного без родового слова** — там и рождается «сближение с
Великобритания», «Казна Франция», «Курс Франция», «под контролем СССР»,
«санкции против Франция». Остальные подставляют имя в приложении после
родового слова («региону {{region}}», «страну {{country}}», «из региона
{{region}}») — это нормативный русский, чинить нечего.

Механизм: `usePrimitiveOutcomeText` разрешает `LocalizedText` в **именительный**
падеж и подставляет в шаблон (`primitiveOutcomeText.ts:24`). Значит починка НЕ
требует падежных форм в `shared/src/types/i18n/LocalizedText` — достаточно
переписать 11 русских шаблонов так, чтобы имя стояло в именительном:
через тире или после родового слова. Этот приём в файле уже основной
(«Реформа проведена — {{country}}», «Автономия предоставлена — {{region}}»).
Английский не затронут.

### Д-5 Непроверенные ветки — заявка уточнена

`LlmCycleOutcome` (`Screen.tsx:1308`, `1355`): ветка
`narrativeCanonized: false` и СПИСОК ОТКАЗОВ существуют в коде и на живом
прогоне 2026-08-07 не рисовались.

Заявка «нужен прогон, где отказ реально случится» неверна как требование.
`LlmCycleOutcome` рисует то, что лежит в `result` — объект состояния
компонента. Обе ветки достигаются render-тестом с придуманным `result` за
минуты, без живой модели и без ожидания, пока движок что-нибудь отклонит.
Живой прогон нужен для другого — для карты и раскладки (Т-6), где подделать
нечего.

`ErrorBoundary` (`components/ErrorBoundary.tsx`) локализован (namespace
`errorBoundary`), живым крашем не проверялся — тоже покрывается render-тестом
с бросающим ребёнком.

### Пересечения с параллельными доменами

Живы `claude/geometry-orchestrator`, `claude/logic-orchestrator`,
`claude/lead-registry`. Активного ExecPlan'а по интерфейсу нет:
`interface-rebuild.md` — `done`, `ui-port-panels.md` — `complete`,
`ui-onboarding-slice-4.md` — `blocked` и написан против удалённого слоя.

**Домену `logic`: интерфейс не заявляет потребности в `shared/src/types` в
этом круге работ.** Д-4 казался кандидатом на падежные формы в
`LocalizedText` — замер показал, что не нужен. Очередь на общие типы свободна
от UI.

---

## Развилки

### Р-1 Вокабуляр режимов карты — решено оркестратором, вариант (а) с поправкой

Развилка (а) переименовать / (б) таблица `id движка → иконка` / (в) выбор
иконки в модель — техническая, обратимая, целиком внутри `client/**`, без
персистенции и без стыка. По `AGENTS.md` такое решает оркестратор. Решаю (а) —
одна причина: (б) и (в) оставляют два словаря, которые разъедутся снова, а (а)
делает расхождение невозможным физически. Но (а) в исходной постановке не
чинит ЛЕГЕНДУ, поэтому решение шире на три пункта:

1. **Направление переименования — обратное предложенному: не песочница под
   движок, а движок под смысл.** Три идентификатора движка из восьми уже врут
   о том, что показывают: `sta` («stability») выводится как
   «Недовольство»/Unrest, `dip` — как «Блоки»/Blocs, `eco` — как
   «Промышленность»/Industry. Свести словари на трёхбуквенные шифры — значит
   закрепить это вранье в коде; свести на читаемые имена песочницы
   (`powers`, `blocs`, `population`, `unrest`, `industry`, `resources`,
   `infrastructure`) — значит получить `id` = то, что видит игрок.
   Цена одинаковая: ключи локали × 2 локали, один файл раскраски, одна
   строка в `GameShell`.
   Совпадают не все восемь: у песочницы есть `terrain`, которого у движка нет,
   у движка `inf`, которого нет у песочницы, а `mil` уходит по Р-2. Песочница
   отдаёт `terrain` и получает `infrastructure`: она существует, чтобы
   показывать игру, а режим, которого в игре нет, показывает пустоту. После
   этого и после снятия `mil` оба списка — одни и те же семь имён.
2. **ЛЕГЕНДА берётся из `legendForMode`, градиентная `LEGENDS` удаляется.**
   Мёртвая функция уже правильна по форме (дискретные образцы, `labelKey` под
   i18n) и лежит в одном файле с раскраской — значит цвет и его объяснение
   меняются одной правкой, и расхождение, породившее Д-1, для легенды тоже
   становится невозможным. Градиент удаляется как описание карты, которой нет.
3. **Инвариант держит `tsc`, а не тест, где это возможно:**
   `Record<MapMode, ReactNode>` для иконок (пропущенный ключ — ошибка
   компиляции), `ScreenProps.mapMode: MapMode` вместо `string`, приведение
   `as MapMode` в `GameShell.tsx:279` уходит. Тип `MapMode` и `MAP_MODE_ORDER`
   переезжают в `model.ts` — это контракт экрана; `mapModeColors.ts`
   становится их потребителем, а не владельцем.

**Что заставит меня передумать:** если найдётся место, где идентификатор
режима переживает перезагрузку (ссылка с параметром, пользовательская
настройка, серверный рендер карты) — переименование перестаёт быть бесплатным
и правильным становится (б). Искал в `server/src`, `shared/src`,
`localStorage`, `sessionStorage`, параметрах URL — не нашёл; единственный дом
значения — `useState`.

### Р-2 Режим «Армии» показывает не армии — РЕШЕНО пользователем 2026-08-09: скрыть

**Решение: вариант (1), режим убирается до появления реальной линии фронта.**
Следствие для Т-1: `mil` уходит из объединения `MapMode` целиком, а не
прячется условием в рендере — тогда `tsc` сам вычистит ветки в
`computeMapModeColors` и `legendForMode`, и мёртвого кода не остаётся. Режимов
становится семь, и списки игры и песочницы совпадают один в один. Запись о
будущем настоящем режиме — в `docs/IDEAS.md`.

Разбор, из которого выросло решение:

`computeMapModeColors` для `mil` красит «оккупирован → фронт, иначе тыл»
(`mapModeColors.ts:89`), и это признано в комментарии как заглушка: линии
фронта в данных нет. Игрок видит карту с подписью «Армии» и легендой
«фронт / тыл», которая на самом деле отвечает на вопрос «оккупирован ли
регион». Это не вкусовая правка, а вопрос «какой информации у игрока быть не
должно» — по уставу он идёт пользователю.

- **(1) Скрыть режим до появления реальной линии фронта** — рекомендовано и
  принято. Карта, которая врёт, дороже отсутствующей: игрок строит план на
  «тыле», которого нет. Цена: минус одна кнопка.
- **(2) Оставить, переименовав в «Оккупация»** — честно и почти бесплатно, но
  добавляет режим, которого нет в замысле, и занимает место будущих «Армий».
- **(3) Оставить как есть** — самое дешёвое и единственное, что вводит игрока
  в заблуждение.

### Р-3 Онбординг — РЕШЕНО пользователем 2026-08-09: `docs/IDEAS.md` после v1

Соглашаюсь с организатором и добавляю свою причину: у экрана нет ни раскладки
для узких окон, ни полного каталога состояний (`docs/UI_DESIGN.md` §12). Тур
привязывается к координатам и порядку поверхностей — то есть к тому, что ещё
не устоялось. Прецедент под рукой:
`.agent/plans/ui-onboarding-slice-4.md` — целый ExecPlan онбординга, `blocked`,
написанный против Header, которого больше не существует. Второй такой не нужен.

### Р-4 Нижняя поддерживаемая ширина — решено оркестратором: 1280

Приёмка требует замера «минимум на двух ширинах», а нижней границы у экрана
нет: `UI_DESIGN.md` §12 честно пишет, что узкие окна не проектировались.
Назначаю поддерживаемую пару **1280×800 и 1920×1080**, ниже 1280 — вне scope
v1 объявленно, а не случайно. Причина: игра — локальная настольная стратегия с
ПОЛОСОЙ, ЛЕНТОЙ и ЛИСТОМ одновременно на экране; раскладка из трёх столбцов
на 1024 не помещается по замыслу, а не по недосмотру. Решение вкусовое и
обратимое — пользователь вправе его отменить, тогда узкая раскладка становится
отдельной работой, не строчкой в критериях.

---

## Приоритет с обоснованием

1. **Т-1** (режимы карты) — единственный дефект, который игрок видит в первую
   минуту партии: восемь пустых кнопок и никогда не появляющаяся легенда.
   Дешёвый, целиком внутри клиента, и после него инвариант держит компилятор.
2. **Т-2** (render-тест экрана) — не отдельная работа, а негативный контроль
   Т-1 и инструмент Т-4; идёт в той же ветке. Отсутствие этой поверхности —
   причина, по которой Д-1 дожил до игрока.
3. **Т-3** (локализация) — партию не ломает, но дорожает с каждой правкой
   экрана, а защиты у класса нет вовсе (Д-2). Минимальная версия: строки в
   словарь, EN — машинной заготовкой, вылизывание после v1.
4. **Т-4** (ветки окна режиссёра, `ErrorBoundary`) — дешёвые, как только
   существует Т-2.
5. **Т-5** (правило ПРИБОРОВ) — переформулировать в измеримое свойство и
   закрыть замером Т-6.
6. **Т-6** (живой прогон и переполнение) — приёмочный проход, идёт последним и
   покрывает Т-1/Т-3/Т-5 одним заходом.
7. **Т-7** (accessibility) — отдельная работа после того, как строки станут
   ключами: клавиатурный проход и скринридер по экрану с зашитым русским
   `aria-label` пришлось бы делать дважды.

---

## Очередь

Строка `Статус:` появляется в записи в момент выдачи; у Т-3…Т-7 её нет —
критерии записаны, исполнитель не назначен.

### Т-1 Один вокабуляр режимов карты, легенда из источника цвета
Статус: принято
Исполнитель: субагент, дерево `.claude/worktrees/ui-mode-vocabulary`, ветка
`claude/ui-mode-vocabulary` (от `92c1aa9`)
Выдано: 2026-08-09

Дерево заведено штатным `scripts/worktree-new.ps1`, а не автоматической
изоляцией субагента: скрипт подключает `node_modules`, без которых исполнитель
не запустит `npm test`, то есть не сможет выполнить собственные критерии.
Т-2 идёт в этой же ветке как негативный контроль Т-1 — отдельного дерева у
него нет.

**Задача.** Игрок должен видеть иконки на кнопках режимов и легенду там, где
цвет означает величину. Способ — Р-1: один словарь идентификаторов,
читаемые имена, легенда из `legendForMode`, инвариант на `tsc`.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] В игре у каждой из кнопок режимов непустая иконка; проверяется
      render-тестом Т-2, а не глазами.
- [ ] Легенда появляется ровно у тех режимов, у которых `legendForMode`
      отдаёт непустой список, и её образцы цвета совпадают с цветами,
      которые `computeMapModeColors` реально выдаёт для этого режима.
      Градиентной `LEGENDS` в `Screen.tsx` больше нет.
- [ ] Подписи легенды идут через `t()` в обеих локалях (`labelKey` для этого
      и существует); `npm test` в `client/` держит это `localeKeys.test.ts`.
- [ ] `MODE_ICONS` — исчерпывающий `Record<MapMode, …>`;
      `ScreenProps.mapMode` типизирован `MapMode`; приведения `as MapMode`
      в `GameShell.tsx` нет.
- [ ] Песочница (`proto/`) говорит тем же словарём: `terrain` убран,
      `infrastructure` добавлен. После снятия `mil` (Р-2) списки игры и
      песочницы совпадают полностью — семь режимов, один порядок.
- [ ] Режим `mil` снят по Р-2: убран из объединения `MapMode`, а не спрятан
      условием в рендере. Ветки `mil` в `computeMapModeColors` и
      `legendForMode` исчезают, потому что их снимает `tsc`, а не потому что
      их нашли глазами; ключ `mapModes.mil` удалён из обеих локалей.
- [ ] `cd client && npx tsc --noEmit -p tsconfig.app.json && npm test &&
      npm run lint` — все три чистые (baseline чистый, любое падение —
      регрессия).
- [ ] **Негативный контроль:** вернуть один идентификатор режима к прежнему
      написанию — работа обязана перестать собираться (`tsc`), а не просто
      уронить тест. Показать вывод обеих попыток в отчёте. Если расхождение
      ловится только тестом, а не компилятором, пункт 4 не выполнен.

**Вне scope.** `client/src/map/**` (геометрия, топология, ярлыки);
пороги и формулы раскраски в `computeMapModeColors` — цвет решает не эта
задача; локализация остального экрана — это Т-3; **настоящий** режим «Армии»
по реальной линии фронта — не эта задача и не v1 (`docs/IDEAS.md`).
Отдельно запрещено: `docs/TODO.md` (гейт 1393/1400) — дельту исполнитель
возвращает списком в отчёте.

**Аудит 2026-08-09.** Оркестратор: прочитан diff (15 файлов, +645/−143 против
`92c1aa9`), перезапущены в дереве исполнителя на `d88ca5d`, cwd `client/` —
`tsc` exit 0, `npm test` **91 тест в 4 файлах**, `lint` exit 0. Заявка «74 →
91» сходится: новый файл даёт 17. `docs/TODO.md` не тронут
(`git diff --name-only main..HEAD -- docs/TODO.md` пусто),
`docs/DECISIONS.md` 1024 строки из 2000. Прочитан текст теста: модель
собирается `buildScreenModel` из фикстуры `GameState`, а не пишется под тест;
свойство легенды — равенство множеств «нарисованные цвета = объяснённые»;
есть защита от самоподтверждения подписей (`not.toContain("legend.")`).

Субагент `report-auditor` — **оба негативных контроля воспроизведены его
руками, не по отчёту** (оркестратор править `client/**` не может: role-guard):

- Контроль 1: `"unrest"` → `"sta"` даёт `tsc` exit 2, 7 ошибок в 5 файлах,
  среди них TS2353 на исчерпывающем `Record<MapMode, ReactNode>` в
  `Screen.tsx` и TS2366 «lacks ending return statement» в `legendForMode` —
  то есть `switch` легенды исчерпывающий и режим без легенды не соберётся.
  Проверено вторым идентификатором (`"powers"` → `"pol"`): 12 ошибок в 7
  файлах. **Инвариант держит компилятор, а не внимательность.**
- Контроль 2: `MODE_ICONS` с ключами песочницы роняет
  `Screen.mapModes.test.tsx` с сообщением «режим unrest без иконки», 90/91.
- Сверх задания: удаление ключа `legend.high` из `ru/screen.json` роняет три
  теста в двух файлах — защита подписей не декоративная.
- Дерево возвращено в чистое состояние: `status` пуст, HEAD = `d88ca5d`.

**Вердикт: возвращено на доработку** (механика принята, оба замечания —
в доках и в песочнице):

1. `DECISIONS.md` и `UI_DESIGN.md` утверждают «семь ошибок `tsc` в шести
   файлах» — **не воспроизводится**: число зависит от сломанного
   идентификатора (5 файлов на `unrest`, 7 на `powers`), а док подаёт его как
   свойство системы. Это запрещённый `AGENTS.md` класс «утверждение сильнее
   гарантии»; цифру снять.
2. **Найдено аудитором, не автором и не оркестратором:** удалённая градиентная
   `LEGENDS` совпадала с ramp'ами `proto/HexMap.tsx` **побайтно** — она честно
   описывала гексовый макет и врала только про игру. Теперь наоборот: в
   песочнице легенда обещает градации, которых `HexMap` не рисует, а в режимах
   «Ресурсы» и «Инфраструктура» он уходит в `default` и красит цветом
   владельца.
   Решение оркестратора: **`HexMap` на настоящие ведра НЕ переписывать.** Два
   источника цвета — тот самый дефект, который эта задача закрыла; «починить»
   песочницу значит завести его заново в другом месте, и трап тот же —
   песочница, которая выглядит правильно, и есть причина, по которой дефект
   дожил до игрока. Вместо этого песочница объявляется декоративной:
   комментарий в `HexMap.tsx`, оговорка в `UI_DESIGN.md`, строка в `IDEAS.md`
   с горизонтом после v1.

**Доработка `786c71d` — проверена оркестратором, ПРИНЯТО 2026-08-09.**

Мой прогон в дереве исполнителя на `786c71d`: cwd `client/` — `tsc` exit 0,
`npm test` 91 тест в 4 файлах, `lint` exit 0; корень дерева —
public eval 240 passed / 0 failed. `docs/TODO.md` не тронут за всю ветку,
`docs/DECISIONS.md` 1024 строки из 2000. Итог ветки против `main`: 16 файлов,
+678/−143.

Прочитан diff доработки, обе формулировки на месте:

- число снято в двух местах (`DECISIONS.md`, `UI_DESIGN.md` §11); вместо него
  названо СВОЙСТВО — «расхождение не компилируется», плюс прямая оговорка, что
  число ошибок зависит от сломанного идентификатора и свойством системы не
  является. Проверил grep'ом, что старой формулировки в живых доках не
  осталось (совпадения нашлись только в архиве `decisions/2026-07.md` и в
  несвязанной записи о серверных тестах).
- песочница объявлена декоративной в трёх местах: комментарий перед `fillFor`
  в `HexMap.tsx`, оговорка в `UI_DESIGN.md` §6, запись в `IDEAS.md` с
  горизонтом после v1. Во всех трёх сказано главное — «вторую раскраску здесь
  не заводить», то есть запрет на очевидную неправильную починку записан там,
  где его прочитает следующий автор.

Отдельно отмечаю метод исполнителя: он **не принял находку аудитора на веру**,
а сам сверил удалённую `LEGENDS` с ramp'ами `HexMap` (совпали все три) — и
откатил собственную промежуточную правку, куда вписал числа, полученные не им.
Это правильное поведение: чужой замер в свой док от своего имени не идёт.

**Ветка `claude/ui-mode-vocabulary` (`e4179ca`, три коммита) готова к сдаче в
очередь `integrator`** — Т-1, Т-2 и Т-1б приняты, дерево чистое. Доменный
оркестратор в `main` не мержит; влитие инициирует пользователь.

### Т-2 Render-тест игрового экрана
Статус: принято
Исполнитель: тот же субагент, ветка `claude/ui-mode-vocabulary`
Выдано: 2026-08-09

Идёт в ветке Т-1 как его негативный контроль.

**Задача.** Дать игровому слою поверхность, на которой расхождение модели и
оформления видно тестом. Сейчас её нет вовсе, и это причина Д-1.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] Тест монтирует `Screen` с моделью, собранной **`adapter.ts` из
      фикстуры `GameState`**, а не с моделью, написанной под тест: проверяется
      стык адаптера с экраном, а не сам себе тест.
- [ ] Окружение — пофайловое `// @vitest-environment happy-dom`;
      `@testing-library/react` уже в devDependencies, новых зависимостей нет.
- [ ] Остальные 74 теста по-прежнему зелёные и в том же окружении.
- [ ] **Негативный контроль:** тест падает на восстановленном старом
      поведении (иконки по ключам песочницы). Показать вывод падения.

**Вне scope.** Снапшоты вёрстки — снимок ломается от любой правки и
свойства не проверяет; e2e с сервером; покрытие `panels.tsx` целиком.

**Аудит 2026-08-09. Принято.** `client/src/game/__tests__/Screen.mapModes.test.tsx`
прочитан оркестратором построчно, негативный контроль воспроизведён
`report-auditor` (см. Т-1). Критерии выполнены все: окружение — пофайловый
`// @vitest-environment happy-dom`, остальные 74 теста в прежнем окружении и
зелёные, модель идёт через `buildScreenModel`, снапшотов нет, новых
зависимостей нет.

Сверх критериев автор сделал две вещи, которые стоит назвать, потому что они
и есть разница между тестом и покрытием: фикстура регионов покрывает **все
ведра каждого режима** (иначе свойство «легенда объясняет ровно нарисованное»
было бы слабее, чем выглядит: непокрытое ведро легенда обещала бы, а карта не
рисовала), и подпись сравнивается с проверкой `not.toContain("legend.")` —
без неё `t()` вернул бы сам ключ и сравнение сошлось бы само с собой.

Задание остаётся принятым независимо от доработки Т-1: возврат касается
формулировок в доках и песочницы, теста он не трогает.

### Независимое ревью Т-1 — 2026-08-09, и пробел в моих критериях

Устав требует вердикт `ui-reviewer` при приёмке. **Субагент `ui-reviewer` не
поднимается:** тип не зарегистрирован (`Agent type 'ui-reviewer' not found`),
хотя `.claude/agents/ui-reviewer.md` существует и frontmatter валиден, а два
соседа из того же каталога (`report-auditor`, `director-prompt-engineer`)
доступны. Гипотеза, не факт: в `tools:` у него значится `WebSearch`, а в этой
сессии `WebSearch` — deferred; определение с недоступным инструментом, похоже,
отбрасывается. Ревью проведено субагентом с вложенным дословно уставом — суть
получена, но **механизм, который устав объявляет обязательным, сломан**, и это
надо починить отдельно.

Вердикт ревью: **доработать.** Ни одна находка не отменяет приёмку Т-1 —
критерии Т-1 выполнены, и цель после отчёта не переносится. Но три находки
относятся к работе, которую Т-1 сдал, а не к старому долгу:

**Мой пробел, который стоит запомнить.** Критерий гласил «образцы цвета
совпадают с цветами, которые выдаёт `computeMapModeColors`». Он проверяет
СООТВЕТСТВИЕ и ничего не говорит о ВОСПРИНИМАЕМОСТИ: легенда, чьи образцы
физически не видны на панели, проходит его полностью. Ревью посчитало
арифметикой по токенам: `OK_DIM #374a30` («низко») и `HAIRLINE_2 #3a4b3d`
(«бедно») против `--surface #1a1c1e` дают **≈1.8:1** при требуемых WCAG 1.4.11
трёх, и это самое частое ведро на карте. Инсетный хайрлайн
`rgb(0 0 0 / 35%)`, поставленный автором именно против этого, делает тёмный
образец темнее — лечит только светлые. Урок для следующих критериев домена:
рядом с «цвет соответствует» обязателен «цвет различим», с числом.

### Т-1б Доработка ряда РЕЖИМОВ по ревью — ДО интеграции
Статус: принято
Исполнитель: тот же субагент, ветка `claude/ui-mode-vocabulary`
Выдано: 2026-08-09

Идёт в ветке `claude/ui-mode-vocabulary` поверх `786c71d`: это дефекты в
работе, которой ещё нет в `main`, и вливать её с ними незачем.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] **Образцы легенды различимы:** внешний светлый контур
      (`box-shadow: 0 0 0 1px var(--text-faint)`), инсетный чёрный убран.
      Контраст самого тёмного образца к `--panel-bg` посчитан и назван числом,
      ≥3:1 по WCAG 1.4.11.
- [ ] **Панель называет то, что показывает:** у легенды видимый заголовок =
      имя активного режима, он же в `aria-label`. Сейчас режим виден только по
      нажатому глифу, а «Промышленность» и «Инфраструктура» дают одинаковую
      зелёную карту и одинаковые слова — игрок не получает подтверждения, что
      режим сменился.
- [ ] **Состояние доступно не только оформлением:** `aria-pressed` на кнопках
      режимов, групповое имя на сетке. `UI_DESIGN.md` §9 уже требует, чтобы
      смысл, который несёт форма или цвет, дублировался; здесь он не дублирован.
- [ ] **Дыра в углу убрана:** семь кнопок одним рядом (замер ревью: 7×28 +
      6×4 = 220px против ЛЕНТЫ ≥20rem — влезает), легенда переезжает строкой
      над кнопками. Либо второй ряд выровнен по правому краю, чтобы пустая
      ячейка ушла внутрь блока, а не в угол кадра.
- [ ] **Ряды подписей однородны** (решение оркестратора, вкусовое — моё):
      «развитая / средняя / слабая» вместо «высоко/средне/низко» (наречия
      высоты в легенде КАРТЫ читаются как рельеф, а к инфраструктуре не
      относятся вовсе); «спокойствие / напряжение / волнения»;
      «наше / союзное / нейтральное / враждебное» — ряд про регион, а не смесь
      территории и стран. EN: `developed/moderate/weak`, `calm/tension/unrest`,
      `ours/allied/neutral/hostile`. Ключи не меняются, только строки.
- [ ] **Режим `blocs` переименован в `relations` («Отношения»/Relations).**
      Он красится по `owner.diplomacy.relations[playerCountryId]` — парному
      отношению к игроку, — а не по членству в блоке; двух чужих блоков между
      собой не видно. Это тот же класс, что снятые «Армии»: подпись отвечает
      не на тот вопрос, которым красит. Переименование, а не снятие: данные
      честные, врёт только имя. Настоящий режим блоков — `docs/IDEAS.md`.
- [ ] **«Население» перестаёт обещать плотность:** подписи по величине
      («многолюдно / средне / малолюдно»), потому что красится абсолютное
      `r.population`, а не `population / area`.
- [ ] Мелкое: активной кнопке акцент на глифе (сейчас активный вид почти равен
      `hover`, различие держится на 1px контура); `min-height` легенды по
      четырём пунктам, чтобы панель не дышала при переключении; удалить мёртвое
      правило `.legenda + .rezhimy` — `.legenda` лежит ВНУТРИ `.rezhimy`,
      соседями они не бывают.
- [ ] `cd client && npx tsc --noEmit -p tsconfig.app.json && npm test &&
      npm run lint` чистые; `python .agent/evals/public/run_public_evals.py`
      чистый (трогаются `docs/`).
- [ ] **Негативный контроль:** переименование `blocs` → `relations` обязано
      ломать сборку, если пропущено хоть одно место (тот же инвариант, что
      закрыл Т-1) — показать вывод. Плюс существующий render-тест остаётся
      зелёным: он про стык словарей, и правки оформления его ронять не должны.

**Вне scope Т-1б.** Перерисовка глифов (Т-8), палитра под разные вопросы
(Т-8), пороги недовольства (Т-9), локализация остального экрана (Т-3).

**Аудит 2026-08-09. ПРИНЯТО.** Коммит `e4179ca`. Мой прогон: `tsc` exit 0,
`npm test` **93 теста в 4 файлах** (было 91 — прибавились две проверки под
пункт про доступность), `lint` exit 0, `build` exit 0 (осталось только
предсуществующее предупреждение о размере чанка), public eval 240/0.
`docs/TODO.md` не тронут за всю ветку, `docs/DECISIONS.md` 1040 из 2000.

**Числа контраста пересчитал сам** — это единственное в отчёте, что
проверяется арифметикой без браузера. Своя реализация формулы WCAG 2.x
(relative luminance с правильной ветвью для `c > 0.03928` — на этом легко
ошибиться и получить 5.57 вместо 5.30) дала все пять чисел исполнителя до
второго знака: `OK_DIM` 1.78:1, `HAIRLINE_2` 1.83:1, `ACCENT_DIM` 3.40:1,
`OK_MID` 3.51:1, контур `--text-faint` 5.30:1. То есть требование ≥3:1
выполнено не заливкой (она и не может — цвет несёт смысл), а внешним контуром,
который виден при любой заливке. Контур взят переменной, поэтому подстраивается
под каждую из четырёх палитр.

Проверено чтением diff: инсетный чёрный убран; `.legenda + .rezhimy` удалено;
`.rezhimyGrid` стал `flex` (один ряд); акцент активной кнопки повешен на
`aria-pressed`, то есть на само состояние, а не на отдельный класс — разойтись
им нечем; `aria-labelledby` вместо второго слова «Легенда»; ключ `legend.title`
удалён; `mapModesGroup` есть в обеих локалях; `blocs` → `relations`
переименовано во всех семи местах, а вкладка РЕЕСТРА `LEDGER_TABS.blocs`
(таблица блоков мира) сознательно не тронута — другая сущность, и это верно.

**Развилку исполнитель решил сам, и решил правильно.** Мои два ряда подписей
конфликтовали на одном ключе: `legendForMode` отдаёт `medium` и «Промышленности»
(нужно «средняя», прилагательное ж. р.), и «Населению»/«Ресурсам» (нужно
«средне», наречие). Один ключ давал ровно то «богато · средняя · бедно», от
чего мы уходили. Он завёл второй ключ `moderate`, сохранив обе мои
формулировки дословно, и зафиксировал причину в коде и в §6. Это соразмерное
решение: возвращаться с вопросом ради расщепления ключа словаря было бы
дороже, чем решить. Правило «упёрся — вернись с вопросом» касается продуктовой
семантики, а не согласования прилагательных.

**Негативный контроль воспроизведён исполнителем и сходится с механизмом:**
пропущенное место переименования (`case "relations"` → `case "blocs"` в
`proto/HexMap.tsx`) даёт `tsc` exit 2 и `build` exit 2, а render-тест при этом
остаётся зелёным — именно так и задумано: этот класс ловит компилятор, тест
про другое. Отдельно ценно, что контроль сработал у автора сам собой по ходу
работы: пока `MODE_ICONS` не был переименован, `tsc` не дал пропустить место
молча.

**UNKNOWN, честно оставленный автором и мной не закрытый:** EN-локаль глазами в
живой игре не смотрели (нужен поднятый сервер и партия); скриншот снять не
удалось («pane is not displayed»), всё проверено измерениями DOM и вычисленных
стилей. Визуальным QA в полном смысле это не называется — так и помечено.
Закроется в Т-6.

### Т-8 Набор глифов и палитра режимов — ОТДЕЛЬНАЯ работа, не доработка

Ревью нашло два разных дефекта, оба требуют рисования, а не правки строк:

1. **`IconEconomy` стоит одновременно КОРЕШКОМ тома «Экономика» и режимом
   «Промышленность»** — оба видны на экране разом, и новичок учит глиф, получая
   противоречие. «Промышленности» нужен свой (труба, маховик). `IconPeople` в
   двух местах — наоборот правильно: одна величина, один глиф.
2. **Три пары глифов сливаются** при кнопке 28px и штрихе 1.5:
   `IconBlocs` ↔ `IconPeople` (оба «скопление кругов»; связи блоков длиной
   0.7–4.9px при штрихе 1.5 сливаются в кляксу), `IconResources` ↔
   `IconUnrest` (оба единственный замкнутый контур на весь бокс),
   `IconFlagMode` ↔ `IconInfrastructure` (оба вертикально-штриховые).
3. **Три режима дают одинаковую карту:** `industry`, `infrastructure` и
   `population` красятся одной зелёной тройкой `OK/OK_MID/OK_DIM`. Один вопрос
   — один тон; ступени брать из `tokens-1946.css`, недостающий тон добавлять
   туда, а не заводить локальные хексы рядом с существующими.

Вывод ревью `static-only`: различимость глифов выведена из геометрии путей, не
из просмотра. Поэтому критерий Т-8 обязан включать просмотр на реальном экране,
а не только замер.

### Т-15 ШАПКИ нет на экране, попап карты из старого слоя, РЕЖИМЫ прыгают
Статус: принято
Исполнитель: субагент, дерево `.claude/worktrees/ui-frame-fixes`, ветка
`claude/ui-frame-fixes` от `claude/ui-localize-screen` (`c878c40`)
Выдано: 2026-08-09

Пришло скриншотом от пользователя — **первое визуальное свидетельство за всю
сессию**. Три пункта, все установлены моим замером в живом приложении, не
чтением.

**1. ШАПКИ не видно, и она не «пропала», а уехала под нижний край.** Замер в
DOM: элемент существует, `visibility: visible`, `opacity: 1`, размер 581×101,
но **`y = 1065` при высоте окна 1065**. ХОД при этом на `y = 0` правильно.
Причина: `ShapedBar.tsx:223` ставит **инлайновый** `style={{ position:
"relative" }}`, а инлайн сильнее любого правила таблицы — поэтому
`.shapka { position: absolute; top: 0; left: 0 }` из `Screen.module.css` не
может выиграть никогда. Обёртка `<div ref={shapkaRef}>` класса не имеет и стоит
`static` в обычном потоке ПОСЛЕ контейнера MapLibre, который занимает всю
оболочку.

Следствие для игрока тяжелее, чем «некрасиво»: в ШАПКЕ живут ПРИБОРЫ, РАНГ и
КОРЕШКИ ТОМОВ — то есть **ни один ТОМ открыть нельзя**, и ни один показатель не
виден. Это самый тяжёлый дефект, найденный за сессию.

**Третий случай одного класса: в песочнице выглядит правильно.** Там слот карты
не растягивается на всю оболочку, поэтому статичная ШАПКА естественно попадает
наверх. Ровно как с `MODE_ICONS` и с легендой: песочница делает поломку игры
невидимой.

**Мой промах в приёмке, названный прямо.** Критерий «четыре угла: ШАПКА, ХОД,
ПОЛОСА, РЕЖИМЫ» я записал в Т-6 — и Т-6 не выдавал. Исполнители Т-1б и Т-3
мерили `scrollWidth` внутри панелей и честно докладывали «переполнения нет»; я
принял это как проверку раскладки. **Замер переполнения ВНУТРИ панели не
отвечает на вопрос, находится ли панель на экране.** Проверка существовала и не
была выполнена — это дефект моего порядка работ, не их отчётов.

**2. Клик по региону открывает окно старого слоя.** `MapView.tsx:271-288`
создаёт `maplibregl.Popup` и наполняет его `.setHTML()` с вёрсткой
`<div class="region-popup">`; стили живут в `App.css` (`.region-popup`,
`.popup-title`, `.popup-body`, `.popup-resources`, 1381-1432). Новый слой для
этого имеет ПОЛОСУ — попап её дублирует и выглядит чужим. Уцелело потому, что
`client/src/map/**` был явно вне scope ветки, удалившей старый слой.

**Чистка нетривиальна, и это надо сказать заранее:** `App.css` — 1648 строк, и
он **не мёртв**. На его классах до сих пор живёт `ScenarioSelector`
(`.scenario-card`, `.country-list`, `.country-button`, `.start-game-button` —
проверено в DOM живого прогона). Удалить файл целиком нельзя; надо снять
мёртвое и оставить живое.

**3. Панель РЕЖИМЫ прыгает.** Замер: «Державы» — `top 990`, высота 63, легенды
нет; «Промышленность» — `top 955`, высота 98, легенда 31. Низ панели (1053) и
кнопки (1017) стоят, **верхний край скачет на 35 px**. `min-height`, добавленный
в Т-1б против этого, не работает: `.legenda` рендерится условием
`legend.length > 0`, а в «Державах» элемента нет вовсе — `min-height` нечему
применять. «Державы» — режим по умолчанию, поэтому первое же переключение всегда
даёт скачок.

**Критерии приёмки** (записаны 2026-08-09, до выдачи):

- [ ] ШАПКА видна в левом верхнем углу игрового экрана. Проверяется ЗАМЕРОМ:
      `getBoundingClientRect()` ШАПКИ лежит внутри окна (`y ≥ 0`,
      `bottom ≤ innerHeight`), и то же для ХОДА, ПОЛОСЫ, ЛИСТА, РЕЖИМОВ — все
      четыре угла, на 1280 и 1920.
- [ ] Инлайновый `position` из `ShapedBar` убран или сделан переопределяемым;
      причина зафиксирована комментарием, иначе вернётся. `Prototype.tsx`
      (песочница) после правки тоже цел — она использует тот же `ShapedBar`.
- [ ] **Негативный контроль:** вернуть инлайновый `position: relative` — новый
      тест обязан упасть. Тест на «панель внутри окна» обязателен: без него
      дефект вернётся молча, как уже вернулся однажды.
- [ ] Клик по региону открывает ПОЛОСУ и НЕ открывает попап MapLibre.
      `maplibregl.Popup` и `.setHTML` из `MapView.tsx` удалены вместе с
      мёртвыми стилями `.region-popup`/`.popup-*` из `App.css`.
- [ ] `App.css` разобран: что нужно `ScenarioSelector` — осталось, мёртвое
      старого слоя — снято. В отчёте: сколько строк было, сколько осталось,
      чем доказано, что снятое не используется (`rg` по классам).
- [ ] Высота панели РЕЖИМЫ одинакова во всех семи режимах — замером, а не на
      глаз. Место под легенду резервируется всегда; кнопки не двигаются.
- [ ] `cd client && npx tsc --noEmit -p tsconfig.app.json && npm test &&
      npm run lint && npm run build` чистые. Baseline базы: tsc 0, 102 теста в
      6 файлах, lint 0, build 0.

**Вне scope.** Геометрия и данные карты (домен `geometry`); содержимое ПОЛОСЫ;
локализация (закрыта Т-3); глифы и палитра (Т-8); флаг в ШАПКЕ прибит к
`<FlagSU />` независимо от страны игрока — отдельная находка, не эта задача.

**Аудит 2026-08-09, часть оркестратора.** Коммиты `09bf561`, `6c94a77`,
`7292710`. Мой прогон на `7292710`, cwd `client/`: `tsc` 0, `npm test` **112
тестов в 7 файлах** (baseline базы 102/6, прирост +10), `lint` 0, `build` 0,
дерево чистое. По коду сверено: инлайновый `position` из корня `ShapedBar` ушёл,
на его месте `:where(.root)`; `Popup`/`setHTML` в `MapView.tsx` нет (осталась
только запись в комментарии, что там было); `popup` в `App.css` — **0**
вхождений; `App.css` **1648 → 454**; коробка легенды рендерится безусловно, а
`legend.length` используется только для `aria-labelledby`/`aria-hidden`.
Негативные контроли — у `report-auditor`, приёмку до его вердикта не ставлю.

**Решение исполнителя, которое стоит запомнить: `:where(.root)`.** Класс
компонента и класс вызывающего висят на ОДНОМ элементе, специфичность равная —
значит победителя решал бы порядок подключения таблиц, а у CSS-модулей он не
гарантирован. `:where()` снимает специфичность до нуля и превращает правило
компонента в значение по умолчанию: класс вызывающего перебивает его
детерминированно. Это лучше, чем проп `position`: проп расширил бы API и не
защитил бы от следующего инлайна.

**Он же нашёл вторую мину, которой не было в задании.** Убрав инлайн, он
удалил и обёртку `<div ref={shapkaRef}>` — иначе приклеенная панель вышла бы из
потока, обёртка сплющилась в ноль, а по её высоте считается `--frame-h`
(выравнивание ШАПКИ и ХОДА). То есть «починка» пункта 1 сломала бы выравнивание
**молча**, без единого красного теста. Он перенёс `ref` на корень панели.

**Поправка к МОЕМУ методу, которую он принёс.** `preview_start` поднимает
dev-сервер из ОСНОВНОГО checkout, а не из дерева ветки — он обнаружил это
запросом `fetch('/src/ui/ShapedBar/ShapedBar.tsx')` и перемерил всё заново из
своего дерева на отдельном порту. Мои собственные замеры этой сессии (карта,
ШАПКА) сняты на основном checkout. Для диагностики это оказалось безвредно —
`main` несёт те же дефекты, и именно его видит пользователь, — но вывод общий:
**прогон в живом приложении надо привязывать к дереву, иначе меряешь чужой
код.** Заношу как правило приёмки домена.

**Граница нового теста названа исполнителем честно и я её подтверждаю:**
пикселей он не мерит — у happy-dom нет движка раскладки, `getBoundingClientRect`
там нули. Тест держит ПРИЧИНУ (вычисленный `position`, запрет инлайнового), а
«панель попала в окно» доказано замером в живом приложении: ШАПКА `y` 800 → 0 на
1280 и 1080 → 0 на 1920, `inWindow` `false` → `true`. Это правильное разделение:
охранять причину тестом дешевле и надёжнее, чем симптом.

**Вердикт `report-auditor` 2026-08-09: ПРИНЯТО с оговорками.** Статус Т-15 —
`принято`.

Он воспроизвёл оба заявленных контроля (возврат инлайна → два внятных падения;
условная легенда → два падения, при этом `Screen.mapModes.test.tsx` остался
зелёным, то есть новый файл держит именно свой класс) и **проверил тест на
вакуумность**, чего в заявке не было: снял `css: { include: [/\.module\.css$/] }`
из `vitest.config.ts` — упало 7 из 10 кейсов, и первым сработал guard окружения
«таблицы стилей действительно применяются». Без подключённых модулей файл
краснеет, а не зеленеет. Защита настоящая.

`App.css` он проверил не выборочно, а полностью и в обе стороны: 101 исчезнувшее
имя класса против всего дерева — 94 без единого вхождения, 7 совпадений разобраны
и все ложные (`window` как JS-объект, `danger`/`primary` как варианты `Button`,
`label`/`value` как имена пропов); обратной проверкой собрал все глобальные
токены из `className` — все определены. Живое цело.

**Оговорки, обе принимаю:**

1. **`--frame-h` не держит НИ ОДИН тест, и это доказано негативным контролем
   аудитора:** он убрал `ref={shapkaRef}` — то есть вернул ровно ту мину, которую
   исполнитель нашёл и обезвредил, — и получил `112/112`, `tsc` 0, `lint` 0.
   Всё зелёное. В happy-dom `offsetHeight` равен нулю, ветка `if (frame > 0)` не
   исполняется, и класс дефекта в `npm test` недостижим в принципе. При этом
   `Screen.module.css` и `UI_DESIGN.md` утверждают «мерится сама панель» — то
   есть утверждение снова сильнее гарантии. → Т-17.
2. **Число в доках подано как свойство.** Мой инструктаж называл `y = 1065` при
   `innerHeight = 1065`, документы того же коммита — `1080` при `1080`.
   Противоречия нет: я мерил в окне 1125×1065, исполнитель на 1280×800 и
   1920×1080, и «панель ровно на нижней кромке» верно в обоих случаях. Но в
   доке число без названного окна читается как свойство системы. **Это пятый
   случай того же класса за сессию**, и на этот раз его посеял я. → Т-17.
3. Тест запрещает инлайновый `position` только на ШАПКЕ; ХОД, ПОЛОСА, ЛИСТ и
   `rightStack` проверяются лишь на вычисленный `absolute`, а `width:
   max-content` не держит ничто. → Т-17.

**Чего аудитор НЕ проверял и сказал прямо:** все пиксельные числа отчёта
(четыре угла на двух ширинах, 99 px во всех режимах) — у него нет инструмента
предпросмотра, dev-сервер он не поднимал. Эти замеры остаются на одном источнике
— исполнителе. Я их тоже не переснимал: мой замер был до починки и на основном
checkout. **Формально они `static-only` в части «после».**

### Т-16 `RegionDetail` упадёт на регионе без `demographics` — ЛАТЕНТНЫЙ

Найдено исполнителем Т-15, подтверждено аудитором по коду и типу:
`panels.tsx:471` читает `region.groups[0].name` без проверки; тип
(`shared/src/types/map/Region.ts:55`) допускает отсутствие
(`demographics?: …`), а `adapter.ts:159` превращает отсутствие в пустой массив
— цепочка реальна, и такой регион уронил бы весь экран в `ErrorBoundary`.
Строка и её номер те же на `c878c40`: предсуществующее, не регрессия этой ветки.

**Исправляю собственное преувеличение.** Я записал «дороже всего, что осталось
в очереди». Аудитор проверил достижимость, чего не сделали ни исполнитель, ни я:
в поставляемом сценарии 1946 **все 1389 регионов размечены** (`demographics.json`
1389 записей, пересечение с `regions.core.json` полное), ни одна не пуста. Из
стартового состояния дефект **не достигается** — он латентный, а не активный
краш. Серверный код везде страхуется `?? []`, клиентский нет.

Приоритет соответственно: не первый, а обычный — дешёвая страховка от состояния,
которое законно по типу и появится при первом же регионе без разметки. Урок
метода: **«тип допускает» не равно «данные содержат»**, и различает их только
замер данных.

### Т-17 Хвосты Т-15 по оговоркам аудита

1. Выравнивание рамы (`--frame-h`) не покрыто: либо тест на присутствие `ref` на
   корне `ShapedBar`, либо запись о непокрытом классе — но не молчаливое
   утверждение в комментарии и доке.
2. Числа замеров в `DECISIONS.md` и `UI_DESIGN.md` §11 — назвать окно, в котором
   они получены, либо снять цифру и оставить свойство («панель оказывалась ровно
   на нижней кромке окна»).
3. Запрет инлайнового `position` расширить на все панели рамы; решить, охранять
   ли `width: max-content`.

Попутно тем же исполнителем названо: **у `npm test` нет способа мерить
геометрию** (happy-dom без движка раскладки). Если раскладку когда-нибудь надо
охранять пикселями — это `vitest browser mode` или Playwright, отдельное
решение и отдельная зависимость. Пока — замер живого DOM руками и `static-only`
в отчётах.

### Т-14 Незарегистрированный namespace никем не ловится

Найдено `report-auditor` при приёмке Т-3 и воспроизведено им: снятие одной
строки регистрации в `client/src/i18n/index.ts` (`adapter: ruAdapter`) оставляет
`tsc` и все 102 теста зелёными, а игрок видит сырые идентификаторы вместо 36
подписей. `localeKeys.test.ts` проверяет наличие ключа **в JSON-файле**, а не то,
что файл подключён к i18n.

**Задача.** Проверка: каждый namespace из `client/src/i18n/locales/*/`
зарегистрирован в `resources` ОБЕИХ локалей. Плюс назвать границу в
`docs/UI_DESIGN.md` §10 — сейчас список «почему это не может повториться»
читается как исчерпывающий, а регистрация в нём не покрыта.

**Негативный контроль:** снятие одной строки регистрации обязано ронять
`npm test`. Сегодня не роняет — это и есть доказательство, что задача нужна.

**Это четвёртый случай одного класса за сессию: утверждение сильнее гарантии.**
(1) `localeKeys.test.ts` считался защитой от строк в JSX — не был;
(2) урок «оформление не ключуется строкой» был записан в док без теста —
закрыто Т-1; (3) «семь ошибок `tsc` в шести файлах» не воспроизводилось —
снято Т-1б; (4) §10 перечисляет механизмы защиты как полный список, а
регистрация вне него. `AGENTS.md` уже запрещает «никогда/всегда» без теста;
evidence на повторяемость собрана и передаётся `lead` как кандидат в
`.agent/EVOLUTION.md` — durable-правка не моя область.

### Т-11 Подсветка своей державы в РЕЕСТРЕ прибита к «SUN»

Найдено исполнителем Т-3 попутно, вне его scope; **проверено мной по коду, и
мест оказалось два, а не одно** — исполнитель назвал одно.

`panels.tsx:660` подсвечивает строку игрока условием `country.id === "SUN"`
вместо `model.playerId`. Наследие песочницы, где игрок всегда СССР. **Это
дефект правильности, а не оформления:** в партии за любую другую державу
подсветка укажет на чужую строку — интерфейс соврёт о том, кто игрок, в
таблице, которую открывают ради сравнения с собой.

Сюда же мелочь того же рода: `№` (`adapter.ts:449`, `panels.tsx:493`) — русская
типографика номера, в английской локали должно быть `#`. Новый тест на литералы
этого не ловит и не может: `№` (U+2116) не кириллица. Урок для критериев:
**защита от «русской строки» не есть защита от «русской типографики»**.

### Т-13 Вкладка «Блоки» РЕЕСТРА показывает игроку выдумку

Второе место с прибитым `"SUN"` (`panels.tsx:737`) оказалось не тем же дефектом,
а хуже: **вся вкладка «Блоки» — захардкоженная таблица из двух строк** («Сфера
СССР», «Бреттон-Вудс») с придуманными лидерами и числами участников 1 и 3.
Клик всегда открывает досье СССР или США, потому что строки не из данных.

Это прямо нарушает правило, записанное в `docs/UI_DESIGN.md` §2: «Панель,
заполненная правдоподобной выдумкой, обесценивает…» и «чего в состоянии нет —
приходит пустым и не рисуется».

**И Т-3 сделал это хуже, не желая того:** заглушечные строки теперь вынесены в
словарь и переведены на английский. Локализация выдумки — способ, которым
заглушка превращается в фичу: она становится похожа на всё остальное и
перестаёт выделяться. Исполнитель поступил правильно в рамках своего задания
(перенос без переписывания) и честно об этом сказал; вывод не к нему, а к
порядку работ — **заглушку надо снимать до локализации, а не после**.

Корень общий с Р-2 и с переименованием `blocs` → `relations`: **сущности «блок»
в модели нет**, `diplomacy.allies`/`rivals` раскраской не читаются. Две
поверхности делали вид, что блоки есть; одну (режим карты) уже поправили, эта
осталась. Решение: либо вкладка собирается из реальных данных, либо исчезает по
§2 — правило существует, продуктовой развилки здесь нет.

### Т-12 Расширить защиту от литералов на `client/src/ui/**`

Тест `gameLayerLiterals.test.ts` покрывает `client/src/game/**`. По замеру
исполнителя кириллица остаётся: `src/ui` 185 строк, `src/map` 157, `src/proto`
265, `src/dev` 213 (грубый счёт, код и комментарии вместе).

`src/proto` и `src/dev` — сознательно вне словаря (решение Т-1б), их не трогаем.
Но **`src/ui` объявлен локализованным** (`UI_DESIGN.md` §10: набор компонентов
берёт строки из namespace `ui`). Если это правда, расширение теста ничего не
сломает и зафиксирует уже существующее свойство; если неправда — мы узнаем это
тестом, а не в английской партии. Проверить и расширить. `src/map` — отдельно,
там UI-слой карты мой, а движок геометрии трогать незачем.

### Т-9 Пороги недовольства живут в одном месте

Ревью: карта считает «напряжённо» при `stability < 0.6`, панель региона
предупреждает при `discontent > 0.45`, список очагов начинается с `> 0.3`
(`mapModeColors.ts:88`, `panels.tsx:274`, `panels.tsx:266`). Регион с
недовольством 0.35 попадает в список проблемных и остаётся зелёным
«спокойствие» на карте. Это тот же класс, что закрыл Т-1: одна величина, три
копии порога. Пороги переезжают к раскраске, панели их импортируют.

Отдельно ревью **исправило меня** по существу: подпись «Недовольство» при
раскраске по `stability` — честная, а не инвертированная. Весь остальной
интерфейс уже показывает игроку недовольство (`adapter.ts:143` считает
`1 − stability`), устойчивость региона игрок не видит нигде. Переименовывать
нечего, сводить надо пороги.

### Карта в интерфейсе — установлено прогоном 2026-08-09

Задание от пользователя через `lead`: «карта — заглушка». `lead` прочитал код и
предложил три гипотезы, честно назвав их непроверенными. **Поднял приложение
сам** (`.claude/launch.json`, конфигурации `server` и `client`, цель — `main`,
где режимы уже влиты), прошёл сценарий → страну → игровой экран.

Установлено:

1. **В игровом пути карта подключена, а не заглушена.** `MapView` смонтирован,
   MapLibre создан (канвас в DOM), гекс-сетка стоит только в песочнице. Гипотеза
   «надо подключить» неверна.
2. **Геометрия отдаётся быстро.** Замер из страницы: `/world_1946.geojson` →
   200, **41,7 МБ, 1591 фича, 942 мс** (TTFB 27, download 399, parse 516).
   Гипотеза «43 МБ тяжелы для первого кадра» не подтвердилась.
3. **Ошибок в консоли нет**, `maplibre-gl` загружается, `POST /game/start` →
   200.
4. **Геометрия не запрашивается вообще** — но это артефакт инструмента, не
   дефект: загрузка висит на событии `load` MapLibre (`MapView.tsx:300`), а оно
   не наступает. В консоли нет лога со строки 306.
5. **Живой прогон карты доступным инструментом НЕВОЗМОЖЕН.** Browser pane
   держит вкладку `document.hidden = true`, `innerWidth/innerHeight = 0`,
   контейнер карты 0×0, канвас падает на дефолт MapLibre 400×300. Без размера и
   без `requestAnimationFrame` карта не инициализируется и не рисует. Настоящий
   Chrome не подключён — `list_connected_browsers` вернул пусто.
   **Это третий независимый случай:** Срез 2г, исполнитель UI-ветки дважды
   («pane is not displayed»), теперь я. Evidence повторяемая — кандидат в
   `.agent/EVOLUTION.md`: критерий приёмки «проверено в живом приложении»,
   записанный в уставе роли `ui`, в этом окружении невыполним, и устав обязан
   это признавать, а не требовать невозможного.

**Что из этого следует для дефекта пользователя.** Ни подтвердить, ни
опровергнуть «заглушку» я не могу — увидеть карту нечем. Но найдено то, что
объясняет ЛЮБОЙ из вариантов и является дефектом само по себе → Т-10.

**ЗАКРЫТО ответом пользователя 2026-08-09: «карта грузится, я песочницу
смотрел до этого».** Дефекта карты нет. Из трёх гипотез `lead` верной оказалась
третья — та, которую он сам назвал последней и наименее вероятной.

Что из этого стоит унести как метод, а не как факт:

- **Правильный вопрос дешевле правильной догадки.** Я потратил прогон, замер
  941 мс и попытку поднять второй браузер, прежде чем спросить пользователя,
  ЧТО он видел. Вопрос стоило задать первым: он единственный различал варианты,
  а всё остальное различить не могло.
- **Формулировка жалобы указывала на песочницу с самого начала.** Слово
  «заглушка» — точное описание гекс-сетки, а не пустой карты; в проекте так
  называется именно она. Я читал это как метафору, а это был термин.
- Прогон не потрачен зря: он дал Т-10 и, главное, доказал пункт 5 —
  инструмент не может проверить карту. Это было предположением трёх сессий,
  теперь это замер.

**Приоритет Т-10 понижен:** он больше не про жалобу пользователя. Остаётся
настоящим дефектом (молчащая ошибка загрузки) и идёт после Т-3, не раньше.

### Т-10 У карты нет состояний загрузки и ошибки

**Задача.** `MapView.tsx:503` на провал загрузки геометрии делает только
`console.error('Ошибка загрузки карты')`. Игрок видит пустое место — навсегда и
без объяснения. Пока данные едут и строится топология, тоже пусто и тоже без
индикатора. **Пустая карта от заглушки неотличима**, и именно это описал
пользователь словом «заглушка», что бы ни было первопричиной. `UI_DESIGN.md`
§12 уже признаёт, что каталог состояний не полон: «правило „нет данных — нет
раздела“ покрывает пустоту, но не ошибку загрузки». Вот она.

Приоритет: **выше Т-3 по важности, но Т-3 не останавливаю** — он идёт в своём
дереве, своих файлах (`Screen.tsx`, `panels.tsx`, `adapter.ts`, `model.ts`), с
Т-10 (`client/src/map/MapView.tsx`) не пересекается. Бросать начатое ради
нового дороже, чем закончить оба.

**Критерии приёмки** (записаны 2026-08-09, до выдачи):

- [ ] Пока геометрия грузится, на месте карты видно, что идёт работа, а не
      пустота. Состояние снимается, когда карта отрисована, а не когда пришёл
      ответ сети.
- [ ] Провал загрузки даёт видимую игроку ошибку с возможностью повторить, а не
      только строку в консоли, которую он не откроет.
- [ ] Оба состояния локализованы в обеих локалях (namespace существует).
- [ ] Состояния достижимы В ТЕСТЕ: подделанный провал `fetch` рисует ошибку,
      незавершённая загрузка рисует индикатор. Живой прогон недоступен (см.
      пункт 5 выше) — поэтому покрытие тестом здесь не «дополнительно», а
      единственный способ доказать, и в отчёте это помечается `static-only`.
- [ ] **Негативный контроль:** убрать рендер ошибки — тест обязан упасть.
- [ ] `cd client && npx tsc --noEmit -p tsconfig.app.json && npm test &&
      npm run lint && npm run build` чистые.

**Вне scope.** Геометрия, топология и содержимое `world_1946.geojson` — домен
`geometry`; упрётся исполнитель в данные — возвращает вопрос, а не правит.
Оптимизация размера файла и времени сборки топологии — отдельная работа, и
сначала её надо измерить: 942 мс приходятся на сеть и разбор, топология не
мерялась.

### Т-3 Игровой слой говорит через словарь
Статус: принято
Исполнитель: субагент, дерево `.claude/worktrees/ui-localize-screen`, ветка
`claude/ui-localize-screen` — **от `claude/ui-mode-vocabulary` (`e4179ca`), не
от `main`**
Выдано: 2026-08-09, передано второму исполнителю 2026-08-09 после обрыва

**Обрыв первого исполнителя — не брак.** Сессия упала на лимите API
посередине: `Agent terminated early due to an API error: You've hit your session
limit`. Состояние, снятое мной в дереве: 414 строк изменений, **ничего не
закоммичено**, сборка красная (11 ошибок `tsc`) — `model.ts` переименовал
`TOMES` → `TOME_IDS`, `LEDGER_TABS` → `LEDGER_TAB_IDS`, `REGION_TABS` →
`REGION_TAB_IDS`, а `Screen.tsx` и `panels.tsx` ещё зовут старые имена. Это
ожидаемая картина обрыва миграции, а не ошибка проектирования.

**Замысел первого исполнителя принят и подтверждён — он сильнее моего
задания.** Я требовал впрыснуть переводчик функцией и расширить
`localeKeys.test.ts`. Он сделал больше: завёл `client/src/i18n/Translator.ts` с
типом `Translator<Namespace>` и фантомным полем `__namespace`, где **namespace
заявляется ТИПОМ, а не соглашением об имени переменной**. Две причины, обе его
и обе верные: связать объявление переводчика с вызовами в ДРУГОМ файле можно
только аннотацией рядом с вызовами (иначе ключи остаются вне проверки), и
подмену словаря при этом ловит компилятор — `Translator<"screen">` в слот
`Translator<"adapter">` не проходит. Плюс раскладка на три namespace по
потребителям: `screen` (рама), `panels` (тела ТОМОВ и РЕЕСТРА), `adapter`
(подписи величин), с обоснованием в `i18n/index.ts`.

Второму исполнителю передано: сначала контрольный коммит, чтобы 414 строк не
погибли от случайного `checkout`, затем `Screen.tsx`, `panels.tsx` (ключи уже
написаны — 117 строк в обеих локалях), фикстура `Screen.mapModes.test.tsx`, и
три теста — обещанный `translatorNamespace.test.ts`, расширение
`localeKeys.test.ts`, новый тест на класс дефекта. Критериев не менял: они
записаны до первой выдачи. Добавлен третий негативный контроль — на обещание
`Translator.ts`, потому что «никогда» без теста запрещено `AGENTS.md`, а
обещание дал первый исполнитель в комментарии.

**База — чужая ветка, и это осознанно.** Т-3 переписывает те же три файла
(`Screen.tsx`, `adapter.ts`, `model.ts`), что ещё не влитая
`claude/ui-mode-vocabulary`. От `main` исполнитель разрешал бы конфликт в
файлах, которых не менял. Ветки складываются стопкой: интегратор вливает
`ui-mode-vocabulary` (пятой по порядку `lead`), затем эту. Ребейза и мержа
чужого никто не делает — `worktree-new.ps1` принимает `-Base`.

**Задача.** Вынести русские строки `Screen.tsx`, `panels.tsx`, `adapter.ts`,
`model.ts` в namespace'ы `i18n/locales/{ru,en}/`. Английский — заготовка;
вылизывание перевода после v1. Минимальная версия по рекомендации
организатора, с ней согласен: без этого каждая следующая правка экрана
углубляет долг.

**Замер на `e4179ca`** (мой, а не из отчёта): двухкавычечных литералов с
кириллицей в `client/src/game/**` — **171**: `Screen.tsx` 61, `panels.tsx` 40,
`adapter.ts` 31, `model.ts` 15, `GameShell.tsx` 0. Это верхняя граница: часть
попадает внутрь комментариев. Точное число исполнитель уточнит; цель — не
число, а тест.

**Архитектурное решение оркестратора: `t` впрыскивается в адаптер функцией.**
`adapter.ts:355` объявляет намерение — «адаптер словаря не знает и знать не
должен». Оно сохраняется: адаптер не импортирует i18n, а получает переводчик
в `ScreenModelInput`, ровно как уже получает `domainName: (id) => string` и
`renderLine` — оба и есть впрыснутые переводчики. Протаскивать тридцать строк
отдельными полями входа нельзя, это не контракт, а свалка.

**Следствие, которое нельзя пропустить.** `localeKeys.test.ts:76` узнаёт
переводчик ТОЛЬКО по объявлению `useTranslation("ns")`. Ключи, которые
адаптер попросит у впрыснутого `t`, для теста невидимы — то есть тридцать
новых ключей окажутся без защиты, ради которой этот тест и написан. Тест
обязан быть расширен в этой же задаче.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] Пользовательских русских литералов в `client/src/game/**` не осталось;
      `aria-label`, `title` и `placeholder` считаются пользовательскими
      строками — первые два читает скринридер.
- [ ] `TOME_NAMES` в `Screen.tsx` не существует: имя ТОМА берётся из одного
      места. Тот же класс, что закрыл Т-1, — два словаря об одном.
- [ ] Адаптер получает переводчик функцией через `ScreenModelInput`, i18n не
      импортирует; комментарий про «словаря не знает» уточнён, а не удалён.
- [ ] **`localeKeys.test.ts` расширен** и видит ключи, которые просит адаптер
      через впрыснутый `t`. Негативный контроль на это отдельный (ниже).
- [ ] **Новый тест держит класс дефекта**: русский литерал в
      `client/src/game/**` роняет `npm test` с указанием файла и строки.
      Существующий `localeKeys.test.ts` этого не ловит и никогда не ловил — он
      проверяет обратное свойство (см. Д-2), поэтому ссылка на него покрытием
      не считается. Тест обязан отличать литерал в коде от текста внутри
      комментария, иначе его придётся глушить исключениями и он умрёт.
- [ ] Оба словаря полны: наборы namespace в `ru` и `en` совпадают, пустых и
      заглушечных значений (`TODO`, дубль русского) в `en` нет.
- [ ] `cd client && npx tsc --noEmit -p tsconfig.app.json && npm test &&
      npm run lint && npm run build` — чистые. Render-тест Т-2 остаётся
      зелёным: он ищет кнопки и легенду по локализованным именам, и это
      правильный способ его сломать, если ключи разъедутся.
- [ ] **Негативный контроль 1:** вернуть одну строку литералом в JSX — новый
      тест обязан упасть с указанием файла и строки.
- [ ] **Негативный контроль 2:** удалить из `ru/*.json` один ключ, который
      просит АДАПТЕР — расширенный `localeKeys.test.ts` обязан упасть. Без
      этого контроля расширение теста недоказано, а тридцать ключей адаптера
      остаются без защиты.

**Вне scope.** Литературное качество EN; `proto/**` и `dev/**` — песочница и
dev-поверхности живут без словаря сознательно (`HexMap.tsx`, решение Т-1б),
там ~183 русские строки и они остаются; склонение имён — Т-4; строки
`client/src/map/**`; пороги и палитра. Отдельно: улучшать русские
формулировки по ходу нельзя — перенос и переписывание в одном коммите не
ревьюятся.

**Аудит 2026-08-09. ПРИНЯТО** (коммиты `06cd3b4` контрольный, `506994a`,
`c878c40`).

Оркестратор: `tsc` 0, `npm test` **102 теста в 6 файлах** (baseline базы 93/4,
прирост +9), `lint` 0, `build` 0, дерево чистое, `docs/TODO.md` не тронут, diff
20 файлов только в `client/**` и `docs/**`. Прочитан `gameLayerLiterals.test.ts`
целиком.

`report-auditor` воспроизвёл **все четыре** негативных контроля своими руками:
литерал в JSX роняет тест с файлом и строкой; удаление ключа, который просит
АДАПТЕР, роняет `localeKeys` (подтверждено вдвойне — в `adapter.ts` ноль
вхождений `useTranslation`, значит ключ найден только по ветке впрыснутого
переводчика, то есть расширение теста работает, а не декоративно); подмена
namespace даёт TS2322; ослабление типа в `Translator.ts` роняет
`translatorNamespace.test.ts`. Сверх задания он закрыл второй путь обхода
последнего контроля (ослабить тип И подогнать значения) — тогда молчит `tsc`, но
падает `npm test`. Обе лазейки перекрыты.

Сверх задания аудитор проверил отсутствие литералов **независимым методом**: не
тестом исполнителя, а прогоном восьми файлов через `esbuild --minify` (вырезает
комментарии, сохраняет строки) — ноль кириллических последовательностей. И
закрыл то, о чём не спрашивали: три `.css` в `src/game/` тест не сканирует, но
`content:` с текстом там нет.

**Дыра, найденная аудитором и не покрытая ничем в ветке.** Связь «файл словаря →
регистрация в `i18n/index.ts`» не проверена: он убрал строку `adapter: ruAdapter`
из `resources.ru` — **`tsc` 0, `npm test` 102/102 зелёные**, а игрок в русской
локали увидел бы сырые `stat.gdp` и остальные 35 подписей адаптера. Это ровно
тот дефект, против которого затевалась работа. Класс предсуществующий
(`index.ts` покрытия не имел никогда), поэтому регрессией не считается и приёмку
не блокирует — но ветка добавляет ТРИ новых namespace и целиком опирается на это
звено, значит закрывать надо сейчас → Т-14.

**Вывод для гейта приёмки, важный за пределами этой задачи.** Два новых
механизма держит `tsc`, а не `npm test`: исчерпывающие `Record` и
`translatorNamespace.test.ts` (под ослабленным типом тесты остаются зелёными).
Приёмка «прогнал `npm test` — принято» этот класс не увидит вовсе. Устав роли
(`.agent/roles/ui.md`) требует все три команды — правило есть, и здесь видно,
зачем именно.

**Ревью на обе локали не проводилось** — вердикт `ui-reviewer` к этой приёмке не
приложен, и это отклонение от устава, названное явно. Причина: EN глазами
недостижим (см. пункт 5 разбора карты), а ревью по словарю без вёрстки решает
меньше, чем стоит. Перенесено в Т-6.

### Т-4 Три непроверенные ветки и склонение имён

**Задача.** Закрыть Д-5 и Д-4: список отказов, `narrativeCanonized: false`,
`ErrorBoundary`, и 11 русских шаблонов, ставящих имя после предлога.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] Render-тест рисует `LlmCycleOutcome` с придуманным `result`, где есть
      отклонённые примитивы и `narrativeCanonized: false`; обе ветки видны в
      выводе. Живого отказа движка НЕ ждём — заявка «нужен прогон, где отказ
      случится» разобрана в Д-5 как неверная.
- [ ] Render-тест ловит краш ребёнка `ErrorBoundary` и видит текст из
      namespace `errorBoundary`.
- [ ] Все 11 шаблонов Д-4 переписаны так, что имя стоит в именительном
      падеже — через тире или после родового слова. Английские шаблоны не
      тронуты.
- [ ] Русский текст перечитан вслух на трёх реальных именах разного рода
      («Франция», «СССР», «Соединённые Штаты Америки»): согласование не
      ломается ни на одном.
- [ ] **Негативный контроль:** тест на ветки падает, если убрать рендер
      списка отказов.

**Вне scope.** Падежные формы в `shared/src/types` — замер показал, что не
нужны (Д-4); тексты `primitiveRejection`, кроме перечисленных шаблонов.

### Т-5 Правило ПРИБОРОВ — измеримое свойство вместо счётчика

**Задача.** Снять из `model.ts:195` и `docs/UI_DESIGN.md` §6 правило «ровно
пять» и записать то, что действительно должно держаться: **ряд ПРИБОРОВ не
переполняет ШАПКУ на поддерживаемой ширине** (Р-4: 1280 и 1920). «Всегда» без
теста запрещено `AGENTS.md`; тест на счётчик охранял бы не то, что ломается
(шесть узких приборов пройдут, пять широких — нет).

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] Формулировка правила в коде и в `UI_DESIGN.md` §6 совпадает и говорит о
      переполнении, а не о числе.
- [ ] Свойство держится замером: `scrollWidth` ряда ПРИБОРОВ против
      `clientWidth` контейнера на 1280 и 1920 (Т-6), либо тестом, если он
      честно мерит ширину.
- [ ] Расхождение «игра пять, песочница шесть» либо перестаёт быть дефектом
      (оба проходят замер), либо чинится — молча остаться не может.

**Вне scope.** Состав ПРИБОРОВ (какие показатели в ШАПКЕ) — это композиция,
решает оркестратор отдельно, не исполнитель.

### Т-6 Живой прогон и замер переполнения — приёмочный проход

**Задача.** То, что подделать нечем: настоящая MapLibre, настоящая раскладка,
настоящие длины строк в двух локалях.

**Критерии приёмки** (записаны 2026-08-09, до старта):

- [ ] Клиент и сервер поднимаются через `.claude/launch.json`
      (конфигурации `server` и `client`), партия 1946 открыта.
- [ ] Переключены ВСЕ режимы карты; для каждого показано, что иконка
      непустая, легенда соответствует Т-1, и раскраска регионов на карте
      реально меняется (незакрытый пункт «визуальный QA перекраски по
      mapmode», `docs/TODO.md` §UI-редизайн — он закрывается здесь).
- [ ] `scrollWidth` против `clientWidth` для ШАПКИ, ряда ПРИБОРОВ, ЛЕНТЫ,
      ЛИСТА, ПОЛОСЫ и сетки РЕЖИМОВ — на **1280×800 и 1920×1080**, в **обеих
      локалях**. Числа в отчёте, не «выглядит нормально».
- [ ] Четыре угла: ШАПКА, ХОД, ПОЛОСА, РЕЖИМЫ — ничто не перекрывает
      управление соседа.
- [ ] Стек наложений: ЯЩИК + РЕЕСТР + ПОИСК + МЕНЮ + ПОДТВЕРЖДЕНИЕ —
      снимает по одному сверху (`Esc`); ПОДТВЕРЖДЕНИЕ не оказывается под
      РЕЕСТРОМ.
- [ ] Состояния loading / empty / error пройдены все, включая ошибку
      загрузки (каталог состояний §12 признан неполным именно здесь).
- [ ] Вердикт субагента `ui-reviewer` приложен: он не видел замысла и
      смотрит результат как новый игрок.

**Вне scope.** Ширины ниже 1280 (Р-4); мобильная раскладка; пиксельные
эталоны.

**Уточнение 2026-08-09 — Т-6 заблокировано НЕ целиком, я был неточен.** Сказав
«Т-6 невозможно», я перенёс вывод про карту на всё задание. Разделяю по факту:

- **Работает в Browser pane:** DOM и вычисленные стили. Замеры `scrollWidth`
  против `clientWidth`, число обрезанных элементов, наличие `aria-*`, тексты в
  обеих локалях, переключение локали — всё это исполнители Т-1б и Т-3 реально
  сняли на 1280 и 1024. Значит пункты про переполнение, четыре угла и стек
  наложений выполнимы СЕЙЧАС, и Т-6 в этой части можно выдавать.
- **Не работает:** всё, что требует отрисованного кадра — MapLibre (нет
  размера и нет `requestAnimationFrame` при `document.hidden`), скриншоты,
  оценка типографики и различимости глифов глазами.

Устав роли при этом менять не надо: он уже требует помечать непроверенное в
браузере как `static-only` (`.agent/roles/ui.md`, пункт 2 приёмки). Правило
написано верно — неверно было моё «критерий невыполним».

### Т-7 Accessibility нового экрана

Не выдаётся до Т-3: клавиатурный проход и скринридер по экрану с зашитым
русским `aria-label` пришлось бы делать дважды. Критерии будут записаны при
выдаче; известное состояние — `UI_DESIGN.md` §9 («сплошного прохода по WCAG
нет, контраст мерялся точечно»).

---

## Вердикт по `docs/TODO.md` — ПРИМЕНЁН этой веткой 2026-08-09

**Гейт снят ролью `lead`** тем же днём: параллельный триаж разрешён по карте
разделов, потому что триаж преимущественно УДАЛЯЕТ строки — порог он лечит, а
не расходует, а разные области одного файла git сливает без конфликта.
Мои разделы: `:975` (UI-редизайн), `:1109` (контракты стыка), `:1124`
(Интерфейс) на `92c1aa9`.

Итог правки: **1393 → 1374 строки, дельта −19** (правило `lead` требует ≤ 0).
Хунки диффа лежат только в трёх моих диапазонах; `:1014` «Крупные системы»,
`:1046` «P4», `:1098` «Решения пользователя» и `:1155` «Карта/регионы» не
тронуты, шапка файла не тронута. Public eval после правки — 240/0.

**Проверено по коду, а не по тексту записи** (требование `lead`, и оно
сработало трижды):

- `stockpile`: замер 2026-08-09 — **157 стран, поля нет ни у одной.** Пункт
  жив, но он не мой (домен `world`); оставлен сжатым.
- Бюджет двумя формулами: `EconomyTick.ts:91` против `CountryService.ts:24` —
  расхождение на месте. Пункт жив, домен `logic`.
- Леджер уведомлений: в `client/src` **нет ни одного упоминания уведомлений** —
  колокольчик жил в удалённом Header. Формулировка пункта протухла целиком,
  отложенная идея уехала в `IDEAS.md`.

**Что не сделал и почему.** Формулировка «Прежние UI-линии заморожены, идёт
третья — пересборка с нуля» в «Постоянных рамках» устарела: третья линия влита.
Но это ШАПКА файла, а правило `lead` её трогать запрещает — возвращаю строкой
сюда: рамка должна читаться как «канонический слой —
`client/src/{game,ui,styles}`, прежние линии удалены, возврата нет».

**Своя ошибка, найденная и исправленная 2026-08-09.** Раздел про бюджет двумя
формулами я переписал как ОТКРЫТЫЙ, проверив код на своей базе `92c1aa9` — и
там он был открыт. Но `main` ушёл вперёд: `claude/budget-one-base` завела
`server/src/simulation/economy/budgetBase.ts` («единственное определение дохода
и базы росписи»), и теперь `EconomyTick.ts:83` и `CountryService.ts:32` оба
берут `disposableIncome(country)`. Дефект закрыт, а моя запись уже лежала в
`main` и утверждала обратное. Раздел снят: закрытие механически проверяемое —
оба вызова читаются в коде, — а такое снимает держатель раздела за любой домен.
Урок метода: **проверка на своей базе не есть проверка на `main`**; ветка
триажа живёт дольше одного влития, и перед сдачей факты надо переснимать
против свежего `main`, а не против собственной базы.

**Адресация разделов — по тексту заголовка, не по номеру строки** (правило
`lead`, наблюдение домена `logic`). Мой триаж этому уже соответствовал: все
правки делались подстановкой точного текста, а не по номерам, поэтому сдвиг
`main` на девять строк ничего не сломал — проверено по хунками диффа, чужие
разделы не тронуты.

**Что отдаю чужим доменам.** Пункт «Динамическая смена цвета в рантайме и
Режимы карты» (`:1253`, раздел «Карта/регионы» — домен `geometry`) устарел
наполовину: из четырёх названных ненаписанными режимов два существуют —
«Дипломатический/Блоки» это `relations`, «Экономический» это
`industry`/`resources`. Не написан только Идеологический. Сам не правлю: чужой
раздел.

**Конфликт при интеграции, предупреждение интегратору.** `docs/IDEAS.md`
дописан в конец и этой веткой, и `claude/ui-mode-vocabulary` — при влитии
второй из них будет конфликт append/append в конце файла. Решение
содержательного выбора не требует: **оставить оба блока.** Порядок `lead`
(`ui-mode-vocabulary` пятой, `ui-orchestrator` последней) означает, что
разрешать придётся мою.

### Подтверждено открытым

- ~~**Иконки и ЛЕГЕНДА режимов**~~ — **ЗАКРЫТО** веткой
  `claude/ui-mode-vocabulary` (`786c71d`), см. «Закрыто, снимается» ниже.
- **Не проверены две ветки окна ручного цикла** — открыто, но **формулировку
  исправить**: «нужен прогон, где отказ реально случится» неверно, обе ветки
  берутся render-тестом (Д-5).
- **`primitiveOutcome` не склоняет имена** — открыто; уточнить числом: 11
  шаблонов из 101, падежные формы в `shared` не нужны.
- **`ErrorBoundary` не проверен живым крашем** — открыто, дешевле тестом.
- **Accessibility нового экрана не проверялась** — открыто (Т-7).
- **Новый экран почти не локализован** — открыто; **убрать из пункта ссылку
  на `localeKeys.test.ts` как на защиту** (Д-2), она вводит в заблуждение.
- **Правило «ПРИБОРОВ ровно пять»** — открыто, но переформулировать по Т-5:
  развилка «тест или снять» ложная.
- **Визуальный QA перекраски карты по mapmode** (§UI-редизайн) — открыто,
  переезжает в критерии Т-6.
- **`1946 countries.json` не содержит `stockpile`** (§UI-редизайн) — открыто,
  но **не мой домен**: это данные сценария. Передать `world`.
- **`shortName.ru` США обрезан** (§Карта/регионы) — открыто, домен `world`;
  подтверждаю интерес интерфейса: короткое имя рисуется в ШАПКЕ, ярлыках
  событий и РЕЕСТРЕ, то есть дефект данных видно в трёх местах экрана.

### Закрыто, снимается

- **«Иконки и ЛЕГЕНДА режимов карты не работают в игре»** (§Интерфейс, строки
  1128–1133 на `92c1aa9`) — **закрыто** веткой `claude/ui-mode-vocabulary`
  (`d88ca5d` + `786c71d`), принято оркестратором после аудита. Удалить пункт
  целиком. Проверить, что после удаления фраза «Осталось от того списка только
  это:» выше не остаётся с висящим двоеточием — она станет вести к пункту про
  ПРИБОРОВ, и это верно.
- **«Динамическая смена цвета в рантайме и Режимы карты»** (§Карта/регионы →
  «Визуал карты», строки 1253–1257) — **устарело наполовину**, проверено
  чтением: из четырёх названных «ненаписанными» режимов два существуют —
  «Дипломатический/Блоки (относительно игрока)» это `blocs`, «Экономический
  (интенсивность по ВВП/ресурсам)» это `industry`/`resources`. Не написан
  только Идеологический. Пункт переписать в то, что действительно открыто:
  идеологический режим и цвет как НЕПРЕРЫВНАЯ функция состояния (сейчас
  раскраска дискретна по замыслу — три-четыре ведра). Находку принёс
  исполнитель Т-1, я её перепроверил.
- **«`docs/UI_DESIGN.md` ниже шапки описывает удалённый слой — переписать»** —
  закрыто. Проверено чтением: §6 описывает ШАПКУ/ЯЩИК/ЛЕНТУ/ЛИСТ/ПОЛОСУ/
  РЕЖИМЫ/РЕЕСТР нового слоя, §11 уже документирует Д-1, §12 честно перечисляет
  чего нет. Документ актуален.
- **«Новая композиция Header и плоских панелей (линия B), ждёт выбора
  канонической UI-линии»** (§UI-редизайн) — закрыто событиями: линия выбрана
  тем, что третий слой собран и влит, старый удалён.
  `docs/UI_LAYOUT_PROPOSAL.md` и `docs/UI_VISUALIZATION_PROMPT.md` описывают
  линию B и нормативными больше не являются.
- **«Mobile Header overflow (<600px)»** (§UI-редизайн) — снимается: этого
  Header больше не существует. Живой остаток факта («узкие окна не
  проектировались») уже записан в `UI_DESIGN.md` §12 и в Р-4, дубль в `TODO`
  не нужен.
- **Заголовок «UI-редизайн (весь трек заморожен)»** — заморозка снята
  событиями: трек не заморожен, он исполнен и продолжается этим реестром.
  Раздел после трёх снятий содержит только два пункта чужих доменов
  (`stockpile`, леджер) — их место в разделах этих доменов, а раздел
  удаляется целиком.
- **«Прежние UI-линии заморожены, идёт третья»** (§Постоянные рамки) —
  формулировка устарела: третья линия влита. Рамка должна остаться, но как
  «канонический слой — `client/src/{game,ui,styles}`, прежние линии удалены,
  возврата нет».

### В `docs/IDEAS.md`

- **Онбординг нового экрана** — после v1, решение пользователя 2026-08-09
  (Р-3), с причиной: тур привязан к раскладке, которая не устоялась; прецедент
  — `ui-onboarding-slice-4.md`.
- **Режим карты «Армии» по реальной линии фронта** — новая запись по Р-2:
  режим-заглушка снят 2026-08-09, вернётся, когда в данных появится фронт, а
  не оккупация. Вместе с ним — вопрос, откуда фронт берётся (домен `logic`).
- **Леджер уведомлений** (§UI-редизайн) — отложен решением пользователя
  2026-07-13, условие возврата описано. Это отложенная идея, а не backlog.
- **Раскладка для окон уже 1280** — новая запись по Р-4: объявленный предел
  v1, а не дефект.
- **Настоящий режим блоков** (НАТО/ОВД, чужие блоки между собой) — после
  переименования `blocs` → `relations` в Т-1б место свободно; данных о блоках
  раскраска сейчас не читает вовсе.
- **«Ресурсы» складывают разнородные залежи в одно число** — игрок видит
  «богато», но не узнаёт, чего именно. Ревью само называет это наблюдением, а
  не блокером v1; беру как есть, запись на будущее.
- **Плотность населения вместо абсолютного числа** (`population / area`) —
  если когда-нибудь понадобится настоящая плотность; в Т-1б чинится подписью,
  что дешевле и честно.

---

## Открытые вопросы пользователю

1. **Когда сдавать `claude/ui-mode-vocabulary` в очередь `integrator`.**
   Рекомендация изменилась после ревью: **не сейчас, а после Т-1б.** Раньше я
   рекомендовал сдавать сразу; ревью нашло в этой ветке невидимые образцы
   легенды и дыру в углу сетки — дефекты в работе, которой ещё нет в `main`, и
   вливать её с ними незачем. Т-1б идёт в той же ветке, потом одно влитие.
   Т-3 (локализация) трогает те же три файла и стартует от влитой базы, иначе
   разрешает конфликт в файлах, которых не менял, — то есть Т-3 ждёт этого
   влития. Само влитие в `main` инициирует пользователь: доменный оркестратор
   не мержит.

Р-2 и Р-3 закрыты решениями пользователя 2026-08-09, оба совпали с
рекомендацией оркестратора. Следующий вопрос появится, если приёмка Т-6
покажет, что раскладка не держит 1280 без изменения композиции — это уже
продуктовая развилка, а не вкусовая.
