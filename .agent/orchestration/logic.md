# Реестр logic

Формат — `.agent/orchestration/README.md`. Устав — `.agent/roles/logic.md`.
Сюда попадает только то, что оркестратор проверил сам; числа из чужих отчётов
живут в их отчётах.

## Состояние, установленное замером — 2026-08-09

Дерево `.claude/worktrees/logic-orchestrator`, ветка `claude/logic-orchestrator`
от `main` = `92c1aa9` (стартовый промт называл `9b71ead` — на момент сессии
`main` уже был другим; исходить из `92c1aa9`).

- **Серверный набор зелёный:** 95 файлов, **1445 passed, 1 skipped**, 94 с
  (`npx vitest run` в `server/`, прогон оркестратора). Skipped — единственный:
  живой вызов Gemini, гейт `describe.skipIf(!RUN_LIVE_LLM_TESTS ||
  !GEMINI_API_KEY)` (`server/src/llm/providers/__tests__/GeminiProvider.test.ts:232`).
- **Сейвов игрока нет.** В `server/data/saves/` лежат три файла
  `__test_*.json` от 2026-08-08 — артефакты теста, не партии.
- **Алфавит примитивов — 22 глагола** (`VERB_AGENCY`,
  `server/src/llm/primitiveAgency.ts`; `docs/PRIMITIVES.md` §2 «Состав (22
  глагола на 2026-08-02)»).

## Вердикты по пунктам, вынесенным домену

### L-1 Роспись бюджета считается от двух разных баз — ОТКРЫТО, и шире заявки

Проверено чтением кода:

| путь | база пяти статей | импорт в расходах |
|---|---|---|
| `simulation/economy/EconomyTick.ts:91` (тик) | `max(0, income − importSpending)` | да (`:109`) |
| `services/CountryService.ts:24` (`PUT /budget`) | полный `income` | **нет** (`:31`) |
| `commands/economy.ts:148` `totalIncomeOf` (правила ИИ) | полный `income` | — |

Третий путь в заявке не назван, а он того же класса: `applyDeficitAusterityCut`,
`applyAusterityRecoveryRaise`, `setMilitaryShare`, `moveShare` приводят суммы к
долям от ПОЛНОГО дохода, и собственные комментарии этих команд («между командой
и тиком состояние обязано быть согласованным») ровно этого и не обеспечивают.

**Развилка НЕ открыта — решение принято пользователем 2026-08-03** («учитывать
импорт, стартовые доли не нормировать»), записано `docs/decisions/2026-08.md`
§«2026-08-04 — Импорт вошёл в роспись», и клиент на эту базу уже переведён
(`client/src/game/adapter.ts:307`). Открыт не вопрос, а хвост применения: два
серверных пути записи не догнали решение. Спрашивать пользователя заново
незачем — это техническое выравнивание.

**Что видит игрок.** Расхождение баланса алгебраически равно `(1 − S) ×
importSpending`, где `S` — сумма пяти долей (0,849 на старте): показанный после
сохранения росписи баланс расходится с тем, который посчитает тик. Симуляцию это
НЕ искажает: `economyTick` идёт первым в ходе (`SimulationEngine.ts:41`) и
переписывает суммы до любого потребителя. Дефект — в числах, показанных игроку и
записанных в сейв между сохранением росписи и ходом.

**Почему не поймано тестами.** `CountryService.test.ts` фиксирует именно старую
формулу (`:39-44`, `:65`) на фикстуре с нулевым импортом — на такой фикстуре обе
базы совпадают. Это тот самый «тест на снимке», о котором предупреждает устав.

### L-2 `damageExtraction` — ОТКРЫТО, мёртвый экспорт (доки не врут)

`server/src/commands/resources.ts:91`. Вызовов в продакшене ноль (весь репозиторий:
только `commands/__tests__/resources.test.ts` и упоминания в доках). Заявка
«подано в доках как отсутствующая фича» **не подтвердилась**: `docs/ECONOMY.md:187`,
`docs/ARCHITECTURE.md:163` и комментарий `resources.ts:88` прямо говорят «не
подключена к `WarTick.ts`». Дефект настоящий (мёртвый код), но честный.
Решение «подключить или снять» — ниже, в вопросах пользователю.

### L-3 `SAVE_VERSION` 13 → 14 — ЗАКРЫТО, цена нулевая

`shared/src/types/SaveFile.ts:93` = 14; `SaveService.ts:67` отклоняет чужую
версию. Решение записано (`docs/DECISIONS.md:575`). Сейвов игрока не существует
(см. «Состояние»), поэтому ломать нечего. Обязательство роли — сказать
пользователю прямым текстом — исполнено в отчёте сессии 2026-08-09.

### L-4 Датированное событие не всегда есть в прозе — ОТКРЫТО, решение продуктовое

Числа перепроверены по журналу замера, не по пересказу
(`.agent/runs/dated-events-2026-08-08/README.md`): 219 событий, 72 хода из 72 с
событием, перекрытие грубой метрикой 185/219 (84 %), подтверждённый случай —
«Всеобщие выборы в Японии» 1947-04. Отказов `datedEventInvalid` ноль, промт
+3,0 %. Вреда нет, факт верный. Ужесточать промт или нет — вопрос пользователю;
правка со своим замером, исполнитель — `director-prompt-engineer`.

### L-5 Живой гейтованный тест Gemini — НЕ ЗАДАЧА ДОМЕНА

Это ручной прогон при живой квоте, а не открытый пункт:

```text
RUN_LIVE_LLM_TESTS=1 npx vitest run src/llm/providers   (cwd: server/)
```

Требует `GEMINI_API_KEY` и тратит квоту — запускает пользователь.

### L-6 Милстоун 1: «главный блокер» роадмапа устарел

`docs/plans/11_MVP_ROADMAP.md:105` называет «детализацию алфавита примитивов +
контракт валидатора» главным блокером М1, требующим отдельной дизайн-сессии.
По коду и `docs/PRIMITIVES.md` эта работа **сделана** и не одной сессией:
22 глагола под движковым валидатором (предпосылки / магнитуда из коридора /
палитра эффектов), атомарный батч с откатом и пост-инвариантами
(`PrimitiveEngine.applyPrimitiveBatch`), структурные коды отказов
(`primitives/rejections.ts`), сверка результата с дифом, полный словарь границы
агентности по типу `Record<PrimitiveVerb, …>`, канал `actions` удалён 2026-08-02.
Открыт не дизайн алфавита, а **калибровка его чисел** (капы, коридоры — пункты
`docs/TODO.md`) — работа другого класса и другого размера.

Следствие: в М1 остаётся **один** содержательный пункт — канал «предложение
игроку» (`player_proposed`). Сегодня граница агентности закрыта ОТКАЗОМ:
`splitByAgency` выбрасывает акт государственной политики, применённый за игрока,
и отдаёт его как `RejectedPrimitive` с кодом `agencyPlayerDecision`
(`server/src/llm/primitiveAgency.ts:185`). Предложить игроку то же действие
режиссёр не может — канала нет.

**Попутный docs drift в каноне домена:** `docs/PRIMITIVES.md:71` пишет
«Реализовано на сегодня ВОСЕМНАДЦАТЬ глаголов», а `:125` — «Состав (22 глагола
на 2026-08-02)». Один документ, два числа.

## Триаж `docs/TODO.md` — вердикт домена

Гейт lead действует: **в `docs/TODO.md` эта сессия не пишет** (1393 из 1400
строк). Ниже — список для той ветки, которая будет применять правки.

### Снять (проверено, утверждение не подтвердилось или неактуально)

- **`aggregateCountryFromRegions` «не присваивает результат никуда»**
  (`TODO.md:336`) — утверждение неверно: функция `void`, мутирует страну на
  месте, шесть боевых вызовов (`PrimitiveEngine.ts:2762-2763`,
  `polityLifecycle.ts:510,513,661`, `aggregateCountryData.ts:136`). Присваивать
  нечего.
- **Живой гейтованный тест Gemini** как пункт домена — см. L-5.

### Оставить открытым (проверено кодом в этой сессии)

- **Две базы росписи** (`TODO.md:322-329`) — L-1; формулировку дополнить третьим
  путём (`commands/economy.ts::totalIncomeOf`).
- **`damageExtraction` — мёртвый экспорт** (`TODO.md:138`) — L-2; убрать из
  формулировки «подано в доках как отсутствующая фича»: доки честны.
- **`Country.aiTraits` осиротели** (`TODO.md:228`) — подтверждено: читателей нет
  ни одного, только посев (`CreateGame.ts:174`), копирование при расколе
  (`polityLifecycle.ts:460`) и фикстуры. `AiBehaviorTick.ts` их не читает.
- **`calculateDiplomaticTension` без потребителя** (`TODO.md:335`) — подтверждено:
  `DiplomacyService.ts:302` + собственный тест, боевых вызовов ноль.
- **Три вида санкций из четырёх репутационные** (`TODO.md:670`) — подтверждено:
  торговлю читает только `trade_embargo`; `economic_/military_/diplomatic_
  sanctions` живут в состоянии и в тестах.
- **`alliedWith` сравнивает с ПЛОСКИМ порогом** (`TODO.md:462`) — подтверждено:
  `PrimitiveEngine.ts:375` берёт `ALLY_RELATION_THRESHOLD = 70`
  (`shared/src/defines/diplomacy.ts:89`), тогда как союзнический уровень с
  2026-07-28 свой у каждой пары (`allianceThreshold`, `simulation/diplomacy/
  affinity.ts:153`). Формулировку «порога 70 больше нет» уточнить: константа
  жива, устарела её плоскость.
- **`MAX_PRIMITIVES_PER_TARGET_PER_TURN = 1` — плейсхолдер** (`TODO.md:384`) —
  подтверждено: `shared/src/defines/discontent.ts:454`, читается движком
  (`PrimitiveEngine.ts:3609`) и текстом контракта в промте
  (`llm/primitiveContract.ts:265`). Калибровка на живых прогонах не делалась.

### Docs drift (правка доков, не кода)

- `developSectors` / `investInSector` названы «мёртвым кодом» в `CONCEPT.md:228`
  и `11_MVP_ROADMAP.md:42,133` — **их не существует**: ноль вхождений в `*.ts` по
  всему репозиторию. Убрать упоминания, а не «оживлять».
- `docs/PRIMITIVES.md:71` против `:125` — восемнадцать против двадцати двух
  глаголов в одном документе.
- `11_MVP_ROADMAP.md:105` — «главный блокер М1» устарел (L-6).

### Структурное предложение (не пункт, а причина переполнения)

Шесть разделов «хвосты сессии …» (`TODO.md:548`, `:579`, `:638`, `:665`, `:708`,
`:736`) — **журнал сделанного, а не backlog**: почти каждый пункт заканчивается
словами «решено НЕ чинить сейчас», «отложено осознанно», «решать вместе с
механикой, которой ещё нет». Это записи решений, и их место —
`docs/decisions/2026-08.md`, а горизонт «после v1» — `docs/IDEAS.md`. Перенос
этих шести разделов целиком возвращает `TODO.md` запас строк, ради которого
стоит гейт, и не теряет ни одного факта.

Не переносить (остаётся backlog'ом): пункты, названные выше «оставить
открытым», и всё, что блокирует М1–М3.

**Пометка честности.** Разделы «Реанимация симуляции» и «Милстоун 0 — хвосты»
прочитаны целиком, но кодом в этой сессии проверены только перечисленные выше
пункты. Остальные — `UNKNOWN`: их формулировки в TODO уже несут собственные
даты и числа, и перепроверка каждого — отдельная read-only работа.

## Задания

Спецификации написаны ДО старта исполнителей (`orchestration/README.md`).
Строка `Статус:` появляется в момент выдачи — приоритет утверждает пользователь.

### T-1 Одна база бюджетной росписи на всех путях записи
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/budget-one-base` (`88a25a2`)
Выдано: 2026-08-09 (приоритет утверждён пользователем — В-1)

**Задача.** Свести три пути, пишущих `*Spending`, к базе движка
(`disposableIncome = max(0, income − importSpending)`), чтобы игрок,
сохранивший роспись, видел те же числа, что посчитает ход. Решение о самой базе
принято пользователем 2026-08-03 — задача его применяет, а не пересматривает.

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] `income` и `disposableIncome` считаются ОДНОЙ функцией, которую вызывают
      `EconomyTick.updateBudget`, `CountryService.updateBudget` и
      `commands/economy.ts::totalIncomeOf`; второго определения формулы в
      `server/src` не остаётся (показать `rg`-выдачей).
- [ ] `CountryService.updateBudget` включает `importSpending` в `expenses` —
      после сохранения росписи `budgetBalance` совпадает с тем, что даст
      следующий `economyTick` при неизменном состоянии (тест сравнивает два
      числа, а не проверяет каждое по отдельности).
- [ ] Негативный контроль: новый тест на фикстуре с `importSpending > 0` падает
      при возврате любого из трёх путей на полный `income`. В отчёте показан
      падающий прогон. Существующие ожидания `CountryService.test.ts:39-44,65`
      сняты на фикстуре с нулевым импортом и обе формулы не различают — их
      правка обязательна и не считается покрытием.
- [ ] `server: npx tsc --noEmit -p tsconfig.json` и `server: npm test` зелёные;
      база сравнения — 1445 passed / 1 skipped (замер оркестратора 2026-08-09).
- [ ] `docs/ECONOMY.md` описывает базу росписи одним абзацем для ВСЕХ путей
      записи, а не только для тика.

**Вне scope.** Величина долей, нормировка стартовых долей, отдельная строка
«импорт» в интерфейсе, `client/**`, `shared/src/types/**` (менять типы не
требуется — задача не трогает форму состояния и `SAVE_VERSION`).

**Аудит — 2026-08-09, оркестратор (не субагент), по diff и своим прогонам.**

Перезапущено мной в дереве исполнителя, числа мои:

- `npx tsc --noEmit -p tsconfig.json` — exit 0;
- `npx vitest run` — 95 файлов, **1450 passed / 1 skipped**. База 1445/1 плюс
  пять новых тестов (три в `commands/__tests__/economy.test.ts`, два в
  `services/__tests__/CountryService.test.ts`) — сходится ровно;
- `python .agent/evals/public/run_public_evals.py` — 240/240.

Проверено чтением diff:

- формула действительно одна — `simulation/economy/budgetBase.ts`
  (`grossIncome`, `disposableIncome`); `rg` по `server/src` не находит второго
  определения в боевом коде (совпадения остались только в тестах, где сумма
  дохода выписана как ожидаемое значение, — это правильно);
- `CountryService.updateBudget` включает `importSpending` в `expenses`;
- фикстуры новых тестов идут с НЕНУЛЕВЫМ импортом — на нулевом обе формулы
  совпадают и тест ничего не различает;
- решающий тест сравнивает два числа (показанное игроку и посчитанное
  `economyTick`), а не проверяет каждое по отдельности — это свойство, а не
  снимок;
- `shared/src/types` и `SAVE_VERSION` не тронуты, доли и их нормировка не
  тронуты.

**Расхождения с заданием — оба в пользу задания, приняты:**

1. Сняты ещё две копии той же формулы дохода — в `AiBehaviorTick.ts` и
   `PoliticsTick.ts`. Формально это шире трёх названных путей, по существу —
   ровно критерий «второго определения не остаётся». Поведение не меняется:
   `grossIncome` тождественна снятым выражениям.
2. Найдена **четвёртая база**, и исполнитель её НЕ тронул: посев
   `CreateGame.ts:50` считает стартовые суммы от `domesticIncome` (дохода без
   экспорта) — сознательное решение 2026-07-30 с собственным замером. Проверено
   мной по коду и комментарию на месте: правка там была бы пересмотром чужого
   решения, а не выравниванием. Записано в `docs/IDEAS.md` как наблюдение.

**Негативный контроль — принят с оговоркой о методе.** Автор заявляет прогон по
всем трём путям (`docs/DECISIONS.md`, запись 2026-08-09), падающего вывода в
отчёте нет. Переиграть его я не могу: у роли нет рук, а восстановление старой
формулы — правка кода. Проверил вывод по самому тесту: при старой формуле
сравниваемые числа расходятся на `(1 − Σдолей) × importSpending`, в фикстуре это
`0,5 × 30 млрд ≠ 0` при точном `toBe` — тест обязан падать. Это вывод, а не
прогон, и назван таковым.

**Метод на будущее:** исполнитель обязан класть падающий вывод негативного
контроля В ОТЧЁТ текстом. Оркестратор без рук воспроизвести его не может, и
«прогнан» на слово — ровно то, против чего заведён реестр.

### T-2 Снять `damageExtraction`
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/drop-damage-extraction`
(`be01cfd`, `0f2e86d`)
Выдано: 2026-08-09 (решение пользователя — В-2)

**Задача.** Убрать мёртвый экспорт: функция существует, не вызывается ниоткуда и
обрастает тестами, создавая впечатление работающей фичи. Разрушение добывающих
мощностей войной заводится вместе с War Phase, когда появятся числа для
калибровки, — а не «оставим, вдруг пригодится».

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] `damageExtraction` удалена из `server/src/commands/resources.ts` вместе с
      блоком тестов `commands/__tests__/resources.test.ts:115-137`; `rg
      damageExtraction server client shared` не даёт ни одного вхождения в коде.
- [ ] Упоминания сняты из `docs/ECONOMY.md:184,187` и `docs/ARCHITECTURE.md:158,163`
      — сегодня они описывают её как существующую, но не подключённую.
- [ ] В `docs/IDEAS.md` — запись «разрушение добывающих мощностей войной», с
      горизонтом «вместе с War Phase» и ссылкой на удалённую реализацию по
      git-хэшу: возвращать придётся с числами, а не с этим кодом.
- [ ] `buildExtraction` с `delta < 0` (снос) продолжает работать — его тесты
      зелёные и не тронуты: снос мощностей игроком остаётся, уходит только
      безусловное разрушение.
- [ ] `server: npx tsc --noEmit -p tsconfig.json` и `server: npm test` зелёные;
      база сравнения — 1445 passed / 1 skipped минус удалённые три теста.

**Вне scope.** Стартовый уровень мощностей (задание домена `world` от влитой
базы карты), `MAX_EXTRACTION_LEVEL`, `WarTick.ts`, любая новая механика войны.

**Негативного контроля нет и не требуется:** задача удаляет поведение, а не
вводит его. Доказательство — отсутствие вхождений и зелёный набор.

**Аудит — 2026-08-09, оркестратор, по diff и своим прогонам.**

- `npx tsc --noEmit -p tsconfig.json` — exit 0 (важно: после удаления функции
  не осталось висящих импортов);
- `npx vitest run` — 95 файлов, **1442 passed / 1 skipped** = база 1445 минус
  три удалённых теста. Файл `resources.test.ts` жив: тесты `buildExtraction`,
  включая снос при `delta<0`, зелёные;
- `python .agent/evals/public/run_public_evals.py` — 240/240;
- `rg damageExtraction` по `server`/`client`/`shared` — ноль вхождений;
- `docs/ECONOMY.md` и `docs/ARCHITECTURE.md` больше не описывают несуществующий
  канал войны, а прямо говорят, что разрушения мощностей войной СЕГОДНЯ НЕТ;
- запись `docs/IDEAS.md` несёт полный хэш удаляющего коммита и список вопросов,
  на которые придётся ответить при возврате, — восстанавливать будут механику,
  а не четыре строки.

**Остаток, закрываемый не этой веткой:** `docs/TODO.md:133-141` и `:1073`
по-прежнему говорят о `damageExtraction` как об открытом выборе. Ветка не
трогала `TODO` по действующему гейту и назвала это в своей записи решений —
поведение верное. Пункты уходят в T-4.

### T-3 Канал «предложение игроку» — ЗАКРЫТО ОТСРОЧКОЙ, дизайн-сессия 2026-08-09

**Итог: работы не будет — решение пользователя 2026-08-09 «все предложения
после v1, не уверен, что они будут нужны».** Заданий не выдано ни одного, это
правильный исход дизайн-сессии, а не её провал: развилка разобрана до формы, и
цена формы оказалась выше уверенности в надобности.

Конструкция, до которой договорились, и отвергнутое с причинами — в
`docs/IDEAS.md` (2026-08-09); что это значит для §7.2 и Милстоуна 1 —
`docs/DECISIONS.md` (2026-08-09). Здесь ниже остаётся ход рассуждения, потому
что при возврате переигрывать будут именно его.

**Как менялась рекомендация по ходу сессии — на будущее.** Я предлагал вариант
без хранения (предложение живёт в ответе цикла) и ошибся в цене: партия лежит
в оперативной памяти сервера, клиент держит только запрошенное, поэтому
предложение терялось бы при обычном F5, а не только при выходе из игры. Довод
«зато не растёт `SAVE_VERSION`» тоже оказался пустым — сейвов игрока нет,
цена версии сегодня нулевая, я сам измерил это в начале сессии и не связал с
развилкой. Пользователь при этом предложил форму лучше обеих моих
(блокирующий ивент), и она сняла возражение по §7.2 целиком.

#### Разбор (сохранён для возврата)

Закрывает последний открытый пункт Милстоуна 1. Ниже — установленный факт,
варианты и рекомендация; спецификация исполнителю пишется ПОСЛЕ ответов
пользователя (В-4…В-6).

#### Факт, от которого зависит вся конструкция

**Ход — одна кнопка, и внутри него у игрока нет второй точки входа.**
`client/src/game/GameShell.tsx:237-260`, последовательность жёсткая:

1. приказы игрока → `POST /primitives/apply` (по одному, сверху вниз);
2. нераспознанный текст → `POST /player-intent`;
3. `POST /llm/auto` — ответ режиссёра (`LLMService.runAutoCycle`);
4. `POST /game/next-turn` — тик симуляции.

Режиссёр отвечает на шаге 3, то есть **после** того, как игрок отдал приказы, и
**до** тика. Отсюда следствие, которое снимает целый класс вариантов: предложение,
родившееся на шаге 3, в этом же ходу принять НЕКОМУ. Оно адресовано следующему
ходу — значит должно как-то дожить до него. «Показать модальное окно посреди
хода» — это не правка канала, а переделка структуры хода.

**Приёмная половина канала уже построена, и это главное.** Путь «показали
распознанное → игрок подтвердил → движок применил» существует целиком:
`POST /primitives/translate` возвращает примитивы и `preview` без величин,
`POST /primitives/apply` применяет подтверждённое через ту же границу хода
(`turnBatch`), с бюджетом, квитанцией и кодами отказа. `/apply` уже требует
`sourceCountryId === playerCountryId` — а у предложения источник ровно такой.
**Предложение — это примитив, попавший в панель приказов не от перевода, а от
режиссёра.** Новой механики применения не нужно вовсе.

**Точка рождения — одна.** `splitByAgency`
(`server/src/llm/primitiveAgency.ts:185`) сегодня возвращает `{allowed, refused}`;
предложению нужна третья корзина. Словарь `VERB_AGENCY` не меняется: он уже
делит алфавит ровно по нужной границе.

**Бюджет хода вопросом не является:** «ответ модели и приказ игрока тратят ОДИН
бюджет» — действующее решение (`docs/PRIMITIVES.md` §«…SAVE_VERSION 5→6»).
Принятое предложение тратит бюджет как любой приказ, иначе режиссёр становится
способом обойти кап.

#### Варианты

**А — предложение живёт в ответе цикла, состояния не трогает.**
`splitByAgency` отдаёт третью корзину, она едет в `LlmCycleResult`, клиент
кладёт её в лист приказов следующего хода как готовую строку с кнопкой.
Принятие — существующий `/apply`. `SAVE_VERSION` не растёт, серверного
состояния не прибавляется.
*Цена:* предложение живёт в памяти клиента. Закрыл игру между ходами — оффер
исчез. Истории «что предлагали и что игрок ответил» не существует, и режиссёр
в следующем промте не узнаёт судьбу своего предложения.

**Б — предложение становится объектом состояния.** `GameState.pendingProposals`
(или список в `ResponseReceipt`): примитив, автор-ответ, срок жизни, исход
(`accepted`/`declined`/`expired`). Переживает сохранение, попадает в промт
следующего хода как обратная связь.
*Цена:* `SAVE_VERSION` 14 → 15 (старые сейвы отклоняются — сегодня их нет,
цена нулевая, но правило есть правило); новый тип в `shared/src/types`, то
есть очередь на них; срок жизни и правило истечения — ещё одна калибровка;
работа примерно вдвое больше.
*Что даёт:* честную петлю. Режиссёр видит «предложил уступку — игрок отказался»
и в следующем месяце может это отыграть. Сегодня он не видит НИЧЕГО: его
предложение просто исчезает.

**В — канала не заводить.** Оставить отказ, а «предложение» держать прозой:
режиссёр описывает возможность словами, игрок сам набирает приказ.
*Цена:* работает уже сегодня, ноль кода. Но §7.2 просит канал, а не намёк;
и текст, не связанный с примитивом, теряет гарантию «движок сделает ровно то,
что показано».

#### Рекомендация — А, с одной оговоркой

**Беру А.** Причина не в экономии: приёмная половина канала уже есть, и А
достраивает недостающую четверть, а Б добавляет к ней новый объект состояния,
срок жизни и правило истечения — три калибровочных решения там, где ни одного
живого замера нет. Проект уже носит несколько плейсхолдеров, поставленных
именно так; четвёртый не нужен.

Оговорка про потерю оффера при закрытии игры — реальная, но её цена ровно один
ход, и она снимается тем же способом, что и всё прочее в проекте: если на живой
партии окажется, что предложения теряются заметно часто, А достраивается до Б
без переделки — корзина уже есть, к ней добавляется хранение.

**Что заставит меня передумать:** ответ «да» на В-6 (режиссёр должен узнавать
исход). Обратная связь модели требует хранения исхода, а это уже Б — и тогда
делать А первым шагом значит делать работу дважды.

#### Пересечение доменов — назвать ДО задания

Канал кросс-доменный: серверная половина моя, показ игроку — домен `ui`.
Контракт стыка (что именно едет в `LlmCycleResult`, как называются поля, кто их
владелец) обязан пройти через `lead` ДО выдачи заданий обоим доменам. Вариант Б
дополнительно занимает `shared/src/types` — очередь на них веду я, и на время
задачи она перестаёт быть пустой.

Промтовая часть — отдельная работа `director-prompt-engineer` со своим замером:
модель должна ПРЕДЛАГАТЬ («советники настаивают на уступке»), а не рассказывать
о свершившемся («власти пошли на уступку»). Иначе проза будет утверждать то,
от чего игрок отказался, — известный дефект (`splitByAgency`, JSDoc «цена
названа прямо»), который канал предложений делает заметнее.

### T-4 Применение вердиктов домена к `docs/TODO.md` — одной веткой
Статус: выдано
Исполнитель: отдельная сессия, ветка `claude/todo-verdicts-logic`
Выдано: 2026-08-09

**Задача.** Применить раздел «Триаж `docs/TODO.md`» этого реестра. Отдельной
веткой — потому что четыре домена одновременно упираются в один файл на пороге
1393/1400, и гейт `lead` требует, чтобы правил его ОДИН исполнитель.

**Критерии приёмки** (записаны 2026-08-09):
- [ ] Снят пункт про `aggregateCountryFromRegions` (утверждение опровергнуто —
      см. триаж), сняты строки про `damageExtraction` (`TODO.md:133-141`
      частично и `:1073`) — их закрыла T-2.
- [ ] Уточнены формулировки трёх пунктов: `alliedWith` («устарела плоскость
      порога, а не сам порог»), `damageExtraction` (доки не врали), две базы
      росписи — снять целиком, закрыто T-1.
- [ ] Docs drift исправлен: `developSectors`/`investInSector` убраны из
      `CONCEPT.md:228` и `11_MVP_ROADMAP.md:42,133` (кода никогда не было);
      `PRIMITIVES.md:71` приведён к `:125` (22 глагола, не 18);
      `11_MVP_ROADMAP.md:105` — «главный блокер М1» снят с доказательством
      (см. L-6), М1 остаётся с одним открытым пунктом.
- [ ] Шесть разделов «хвосты сессии …» перенесены в `docs/decisions/2026-08.md`
      (решения) и `docs/IDEAS.md` (горизонт «после v1») ЦЕЛИКОМ, без потери
      формулировок; в `TODO.md` остаётся только то, что названо в триаже
      «оставить открытым» и что блокирует М1–М3.
- [ ] `python .agent/evals/public/run_public_evals.py` — 240/240, и `TODO.md`
      уходит от порога с запасом (назвать число строк до и после).
- [ ] Ни одной правки кода: `git diff --stat` показывает только `docs/`.

**Вне scope.** Разделы TODO чужих доменов (`ui`, `geometry`, `world`),
поднятие порога в `run_public_evals.py`, любая правка `server/`, `client/`,
`shared/`.

#### Дополнено 2026-08-09 по карте разделов от `lead` (исполнитель ещё не начал)

Дерево `claude/todo-verdicts-logic` заведено и стоит на базе `92c1aa9` без
коммитов — критерии дополняются ДО старта, а не в процессе.

**Разделы этой ветки и только они. Ищи по ЗАГОЛОВКУ, а не по номеру строки.**

Номера в карте `lead` сняты на `92c1aa9`, а `claude/budget-one-base` уже влита
(`main` = `27e3229`) и сняла девять строк в разделе «Реанимация симуляции».
Перемерено мной на влитой базе 2026-08-09 — сместилось всё, кроме первого:

```text
:98  Реанимация симуляции          :656 Дипломатический блок алфавита
:329 Милстоун 0 — хвосты           :699 Канал actions → алфавит
:471 Статусы суверенитета          :727 Мягкие глаголы алфавита
:483 Недовольство: хвосты          :786 Что придётся менять в Милстоуне 1
:510 Подтверждение необратимого    :859 LLM-цикл
:539 Милстоун 1 — хвосты           :879 Приёмы из разбора Open-Historia
:570 Жизненный цикл государств
:629 Формы правления
```

Эти числа тоже устареют при следующем влитии. **Номер строки — не адрес
раздела, а его сегодняшнее положение**; адрес — текст заголовка.

Размер файла на влитой базе: **1384 строки** (порог 1400).

Не трогать вообще: шапку файла, разделы `ui` (:975, :1109, :1124),
`geometry` (:1155 и вложенные), а также ничьи в этот заход — :1014 «Крупные
системы», :1046 «P4 — Инженерный долг», :1098 «Решения пользователя
2026-08-03». Владельца последним трём назначает `lead`.

**Три правила гейта (от `lead`, обязательны):**
1. только перечисленные разделы;
2. порог в `run_public_evals.py` не поднимается ни при каких обстоятельствах;
3. **итоговая дельта ветки по `docs/TODO.md` — не больше нуля.** Триаж,
   добавивший строк, триажем не является. Пункт, которому нужно разъяснение
   длиннее снятого, — заявка в `DECISIONS`/`IDEAS`, не строка backlog'а.

**Добавлено к списку правок:**

- **Пункт про перекрытие 84 % (`TODO.md:157-163`) — снять:** закрыт решением
  пользователя 2026-08-09 (В-3, промт не ужесточать; вред не измерен, факты
  верные). В `main` он ещё открыт — решение существует только в этом реестре и
  приезжает в репозиторий вместе с этой веткой.
- **Шесть пунктов LLM-области, перешедших домену вместе с `server/src/llm/**`**
  (:119, :124, :312, :344, :351, :721) — остаются открытыми, формулировки не
  трогать; они уже точные.
- **Четыре пункта, которые я отказался брать в работу 2026-08-09**, оформить
  как решения, а не как молчание: три репутационные санкции (фича Милстоуна 4,
  не долг) — в `IDEAS`; калибровка `MAX_PRIMITIVES_PER_TARGET_PER_TURN` (нет
  повода) — остаётся строкой; второй вызов модели (решение про деньги и
  задержку, за пользователем) — в `IDEAS` с этой формулировкой;
  `spawn_incident` без источника — остаётся, но с пометкой «сначала длинный
  замер, потом правка».
- **Закрытое заданиями T-1/T-2** снять целиком: две базы росписи (`:322-329`),
  `damageExtraction` (`:133-141` частично и `:1073`).

**Раздел «LLM-цикл» смешанный — и это ограничение, а не повод.** Из его пунктов
мои только рост `eventHistory` и миграция `Country.name` → `LocalizedText` в
части типов. «Смена названия страны на карте + Map Feature» — `ui`/`geometry`,
«каталог исторических развилок» и содержательная часть мультиязычности —
`world`. Раздел взят целиком только чтобы два исполнителя не редактировали одни
строки, а не ради права решать за соседей.

**Что можно делать с чужим пунктом** (правило `lead` от 2026-08-09, принято):

- **снять ЗАКРЫТЫЙ — это факт, а не суждение**, и держатель раздела делает это
  независимо от того, чей пункт. Но факт обязан быть ПОКАЗАН прогоном по коду
  или данным, в отчёте, с командой;
- **переклассифицировать ОТКРЫТЫЙ** (в `IDEAS`, привязать к милстоуну, удалить
  как наблюдение) — решение чужого домена. Держатель его не принимает, а
  помечает в вердикте, кому адресовано.

**Граница этого правила, и её надо держать.** «Закрыт» бывает двух родов.
Механически проверяемое — «создаёт ли LLM-канал объекты карты», «остался ли
`Country.name` строкой» — проверяется прогоном, и тогда снятие законно. А
«достаточно ли в каталоге исторических развилок записей» проверкой не
устанавливается вовсе: это суждение домена о полноте наполнения. Такой пункт
остаётся, даже если кажется закрытым. **Рассуждение доказательством закрытия не
является** — иначе правило «снимай факты» превращается в «снимай то, что счёл
неактуальным», а это ровно то, против чего заведён гейт.

## Очередь на интеграцию (не мой мерж — `integrator`)

- `claude/budget-one-base` — принято 2026-08-09, **влито** (`main` = `27e3229`).
- `claude/drop-damage-extraction` — принято 2026-08-09.
- `claude/diplomacy-debts` (`f25aa93`) — принято 2026-08-09.
- `claude/schema-rejection-field` (`fb13b81`) — принято 2026-08-09.
- `claude/ai-traits-consumer` (`bec8ca5`) — принято 2026-08-09.
- `claude/prompt-prefix-cache` — **возвращено**, в очередь не идёт.

### Порядок влития и базы — измерено 2026-08-09

Отставание и базы сняты мной (`git merge-base`, `rev-list --count`), а не взяты
из сводки:

```text
diplomacy-debts        отстаёт 54   база 92c1aa9
schema-rejection-field отстаёт 54   база 92c1aa9
prompt-prefix-cache    отстаёт 54   база 92c1aa9   (возвращена)
ai-traits-consumer     отстаёт 39   база ce74d66 — уже содержит budget-one-base
                                     и drop-damage-extraction
```

**Пересечение с `main` по КОДУ у первых двух — ноль.** Единственный общий файл —
`docs/DECISIONS.md`, куда дописывают все. То есть обновление базы для них
механическое, а не смысловое.

**Пересекаются они друг с другом, а не с `main`** — четыре файла:
`server/src/primitives/PrimitiveEngine.ts`, его тест, `docs/PRIMITIVES.md`,
`docs/DECISIONS.md`. Правки в разных местах движка (кап записи об отказе против
предпосылки `border_dispute`), поэтому git сольёт их «чисто» — и именно поэтому
порядок назначаю явно:

1. **`claude/schema-rejection-field` первой.** Радиус шире: она трогает
   `shared/src/defines/discontent.ts` и путь отказа, общий для всех глаголов.
2. **`claude/diplomacy-debts` второй** — обновляет базу от влитой первой и
   **прогоняет `server: npm test` заново у себя**, а не переносит мои числа.
3. `claude/ai-traits-consumer` — вне этой пары: по коду с ними не
   пересекается (`AiBehaviorTick` против `PrimitiveEngine`), база свежее.
   Порядок относительно них безразличен.

**Поправка к моему же вердикту T-8.** Я написал «база ветки 1445/1 плюс восемь
новых». База у неё другая — `ce74d66`, где уже влиты `budget-one-base` (+5
тестов) и `drop-damage-extraction` (−3), то есть **1447/1, и новых тестов
шесть, а не восемь**. Измеренное число 1453 верно, ошибочна была арифметика
разности. Причина ошибки та же, что я разбирал с `lead` двумя ходами раньше:
взял базу по умолчанию вместо измеренной.

**Предупреждение интегратору.** Обе ветки дописывают `docs/DECISIONS.md` в одно
и то же место (начало журнала, сразу после шапки), и туда же пишет
`claude/ui-mode-vocabulary`. Конфликт при втором и третьем влитии ожидаем и
разрешается сохранением ВСЕХ трёх записей, а не выбором одной. `docs/TODO.md`
трогает только `claude/budget-one-base` (−9 строк) — там конфликта нет, но T-4
обязана стартовать ОТ ВЛИТОЙ базы, иначе она правит строки, которых уже нет.

Порядок влития значения не имеет: по коду ветки не пересекаются
(`commands/resources.ts` против `simulation/economy/*`).

### T-5 Мёртвая функция и разошедшийся порог союзника
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/diplomacy-debts` (`f25aa93`)
Выдано: 2026-08-09 («возьмись за долги», приоритет пользователя)

**Аудит — 2026-08-09, оркестратор, свои прогоны в дереве исполнителя.**
`tsc` exit 0; `npx vitest run` — 95 файлов, **1444 passed / 1 skipped**;
public eval 240/240. Число сходится от базы ветки (`92c1aa9`, 1445/1): минус три
удалённых теста `calculateDiplomaticTension`, плюс два новых.

Проверено чтением diff:
- функция снята, `rg` вхождений не даёт;
- выбран исход **(а)** — попарный порог. Сделан не копией формулы, а экспортом
  её из `DiplomacyTick` (`commonEnemyPressure`, `ideologyDistanceBetween`,
  `foesOf`, `sharesOpponentWith`); `collectPairStandings` переведён на те же
  функции — это и объясняет +57 строк в тике. Второго определения не заведено,
  то есть исполнитель применил тот же принцип, что T-1 к базе росписи;
- `ALLY_RELATION_THRESHOLD` не тронута — она осталась базой попарной формулы,
  как я и требовал;
- слоение: `primitives/` теперь импортирует из `simulation/diplomacy/`.
  Названо в отчёте, `architecture.test.ts` зелёный — запрета на это направление
  в проверках нет.

**Отклонение от задания, принятое:** экспорт четырёх функций из `DiplomacyTick`
шире буквы задания, но это прямое следствие требования «мерка одна и берётся из
одного места». Поведение `collectPairStandings` не изменилось — извлечение
чистое.

**Замечание на будущее, не блокирующее приёмку.** Ветка живёт на фикстуре:
в сценарии 1946 у всех 157 стран `relations` пусты, поэтому попарный порог на
живых данных сегодня не срабатывает ни разу. Правка верна и проверяема, но её
эффект в партии равен нулю до наполнения стартовой дипломатии (домен `world`).

**Задача.** Два подтверждённых мной по коду долга дипломатической области, оба
малые и оба одного класса — код утверждает больше, чем делает.

**Часть 1 — снять `calculateDiplomaticTension`.**
`server/src/services/DiplomacyService.ts:302`, ~30 строк. Боевых вызовов ноль:
только собственный тест (`__tests__/DiplomacyService.test.ts:216-240`). Тот же
случай, что `damageExtraction` (снят T-2 2026-08-09), и решается так же —
удалением, а не поиском применения задним числом. Если напряжение когда-нибудь
понадобится, его посчитают от тогдашней модели отношений, а не от этой.

**Часть 2 — `alliedWith` перевести на попарный порог.**
`server/src/primitives/PrimitiveEngine.ts:375` читает «отношения не хуже
союзнических» как `relations >= ALLY_RELATION_THRESHOLD` (плоские 70), а с
2026-07-28 союзнический уровень свой у каждой пары —
`allianceThreshold(ideologyDistance, commonEnemyPressure)`
(`server/src/simulation/diplomacy/affinity.ts:153`). Сама константа НЕ мертва:
она база попарной формулы (`affinity.ts:158`), её не трогать. Разошлось одно —
предпосылка примитива `spawn_incident(border_dispute)` судит союзничество не
той меркой, какой его судит дипломатия: строже нужного для родственных
режимов, мягче для антиподов.

**Первый шаг — установить, а не чинить.** `allianceThreshold` требует
`PairStanding` (идеологическая дистанция, давление общего врага), и на месте
вызова этих данных может не оказаться дёшево. Исполнитель обязан сперва
выяснить, и дальше допустимы РОВНО два исхода:
- **(а)** `PairStanding` собирается на месте без обхода мира → перевести
  `alliedWith` на попарный порог;
- **(б)** дёшево не собирается → оставить плоский порог, но **закрыть
  утверждение тестом** и написать в JSDoc прямо: «здесь плоская мерка, и она
  сознательно грубее дипломатической, потому что <причина>». Молчаливое
  расхождение — то, что чинится; выбранная и названная грубость — нет.
Выбор с обоснованием и ценой — в отчёт; переписывать за меня развилку не надо,
но и спрашивать разрешения на (б) не нужно, если факт её потребует.

**Слоение проверить.** `primitives/` сегодня не импортирует из
`simulation/diplomacy/` — если исход (а) требует такого импорта, назвать это в
отчёте и сверить с `docs/ARCHITECTURE.md`; при конфликте — вернуться с вопросом,
а не заводить новый слой молча.

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] `rg calculateDiplomaticTension` по `server`/`client`/`shared` — ноль
      вхождений; тест-блок удалён вместе с функцией.
- [ ] Исход по части 2 назван и обоснован; при (а) — тест на ДВУХ парах с
      разной идеологической дистанцией показывает разные пороги, при (б) — тест
      фиксирует плоский порог, а JSDoc называет его выбором.
- [ ] Негативный контроль: новый тест части 2 падает на восстановленном
      поведении (плоские 70 при (а); отсутствие теста покрытием не считается
      при (б) — там негативный контроль в том, что тест падает при смене
      константы).
- [ ] `server: npx tsc --noEmit -p tsconfig.json` и `server: npm test` зелёные;
      база — 1445 passed / 1 skipped на `main` = `92c1aa9`, плюс/минус
      удалённые и добавленные тесты, разница объяснена числом.
- [ ] Затронута семантика предпосылки — `docs/DIPLOMACY.md` и/или
      `docs/PRIMITIVES.md` §2 обновлены в том же изменении.

**Вне scope.** Сама формула `allianceThreshold`, значение
`ALLY_RELATION_THRESHOLD`, три репутационные санкции, наполнение стартовой
дипломатии 1946 (домен `world`), `shared/src/types/**`.

### T-6 Отказ `schemaInvalid` называет поле
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/schema-rejection-field` (`fb13b81`)
Выдано: 2026-08-09

**ЗАДАНИЕ ИСХОДИЛО ИЗ НЕВЕРНОЙ ПРЕДПОСЫЛКИ, и это установил исполнитель, а не
я.** Проверил сам, независимо от его отчёта:

- `parsePrimitives` кладёт `issue.path` в причину отказа **с `76bf6c0`**
  (Милстоун 0, сессия A) — то есть путь поля был там задолго до находки
  2026-08-02, на которой стоял пункт `docs/TODO.md:119`. Исполнитель называет
  `13f5193`; настоящий источник ещё раньше, его оценка консервативна;
- английский текст для промта — `rejections.ts:534`:
  `primitive #${position}: ${detail}`, а `detail` и есть `path: message`.
  **Модель поле видит.** Пункт TODO неверен для промта;
- поля нет только в квитанции игрока (`rejections.ts:770` отдаёт одну позицию) —
  ровно как исполнитель и написал.

Похоже, находка 2026-08-02 смотрела на квитанцию, а вывод сделала про промт.
**Мой список открытого нёс эту ошибку дальше** — я перечислил `:119` среди
подтверждённых открытыми пунктов LLM-области, приняв формулировку TODO за факт,
хотя проверял в тот раз другое. Исправлено ниже, в списке для T-4.

**Атрибуция решения проверена у пользователя.** Запись «поле в квитанцию игрока
не заводить — решение пользователя 2026-08-09» подтверждена им напрямую
2026-08-09: решение принято в сессии исполнителя. Спрашивал потому, что чужого
решения в реестре у меня не было, а подпись пользователя под решением
исполнителя — худший вид записи в append-only журнале.

**Что сделано взамен и почему это законно.** Закрыта дыра, которую тот же замер
подтвердил: `MAX_REJECTION_FACT_LENGTH = 512` — граница длины ОДНОЙ
диагностической записи (`docs/TODO.md:554`, «кап обходится именами ключей»).
Она стояла пятым критерием этого же задания, так что подмены scope нет:
исполнитель не нашёл первой цели и закрыл вторую, названную заранее.

**Аудит — 2026-08-09, оркестратор, свои прогоны.** `tsc` exit 0;
`npx vitest run` — 95 файлов, **1450 passed / 1 skipped** (база ветки 1445/1
плюс ровно пять новых тестов); public eval 240/240. Значение капа выведено
замером (самая длинная законная причина сценария — 364 знака, 512 даёт 40 %
запаса), усечение помечается в тексте — иначе модель чинила бы названную часть,
не узнав про обрезанную.

**Задача.** Сегодня отказ разбора несёт глагол и позицию в массиве
(`server/src/services/LLMService.ts:900-909`, код `schemaInvalid`), но не
называет ПОЛЕ, на котором ответ не разобрался. Модель получает диагностику
следующим промтом и починить по ней ничего не может: «примитив №3, глагол
`spawn_incident`, не разобрался» — это сообщение о том, что ей не повезло.
Дефект найден разбором сырых ответов 2026-08-02
(`.agent/runs/director-prompt-regions-2026-08-02`): на форме `target` терялся
целый класс ходов.

**Почему это первое среди промтовых долгов, хотя само промтом не является.**
Правка движковая и доказывается юнит-тестом. Но она удешевляет КАЖДУЮ будущую
промтовую работу: пока отказ безымянный, разбор причин делается чтением сырых
ответов руками — так и был найден этот дефект.

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] Отказ `schemaInvalid` несёт путь поля (`target.regionId`,
      `params.direction`), а не только позицию; источник пути — сама ошибка Zod
      (`issue.path`), а не разбор текста сообщения.
- [ ] Путь доезжает ОБОИМ потребителям: в запись игрока (`rejectionRecord`,
      клиентский словарь `primitiveRejection`) и в английский текст промта
      (`rejectionFactText`). Ключи локализации заведены в обеих локалях.
- [ ] Тест на КАЖДОМ из двух путей: невалидный примитив с известным полем даёт
      отказ, содержащий это поле.
- [ ] Негативный контроль: тест падает, если путь поля из отказа убрать.
- [ ] Кап длины записи не обойдён: путь поля не может раздуть секцию отказов
      (`MAX_PRIMITIVE_ID_LENGTH` и известная дыра «кап обходится ИМЕНАМИ
      ключей», `docs/TODO.md:554`) — либо длина пути ограничена, либо в отчёте
      показано, почему это невозможно.
- [ ] `server: npx tsc --noEmit -p tsconfig.json`, `server: npm test`,
      `client: npm test` (тронуты словари) — зелёные.

**Вне scope.** Промт как текст (замер на живой модели — отдельная работа
`director-prompt-engineer`), второй вызов модели, форма отказов движка.

### T-7 Префиксный кэш: сперва факт, потом перестановка
Статус: возвращено
Исполнитель: отдельная сессия, ветка `claude/prompt-prefix-cache`
(`17187d7`, `1be910b`)
Выдано: 2026-08-09 · Возвращено: 2026-08-09

**Возвращено не за качество, а за незаконченность.** Сделанное ценно, часть его
крупнее самого задания, — но принять ветку в этом состоянии нельзя.

**Причины, каждая проверена мной:**

1. **Работа не закоммичена.** `git status` дерева показывает пять изменённых
   файлов, включая `server/src/services/LLMService.ts` — то есть сам сбор
   промта, ради которого задание и выдавалось. Перестановки секций в коммитах
   НЕТ; она, судя по всему, лежит в рабочем дереве. Чужую незакоммиченную
   работу я не трогаю и не оцениваю.
2. **Исход не объявлен.** Задание требовало назвать (а) или (б) явно. В
   коммитах нет ни `docs/DECISIONS.md`, ни `docs/LLM_RULES.md` — вердикта о
   том, работает ли префиксный кэш на нашем пути, в репозитории не существует.
3. **Висящая ссылка.** `probePromptCache.ts:5` и комментарий
   `GeminiProvider.ts` ссылаются на `docs/LLM_RULES.md` как на источник фактов
   о кэше и о замере моделей — а в `LLM_RULES.md` ветки про кэш нет ни строки
   (`rg -i 'кэш|cache'` даёт одно несвязанное вхождение). Код утверждает
   существование доки, которой нет: тот самый класс дефекта, ради которого
   заведён критерий «доки обновлены в том же изменении».

**Что принято как ценное и должно уцелеть при доделке.** Ветка нашла дефект
крупнее заказанного: прямой путь на Gemini **не работал вовсе**. Плюс
выяснилось, что `DEFAULT_MODEL` — имя модели НА ШЛЮЗЕ, а не id прямого API.
Это находка о боевом пути, а не о промте.

**Адрес находки — цепочка, а не один файл** (уточнено 2026-08-09 по запросу
`lead`, который искал `exclusiveMinimum` текстом в `responseSchemas.ts` и не
нашёл). Ограничение приезжает КОМПОЗИЦИЕЙ:

```text
primitives/primitiveSchemas.ts:68   const regionIdSchema = z.number().int().positive()
llm/responseSchemas.ts:2,96         импортирует primitiveSchema и встраивает его
                                     как primitives: z.array(primitiveSchema)
llm/providers/GeminiProvider.ts:235 z.toJSONSchema(schema) — здесь `.positive()`
                                     превращается в `exclusiveMinimum`
llm/providers/GeminiProvider.ts:180 (ветка) снимает ключ, переводя строгую
                                     границу целого во включающую
```

Поэтому текстовый поиск по `responseSchemas.ts` пуст и будет пуст: файл несёт
конверт, а ограничение живёт в примитиве, который он импортирует. Тот же класс
ошибки, что «разный фильтр, а не арифметика», только по месту, а не по числу:
искали симптом в файле, куда он приходит зависимостью.

**Чего у меня НЕТ:** самого 400-го ответа как артефакта. Текст
`Unknown name "exclusiveMinimum"` я видел только процитированным в комментариях
ветки (`GeminiProvider.ts:180`, `GeminiProvider.test.ts:209`), живого вызова не
делал — он требует ключа и квоты. Цепочку кода проверил сам, ответ провайдера —
нет, и в отчёте это разделено.

**Уцелевшее сверх находки:** ветка завела обобщённую проверку «в схеме нет ни
одного ключа вне диалекта Gemini» (`GeminiProvider.test.ts:207`) — список
РАЗРЕШЁННЫХ, а не запрещённых. Правильный класс защиты: точечные проверки
называли ключи, которые уже подводили, и молчали про следующий.

**Что нужно для приёмки** (критерии не менялись, перечисляю к чему вернуться):
закоммитить или откатить рабочее дерево; назвать исход (а)/(б); при (а) —
числа до/после одним прогоном с `T=0`, три прогона на конфигурацию, доля
отказов не выросла; при обоих исходах — `docs/LLM_RULES.md` с порядком секций и
причиной, плюс запись в `docs/DECISIONS.md` о найденном `exclusiveMinimum`.

**Задача.** `## Instructions` — 61 % промта (18 737 из 30 681 знака) и стоит в
КОНЦЕ (`docs/TODO.md:312`). Провайдеры кэшируют общий ПРЕФИКС запроса: неизменное
обязано идти первым, иначе кэш не срабатывает и неизменные 18 тысяч знаков
оплачиваются каждый ход заново.

**Задание НЕ на перестановку. Первый и обязательный шаг — установить факт:**
поддерживает ли префиксный кэш тот путь, которым мы реально ходим (Gemini через
шлюз, `server/src/llm/providers/`), на какой модели, с какими условиями
(минимальная длина префикса, TTL, явный или неявный) и как это видно в
`usageMetadata`, который мы уже пишем телеметрией
(`server/src/llm/tokenTelemetry.ts`). Источник — официальная документация
провайдера, не память модели.

Дальше ровно два исхода, оба законные:
- **(а) кэш поддерживается** → перестановка секций (неизменное первым,
  изменяемое последним) + замер: расход и стоимость до/после на одинаковом
  прогоне. Порядок секций — контракт с моделью, поэтому замер обязателен и по
  качеству ответа тоже: доля отказов не должна вырасти;
- **(б) не поддерживается или условия не выполняются** → **правку не делать**,
  записать факт с ссылкой на документацию в `docs/LLM_RULES.md` и снять пункт
  из TODO как исследованный. Отрицательный результат здесь — полноценный
  результат: он закрывает вопрос, который иначе всплывёт снова.

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] Факт установлен по документации провайдера, ссылка в отчёте; исход (а)
      или (б) назван явно.
- [ ] При (а): числа ДО и ПОСЛЕ сняты одним и тем же прогоном
      (`server/scripts/runCampaignWithLLM.ts`, одинаковая длина партии, `T=0`,
      три прогона на конфигурацию), приведены медианы `promptTokens`,
      кэшированной доли и доли отказов. Числа старого замера (30 681 знак) сняты
      ДО влития карты и датированных событий — **перемерить, не переписывать**.
- [ ] При (а): доля отказов не выросла — иначе правка возвращается, каким бы
      выигрышем она ни сопровождалась.
- [ ] `docs/LLM_RULES.md` описывает порядок секций и причину — при обоих
      исходах.

**Вне scope.** Содержание инструкций, сокращение промта, кап хроники, смена
модели или провайдера.

### T-8 `aiTraits` получают потребителя
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/ai-traits-consumer` (`bec8ca5`)
Выдано: 2026-08-09 (решение пользователя — В-7)

**Аудит — 2026-08-09, оркестратор, свои прогоны.** `tsc` exit 0;
`npx vitest run` — 95 файлов, **1453 passed / 1 skipped** (база ветки 1445/1
плюс восемь новых); public eval 240/240.

Проверено чтением diff:
- характер читают ровно два места, оба — ПОРОГИ существующих правил
  (`austerityDebtThreshold`, `unrestThresholds`), новых механик и полей
  состояния нет, `SAVE_VERSION` не тронут;
- новых балансовых констант **две**, как и требовало задание
  (`AI_TRAIT_DEBT_TOLERANCE`, `AI_TRAIT_UNREST_TOLERANCE`), плюс общая
  `traitScale` — не константа, а одно место для смысла «характер сдвигает
  порог»; вынесена по тому же основанию, что база росписи в T-1;
- нейтральный характер даёт РОВНО 1 при любой силе, поэтому все существующие
  фикстуры ведут себя как до правки — вот почему 1445 старых тестов не
  потребовали перекалибровки;
- почему порог, а не величина реакции, — обосновано данными: стартовые доли
  приходят тремя наборами по `economyProfile`, множитель на величину был бы жив
  у одного профиля и мёртв у другого. Это сильнее моего задания, где стояло
  просто «один множитель на признак»;
- сила 0,5 выбрана замером с таблицей по трём значениям, а не вкусом: при 0,25
  агрессивная половина признака не спасает от Правила C ни одну страну, то есть
  признак односторонний; при 0,75 коридор уезжает за читаемый смысл
  `STABILITY_LOW`.

**Правка существующего guard-теста разобрана отдельно — она выглядит
ослаблением, но им не является.** `exportIncome.test.ts` держал «к десятому
году ноль стран с долгом свыше 60 % ВВП». Теперь: тяжёлый долг допускается, но
**обязан объясняться характером** — страна с `riskTolerance ≤ 1` в тяжёлом
долгу роняет тест, чего прежняя формулировка не умела вовсе, — плюс держится
масштаб (не более 5 % мира). Негативный контроль показан правильный: с
характерами, зажатыми в 1.0, тяжёлых должников ноль на всех восьми сидах, с
живыми — один-два, и у каждого `riskTolerance` 1,03…1,33 при нагрузке
0,637…0,705, то есть у собственного порога. Прежнее утверждение запрещало ровно
то поведение, которое заказано заданием.

**Требование «прогон на живых данных» выполнено** — `server/scripts/
probeAiTraits.ts`, восемь сидов, 120 месяцев, разброс исходов по 157 странам
показан числом, а не фикстурой.

**Задача.** `Country.aiTraits` (`aggressiveness`, `riskTolerance`) сеются каждой
стране детерминированно (`server/src/game/CreateGame.ts:174`), продвигают
`rngState`, копируются при расколе (`polityLifecycle.ts:460`) — и не читаются
НИ ОДНИМ боевым потребителем. Проверено мной 2026-08-09: единственным читателем
было удалённое Правило D. Решение пользователя — не удалять, а подключить:
кандидат «разные характеры ИИ в экономических правилах A/C» уже записан в
`docs/TODO.md:230` как дешёвое применение.

**Что должно измениться в партии.** Сегодня 157 стран реагируют на дефицит и на
угрозу одинаково — по одним и тем же константам. После правки осторожная страна
начинает аустерити раньше и восстанавливает расходы медленнее, агрессивная
быстрее наращивает военную долю. Это не новая механика: правила A/B/C уже
существуют, меняется только то, что их пороги и скорости перестают быть общими
на весь мир.

**Минимальный scope, и он часть задания.** Один множитель на признак, внутри
существующих правил, без новых правил и без новых полей состояния. Новых
балансовых констант — не больше двух, и обе с обоснованием в
`shared/src/defines/`. `SAVE_VERSION` не меняется: поле уже есть.

**Критерии приёмки** (записаны 2026-08-09, до старта):
- [ ] `aiTraits` читает хотя бы одно боевое место в `AiBehaviorTick`; `rg` по
      `server/src` показывает читателя вне тестов и вне посева.
- [ ] **Прогон на ЖИВЫХ данных, а не на фикстуре** (`server/scripts/
      runCampaign.ts`, сценарий 1946, ≥ 120 месяцев): показать, что страны
      РАЗОШЛИСЬ — распределение момента первого аустерити и военной доли по 157
      странам имеет разброс, а не одно значение. Фикстура доказывает, что
      функция считает; что признак живой — только это.
- [ ] Негативный контроль: тест падает, если множитель признака заменить на
      константу (то есть вернуть прежнее поведение «все одинаковые»).
- [ ] Детерминизм цел: тот же seed даёт тот же результат — существующий тест
      `CreateGame.test.ts` («одинаковый seed даёт идентичные aiTraits») зелёный,
      плюс повторный прогон кампании на том же seed совпадает.
- [ ] Коридор роста не сломан: `growthCorridor.test.ts` зелёный. Если разброс
      характеров выводит мир за коридор — это находка, а не повод двигать
      коридор: вернуться с числами.
- [ ] `server: npx tsc --noEmit -p tsconfig.json`, `server: npm test` зелёные;
      `docs/AI_RULES.md` описывает, на что теперь влияет характер.

**Вне scope.** Новые правила ИИ, военные и дипломатические решения по
характеру, `shared/src/types/**`, удаление или переименование самих полей.

## Граница домена: `server/src/llm/**` принят — 2026-08-09

Решение роли `lead` (cross-session, действует до влития правки устава).
**Принято.** Проверено мной, а не принято на слово:

- ни один устав в `.agent/roles/` не называет `server/src/llm` своей областью —
  слой действительно ничей (`rg 'llm|LLM' .agent/roles/` даёт только упоминания
  в тексте, не в границах);
- каталог: **24 файла, 2 946 строк** по моему счёту (у `lead` 3 286 — число
  расходится, файлов столько же; на решение не влияет, но своё число называю
  своим);
- цена дыры подтверждается историей: `5a2ed6b feat(llm): датированные события…`
  правит `shared/src/types/Event.ts` и `SaveFile.ts` — мою контрактную область —
  из промтовой задачи. Оркестратора над ней не было.

**Оговорка про приёмку принята и важнее самой границы.** Мой якорь начинается с
детерминизма, а промтовый слой доказывается противоположным способом:
статистикой на недетерминированной модели, «одна правка — один замер», три
прогона на конфигурацию (`server/scripts/runCampaignWithLLM.ts`). `npm test`
промтовое изменение не доказывает и доказать не может. Отсюда правило домена:
**промтовые задания — субагенту `director-prompt-engineer`, приёмка по числам
замера; юнит-тесты остаются доказательством только для кода, который читает и
пишет состояние** (схемы, разбор ответа, диагностика отказов, телеметрия).

### Что это добавляет к моей области открытым

Список для T-4 растёт — эти пункты `docs/TODO.md` теперь однозначно мои:

- ~~**`schemaInvalid` не называет поле** (`TODO.md:119`)~~ — **ПУНКТ НЕВЕРЕН,
  снять.** Установлено при приёмке T-6 и перепроверено мной: путь поля лежит в
  причине с `76bf6c0` и доезжает до модели дословно
  (`rejections.ts:534`). Поля нет только в квитанции игрока, и заводить его там
  решено не надо (решение пользователя 2026-08-09). Я нёс этот пункт как
  «подтверждённый открытым», приняв формулировку TODO за проверенный факт, —
  моя ошибка, а не чужая.
- **Кап длины диагностической записи** (`TODO.md:554`) — **закрыт** ветвью
  `claude/schema-rejection-field` (`MAX_REJECTION_FACT_LENGTH`), снять вместе с
  переносом раздела.
- **ISO-дата просачивается в прозу** (`TODO.md:124`) — промтовая, замер
  обязателен, лечится подачей даты, а не ещё одним запретом.
- **`spawn_incident` без `sourceCountryId`** (`TODO.md:721`) — 2 из 61 на
  замере; сначала подтвердить на длинном прогоне, потом чинить.
- **Хроника идёт в промт целиком, без капа** (`TODO.md:351`) — запас по
  бюджету есть, растёт хроника; мерить на длинной партии.
- **`## Instructions` — 61 % промта и стоит в КОНЦЕ** (`TODO.md:312`) —
  префиксный кэш провайдера так невозможен; перестановка дешёвая, эффект не
  проверен. Самый окупаемый кандидат, если важна стоимость.
- **Второй вызов модели «перепиши по фактам»** (`TODO.md:344`) — им закрывается
  сразу два хвоста: числа последствий в нарративе и текст, утверждающий
  отклонённое.

Ни один не блокирует милстоун; ни один не начат.

## Очередь `shared/src/types`

Пусто. T-1 типов не трогает. Пересечение с доменом `ui` возможно только через
`shared/src/types`, и на сегодня его нет.

## Решения пользователя (2026-08-09)

- **В-1 → T-1, затем T-3.** Мелкий бюджетный долг закрывается первым, Милстоун 1
  идёт от чистой базы.
- **В-2 → `damageExtraction` снять.** Оформлено заданием T-2.
- **В-3 → промт датированных событий не трогать.** Пункт `docs/TODO.md` о
  перекрытии 84 % закрывается решением, а не работой: переносится в журнал
  решений строкой «оставлено осознанно, вред не измерен».

Вопросы в том виде, в каком они были заданы, и рекомендации оркестратора:

- **В-1. Приоритет: T-1 (мелкий долг) → T-3 (Милстоун 1) или сразу T-3?**
  Рекомендация: T-1 первым. Он маленький, снимает единственное место, где игра
  показывает игроку неверные деньги, и не конфликтует ни с чем; T-3 после него
  идёт от чистой базы. Передумать заставит одно: если T-1 при реализации
  потянет за собой правку долей ИИ — тогда он больше не мелкий и уходит за T-3.
- **В-2. `damageExtraction` — снять или подключить?** Рекомендация: **снять**
  (10 строк кода и три теста), а разрушение мощностей войной завести вместе с
  War Phase, где появятся числа для калибровки. Основание: завершения войны
  сегодня нет вовсе (решение 2026-07-31 отложило его), и подключать разрушение
  к тику, который не умеет заканчивать войну, значит калибровать вслепую.
  Передумать заставит решение взять «завершение войны» ближайшей задачей —
  тогда разрушение делается тем же заходом.
- **В-3. Ужесточать ли промт под «событие только из прозы»?** Рекомендация:
  **нет** (L-4). Вред не измерен, факты верные, а каждая правка промта стоит
  своего замера на живой модели и квоты.
