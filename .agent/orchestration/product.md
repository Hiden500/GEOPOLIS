# Реестр роли `product`

Очередь продуктовых задач, критерии готовности и вердикты приёмки. Формат —
`.agent/orchestration/README.md`. Числа сюда попадают только те, что снял
владелец продукта или аудитор своим прогоном.

## Состояние на 2026-08-30

**Цель v1 названа и записана** (`docs/DECISIONS.md` 2026-08-30): партия
проходится пользователем насквозь и при этом интересна. **Сквозной критерий
закрытия милстоунов** — достижимость глаголов алфавита с названным актором,
а не список сделанных пунктов; база **10 из 22**
(`.agent/runs/campaign-5y-2026-08-21/`, 60 ходов, 0 ошибок, 262 действия).

Замеры, снятые владельцем продукта 2026-08-30 на `main` (`71fd833`):

| Что | Значение |
|---|---|
| Регионов / водных узлов в сценарии | 1389 / 195 |
| Регионов с записанным берегом | 768 |
| Регионов вообще без связей | **0** (133 без сухопутных соседей имеют воду) |
| Стран в ростере | 157, из них **34 колониальные/оккупационные** (`Q*`) |
| `sovereigntyStatus` в `government.json` | размечен на 157 |
| Читателей `sovereigntyStatus` в `server/src/simulation/` | **0** (примитивы вассалитета читают) |

**Морской граф закрыт** (T-12, 2026-08-20) — роадмап нёс устаревшее «0 водных
соседей» до 2026-08-30. Блокер сместился с данных на решение: `adjacentWaterIds`
не читает ни один потребитель, `isCoastalRegion()` — литеральный `return false`.

**Опровергнутая премиса, стоившая неверной оценки объёма.** Живой пункт
`docs/TODO.md` утверждал, что колониальные блоки собраны «один блок на
сюзерена, всё британское — единый QCG». Замер: страны `QCG` не существует,
AOF и AEF есть (`QFW`, `QFE`), Вест-Индия разбита на `QWL`/`QWW`, Индия — `IND`
(British Raj). Владелец продукта принял пункт на веру и записал в журнал вывод
«Индии неоткуда отделиться» — опровергается одной строкой данных. Правило
«прежде чем судить о пункте backlog — искать его починку в истории»
существовало и применено не было. Записи исправлены тем же ходом.

## Открытые развилки к пользователю (не больше трёх одновременно)

1. **Чем кончается партия.** Горизонт даты, условия победы или явное решение
   оставить бесконечной. Единственное терминальное состояние сегодня —
   `defeated` при потере всех регионов (`shared/src/types/Campaign.ts`). Без
   ответа главный критерий v1 непроверяем: «насквозь» нечем измерить.
2. **Судьба `build_islands_preview.py`** (унаследовано из `geometry.md`, вопрос
   4). Удаление необратимо, поэтому решает пользователь. Рекомендация домена и
   владельца продукта совпадают: удалить вместе с отработавшей записью
   `region_edits_islands.json`.
3. *(свободна)* — прежний пункт «порог отсечения мелких стран» снят вместе с
   отложенной линией зависимости (см. ниже).

**Отложено пользователем 2026-08-30 («отмена по автономиям, потом вернёмся»):**
линия механики зависимости — три типа плюс шкала давления за независимость.
Наработанное не потеряно: замеры, сжатие 10 статусов в 3, исследование жанра
(HOI4, EU4, Victoria 3) и цена записаны в `docs/IDEAS.md` записью 2026-08-30,
горизонт — возврат по запросу пользователя. Переисследовать при возврате не
нужно.

**Закрыто черновиком «переголосуем на фактах»:** полигон Ватикана (0,0107 км²
против ~0,44). До v1 не чинится, причина записана в `KNOWN_ZERO_AREA`
(`verify_geojson_field_contract.py`). Внешний источник контура — вопрос
лицензии, а он относится к гейту релиза, не к разработке.

## Решения владельца продукта по висящим вопросам `geometry` (2026-08-30)

Четыре из пяти вопросов домена от 2026-08-09 закрыты здесь — они обратимы и
потому не требуют пользователя:

1. **Целевая гранулярность регионов — оставить как есть до v1.** Совпадает с
   рекомендацией домена: карта играбельна, пере-нарезка тянет каскад
   `region_id` и пересчёт экономики. Турция вчетверо грубее соседей — принято
   как есть.
2. **Восемь источников (G7) — мастер под git считается достаточным источником
   истины.** Докупка geoBoundaries и китайских исторических шейпов ради
   воспроизводимого `--rebuild-master` не оправдана: пересборка мастера не
   входит в путь игрока.
3. **Ганьсу/Синьцзян (G3) — 20,9 км² суммарного расхождения приемлемы**, запись
   уходит из живого backlog в историю решений.
4. **Достижимость 133 островов (G9)** — вопрос частично снят самой работой
   домена: морская смежность экспортирована (вариант «а» фактически сделан).
   Остаётся живая `isCoastalRegion()` — она же блокер морских десантов.
   Решение: делать её, отдельной развилки больше нет.

## Задания

## P-1 Закрыть остатки геометрии
Статус: выдано
Исполнитель: домен `geometry`, дерево `claude/geometry-final`
Модель: **Sonnet, effort high**; механические проходы (перевод читателей,
удаление отработавших планов) — **Haiku, effort low**
Выдано: 2026-08-30 пользователем

**Задача.** Довести геометрию до состояния «к ней почти не возвращаемся»
(формулировка пользователя 2026-08-30). Что станет иначе в партии: игрок
перестаёт видеть безымянные регионы, а морские механики получают данные, на
которые смогут опереться в М4.

**Состав** (из раздела «Карта/регионы» `docs/TODO.md`, проверен 2026-08-30):

- `isCoastalRegion()` в `generateMapFeatures.ts:113` — литеральный
  `return false`, порты не генерируются вовсе;
- два безымянных региона `SAM-0040` и `SAM-0055` — игрок видит пустую подпись;
- реестр отделённых кусков: `config/region_fragment_allowlist.json` не создан,
  критерий `orphan` признан непригодным (все 23 «сироты» — намеренные слияния);
- вторая половина сверки геометрии с экономикой: площадь в `regions.core.json`
  не сверяется с той, из которой посчитаны экономические поля региона;
- `diagnose_coastline_gaps.py` не переведён на мастер (падает
  `FileNotFoundError`);
- дедупликация вершин морских зон (мастер 41,1 МБ);
- 14 планов со `Status: complete` плюс `interface-rebuild.md` — удалить по
  `.agent/PLANS.md`.

**Критерии приёмки** (записаны до старта):

- [ ] `isCoastalRegion()` истинна для регионов с непустым `adjacentWaterIds`;
      число прибрежных регионов совпадает с 768 из замера 2026-08-30 либо
      расхождение объяснено;
- [ ] `python scripts/map/audit_map_geometry.py` — код 0;
- [ ] `python scripts/map/validate_sea_adjacency_1946.py` — код 0;
- [ ] `python .agent/evals/public/run_public_evals.py` — код 0;
- [ ] безымянных регионов ноль: `KNOWN_EMPTY_NAMES` пуст либо каждая запись в
      нём обоснована решением пользователя;
- [ ] негативный контроль: тест на `isCoastalRegion()` падает при возврате
      прежнего `return false`.

**Вне scope.** Пере-нарезка регионов (решение выше — оставить), полигон
Ватикана (закрыт черновиком), каналы и проходы (отдельная развилка,
`docs/plans/14_CANAL_PASSAGE.md`), любые правки `server/src/simulation/**`.

**Аудит.** —

## P-2 Бюджет промта и фактические ошибки ростера
Статус: черновик
Исполнитель: домен `world`, отдельное дерево; **стартует только после влития P-1**
Модель: замер токенов — **Haiku, effort low**; разбор аномалий — **Sonnet,
effort high**
Выдано: —

**Урезано 2026-08-30.** Прежняя редакция включала пересмотр состава ростера по
критерию автономности; линия отложена пользователем, и вместе с ней снят
вопрос «сколько стран». Осталось то, что от него не зависит и полезно само по
себе.

**Задача.** Закрыть два независимых пробела: неизмеренный бюджет промта и
фактические противоречия в данных ростера.

**Шаг 0 — замер бюджета.** Цена одной страны в токенах промта и текущий размер
промта на длинной кампании: бюджет PROMPT < ~20k объявлен в критериях приёмки
v1 и **не замерен ни разу**. Инструмент готов —
`server/src/llm/tokenTelemetry.ts` и `scripts/token-usage-report.mjs`. Число
нужно независимо от состава ростера: оно ограничивает всё, что когда-либо
попадёт в промт.

**Шаг 1 — аномалии данных, найденные владельцем продукта 2026-08-30.** Это не
вопрос вкуса, а внутренние противоречия:

- `QSO British Somaliland` существует как страна, но регионы Bari, Mudug,
  Hiiraan, Bay принадлежат `GBR` напрямую — а это бывшее ИТАЛЬЯНСКОЕ Сомали
  под британской военной администрацией, другая территория;
- Триполитания, Киренаика и Эритрея числятся владением метрополии `GBR`, хотя
  были британской военной администрацией с иной судьбой;
- `leased_territory` объявлен в `SOVEREIGNTY_STATUSES` и не встречается в
  данных ни разу — либо статус лишний, либо кто-то размечен неверно;
- `shortName.ru` обрезан у двух стран: `USA` «Соединённые Штаты Америк»,
  `QAD` «Колония и протекторат Ад».

**Критерии приёмки** (записаны до старта):

- [ ] число: сколько токенов промта стоит одна страна и сколько стоит промт
      сегодня на прогоне не короче 5 лет;
- [ ] по каждой аномалии — вердикт с провенансом: исправлено либо признано
      верным с обоснованием;
- [ ] `python scripts/map/validate_region_economy_1946.py` — код 0;
- [ ] прогон партии на 5 лет после правки: ошибок 0, число разных глаголов не
      упало ниже 10 из 22.

**Вне scope.** Состав ростера и число стран (линия отложена), геометрия
полигонов, код симуляции, UI.

**Аудит.** —

## P-4 Стартовая дипломатия 1946
Статус: черновик
Исполнитель: домен `world`, отдельное дерево
Модель: **Sonnet, effort high**
Выдано: —

**Задача.** Дать миру знать, кто кому кто, на дату старта. Что станет иначе в
партии: первые два года из двадцати перестают быть слепыми.

**Основание — замер.** `allies` и `rivals` пусты у ВСЕХ 157 стран
(`Scenario1946.ts:329`), `relations` тоже. Симуляция наращивает их сама, но к
июлю 1948 — то есть мир узнаёт о холодной войне через два с половиной года
после её начала. Динамика из `.agent/runs/war-never-happens-2026-08-21/`:
1946-01 — 0 соперничеств и 0 союзов, 1948-07 — 85 и 6, 1951-01 — 93 и 46.
`getDiplomaticSituation()` до 1948 печатает режиссёру «No major diplomatic
tensions» — в 1946 году.

**Критерии приёмки** (записаны до старта):

- [ ] на первом ходу партии `rivals` непусты минимум у великих держав, и
      `getDiplomaticSituation()` не печатает «No major diplomatic tensions»;
- [ ] провенанс на каждую пару соперничества и союза;
- [ ] прогон на 5 лет: число разных глаголов НЕ ниже 10 из 22, ошибок 0;
- [ ] негативный контроль: тест на непустую стартовую дипломатию падает при
      возврате пустых значений.

**Вне scope.** Состав ростера, механика зависимости (отложена), геометрия.

**Аудит.** —

## P-3 Промт-диагностика: отделить «не разрешено» от «нечему действовать»
Статус: выдано
Исполнитель: отдельная сессия в дереве `claude/prompt-diagnosis` (устав —
`.claude/agents/director-prompt-engineer.md`)
Модель: **Sonnet, effort high**
Выдано: 2026-08-30

**КАК ЗАПУСКАТЬ ПРОГОН — через `scripts/llm-run.ps1`, не руками.**

```text
pwsh -File "D:/Pax Historia LOCAL/scripts/llm-run.ps1" -Run "npx tsx scripts/runCampaignWithLLM.ts --years 5 --player SUN --out <каталог>"
```

Скрипт закрывает ровно тот затык, в который упирается дерево: читает
`<main>/server/.env` В ПЕРЕМЕННЫЕ ПРОЦЕССА (у linked worktree своего `.env`
нет и копий ключа по деревьям остаться не должно), поднимает
`cli-proxy-api.exe` свёрнутым, если эндпоинт молчит, ждёт **готовности модели в
`/v1/models`**, а не открытия порта, и запускает команду из `server/` текущего
дерева. Работающий прокси не перезапускает и не закрывает — он общий ресурс
машины.

**Поправка владельца продукта 2026-08-30.** Первая редакция этого задания
объявляла шлюз блокером и предписывала исполнителю остановиться: замер
`curl http://localhost:8317/v1/models` дал exit 7. Замер верен, вывод неверен —
скрипт запуска существует с 2026-08-03, и правило «исчерпать поиск перед
выводом "не работает"» применено не было. Блокера нет.

**Что остаётся верным из предупреждений.** Прокси теряет авторизацию во время
длинной работы (160 ответов `503 auth_unavailable` подряд на 15-летнем прогоне,
2026-08-21), лечится перезапуском процесса, а не кодом; пятилетний прогон это
переживает (60/60 ходов, 0 ошибок). И главное: числа из прошлых прогонов не
являются замером исполнителя — база снимается своим прогоном ДО правки.

**Задача.** Отделить две конкурирующие причины немоты глаголов. Что станет
иначе в партии: если причина в промте, часть милстоунов не понадобится для
достижимости — заход объёма S отвечает, нужен ли заход объёма L (М4-ядро).

**Основание.** `guarantee`: механика полная, войны не требует, слот свободен в
80% ходов, и ноль за 480 ходов. Разбор
`.agent/runs/war-never-happens-2026-08-21/`: единственное упоминание войны в
промте — запрет (`LLMService.ts:1118`), разрешающей строки нет ни одной, а
глаголы с явным разрешением (`research_shift`, `production_shift`) — в топе.

**Критерии приёмки** (записаны до старта):

- [ ] прогон на 5 лет ДО правки воспроизводит базу: 10 глаголов из 22;
- [ ] после правки число разных глаголов выросло, и названо какие добавились;
- [ ] доля отказов не выросла; коды отказов приложены;
- [ ] правило «избегай прямой войны ядерных держав» сохранено — прямых войн
      между ядерными в прогоне ноль;
- [ ] негативный контроль: откат правки промта возвращает прежнее число
      глаголов.

**Вне scope.** Механики, данные сценария, геометрия. Только текст промта
режиссёра и замеры.

**Аудит.** —

## Порядок и параллельность (редакция 2026-08-30)

```text
P-1 геометрия ∥ P-3 промт  →  P-4 стартовая дипломатия  →  P-2 бюджет промта
   →  М4-ядро  →  М2  →  М3  →  М4-глубина  →  М5  →  М6
```

- **P-1 и P-3 идут одновременно** — не пересекаются (`scripts/map/**` против
  `server/src/services/`).
- **P-2 не параллелится с P-1**: `scripts/map/**` делится доменами `geometry` и
  `world`, обе стороны заканчивают полным прогоном пайплайна, переписывающим
  `regions.core.json`, `regions.state.json` и `world_1946.geojson`
  (`.agent/orchestration/geometry.md`).
- **P-4 пайплайн карты не гоняет** (данные дипломатии и `Scenario1946.ts`),
  поэтому формально с P-1 не конфликтует. Если исполнитель обнаружит, что
  правка требует перегенерации сценария — ждёт влития P-1, а не решает сам.
- **P-4 перед P-2**, потому что оба ведёт домен `world` и оба трогают данные
  сценария: одна ветка за раз.
