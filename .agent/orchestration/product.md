# Реестр роли `product`

Очередь продуктовых задач, критерии готовности и вердикты приёмки. Формат —
`.agent/orchestration/README.md`. Числа сюда попадают только те, что снял
владелец продукта или аудитор своим прогоном.

## Состояние на 2026-09-04

**Цель v1 названа и записана** (`docs/DECISIONS.md` 2026-09-04): партия
проходится пользователем насквозь и при этом интересна. **Сквозной критерий
закрытия милстоунов** — достижимость глаголов алфавита с названным актором, а
не список сделанных пунктов.

**Достижимость считается с порогом** (2026-09-04, после приёмки P-3): глагол
достижим при ≥3 применениях в прогоне ЛИБО при подтверждении в двух
независимых прогонах; единичное срабатывание — шум. Сырые значения последних
прогонов: 8 разных глаголов на `gemini-3.6-flash-high`, 10 на
`gemini-3.8-flash-high` (`.agent/runs/prompt-diagnosis-2026-08-30/`), 10 в
историческом `campaign-5y-2026-08-21`. База по новому правилу не пересчитана.

**ПРОМТ-ЛИНИЯ ЗАМОРОЖЕНА до конца разработки систем** (решение пользователя
2026-09-04). Пока она заморожена, милстоун закрывается достижимостью **ИГРОКОМ**
— приказ текстом проходит `translatePlayerOrder` и применяется, — а не
режиссёром: режиссёрский путь зависит от промта и потому сейчас неизмерим.
Различение двух путей не изобретено сейчас, оно записано в критериях приёмки
v1 от 2026-08-19.

Замеры, снятые владельцем продукта 2026-09-04 на `main` (`71fd833`):

| Что | Значение |
|---|---|
| Регионов / водных узлов в сценарии | 1389 / 195 |
| Регионов с записанным берегом | 768 |
| Регионов вообще без связей | **0** (133 без сухопутных соседей имеют воду) |
| Стран в ростере | 157, из них **34 колониальные/оккупационные** (`Q*`) |
| `sovereigntyStatus` в `government.json` | размечен на 157 |
| Читателей `sovereigntyStatus` в `server/src/simulation/` | **0** (примитивы вассалитета читают) |

**Морской граф закрыт** (T-12, 2026-08-20) — роадмап нёс устаревшее «0 водных
соседей» до 2026-09-04. Блокер сместился с данных на решение: `adjacentWaterIds`
не читает ни один потребитель, `isCoastalRegion()` — литеральный `return false`.

**Опровергнутая премиса, стоившая неверной оценки объёма.** Живой пункт
`docs/TODO.md` утверждал, что колониальные блоки собраны «один блок на
сюзерена, всё британское — единый QCG». Замер: страны `QCG` не существует,
AOF и AEF есть (`QFW`, `QFE`), Вест-Индия разбита на `QWL`/`QWW`, Индия — `IND`
(British Raj). Владелец продукта принял пункт на веру и записал в журнал вывод
«Индии неоткуда отделиться» — опровергается одной строкой данных. Правило
«прежде чем судить о пункте backlog — искать его починку в истории»
существовало и применено не было. Записи исправлены тем же ходом.

## Открытые развилки к пользователю (не больше трёх одновременно)

1. **Чем кончается партия.** Горизонт даты, условия победы или явное решение
   оставить бесконечной. Единственное терминальное состояние сегодня —
   `defeated` при потере всех регионов (`shared/src/types/Campaign.ts`). Без
   ответа главный критерий v1 непроверяем: «насквозь» нечем измерить.
2. **Судьба `build_islands_preview.py`** (унаследовано из `geometry.md`, вопрос
   4). Удаление необратимо, поэтому решает пользователь. Рекомендация домена и
   владельца продукта совпадают: удалить вместе с отработавшей записью
   `region_edits_islands.json`.
3. *(свободна)* — прежний пункт «порог отсечения мелких стран» снят вместе с
   отложенной линией зависимости (см. ниже).

**Отложено пользователем 2026-09-04 («отмена по автономиям, потом вернёмся»):**
линия механики зависимости — три типа плюс шкала давления за независимость.
Наработанное не потеряно: замеры, сжатие 10 статусов в 3, исследование жанра
(HOI4, EU4, Victoria 3) и цена записаны в `docs/IDEAS.md` записью 2026-09-04,
горизонт — возврат по запросу пользователя. Переисследовать при возврате не
нужно.

**Закрыто черновиком «переголосуем на фактах»:** полигон Ватикана (0,0107 км²
против ~0,44). До v1 не чинится, причина записана в `KNOWN_ZERO_AREA`
(`verify_geojson_field_contract.py`). Внешний источник контура — вопрос
лицензии, а он относится к гейту релиза, не к разработке.

## Решения владельца продукта по висящим вопросам `geometry` (2026-09-04)

Четыре из пяти вопросов домена от 2026-08-09 закрыты здесь — они обратимы и
потому не требуют пользователя:

1. **Целевая гранулярность регионов — оставить как есть до v1.** Совпадает с
   рекомендацией домена: карта играбельна, пере-нарезка тянет каскад
   `region_id` и пересчёт экономики. Турция вчетверо грубее соседей — принято
   как есть.
2. **Восемь источников (G7) — мастер под git считается достаточным источником
   истины.** Докупка geoBoundaries и китайских исторических шейпов ради
   воспроизводимого `--rebuild-master` не оправдана: пересборка мастера не
   входит в путь игрока.
3. **Ганьсу/Синьцзян (G3) — 20,9 км² суммарного расхождения приемлемы**, запись
   уходит из живого backlog в историю решений.
4. **Достижимость 133 островов (G9)** — вопрос частично снят самой работой
   домена: морская смежность экспортирована (вариант «а» фактически сделан).
   Остаётся живая `isCoastalRegion()` — она же блокер морских десантов.
   Решение: делать её, отдельной развилки больше нет.

## Задания

## P-1 Закрыть остатки геометрии
Статус: возвращено
Исполнитель: домен `geometry`, дерево `claude/geometry-final`
Модель: **Sonnet, effort high**; механические проходы (перевод читателей,
удаление отработавших планов) — **Haiku, effort low**
Выдано: 2026-09-04 пользователем

**Задача.** Довести геометрию до состояния «к ней почти не возвращаемся»
(формулировка пользователя 2026-09-04). Что станет иначе в партии: игрок
перестаёт видеть безымянные регионы, а морские механики получают данные, на
которые смогут опереться в М4.

**Состав** (из раздела «Карта/регионы» `docs/TODO.md`, проверен 2026-09-04):

- `isCoastalRegion()` в `generateMapFeatures.ts:113` — литеральный
  `return false`, порты не генерируются вовсе;
- два безымянных региона `SAM-0040` и `SAM-0055` — игрок видит пустую подпись;
- реестр отделённых кусков: `config/region_fragment_allowlist.json` не создан,
  критерий `orphan` признан непригодным (все 23 «сироты» — намеренные слияния);
- вторая половина сверки геометрии с экономикой: площадь в `regions.core.json`
  не сверяется с той, из которой посчитаны экономические поля региона;
- `diagnose_coastline_gaps.py` не переведён на мастер (падает
  `FileNotFoundError`);
- дедупликация вершин морских зон (мастер 41,1 МБ);
- 14 планов со `Status: complete` плюс `interface-rebuild.md` — удалить по
  `.agent/PLANS.md`.

**Критерии приёмки** (записаны до старта):

- [ ] `isCoastalRegion()` истинна для регионов с непустым `adjacentWaterIds`;
      число прибрежных регионов совпадает с 768 из замера 2026-09-04 либо
      расхождение объяснено;
- [ ] `python scripts/map/audit_map_geometry.py` — код 0;
- [ ] `python scripts/map/validate_sea_adjacency_1946.py` — код 0;
- [ ] `python .agent/evals/public/run_public_evals.py` — код 0;
- [ ] безымянных регионов ноль: `KNOWN_EMPTY_NAMES` пуст либо каждая запись в
      нём обоснована решением пользователя;
- [ ] негативный контроль: тест на `isCoastalRegion()` падает при возврате
      прежнего `return false`.

**Вне scope.** Пере-нарезка регионов (решение выше — оставить), полигон
Ватикана (закрыт черновиком), каналы и проходы (отдельная развилка,
`docs/plans/14_CANAL_PASSAGE.md`), любые правки `server/src/simulation/**`.

**Аудит — владелец продукта, 2026-09-04. Вердикт: ВОЗВРАЩЕНО.**

Причина одна и она перевешивает всё остальное: **сценарий не загружается**.

```
ScenarioDataError: demographics.json: нет региона id=1372 в сценарии
  applyDemographicLayers  Scenario1946.ts:200
  buildScenario1946       Scenario1946.ts:425
```

Прогнано владельцем продукта, а не взято из отчёта:

| Проверка | Ветка `claude/geometry-final` | `main` |
|---|---|---|
| `npx vitest run src/scenarios src/primitives` | **8 файлов из 17 упали** | — |
| `npx vitest run src/scenarios` | — | **5 файлов, 58 тестов, зелено** |
| `python scripts/map/validate_demographics_1946.py` | **exit 1** | — |
| `python scripts/map/validate_region_economy_1946.py` | **exit 1** | — |
| `python scripts/map/audit_map_geometry.py` | **exit 0** ✓ | — |
| `python scripts/map/validate_sea_adjacency_1946.py` | **exit 0** ✓ | — |

**Справедливости ради: собственно геометрия чиста.** Оба геометрических
критерия задания выполнены — падают только валидаторы данных, зависящих от
состава регионов. Исполнитель закрыл свою область и не закрыл каскад в чужую.

**Это регрессия, а не предсуществующий failure:** те же тесты на `main`
проходят полностью. Партию на этой ветке начать нельзя.

**Механизм.** Регионы укрупнены 1389 → **1371** (минус 18), а
`demographics.json` остался на 1389 записей: сверка множеств даёт **18 висячих
ссылок** (id 1372…1389), и первая же из них роняет загрузку исключением.
`regions.state.json`, `regions.core.json` и `capitalRegionId` всех стран
перенумерованы согласованно — висячих ссылок между ними ноль, столиц вне
ростера ноль. Каскад закрыт везде, кроме демографии.

Укрупнение Руанды, Бурунди и Верхней Вольты сделано по прямому указанию
пользователя исполнителю, вне этого задания (в задании пере-нарезка стояла во
«Вне scope»). Указание пользователя выше задания владельца продукта, и работа
как таковая под сомнение не ставится — но **каскад, который она порождает,
обязан быть закрыт до сдачи**: домен `geometry` сам предупреждал об этом
формулировкой «пере-нарезка тянет каскад `region_id` и пересчёт экономики».

**Что в работе сделано и при повторной сдаче не переделывается:**

- `isCoastalRegion()` перестал быть заглушкой:
  `return region.adjacentWaterIds.length > 0`, порты генерируются;
- добавлен тест `generateMapFeatures.test.ts` — два кейса, первый структурно
  падает при возврате `return false` (негативный контроль владельцем продукта
  собственным прогоном НЕ перепроверен: правка кода роли запрещена, поэтому
  критерий засчитан по структуре теста, а не по факту падения);
- `SAM-0040` и `SAM-0055` получили имена (Малпело, Авес), `KNOWN_EMPTY_NAMES`
  пуст — критерий выполнен;
- `diagnose_coastline_gaps.py` переведён на общий резолвер;
- `build_islands_preview.py` удалён — решение пользователя исполнено;
- классификатор фрагментов больше не путает пустое имя сырья с сиротой;
- прибрежных регионов 767 против 768 в замере до работы — расхождение на
  единицу объясняется укрупнением, вопросов не вызывает.

**Не сделано из состава задания:** `region_fragment_allowlist.json` не создан;
дедупликация вершин не выполнена (мастер 41,0 МБ против 41,1 — то есть не
тронут); из 15 отработавших планов удалено 8, осталось 7.

**Условие приёмки при повторной сдаче:**

- [ ] `npx vitest run src/scenarios src/primitives` в дереве — 0 упавших
      файлов; демография пересчитана под 1371 регион либо перенумерована;
- [ ] `python scripts/map/validate_demographics_1946.py` — код 0;
- [ ] `python scripts/map/validate_region_economy_1946.py` — код 0;
- [ ] негативный контроль по демографии: тест или валидатор падает, если
      вернуть висячую ссылку;
- [ ] три невыполненных пункта состава — либо сделаны, либо явно вынесены
      пользователем в отдельную задачу.

**Снято с задания владельцем продукта как его собственный дефект:** критерий
«`run_public_evals.py` — код 0» заведомо невыполним, потому что в нём живёт
предсуществующий провал `audit fresh: living-docs (36d / 30d)`, идентичный на
`main`. Правильная формулировка — «не появилось НОВЫХ провалов сверх
living-docs»; исполнителю это в вину не ставится.

**Отдельная находка исполнителя, принимается как ценная:** в
`demographics_1946.json` обнаружен пред-существующий дефект — Коморы и Эквадор
перепутаны местами, Южная Америка не сопоставляется с эталонным снимком
`1b67bcd`. К возврату отношения не имеет, задокументирован, домен —
наполнение.

## P-2 Бюджет промта и фактические ошибки ростера
Статус: черновик
Исполнитель: домен `world`, отдельное дерево; **стартует только после влития P-1**
Модель: замер токенов — **Haiku, effort low**; разбор аномалий — **Sonnet,
effort high**
Выдано: —

**Урезано 2026-09-04.** Прежняя редакция включала пересмотр состава ростера по
критерию автономности; линия отложена пользователем, и вместе с ней снят
вопрос «сколько стран». Осталось то, что от него не зависит и полезно само по
себе.

**Задача.** Закрыть два независимых пробела: неизмеренный бюджет промта и
фактические противоречия в данных ростера.

**Шаг 0 — замер бюджета.** Цена одной страны в токенах промта и текущий размер
промта на длинной кампании: бюджет PROMPT < ~20k объявлен в критериях приёмки
v1 и **не замерен ни разу**. Инструмент готов —
`server/src/llm/tokenTelemetry.ts` и `scripts/token-usage-report.mjs`. Число
нужно независимо от состава ростера: оно ограничивает всё, что когда-либо
попадёт в промт.

**Шаг 1 — аномалии данных, найденные владельцем продукта 2026-09-04.** Это не
вопрос вкуса, а внутренние противоречия:

- `QSO British Somaliland` существует как страна, но регионы Bari, Mudug,
  Hiiraan, Bay принадлежат `GBR` напрямую — а это бывшее ИТАЛЬЯНСКОЕ Сомали
  под британской военной администрацией, другая территория;
- Триполитания, Киренаика и Эритрея числятся владением метрополии `GBR`, хотя
  были британской военной администрацией с иной судьбой;
- `leased_territory` объявлен в `SOVEREIGNTY_STATUSES` и не встречается в
  данных ни разу — либо статус лишний, либо кто-то размечен неверно;
- `shortName.ru` обрезан у двух стран: `USA` «Соединённые Штаты Америк»,
  `QAD` «Колония и протекторат Ад».

**Критерии приёмки** (записаны до старта):

- [ ] число: сколько токенов промта стоит одна страна и сколько стоит промт
      сегодня на прогоне не короче 5 лет;
- [ ] по каждой аномалии — вердикт с провенансом: исправлено либо признано
      верным с обоснованием;
- [ ] `python scripts/map/validate_region_economy_1946.py` — код 0;
- [ ] прогон партии на 5 лет после правки: ошибок 0, число разных глаголов не
      упало ниже 10 из 22.

**Вне scope.** Состав ростера и число стран (линия отложена), геометрия
полигонов, код симуляции, UI.

**Аудит.** —

## P-4 Стартовая дипломатия 1946
Статус: черновик
Исполнитель: домен `world`, отдельное дерево
Модель: **Sonnet, effort high**
Выдано: —

**Задача.** Дать миру знать, кто кому кто, на дату старта. Что станет иначе в
партии: первые два года из двадцати перестают быть слепыми.

**Основание — замер.** `allies` и `rivals` пусты у ВСЕХ 157 стран
(`Scenario1946.ts:329`), `relations` тоже. Симуляция наращивает их сама, но к
июлю 1948 — то есть мир узнаёт о холодной войне через два с половиной года
после её начала. Динамика из `.agent/runs/war-never-happens-2026-08-21/`:
1946-01 — 0 соперничеств и 0 союзов, 1948-07 — 85 и 6, 1951-01 — 93 и 46.
`getDiplomaticSituation()` до 1948 печатает режиссёру «No major diplomatic
tensions» — в 1946 году.

**ВХОД ГОТОВ — черновик сгенерирован владельцем продукта 2026-09-04**
(`.agent/runs/world-fill-diplomacy-2026-09-04/`): 71 соперничество и 91 союз,
0 неверных id, 0 дублей, 78 стран из 157 задействовано. Исполнителю НЕ нужно
генерировать заново — его работа начинается с ревизии этого черновика.

**Замер устойчивости, меняющий метод приёмки.** Два прогона одним промтом дали
43/50 и 65/77 пар; воспроизвелось в обоих только **37 соперничеств и 36
союзов** — 52 % и 40 % от объединения. Один прогон слоя не даёт. Каждая пара в
`merged-draft.json` несёт `confidence`: `both` либо имя одного прогона.
Расхождение — не мусор: хвост содержит Дюрандову линию, Триест, Фолкленды,
Эритрею. Отдельный класс, который модель не решит: `AFG`–`GBR` против
`AFG`–`IND` для одной и той же Дюрандовой линии — атрибуция зависимой единицы
решается человеком.

**Метод канала установлен пробой 2026-09-04**
(`.agent/runs/world-fill-probe-2026-09-04/`): генерация идёт через локальный
прокси одним запросом на ВЕСЬ ростер, а не порциями. Три числа оттуда:
весь ростер из 157 строк стоит 2058 токенов промта; ответ приходит за 15 секунд;
машинная сверка дала 0 неверных id и 0 дублей. Порционирование по алфавиту
задачу уничтожает — связь существует только когда обе её стороны в одном
запросе (первый заход: 12 стран подряд дали одну пару). Внешний скрипт
генерации обязан иметь свой повтор на `429`: он приходит и на одиночном
запросе.

**Критерии приёмки** (записаны до старта):

- [ ] на первом ходу партии `rivals` непусты минимум у великих держав, и
      `getDiplomaticSituation()` не печатает «No major diplomatic tensions»;
- [ ] каждая пара черновика имеет вердикт: принята с провенансом, исправлена
      или отброшена с причиной. Пары с `confidence != both` проверяются в
      первую очередь;
- [ ] развилки атрибуции решены явно: для зависимой единицы названо, кто
      сторона связи — она сама или её сюзерен (образец: Дюрандова линия);
- [ ] полнота названа числом и обоснована: 71 соперничество и 91 союз — оценка
      модели, а не критерий; граница «сколько достаточно для 1946» ставится
      исполнителем;
- [ ] 13 суверенных стран без единой связи (Швейцария, Швеция, Финляндия,
      Ирландия, Исландия, Либерия, Непал, Оман, Ватикан, четыре микрогосударства)
      — подтверждены как нейтралы либо дополнены; Финляндия проверяется
      отдельно (положение под Союзной контрольной комиссией);
- [ ] провенанс на каждую пару соперничества и союза;
- [ ] прогон на 5 лет: число разных глаголов НЕ ниже 10 из 22, ошибок 0;
- [ ] негативный контроль: тест на непустую стартовую дипломатию падает при
      возврате пустых значений.

**Вне scope.** Состав ростера, механика зависимости (отложена), геометрия.

**Аудит.** —

## P-3 Промт-диагностика: отделить «не разрешено» от «нечему действовать»
Статус: принято
Исполнитель: отдельная сессия в дереве `claude/prompt-diagnosis` (устав —
`.claude/agents/director-prompt-engineer.md`)
Модель: **Sonnet, effort high**
Выдано: 2026-09-04

**КАК ЗАПУСКАТЬ ПРОГОН — через `scripts/llm-run.ps1`, не руками.**

```text
pwsh -File "D:/Pax Historia LOCAL/scripts/llm-run.ps1" -Run "npx tsx scripts/runCampaignWithLLM.ts --years 5 --player SUN --out <каталог>"
```

Скрипт закрывает ровно тот затык, в который упирается дерево: читает
`<main>/server/.env` В ПЕРЕМЕННЫЕ ПРОЦЕССА (у linked worktree своего `.env`
нет и копий ключа по деревьям остаться не должно), поднимает
`cli-proxy-api.exe` свёрнутым, если эндпоинт молчит, ждёт **готовности модели в
`/v1/models`**, а не открытия порта, и запускает команду из `server/` текущего
дерева. Работающий прокси не перезапускает и не закрывает — он общий ресурс
машины.

**Поправка владельца продукта 2026-09-04.** Первая редакция этого задания
объявляла шлюз блокером и предписывала исполнителю остановиться: замер
`curl http://localhost:8317/v1/models` дал exit 7. Замер верен, вывод неверен —
скрипт запуска существует с 2026-08-03, и правило «исчерпать поиск перед
выводом "не работает"» применено не было. Блокера нет.

**Что остаётся верным из предупреждений.** Прокси теряет авторизацию во время
длинной работы (160 ответов `503 auth_unavailable` подряд на 15-летнем прогоне,
2026-08-21), лечится перезапуском процесса, а не кодом; пятилетний прогон это
переживает (60/60 ходов, 0 ошибок). И главное: числа из прошлых прогонов не
являются замером исполнителя — база снимается своим прогоном ДО правки.

**Задача.** Отделить две конкурирующие причины немоты глаголов. Что станет
иначе в партии: если причина в промте, часть милстоунов не понадобится для
достижимости — заход объёма S отвечает, нужен ли заход объёма L (М4-ядро).

**Основание.** `guarantee`: механика полная, войны не требует, слот свободен в
80% ходов, и ноль за 480 ходов. Разбор
`.agent/runs/war-never-happens-2026-08-21/`: единственное упоминание войны в
промте — запрет (`LLMService.ts:1118`), разрешающей строки нет ни одной, а
глаголы с явным разрешением (`research_shift`, `production_shift`) — в топе.

**Критерии приёмки** (записаны до старта):

- [ ] прогон на 5 лет ДО правки воспроизводит базу: 10 глаголов из 22;
- [ ] после правки число разных глаголов выросло, и названо какие добавились;
- [ ] доля отказов не выросла; коды отказов приложены;
- [ ] правило «избегай прямой войны ядерных держав» сохранено — прямых войн
      между ядерными в прогоне ноль;
- [ ] негативный контроль: откат правки промта возвращает прежнее число
      глаголов.

**Вне scope.** Механики, данные сценария, геометрия. Только текст промта
режиссёра и замеры.

**Аудит — владелец продукта, 2026-09-04. Вердикт: ПРИНЯТО.**

Числа ниже сняты собственным пересчётом по `llm.jsonl` и `world.csv` ветки
`claude/prompt-diagnosis`, а не переписаны из отчёта:

| Прогон | Разных глаголов | Применений | Отказов |
|---|---:|---:|---:|
| `before` (`gemini-3.6-flash-high`) | **8** | 228 | **0** |
| `before-gemini-3.8-flash-high` | **10** | 352 | 0 |
| `campaign-5y-2026-08-21` (историческая база) | 10 | 262 | 5 |

Проверено и подтвердилось: `activeWars` равно нулю во ВСЕХ 61 строках
`world.csv` обоих прогонов; `SANCTION_TYPES` содержит четыре значения
(`DiplomacyState.ts:37-42`), а контракт промта называет по имени ровно одно —
`trade_embargo`, остальные три безымянно («the other three kinds are
reputational», `primitiveContract.ts:112-115`); запрет прямой войны ядерных
держав стоит на `LLMService.ts:1118` и разрешающей строки рядом нет.
Кросс-модельная проверка воспроизведена по артефактам: `guarantee` 0→3,
`sanction` 0→1, `condemn` 0→5 **без единой правки промта** — доказательство
доступности предпосылки сильнее косвенного.

Единственное расхождение с отчётом: `allyRecords` в `before` заканчивается на
**50**, отчёт называет 48. На вывод не влияет.

**Почему принято, хотя два критерия из пяти не выполнены.** Критерии «после
правки число глаголов выросло» и «негативный контроль отката» не выполнены,
потому что правки нет. Это дефект МОЕГО задания, а не работы: я слил в одно
задание диагностику и правку, тогда как устав `director-prompt-engineer`
требует замера до/после на КАЖДУЮ отдельную правку, а находок оказалось пять
независимых. Исполнитель прав, что остановился на диагностике, и сделал сверх
задания то, чего в нём не было, — кросс-модельную проверку. Оба несостоятельных
критерия снимаются с P-3 и переезжают в P-5.

**Что установлено (принимается как факт):** молчание по 14 глаголам из 22 —
это в основном гипотеза Б («промт не продаёт»), а не А («нечему действовать»).
Б расслаивается: у `war` — твёрдая форма (явный запрет, ноль на обеих
моделях), у `guarantee`/`sanction`/`condemn` — мягкая (тишина в промте,
сильная модель иногда преодолевает, слабая — нет). Каскадная А у
`peace`/`support_proxy`/`annex`/`merge_countries` вызвана отсутствием `war` и
`puppet`, то есть той же Б выше по цепочке. UNKNOWN остаются
`split_country`, `capital_flight`, `create_country`, `build_extraction.dismantle`
— по ним нужен per-region замер, которого этот прогон не даёт.

## P-5 Первая правка промта: перечислить типы `sanction`
Статус: отложено до конца разработки систем (решение пользователя 2026-09-04)
Исполнитель: сессия в дереве `claude/prompt-sanction-fix` (устав —
`.claude/agents/director-prompt-engineer.md`)
Модель: **Sonnet, effort high**
Выдано: —

**Задача.** Одна правка — один замер. Контракт промта называет по имени один
тип санкции из четырёх (`trade_embargo`), остальные три упоминает безымянно,
хотя схема принимает все (`z.enum(SANCTION_TYPES)`, отказа по схеме быть не
может — и отказов за оба прогона ноль). Это тот же класс дефекта, который уже
чинили дважды: `research_shift` без перечисленных доменов давал 1 применение
из 72 ходов, после перечисления — 154 (`getResearchDomainsLine`,
`LLMService.ts:1227-1257`); `build_extraction` починен так же
(`RESOURCE_NAMES`). К `sanction` тот же фикс никто не применил.

Что станет иначе в партии: санкции появляются как инструмент холодной войны —
сегодня их нет вовсе на слабой модели и одна штука на сильной.

**Критерий успеха, взятый из диагностики P-3 и он строже обычного:** правка
должна снять ЗАВИСИМОСТЬ ОТ СИЛЫ МОДЕЛИ, а не поднять число на сильной. Поэтому
замер на ОБЕИХ моделях, до и после.

**Критерии приёмки** (записаны до старта):

- [ ] замер до/после на `gemini-3.6-flash-high` И на `gemini-3.8-flash-high`,
      прогоны по 5 лет, командой `scripts/llm-run.ps1` с `-Model`;
- [ ] `sanction` на СЛАБОЙ модели вырос с нуля — иначе правка не сработала
      там, ради чего делалась (`docs/CONCEPT.md` §7: слабая локальная модель
      названа целевым свойством архитектуры);
- [ ] общее число применений и число разных глаголов не упало ни на одной
      модели;
- [ ] доля отказов не выросла; коды отказов приложены;
- [ ] негативный контроль: откат правки возвращает `sanction` к нулю на слабой
      модели.

**Вне scope.** Остальные четыре находки диагностики (`guarantee`, `condemn`,
`puppet`, `grant_autonomy`) — каждая своим заданием со своим замером, иначе
эффекты смешаются. Механики, данные сценария, геометрия.

**ОТЛОЖЕНО 2026-09-04 вместе со всей промт-линией.** Основание пользователя:
сначала все системы, потом промт. Владелец продукта согласен по существу —
правка промта под несуществующие механики бессмысленна (продавать режиссёру
`annex`, когда войны нет, впустую), а каждая правка стоит пары прогонов и
устареет после следующей системы. Цена решения названа в `docs/DECISIONS.md`:
счётчик достижимости режиссёром не двинется всё время разработки, и прецедент
`research_shift` (1 применение из 72 ходов при полностью рабочей механике)
показывает, чем это грозит — рабочую систему можно счесть ненужной. Страховка —
закрытие милстоунов по пути ИГРОКА, см. шапку реестра.

**Аудит.** —

## Порядок и параллельность (редакция 2026-09-04)

```text
P-1 геометрия  →  P-4 стартовая дипломатия  →  P-2 бюджет промта
   →  М4-ядро  →  М2  →  М3  →  М4-глубина  →  М5  →  М6
   →  ПРОМТ-ЛИНИЯ целиком (P-5 и четыре находки диагностики)
```

**Промт-линия ушла в конец** решением пользователя 2026-09-04. P-3
(диагностика) принята и остаётся в силе: её выводы — готовый план правок на тот
момент, когда линия разморозится, переисследовать не придётся.

- **P-1 и P-3 идут одновременно** — не пересекаются (`scripts/map/**` против
  `server/src/services/`).
- **P-2 не параллелится с P-1**: `scripts/map/**` делится доменами `geometry` и
  `world`, обе стороны заканчивают полным прогоном пайплайна, переписывающим
  `regions.core.json`, `regions.state.json` и `world_1946.geojson`
  (`.agent/orchestration/geometry.md`).
- **P-4 пайплайн карты не гоняет** (данные дипломатии и `Scenario1946.ts`),
  поэтому формально с P-1 не конфликтует. Если исполнитель обнаружит, что
  правка требует перегенерации сценария — ждёт влития P-1, а не решает сам.
- **P-4 перед P-2**, потому что оба ведёт домен `world` и оба трогают данные
  сценария: одна ветка за раз.
