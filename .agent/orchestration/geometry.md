# Реестр домена `geometry`

Очередь, критерии приёмки и вердикты аудита по геометрии карты. Формат —
`.agent/orchestration/README.md`. Числа сюда попадают только те, что снял
оркестратор или аудитор своим прогоном; числа из отчёта исполнителя — нет.

## Замер базы (2026-08-09, ветка `claude/geometry-orchestrator` от `92c1aa9`)

Снято этой сессией на живых данных, не принято из чужого отчёта. Инструменты:
`pyproj.Geod(WGS84)` по геометрии, `out/neighbor_graph.json` по связности.

| Величина | Значение |
|---|---|
| Фич в мастере `world_1946.master.geojson` | 1591 |
| — суша | 1396 (146 325 933 км²) |
| — морские зоны `SEA-` | 182 (362 851 398 км²) |
| — озёра `LAK-` | 13 (887 967 км²) |
| Сумма покрытия | 510,07 млн км² — площадь Земли, расхождение 0,002% |
| `client/public/world_1946.geojson` | те же 1591 фича, те же площади |
| Граф смежности | 1591 узел, 0 изолятов, 0 расхождений с мастером |
| Компонент суши по сухопутным рёбрам | 155 (крупнейшая 848, вторая 266) |
| Регионов без сухопутных соседей | **133**, морской сосед есть у всех 133 |

Прогоны, давшие код 0: `build/audit_map_geometry.py` (12 классов, новых
дефектов нет), `build/verify_china_curation.py`, `build/verify_master_freshness.py`
(41 генератор, мастер свежий), `build/audit_region_fragments.py`.

**Опровергнутые числа из `docs/TODO.md` и `.agent/plans/`.** «174 региона без
сухопутных соседей» и «сухопутных регионов 1451» (`geometry-snapshot-gate.md`,
2026-08-02) — устарели: сейчас 133 и 1396. Свойство «у островного региона есть
морской сосед» держится, изменилось только число. Комментарий в
`build/build_neighbor_graph.py:30` про «те же 1577 фич» тоже устарел (1591).
**Ошибка этой сессии, исправлена 2026-08-09.** Я объявил порог `docs/TODO.md`
не горящим: «1286 строк из 1400, запас 114», и повторил это в реестре
интегратора. Неверно. Мерил
`(Get-Content $f | Measure-Object -Line).Lines` — этот счётчик **пропускает
пустые строки**, и 1286 оказалось числом непустых. Public eval считает
`text.count("\n") + 1` (`run_public_evals.py:112`) и видел 1393 из 1400.
Заявленные «три точки проверки» защиты не дали: три прогона одного неверного
метода дают одно неверное число трижды. Урок в мою же область: сверять надо
метод, а не повторять замер. После триажа (ниже) файл 1301 строка.

## Триаж раздела «Карта/регионы» — ПРИМЕНЁН этой веткой 2026-08-09

Роль `lead` сняла запрет доменам править `docs/TODO.md` и отдала мне раздел
целиком. Правила: только свой раздел, порог не поднимать, дельта ≤ 0.

**Раздел адресуется ЗАГОЛОВКОМ `## Карта/регионы`, а не номером строки**
(правило `lead` 2026-08-09; наблюдение домена `logic`). Номер устаревает при
первом же чужом влитии: присланный мне `:1155` протух в момент отправки —
`claude/budget-one-base` сняла девять строк выше. Мой сплайс искал строку по
заголовку, поэтому не пострадал; проверено построчным сравнением с `main`:
разделы «Решения пользователя 2026-08-03», «Интерфейс — контракты стыка»,
«Интерфейс», «Крупные системы», «P4 — Инженерный долг» СОВПАДАЮТ с `main`
байт в байт. Ни один соседний раздел не задет.

Итог: раздел 209 строк → 119, файл **1394 → 1304**, дельта **−90**. Снятое не
исчезло: то, что нужно после v1, дословно перенесено в `docs/IDEAS.md` записью
того же дня. Вход для доменов `world` и `ui` помечен внутри раздела как вход,
а не как их файл.

**Уточнение правила и правка по нему (роль `lead`, 2026-08-09,
`claude/lead-triage-rule` `de6c58f`).** Указание «закрываешь за обоих»
отменено; владение разделом — не владение решением. Снять ЗАКРЫТЫЙ пункт —
факт, проверяемый прогоном, и снимается независимо от домена;
переклассифицировать ОТКРЫТЫЙ (в `IDEAS`, к милстоуну, удалить как
наблюдение) — решение его домена.

Проверил свой триаж по этому правилу. Нарушение нашлось **одно**: «текстурная
подложка морей» — открытый пункт домена `ui` — был перенесён мной в `IDEAS`.
Возвращён в `TODO`, из `IDEAS` убран. Все прочие чужие открытые пункты
(`shortName` USA/QAD, потолок добычи, колониальные блоки, иконки `MapFeature`,
режимы карты, `tier`, выбор страны, границы по зуму, флаги) остались в `TODO`
и помечены как вход — их я не трогал по существу.

**Вторая поправка `lead` (2026-08-09): «рассуждение доказательством закрытия
не является».** «Закрыт» бывает двух родов — механически проверяемое (сколько
регионов без соседа, ремапится ли позиционный файл) устанавливается прогоном
и снимается за любой домен; суждение о полноте наполнения прогоном не
устанавливается вообще и принадлежит домену. В моём разделе линия проходит
ровно посередине: геометрия и графы меряются, наполнение и историчность
судятся.

Перепроверил свои снятия по этому. Все закрытия геометрии сделаны прогоном:
«шесть читателей» — чтением кода и запуском скриптов; «18 морских мостов» —
`git log --all --diff-filter=A` и поиском по репозиторию; Ганьсу/Синьцзян —
прогоном `verify_china_curation.py`. Ни одного «счёл неактуальным» среди них
нет.

**Одно снятие я забрал обратно.** Узкие полосы у зоны Панамского канала я снял
по морфологическому замеру (у зоны канала 0 тонких кусков, крупнейший остаток
в районе 1,41 км²). Но исходная жалоба пользователя была ВИЗУАЛЬНОЙ, а замер
численный: закрывать наблюдение глазами измерением другого рода — тот же класс
ошибки, о котором поправка. Пункт возвращён в `TODO` с условием снятия
(рендер, T-3) и с числами замера, чтобы работа не потерялась. Цена — 8 строк,
дельта остаётся отрицательной.

Пограничный случай, называю явно, чтобы он был отслеживаем: у домена `world`
было ДВА пункта об одном и том же — «колониальные блоки не историческая
структура» и «`country_merge.json` — дефолт, а не финал». Я их слил в один,
сохранив более сильную формулировку, и пометил слияние прямо в тексте пункта.
Это сокращение дубля, а не переклассификация: содержание осталось в `TODO`,
из живого backlog ничего не ушло. Если `world` сочтёт иначе — разъединить
обратно стоит одну строку.

---

# Вердикт по разделу «Карта/регионы» — применён (см. выше)

## Подтверждено открытым

**G1. `build/build_islands_preview.py` падает.** Не «источник расхождения», а
жёсткий отказ: `FileNotFoundError` на `out/world_1946.geojson`, которого в
дереве нет и взяться неоткуда (8 из 15 входов пайплайна отсутствуют). Прогон
2026-08-09, код 1.

**G2. Закоммиченный `out/islands_index.json` описывает несуществующую карту.**
Файл под git заявляет `scenario_regions: 1444`, `land_components: 200`,
`islands: 198`, `island_regions: 327`. Свежий прогон `build_islands_index.py` на
живой геометрии: **1389 и 154**. Сверка: 1396 сухопутных минус 7 антарктических
секторов = 1389, и те же 7 дают одну компоненту из 155 — мой независимый счёт и
скрипт сходятся точно, устарел именно закоммиченный файл. Ничто этого не ловит.

**G3. Кураторская нарезка Китая: Ганьсу −18,477 км², Синьцзян +1,617 км².**
Перемерено мной прогоном `verify_china_curation.py` на мастере. Заявленных
расхождений в ALLOWLIST четыре, не два: сверх названных — Heilongjiang −0,403 и
Songjiang +0,408, и у этих двух причина в файле записана. Сверено 47 фич,
расхождений вне заявленных 0, потолок держит.

**G4. Реестр отделённых кусков не закончен.** `config/region_fragment_allowlist.json`
отсутствует (проверено). Живой прогон: 3581 кусок от 1 км² — `island` 3314,
`enclave` 135, `foreign_adm1` 87, `orphan` 23, `unsourced` 22.

**G5 (новое, вытекает из G4). Критерий `orphan` непригоден в нынешнем виде.**
Все 23 «настоящие сироты» приходятся на шесть регионов — `NAM-0050` Leeward
Islands (8), `NAM-0067` Windward Islands (7), `EUR-0125` Channel Islands (2),
`NAM-0003` Leeward Antilles (2), `NAM-0004` Windward Netherlands Antilles (2),
`AFR-0002` Comoros (2). Каждый из них — намеренное слияние нескольких
современных ISO-сущностей: `config/country_merge.json` сводит ATG/AIA/KNA/MSR/VGB
в `QWL`, DMA/GRD/LCA/VCT в `QWW`, ABW/CUW/SXM в `QND`, COM в `MDG`. Критерий
сравнивает ISO сырой ADM1 под куском с ISO под телом региона и на слитых
группах даёт ложную сироту **по построению**. Сирот, подлежащих удалению, на
сегодняшних данных **ноль**. Строить allowlist на этом критерии нельзя —
запись «закрыто навсегда» встанет поверх неверной классификации.

**G6 (уточняет G4). Остаток `foreign_adm1` — 15 записей, не 87 и не 103.**
72 из 87 — антарктические сектора с ПУСТЫМ именем единицы в сырье («лежит в
«»»), то есть один случай, а не 72. Разбора глазами требуют 15 записей в
четырёх регионах: `AFR-0071` (7), `NAM-0149` (5), `AFR-0072` (2), `AFR-0055` (1).

**G9 (по материалу роли `lead`, 2026-08-09). Влитие морских зон достижимость
островов НЕ обеспечило — F64 открыт по существу, изменилось только число.**
Оба вопроса `lead` закрыты замером на `server/data/scenarios/1946/regions.core.json`:

- `neighboringRegionIds` содержит **только сухопутные связи**. Разрешил все
  ссылки через `geoJsonId`: видов соседей ровно один — `land`, водных соседей
  0, неразрешённых ссылок 0. Схема (`server/src/scenarios/scenario1946Schemas.ts:40`)
  — `z.array(z.number().int())`, то есть числовые id сценария;
- **морского слоя в сценарии нет вообще.** В сценарии 1389 регионов, у всех
  `geoJsonId` — суша; морских и озёрных `geoJsonId` ноль. Из мастера в сценарий
  не попали 202 фичи: 195 водных (182 моря + 13 озёр) и 7 антарктических
  секторов. Строки `SEA-` нет ни в одном файле `server/data/scenarios/1946/`;
- у 133 островных регионов список соседей **пуст**. Морской сосед у всех 133
  есть — но в `scripts/map/out/neighbor_graph.json`, сборочном артефакте,
  который в игру не экспортируется.

То есть ветка морских зон принесла морскую **геометрию** (182 зоны рисуются),
а не морскую **смежность в данных сценария**. Потребители пустого списка:
`DiplomacyTick.ts:180`, `CountryService.ts:83`, `WarTick.ts:103` (там же в
комментарии на стр. 63 пробел назван прямо), `GeometryEngine.ts:408` на
клиенте. Для 133 регионов дипломатическое соседство, военный контакт и сборка
меток страны работают на пустом множестве.

**Это кросс-доменный стык, а не моя задача в одиночку.** Экспорт морской
смежности в сценарий — это `scripts/map/import_to_game.py` (моё) плюс тип
региона и схема (`logic`), и цена вопроса — новое поле, а не правка
существующего: расширить `neighboringRegionIds` водными id молча нельзя, его
трассируют военный контакт и дипломатия. Контракт стыка пишет `lead`.

**Пункт F64 роадмапа** (`docs/plans/11_MVP_ROADMAP.md:285-289`) переписать: он
называет 154 из 1399 и ссылается на невлитую ветку `claude/sea-shelf-zones`,
которой в репозитории нет. Верно: **133 из 1389**, ветка влита, недостижимость
осталась. Заголовок пункта «143 изолированных острова» тоже не сходится ни с
одним замером — трёх чисел в одной записи быть не должно.

**G7. Восемь источников карты из пятнадцати отсутствуют.** Подтверждено
сверкой `docs/provenance/MAP_GEOMETRY_PROVENANCE.md` с содержимым хранилища:
нет geoBoundaries BRA/CHN/USA, палестинских ADM2 ×2, `ne_10m_lakes`, кипрского
ADM1, китайских исторических шейпов. `--rebuild-master` невозможен. Это не
авария: мастер под git, свежесть сторожит `verify_master_freshness.py`.

**G8. 14 планов со `Status: complete` в `.agent/plans/`.** Пересчитано
поимённо — ровно 14. Пятнадцатый кандидат: `interface-rebuild.md` со
`Status: done (влито в main мержем f88c2d6)` — та же категория, другая
формулировка.

## Закрыто — снимается из TODO

**C1. «Шесть читателей `out/world_1946.geojson` не переведены на мастер» —
запись неверна в трёх шестых.** Проверено по коду:

| Скрипт | Факт |
|---|---|
| `audit_map_geometry.py` | мастер-первым, `out/` как fallback (стр. 749–750) — переведён |
| `audit_region_fragments.py` | мастер-первым (стр. 349–350) — переведён |
| `merge_world_1946.py` | **пишет** `out/world_1946.geojson`, а не читает; стоит внутри `MASTER_REBUILD_STEPS` выше заморозки — переводить нечего |
| `translate_world.py` | читает и пишет тот же промежуточный файл там же, до `freeze_master_map.py` — работает с артефактом, который мастером ещё не стал |
| `build_islands_index.py` | `out/`-первым, fallback на `client/public` — сегодня отрабатывает (код 0) на fallback, потому что тот побайтно совпадает с мастером |
| `build_islands_preview.py` | только `out/`, падает — см. G1 |

Остаётся **один сломанный читатель и один на неканоническом fallback**. Запись
про «шесть» и вывод «после перевода `_frozen_out/world_1946.geojson` в хранилище
не нужен» надо переписать: `_frozen_out` держат ещё и шесть континентальных
сборок, чьи входы отсутствуют, — его судьба к этим читателям не сводится.

**C2. Узкие полосы вдоль зоны Панамского канала — класса дефекта больше нет.**
Диагноз поставлен впервые (запись в TODO честно говорила «диагноз не
ставился»). Метод: морфологическое открытие радиусом 0,005° по каждому региону
целиком — тот же приём, что в `give_canal_zone_spike_to_water.py`, и он, в
отличие от `SPIKE`/`SLIVER`, площадь региона не смотрит и полосу внутри
большого тела видит. Результат по всем регионам перешейка:

| Регион | Тонких кусков | Крупнейший |
|---|---|---|
| `NAM-0251` Panama Canal Zone | 0 | — |
| `NAM-0128` Darién (3) | 0 | — |
| `NAM-0130` Los Santos (7) | 2 | 1,41 км², 9,9 × 3,31 км |
| `NAM-0127` Panama (3) | 3 | 0,89 км², 8,3 × 2,33 км — клин у Колона |
| `NAM-0129` Ngöbe Buglé (3) | 2 | 0,46 км² |

Исходная жалоба пользователя была про дамбу Амадор — 5 км при ширине ~1 км,
читавшуюся на карте линией; её закрыл `give_canal_zone_spike_to_water.py`.
Оставшиеся клинья втрое шире и линиями не читаются. Клин у Колона —
тот самый, который скрипт намеренно не тронул («другие соседние не трогать»).
**Оговорка:** жалоба была визуальная, и численный замер её не закрывает до
конца — закрывает один рендер перешейка (`build/render_region.py`). Это T-3.

## Уходит в `docs/IDEAS.md` — задачей не является

**I1. «18 открытых морских мостов — число неверное, перемерить».** Мерить
нечего: `out/sea_graph.json`, `config/sea_chokepoints.json` и ветка
`claude/sea-shelf-zones` **отсутствуют в репозитории целиком** — ни в рабочем
дереве, ни в истории любой ветки (`git log --all --diff-filter=A`). Слово
`chokepoint` встречается ровно в одном файле проекта — в самом `docs/TODO.md`.
Запись описывает счётчик в файле, которого нет.

Ценность в ней — метод, и он воспроизводится на живых данных. Подграф
море-море из `neighbor_graph.json`: **182 узла, 395 рёбер, 1 компонента,
25 мостов** (ребро, удаление которого увеличивает число компонент). Из 25
тринадцать — тупиковые заливы, где у одного конца степень 1 (Азовское, залив
Фанди, Рижский, Сиамский, Аденский–Акаба, Ла-Плата и др.): ребро в мешок
мостом является всегда и стратегическим горлышком не является. **Настоящих
горлышек 12:** Гибралтар (2 ребра), Альборан–Западное Средиземноморье,
Баб-эль-Мандеб (2), Ормуз, Дарданеллы, Босфор, Эгейское–Восточное
Средиземноморье, датские проливы (3). Малаккского пролива среди мостов нет —
обход существует, и это сходится с прошлым выводом.

В IDEAS уйдёт: определение моста, разделение «тупик / горлышко» и число 12/25
на 2026-08-09, чтобы будущая система морских узких мест начиналась не с нуля.

## Каналы — разобрано 2026-08-09 по запросу пользователя

Вопрос был «добавить ли Волго-Дон и Беломорканал». Замер отвечает раньше, чем
начинается выбор: **ни один канал в мире сегодня не работает как канал.**

Три канала, существовавшие в 1946, на карте есть — и все три сухопутными
регионами, каждый касается ровно тех двух вод, которые соединяет:

| Канал | Регион | Касается | Проход по воде |
|---|---|---|---|
| Суэцкий | `AFR-0016` Suez Canal Zone | `SEA-0044` Вост. Средиземное, `SEA-0043` Суэцкий залив | **нет**; обход вокруг Африки — 14 переходов |
| Панамский | `NAM-0251` Panama Canal Zone | `SEA-0022` Карибское, `SEA-0158` Western Canal zone | **нет**; обход вокруг Ю. Америки — 6 переходов |
| Кильский | `EUR-0374` Kiel Canal Zone | `SEA-0082` Северное, `SEA-0057` Балтийское | **нет**; обход через Скагеррак и Каттегат — 3 перехода |

Геометрия про каналы знает, граф — нет. Суэц как крюк вокруг мыса Доброй
Надежды — крупнейший геополитический факт, которого на карте не хватает, и
рядом с ним советские внутренние каналы не стоят.

**Горизонт кампании — до 2000 (уточнение пользователя 2026-08-09), и это
меняет постановку.** Мой первый вывод «Волго-Дон анахронизм» неверен: канал
открыт 31 июля 1952, то есть попадает внутрь кампании. Правильная постановка —
не «есть канал или нет», а **канал как проход с состоянием во времени**:

- не построен → строится → работает;
- работает → закрыт → снова открыт.

Второе не менее важно первого: Суэц был закрыт в 1956–57 (Суэцкий кризис) и в
1967–75 (после Шестидневной войны). Восемь лет без Суэца — событие того же
масштаба, что его постройка, и статичная карта его выразить не может.

События внутри горизонта 1946–2000: Коринфский открывается заново в 1948
(разрушен немцами в 1944, в 1946 **заблокирован**); Волго-Дон 1952; Суэц закрыт
1956–57 и 1967–75; Путь Святого Лаврентия 1959; Волго-Балт в современном виде
1964; Дунай — Чёрное море 1984; Рейн — Майн — Дунай 1992.

**Что для этого уже есть на карте (замер 2026-08-09).** Все водные тела, нужные
советским каналам, существуют: Каспий `LAK-0001` (озеро, 396 300 км²), Азовское
`SEA-0085`, Чёрное `SEA-0084`, Белое `SEA-0060`, Балтийское `SEA-0057`, Ладога
`LAK-0007`. То есть Волго-Дон выражается ребром `LAK-0001`↔`SEA-0085`, а
Беломорканал — `SEA-0060`↔`SEA-0057`. Отдельно отметить: **Каспий классифицирован
озером**, и модель смежности обязана допускать ребро озеро↔море, иначе Волго-Дон
не выразим в принципе.

**Беломорканал** открыт 1933, существовал в 1946, исторически по нему
перебрасывали подлодки и малые корабли на Северный флот. Осадка ~3,6 м —
крупные корабли не проходят. Значим, если игра различает класс корабля.

**Разделение ответственности, которое я предлагаю:**

- **геометрия (моё):** коридор канала — какие два водных тела он соединяет и
  какой сухопутный регион его контролирует. Для Волго-Дона это Волга у
  Волгограда → Дон у Калача, ~101 км. Коридор кладётся на карту ОДИН раз и не
  зависит от того, построен канал или нет;
- **симуляция (`logic`):** состояние прохода (не построен / строится / открыт /
  закрыт) и кто его закрывает;
- **мир (`world`):** исторические даты и события открытия и закрытия.

**G10 (находка пользователя 2026-08-09, подтверждена замером). Онежского озера
на карте нет — Беломорканал сегодня не выразим.** Озёр в мастере всего 13:
Каспий, пять Великих, Байкал, Ладога, Виктория, Танганьика, Мёртвое, Арал,
Кинерет, Ньяса. Онеги нет; поиск по именам даёт единственное совпадение
`EUR-0268 Voronezh Oblast` — ложное, на подстроке. Маршрут Беломорканала
идёт Белое море → Выг → **Онежское** → Свирь → Ладога → Нева → Балтика, и
среднего звена не существует.

Рядом — вторая половина той же дыры: **`LAK-0007` Ладога имеет ноль водных
соседей**, `SEA-0060` Белое море — одного (Баренцево). То есть даже
существующие звенья маршрута в графе не связаны ни с чем.

Чинится это хирургической правкой мастера, а не пересборкой: базовый
`lakes_1946.geojson` пришёл из источника, который сам генератор
`refresh_lakes_from_ne10m.py` называет неизвестным («их провенанс не
`ne_10m_lakes` напрямую, а какой-то более ранний источник»), а сам генератор
заменяет только Великие озёра и добавляет Ньясу. Прецедент добавления одного
озера в нём и есть — Ньяса добавлена ровно так.

**Что механика каналов требует от геометрии (описание пользователя
2026-08-09: открыть, закрыть, повредить; спорное владение — проход закрыт
всем).** Само поведение — не мой домен, но два его следствия мои, и оба
проверены:

1. **У каждого канала обязан быть отдельный сухопутный регион с однозначным
   владельцем** — иначе на вопрос «кто контролирует канал» ответить нечем. Три
   существующих канала это условие выполняют: `AFR-0016` Suez Canal Zone → EGY,
   `EUR-0374` Kiel Canal Zone → QGB (через `config/occupation_overlay.json`),
   `NAM-0251` Panama Canal Zone → USA. У Волго-Дона и Беломорканала **регионов
   коридора не существует вовсе** — их придётся заводить, и это работа
   геометрии.
2. **«Спорное владение» сегодня выразить нечем.** В `regions.state.json` поле
   владения ровно одно — `ownerCountryId`; поля контролирующей стороны нет.
   А исторически оба крупных канала 1946 года — ровно тот случай, который
   описывает механика: Суэцкая зона числится за Египтом, но занята
   британскими войсками до 1956; Зона Панамского канала — территория США
   внутри Панамы. Модель, у которой один владелец, различить «чей он» и «кто
   его держит» не может. Это домен `logic`, но без этого поля механика
   пользователя не реализуема, и в контракт G9 это входит наравне с самим
   проходом.

**Рекомендация не изменилась в главном.** Каналы — подслучай G9: пока морской
смежности нет в данных сценария, ребро канала добавлять некуда, это дорога в
графе, которого игра не видит. Но требование пользователя фиксируется как
ограничение контракта, который напишет `lead`: **проход должен иметь состояние
во времени, а не только существование**. Спроектировать смежность без этого —
значит переделывать её, как только кампания дойдёт до 1952.

Первая очередь прохода: Суэц, Панама, Киль — существуют, регионы коридора и
владельцы на месте, добавить нужно только ребро с состоянием. Вторая:
Волго-Дон, Беломорканал, Коринфский со статусом «закрыт до 1948» — этим
требуется работа геометрии до всякой симуляции (Онежское озеро, регионы
коридоров). Остальные — в `docs/IDEAS.md` после v1.

**T-7 Коридоры каналов и Онежское озеро**
Готовность: спроектировано вчерне, не выдано — ждёт решения по G9

Заводится сейчас, чтобы не потеряться, но выдаётся только после контракта:
делать коридор Волго-Дона раньше, чем решено, как выглядит проход, — значит
класть регион под неизвестную форму.

**Состав.** (а) Онежское озеро новой фичей `LAK-00xx` по образцу добавления
Ньясы в `refresh_lakes_from_ne10m.py`; (б) регионы коридоров Волго-Дона
(Волга у Волгограда → Дон у Калача, ~101 км) и Беломорканала; (в) связать
Ладогу и Белое море с маршрутом.

**Критерии приёмки — черновик, дописать после контракта:**
- [ ] число озёр 13 → 14, покрытие мира не изменилось больше 0,01%;
- [ ] Онега имеет площадь порядка 9 700 км² по собственному пересчёту
      `pyproj`, а не по свойству файла;
- [ ] `region_id` существующих регионов не поехали — позиционный каскад
      проверяется отдельно (`remap_region_ids.py`);
- [ ] у каждого нового региона коридора владелец однозначен в `regions.state.json`;
- [ ] `audit_map_geometry.py` код 0, `WATER_SHATTERED` 0.

**Аудит.** Не проводился.

## Не моё

`shortName.ru` США обрезано, потолок `extraction` 10/10 у всех 2041 пары —
домен `world` (наполнение сценарных данных). Иконки `MapFeature`, цветовые
режимы, поле `tier` в UI, выбор страны, флаги — домен `ui`. Не трогаю и в свою
очередь не беру.

---

# Предлагаемая очерёдность — приоритет утверждает пользователь

Порядок задан одним соображением: **сначала перестать врать самим себе, потом
чинить геометрию.** Площади считает домен `world` после меня, и он берёт их из
производных файлов; пока производный файл может молча разойтись с картой,
любая моя правка геометрии приезжает к нему в неизвестном виде.

1. **T-1 — гейт устаревания производных артефактов** (G1, G2, C1). Прямая
   причина: файл под git описывает карту на 55 регионов больше живой, и это
   нашлось случайно. Здесь же чинятся оба читателя. **Выдан 2026-08-09.**
2. **T-5 — контракт полей geojson.** Поднят на второе место после замера
   2026-08-09: 97 морских зон из 182 не атрибутированы, `naval_terrain` и
   `ocean` не доезжают до клиента вовсе, три поля живут на одной фиче каждое.
   Идёт ПЕРЕД правкой границ воды, иначе те же зоны придётся трогать дважды:
   сначала линию, потом поле.
3. **T-6 — лестница на линиях раздела морских зон.** Спроектирован после
   скриншота пользователя 2026-08-09: 16 зон последней нарезки, дефект формы
   линии при целой топологии. Идёт сразу за T-5, потому что трогает те же зоны.
4. **T-2 — критерий `orphan` и allowlist кусков** (G4, G5, G6). Allowlist —
   запись «закрыто навсегда»: класть её поверх критерия, дающего 23 ложных
   сироты из 23, дороже, чем не класть вовсе. **Интерактивная**, выдаётся под
   присутствие пользователя, поэтому в очереди стоит там, где ему удобно, а не
   строго здесь.
5. **T-3 — рендер перешейка** (C2). Пятнадцать минут, закрывает
   пользовательскую жалобу или опровергает мой численный вердикт.
6. **T-4 — уборка 14+1 планов** (G8). Механическая, к геометрии отношения не
   имеет, идёт последней и может уехать в чужую ветку целиком.

Каналы отдельной задачей не идут — см. раздел «Каналы»: это подслучай G9.
5. **Ганьсу/Синьцзян** (G3) — **не брать**. 18,5 км² на регионе 405 565 км² —
   0,0046%, невидимо и в игре, и на экране. Риск не в площади, а в неизвестном
   событии, которое её сдвинуло; этот риск уже удержан потолком в ALLOWLIST,
   который упадёт при росте. Работа даст запись в журнале и ноль в партии.
6. **Восемь источников** (G7) — **не брать без решения пользователя**: это не
   инженерная задача, а закупка внешних данных с проверкой provenance и
   лицензии, и мастер под git делает её несрочной.

---

# Задания

Критерии приёмки записаны ДО выдачи. Порядок утверждён пользователем
2026-08-09 («делай по порядку»). Выдаются строго по одному: T-1…T-4 делят
`scripts/map/**`, и два исполнителя в нём разойдутся по смыслу, даже сидя в
разных деревьях. Следующее задание выдаётся после вердикта аудита по
предыдущему. Строку `Статус:` получает только выданное.

## T-1 Гейт устаревания производных артефактов
Статус: принято
Исполнитель: отдельная сессия, ветка `claude/derived-staleness-gate` (`fbc0139`)
Выдано: 2026-08-09 | Принято: 2026-08-09
Область: `scripts/map/build/**`, `scripts/map/config/**`, `scripts/map/make_1946.py`

**Задача.** Производный файл под git обязан либо соответствовать живому
мастеру, либо ронять пайплайн с именем виновника. Сегодня `islands_index.json`
разошёлся на 55 регионов и 46 компонент, и это не заметила ни одна проверка.
Прецедент устройства — `build/verify_master_freshness.py`: он сторожит 41
генератор по коммиту и печатает, что именно ушло вперёд. Класс `ECONOMY_STALE`
из `.agent/plans/geometry-snapshot-gate.md` — частный случай той же болезни;
общий сторож дешевле двух частных, и `ECONOMY_STALE` отдельно не заводить.

**Установлено оркестратором — не переоткрывать.** Все числа сняты 2026-08-09
на `92c1aa9`:

- мастер: 1591 фича (1396 суша / 182 `SEA-` / 13 `LAK-`); сценарий:
  1389 регионов = 1396 − 7 антарктических секторов;
- `out/islands_index.json` под git: `scenario_regions 1444`,
  `land_components 200`, `islands 198`, `island_regions 327`. Свежий прогон
  `build_islands_index.py`: **1389** и **154**. Независимый счёт по
  `neighbor_graph.json` даёт те же 1389/154 — устарел именно файл;
- `build_islands_preview.py` падает `FileNotFoundError` на
  `out/world_1946.geojson`, которого нет и взяться неоткуда;
- `build_islands_index.py` сегодня отрабатывает (код 0) только потому, что
  fallback `client/public/world_1946.geojson` побайтно совпадает с мастером.

**Классификация артефактов `scripts/map/out/*.json` под git — сделана
оркестратором, менять только с обоснованием.** Гейт сторожит ПРОИЗВОДНЫЕ и не
трогает ЗАПИСИ:

| Производные — обязаны сходиться с живым мастером | Записи и эталоны — гейту не подчиняются |
|---|---|
| `islands_index.json` (сегодня устарел — это и есть дефект) | `region_edits_islands.json` — вход `apply_region_edits_islands.py`, 27 операций, намеренно называет ещё не существующие id |
| `neighbor_graph.json` (проверено: 1591 узел, 0 расхождений) | `world_after_edits.json` — сам объявляет `_meta.status: "НЕ ПРИМЕНЕНО к живой карте"`, несёт устаревший `regions_before: 1444` как исторический факт |
| `ownership_1946.json`, `names_ru.json` (проверено: 0 призраков) | `china_1946_historical.json`, `palestine_1946_historical.json`, `ph_provinces_1946.json` — кураторские эталоны, их сторожит `verify_china_curation.py` по своему принципу |
| `countries_1946.json`, `flags_1946.json`, `resource_catalog.json` | |

Разделение обязано быть ЯВНЫМ списком с причиной у каждой записи, как
`NOT_GENERATORS` и `OFF_CHAIN` в `verify_master_freshness.py`. Умолчание
«всё, что в out/» даст ложные падения на трёх эталонах и двух записях.

**Критерии приёмки** (записаны до старта):
- [ ] новый шаг в `STANDARD_STEPS`, выше `import_to_game.py`, падает на
      СЕГОДНЯШНЕМ `islands_index.json` и печатает обе величины «в файле /
      на живой карте»;
- [ ] после регенерации индекса шаг зелёный и `python scripts/map/make_1946.py`
      проходит целиком с кодом 0;
- [ ] `build_islands_preview.py` отрабатывает в дереве без
      `out/world_1946.geojson`; в отчёте показаны оба прогона — падение до и
      код 0 после;
- [ ] `build_islands_index.py` читает мастер первым, fallback на
      `client/public` сохранён;
- [ ] негативный контроль: вернуть `islands_index.json` к версии под git
      (`git checkout HEAD -- scripts/map/out/islands_index.json`) → шаг обязан
      упасть и назвать 1444 против 1389. Вывод показать дословно. Проверка,
      не проверенная на способность падать, покрытием не считается;
- [ ] пять записей и эталонов из правой колонки таблицы гейт НЕ роняют, и это
      показано отдельным прогоном, а не рассуждением;
- [ ] `python .agent/evals/public/run_public_evals.py` — без новых падений;
- [ ] `git status` чист от случайных файлов; `regions.state.json` не обнулён
      (ловушка ниже).

**Вне scope.** Геометрия — ни одной правки полигона. Экономические поля
регионов (домен `world`). Класс `AREA_MOVED` по всему миру: у него другой
источник истины, и `geometry-snapshot-gate.md` прямо запрещает дублировать
китайский шаг. Морская смежность в сценарии (G9) — кросс-доменный стык, ждёт
контракта от `lead`.

**Ловушка.** `import_to_game.py` пишет `regions.state.json` обнулённым:
население и экономику заполняет более поздний шаг. Прогон экспорта в отрыве от
полного пайплайна стирает экономику у всех регионов. После каждого прогона —
`git diff --stat`.

**Аудит (оркестратор, 2026-08-09).** Всё ниже перезапущено мной в дереве
исполнителя; отчёт автора доказательством не считался.

Прогоны и что я увидел:

| Проверка | Результат |
|---|---|
| гейт на текущем состоянии | код 0 |
| негативный контроль: `git checkout main -- out/islands_index.json` | код 1, назвал «регионов сценария 1444/1389, компонент суши 200/154» и сверх заданного «островов 198/152, островных регионов 327/275» |
| неклассифицированный файл под git | код 1, «внеси в WATCHED или NOT_DERIVED» |
| пять записей и эталонов | шаг зелёный; `--why-excluded` печатает по каждому ЗАМЕР, а не только причину |
| полный `python scripts/map/make_1946.py` | **код 0** |
| дерево после полного прогона | чисто; единственный «изменённый» файл `resource_catalog.json` даёт пустой `git diff` — чистый CRLF-шум |
| ловушка `regions.state.json` | цела: 1389 регионов, население 2 294 863 082, нулевых 0, ВВП 549 437 245 346 |
| `run_public_evals.py` в дереве исполнителя | 240 passed, 0 failed |

**Сверх критерия: проверил на способность падать ВСЕ семь чекеров, а не
только заявленный.** Портил по одной записи в каждом файле и откатывал:

| Файл | Порча | Результат |
|---|---|---|
| `islands_index.json` | версия из `main` | код 1, четыре величины |
| `neighbor_graph.json` | удалён узел | код 1 |
| `ownership_1946.json` | удалён `AFR-0003` | код 1, «игровых регионов без владельца 1/0: AFR-0003» |
| `countries_1946.json` | удалён `SUN` | код 1, «владельцев вне каталога 1/0: SUN» |
| `names_ru.json` | удалена запись имени | код 1, назвал регион |
| `flags_1946.json` | удалён флаг `AFG` | код 1, «стран сценария без флага 1/0: AFG» |
| `resource_catalog.json` | `maxExtractionLevel` +1 | код 1, «11/10» |

Заглушек нет. Отдельно отмечу собственную ошибку в аудите, чтобы её не
повторили: первые три попытки порчи ничего не ломали, и я едва не записал три
чекера в слепые. Причины были в тестах, не в гейте: файл вне git (правило
касается файлов ПОД git); плоский словарь, который мой скрипт не трогал; и
`AFR-0001`, чьё владение приходит из `occupation_overlay.json` — удаление
записи в `ownership` для него ничего не меняет, и код это правильно
воспроизводит, потому что переиспользует `import_to_game.resolve_owner`, а не
переписывает его.

**Что сделано лучше задания.** Общий резолвер `paths.world_geojson()`
(мастер → `out/` → копия клиента) вместо правки двух скриптов поштучно, и в
его докстринге отдельно сказано, что `import_to_game.py` им пользоваться НЕ
должен: `client/public/world_1946.geojson` — его собственный выход, чтение
своего выхода замкнуло бы импорт на себя. Это ловушка, которой в задании не
было.

**Критерий 3 НЕ выполнен буквально, и это принято как обоснованная эскалация.**
Я требовал, чтобы `build_islands_preview.py` отрабатывал с кодом 0. Он
по-прежнему код 1, но это уже не `FileNotFoundError`, а осознанный отказ с
объяснением: запись `region_edits_islands.json` применена коммитом `6a997ad`,
`region_id` после этого перенумерованы, и предпросмотр ищет участников по id.
Заставить его «работать» — нарисовать мир, которого нет. В пайплайне скрипта
нет, так что код 1 ничего не ломает. Решение о судьбе скрипта (переписать под
поиск по паре «имя + площадь», как в применителе, или удалить вместе с
отработавшей записью) вынесено пользователю — см. «Вопросы».

**Что требует правки перед влитием (одно).** В `docs/TODO.md` запись
переписана на «четыре читателя не переведены на мастер» и в их числе оставлены
`merge_world_1946` и `translate_world`, с указанием переводить через
`paths.world_geojson()`. Для них это неверно и пошлёт будущего исполнителя не
туда: `merge_world_1946.py` этот файл **пишет**, а `translate_world.py` читает
и пишет тот же промежуточный артефакт внутри `MASTER_REBUILD_STEPS` до
заморозки — оба работают с выходом, который мастером ещё не стал. Верное число
— **два** (`audit_map_geometry` и `audit_region_fragments` уже переведены,
островные два переведены сейчас), а два оставшихся из списка снимаются как
неприменимые. Правка текстовая, влитию не мешает, но должна войти в ту же
ветку.

## T-2 Критерий `orphan` и реестр кусков
Готовность: спроектировано, не выдано — **выдаётся только когда пользователь
готов сидеть в сессии** (его решение 2026-08-09)
Область: `scripts/map/build/audit_region_fragments.py`, `scripts/map/config/`

**Задача.** Довести реестр до состояния, в котором allowlist можно завести без
вранья. Критерий `orphan` должен знать про слитые страны
(`config/country_merge.json`) — иначе каждая колониальная островная группа
вечно будет числиться сиротой.

**РЕЖИМ РАБОТЫ — интерактивный, решение пользователя 2026-08-09.** Исполнитель
готовит и предъявляет, но **не принимает** ни одного решения об исключении.
Причина названа пользователем: в кусках есть нюансы, которых нет ни в сырье, ни
в коде. Практически это значит:

- запись в `config/region_fragment_allowlist.json` появляется только после
  явного «да» пользователя по этой конкретной записи или по названной им
  группе — не по умолчанию, не «остальные аналогично»;
- исполнитель предъявляет куски порциями, с координатами, площадью, именем
  единицы сырья и **картинкой** (`build/render_region.py`), а не одним списком
  на 3581 строку: решение по куску, которого не видно, — не решение;
- где пользователь говорит «не знаю» — кусок остаётся неразобранным и попадает
  в отчёт как открытый, а не заметается в `island` по формальному признаку;
- 3314 кусков группы `island` разбирать поштучно нельзя и не нужно —
  предъявляется правило и выборка для проверки правила, решение принимается по
  правилу.

**Критерии приёмки:**
- [ ] на текущих данных `orphan` = 0 записей, и это объяснено, а не подавлено;
- [ ] негативный контроль: на состоянии до `1bfa7f9`, где куски British
      Columbia числились за Washington — San Juan, критерий ОБЯЗАН вернуть
      `orphan` (требование `region-fragments-audit.md`, не проверялось ни разу);
- [ ] 72 антарктические записи с пустым именем сырья разобраны одним правилом,
      а не 72 строками allowlist;
- [ ] 15 оставшихся `foreign_adm1` разобраны поимённо с причиной;
- [ ] `config/region_fragment_allowlist.json` создан; повторный прогон их не
      показывает; счётчики групп в отчёте совпадают с прогоном аудитора.

**Вне scope.** Удаление чего бы то ни было. Порог `MIN_FRAGMENT_KM2`.

**Аудит.** Не проводился.

## T-5 Контракт полей geojson: что где пишется и что доезжает до игры
Готовность: спроектировано, не выдано
Область: `scripts/map/import_to_game.py`, `build/translate_world.py`, мастер

**Задача.** Решить и закрепить, какие свойства несёт мастер, какие — клиентская
копия, и чем это держится. Сегодня не решено ничем: набор полей сложился
наслоением.

**Замер 2026-08-09 — основание для решения.**

Мастер несёт 12 полей, клиентская копия — 6:

| Поле | В мастере | В клиенте |
|---|---|---|
| `region_id`, `continent`, `name` | 1591 (100%) | 1591 (100%) |
| `region_type` (`land`/`sea`/`lake`) | 1591 (100%) | **нет** — заменено на `type` (`region`/`ocean`) |
| `area_km2` | 1591 (100%) | **нет** |
| `iso_a2` | 1396 (88%, ровно вся суша) | 1591 (100%) — импорт дописывает воде |
| `naval_terrain`, `ocean` | **85 из 182 морских зон** | **нет** |
| `state` | 108 (штаты США) | нет |
| `id` | **1 фича** | 1591 (числовой id сценария) |
| `type` | **1 фича** | 1591 |
| `strategic_points` | **1 фича** | нет |

Три находки, каждая — отдельное решение:

1. **97 морских зон из 182 не имеют ни `naval_terrain`, ни `ocean`.** Это ровно
   раскол двух поколений нарезки: зоны, пришедшие с `sea_zones_2026-08-06`,
   атрибутированы (`deep_ocean` 62, `ocean` 17, `shallow_sea` 4, `fjords` 2),
   ранние именованные моря IHO — нет. Половина водного слоя описана, половина
   нет.
2. **Ни `naval_terrain`, ни `ocean` не доезжают до клиента вообще.** Вся работа
   по атрибуции воды сегодня в игру не попадает. И `region_type` схлопывается в
   `type`, теряя различие озера и моря.
3. **Три поля живут на одной фиче каждое** — следы ручной правки:
   `EUR-0374 Kiel Canal Zone` несёт `id: 371`, `type: "region"` и рукотворный
   `iso_a2: "DE_KC"`; `ASI-0043 Nanjing` несёт `strategic_points` с одной
   записью, которую не читает никто.

**Критерии приёмки:**
- [ ] решение записано таблицей «поле → где живёт → кто читает → чем держится»
      в `scripts/map/AGENTS.md` или `docs/MAP_FEATURES.md`, а не в коммите;
- [ ] поле, которое никто не читает, либо удалено, либо у него назван
      потребитель и срок; `strategic_points` и одиночные `id`/`type` разобраны
      поимённо;
- [ ] `naval_terrain`/`ocean`: либо заполнены у всех 182 зон, либо явно
      объявлены необязательными с причиной — половинчатое состояние не
      остаётся молча;
- [ ] проверка, падающая на фиче с полем вне контракта и на обязательном поле,
      которого нет. Негативный контроль показан;
- [ ] `client/public/world_1946.geojson` пересобран пайплайном, не руками;
      размер файла назван до и после.

**Вне scope.** Геометрия. Смысл `naval_terrain` для боя — домен `logic`.

**Аудит.** Не проводился.

## T-6 Лестница на линиях раздела морских зон
Готовность: спроектировано, не выдано
Область: `build/apply_sea_zones.py`, `build/build_seas_from_iho.py`,
`build/diagnose_seas_iho.py`, `sources/sea_zones_2026-08-06/`, мастер

**Задача.** Линия раздела двух акваторий обязана быть линией, а не пилой.
Пользователь предъявил скриншот 2026-08-09: стык `SEA-0097` / `SEA-0099` идёт
ступеньками. Топология при этом цела — покрытие мира сходится до 0,002%,
`WATER_SHATTERED` 0, аудит зелёный: это дефект формы линии, не разрывов.

**Замер оркестратора 2026-08-09 — и предупреждение о ловушке.**

Первая метрика (доля сегментов, параллельных осям) объявила 163 зоны из 182
чистыми и `SEA-0099` в том числе — **и была неверна**. Ступеньки нарезаны в
проекции и перепроецированы в градусы, поэтому они наклонены: у `SEA-0099`
пары шагов `dx=0,047090 / dy=0,017054` повторяются ровно по 40 раз, но ни один
не равен нулю. Ровно об эту ловушку уже спотыкались — `diagnose_seas_iho.py:216-219`
описывает метрику, которая «показывала 1.000 там, где на рендере видна
лестница». **Мерить поворотами, а не осями.**

Метрика, давшая согласующийся с рендером ответ: доля вершин с поворотом
90°±20°. Прямая даёт ~0, живой берег — малые повороты, лестница — чередование.

| Доля поворотов ~90° | Зон |
|---|---|
| ≥ 40% — лестница | **4** |
| 20–40% — частично | 12 |
| < 20% — чисто | 163 |

Худшие: `SEA-0108` New Caledonia Basin 58,6%, **`SEA-0097` Antarctica Ridge
58,6%**, `SEA-0101` South Atlantic Plateau 47,0%, `SEA-0156` W. Emperor Chain
42,9%; далее `SEA-0106`, `SEA-0103`, **`SEA-0099` Cape Basin 31,7%**, `SEA-0112`,
`SEA-0100`, `SEA-0105`, `SEA-0104`.

**Дефект принадлежит ровно одному поколению нарезки.** Из 16 зон с долей ≥20%
все 16 — из последней партии (те, что несут `naval_terrain`). Среди 97 ранних
зон IHO лестничных **ноль**. Причина видна в цепочке: рабочее решение живёт в
`build_seas_from_iho.py:715-929` — квадрантное дробление даёт лестницу, и оно
заменено разрезом по линии делимитации, точной диагональю в две точки
(«вершин −57%», `verify_master_freshness.py:15`). Последняя партия пришла
другим путём — `apply_sea_zones.py` из внешнего прототипа
`sources/sea_zones_2026-08-06/zones_*.geojson` — и через это решение не
проходила.

**Отличать разрез от берега обязательно.** В список ≥15% попадают Ботнический
залив, Финский залив и Ла-Манш — у них изрезан НАСТОЯЩИЙ берег, и трогать его
нельзя. Правится только линия между двумя акваториями, где берега нет.

**Критерии приёмки:**
- [ ] метрика поворотов добавлена в `diagnose_seas_iho.py` (не новый скрипт) и
      воспроизводит числа выше на текущем мастере ДО правки;
- [ ] у всех 16 зон с долей ≥20% доля падает ниже 20%; у `SEA-0097` и
      `SEA-0099` — ниже 10%;
- [ ] `SEA-0059` Ботнический, `SEA-0058` Финский, `SEA-0079` Ла-Манш не
      изменились ни на одну вершину — берег не тронут. Показать `diff`
      числа вершин по каждой из трёх;
- [ ] рендер стыка `SEA-0097`/`SEA-0099` до и после — тот же кадр, что на
      скриншоте пользователя. Численная метрика этот пункт не заменяет;
- [ ] покрытие мира не изменилось больше чем на 0,01%: суша 146 325 933 км²,
      вода 362 851 398 км², озёра 887 967 км², сумма 510,07 млн км²;
- [ ] `WATER_SHATTERED` 0, `SEA_HOLE` не вырос, `audit_map_geometry.py` код 0;
- [ ] число морских зон осталось 182, `region_id` не поехали.

**Вне scope.** Береговая линия. Атрибуты зон (`naval_terrain`/`ocean` — это
T-5). Число и состав зон — перенарезка не заказана.

**Аудит.** Не проводился.

## T-3 Рендер Панамского перешейка — ОТМЕНЁН 2026-08-09
Готовность: снят, задача не существует

**Причина отмены.** Рендер уже сделан, и не мной. Пользователь указал, что
проблему решали; поиск по истории всех веток дал `6a4bfd5` (2026-07-29
18:26): «Панама — геометрия зоны канала из `D:\MAP` + resync 3 слоёв
позиционных id». В теле коммита дословно: грубая 82-точечная геометрия зоны
заменена уточнённой (192 точки, 1253,4 км²), **«Los Santos/Panama честно
переклипаны»** — ровно те два соседа, чьи полосы и были жалобой, — и
«рендер зоны канала подтверждён визуально». Решение в
`docs/decisions/2026-07.md:1432`.

Сегодняшняя карта несёт именно эту геометрию: `NAM-0251` = 1251,3 км²
собственным геодезическим пересчётом (в коммите 1253,4 — другой метод), и мой
независимый морфологический замер даёт у зоны канала 0 тонких кусков,
крупнейший остаток во всём районе 1,41 км². Два независимых свидетельства
сходятся: дефект закрыт 2026-07-29.

**Как запись пережила свою починку — механизм, а не случайность.** Строка
пришла в `docs/TODO.md` коммитом `498c710` (2026-07-30 17:33), заводившим
реестр отделённых кусков. В самом плане
`.agent/plans/region-fragments-audit.md` она стоит в разделе **«Out of
scope»**: «узкие полосы соседей вдоль зоны Панамского канала — ДРУГОЙ дефект,
данный скрипт их не видит». Это была **граница задачи**, а не задача. При
переносе в живой backlog граница превратилась в пункт, потеряв и адресата, и
признак закрытия — а починка к тому моменту уже сутки как лежала в `main`.

**Моя ошибка поверх этой.** Вчера я снял пункт по своему замеру, потом по
поправке роли `lead` («рассуждение доказательством закрытия не является»)
вернул его обратно с условием «ждёт рендера». Условие было выполнено за
одиннадцать дней до того, как я его выписал. Правильный ход в обоих случаях
был один и тот же и ни разу мной не сделан: **прежде чем судить о статусе
пункта — искать его починку в истории.** Замер отвечает «есть ли дефект
сейчас», история отвечает «а не чинили ли это уже», и второй вопрос дешевле
первого.

---

# Вопросы пользователю

1. **Целевая гранулярность.** 1396 сухопутных регионов, крупнейшая связная
   компонента 848. Турция (~108 тыс. км²/регион) вчетверо грубее соседей,
   Доминикана — 19 регионов на 48 тыс. км². Резать грубое, схлопывать мелкое
   или оставить как есть до v1? Моя рекомендация — оставить: карта играбельна,
   а пере-нарезка тянет каскад `region_id` и пересчёт экономики.
2. **Восемь источников (G7).** Докупать/скачивать geoBoundaries и китайские
   исторические шейпы ради воспроизводимого `--rebuild-master` — или считать
   мастер под git достаточным источником истины? Рекомендую второе.
3. **Ганьсу/Синьцзян (G3).** Подтвердите, что 20,9 км² суммарного расхождения
   под потолком ALLOWLIST — приемлемо, и запись можно снять из живого backlog
   в историю решений.
4. **Судьба `build_islands_preview.py`** (вскрылось при приёмке T-1). Скрипт
   рисовал предпросмотр островных правок по записи, которая уже применена
   (`6a997ad`), и после перенумерации `region_id` ищет участников, которых нет.
   Либо переписать под поиск по паре «имя + площадь», как это делает сам
   применитель, либо удалить вместе с отработавшей записью
   `region_edits_islands.json`. Рекомендую удалить: предпросмотр применённой
   правки не нужен, а держать скрипт «на случай» — это ещё один файл, который
   будет ломаться при каждой пересборке. Решение ваше, потому что удаление
   необратимо.
5. **Достижимость 133 островов (G9)** — продуктовая развилка, не техническая.
   Три варианта, и разница между ними видна в партии: (а) морская смежность
   отдельным полем в сценарии — острова входят в дипломатию и войну как все,
   цена — новое поле в контракте и пересборка сценария; (б) достижимость через
   флот, без смежности вообще — остров берётся десантом, для чего нужна живая
   `isCoastalRegion()`, уже висящая в backlog как блокер морских десантов;
   (в) оставить как есть до v1 — 133 региона остаются вне дипломатического
   соседства и военного контакта. Рекомендую (б): она закрывает и этот пункт,
   и уже стоящий в очереди блокер, и не добавляет поля в контракт. Решение
   ваше; контракт стыка после решения пишет `lead`.

# Ограничение параллельности

`scripts/map/**` делится с доменом `world`, и обе стороны заканчивают полным
прогоном пайплайна, переписывающим `regions.core.json`, `regions.state.json` и
`world_1946.geojson`. Пока живёт ветка этого домена, заданий домену `world` не
выдаётся. Регенерация мира — одна ветка за раз.
