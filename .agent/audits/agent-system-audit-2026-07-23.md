# Аудит агентной системы Geopolis — 2026-07-23

Автор: Claude (Opus 4.8), ветка `claude/agent-infrastructure-audit-4335d1`,
worktree `.claude/worktrees/strategy-game-ui-design-91419b`, базовый commit
`c347305` (= вершина `main` на момент аудита). Режим: read-only исследование,
этот файл — артефакт аудита, не изменение системы.

Метки доказательности: **VERIFIED** — проверено чтением файла/командой в этой
сессии; **INFERRED** — сильный вывод из нескольких VERIFIED-фактов;
**PRIOR-AUDIT** — заявление аудита Codex от 2026-07-15
(`.agent/audits/agents-audit.md` на ветке `codex/global-working-agreements`),
выборочно перепроверенное мной; **UNKNOWN** — недостаточно доказательств.

> **Связь с прежним аудитом.** 2026-07-15 Codex уже провёл brutal-аудит этой же
> системы и реализовал прототип целевой архитектуры (незамерженная ветка
> `codex/global-working-agreements`, 12 коммитов). Настоящий аудит (а) независимо
> перепроверил его ключевые заявления — все выборочно проверенные подтвердились,
> (б) аудирует сам этот прототип как новый элемент системы, (в) покрывает то,
> чего прежний аудит не видел: живое состояние веток/worktree на 2026-07-23,
> user-level конфигурацию Claude, память Claude, фактическую загрузку инструкций
> Claude Code (проверена в этой сессии и по официальной документации).

---

## 1. Краткий вывод

Система состоит из **пяти конкурирующих слоёв инструкций**, из которых боевым
является один (корневой `AGENTS.md` + `.claude/CLAUDE.md`), при этом:

- **Claude Code не загружает `AGENTS.md` автоматически** — общий слой правил
  держится на дисциплине «прочитай по ссылке» (VERIFIED: в текущей сессии
  автозагружен только `.claude/CLAUDE.md`; официальная документация Claude Code
  подтверждает отсутствие нативной поддержки AGENTS.md; работает `@AGENTS.md`
  импорт, который в проекте не используется).
- Codex, второй основной инструмент проекта, **вообще не упомянут** в боевом
  `AGENTS.md` (раздел координации описывает Claude + Gemini) и **не имеет
  Repowise/Context7** (глобальный `~/.codex/config.toml` без `mcp_servers` —
  VERIFIED), хотя «Tooling Authority» обязателен для всех.
- Наиболее зрелая версия целевой системы **уже существует и не смержена 8 дней**
  (`codex/global-working-agreements`), а поверх неё выросла ветка
  `codex/interface-from-scratch` с «reset interface for clean-slate redesign» —
  прямой семантический конфликт с реализованными в `main` срезами 0–3 плана 12.
- Протокол старта (`git checkout main && git pull`) written-in в трёх файлах —
  небезопасен и противоречит фактической worktree-практике; основной checkout
  прямо сейчас занят активной Codex-сессией на ветке `codex/1946-country-borders`
  (VERIFIED: новый commit `a3e3e68` появился между двумя моими проверками в
  течение этой сессии).

Зрелость: **инструментальная база хорошая** (CI с 4 джобами, doc-guardrails,
architecture fitness-тесты, 679+92 зелёных тестов — PRIOR-AUDIT, частично
VERIFIED), но **слой координации и загрузки инструкций сломан**: правила
дублируются, противоречат друг другу, не достигают адресатов и не имеют
процедуры удаления устаревшего.

---

## 2. Инвентаризация

Колонки: Путь · Тип · Кто фактически загружает · Когда · Актуальность · Действие
(рекомендация — см. также матрицу KEEP/REWRITE прежнего аудита, с которой я в
основном согласен; расхождения отмечены).

| Путь | Тип | Кто загружает | Когда | Актуальность | Рекомендация |
|---|---|---|---|---|---|
| `AGENTS.md` (корень, 21 370 Б) | общие правила + геймдизайн + координация | Codex (авто), Gemini (авто, `.gemini/settings.json`), Claude (только вручную) | старт сессии | смешанная: процессные разделы живые, статусы устарели (война «unbuilt», Last updated 2026-06-30) | REWRITE → тонкий нейтральный корень (прототип на codex-ветке) |
| `.claude/CLAUDE.md` (7 206 Б) | Claude-адаптер: протокол Repowise/Context7 | Claude (авто) | старт сессии | актуален, но не импортирует AGENTS.md | REWRITE: добавить `@AGENTS.md`, протокол Repowise сжать/условно |
| `.gemini/GEMINI.md` (14 209 Б) | Gemini-адаптер | Gemini (авто, вместе с AGENTS.md) | старт сессии | **сильно устарел**: client/ у Gemini, фризы карты/UI, Zustand, порт 3001, App.css 30KB | REWRITE до тонкого адаптера (прототип есть на codex-ветке) |
| `docs/agent/MASTER_PROMPT.md` (10 976 Б) | теневой «кандидат на замену AGENTS.md», объявляет приоритет над ним | **никто автоматически** | по ссылке | ценная конституция (10 правил) + устаревшие домены/фризы + несуществующий путь `shared/src/sim/commands/` (VERIFIED) | контент мигрировать (конституция → nested AGENTS/доки), файлу — superseded-баннер |
| `docs/agent/PROTOCOLS.md` (13 488 Б) | пошаговые протоколы, анти-регрессия UI | никто автоматически | по ссылке | процедуры живые, старт-протокол опасен (checkout main) | ценное → skills; superseded-баннер |
| `docs/agent/STACK_PLAYBOOK.md` (8 790 Б) | команды/паттерны стека | никто автоматически | по ссылке | в основном верен | слить с nested AGENTS.md / доками |
| `docs/agent/TEMPLATES.md` (7 967 Б) | шаблоны задач/TDD | никто автоматически | по ссылке | верен | → skill/план-шаблон |
| `docs/agent/ARCHITECTURE_GUARDRAILS.md` (8 609 Б) | реестр fitness-функций | никто автоматически | по ссылке | частично реализован (`architecture.test.ts` существует — VERIFIED; покрытие правил 3/7/8/9 отсутствует — PRIOR-AUDIT) | KEEP как реестр, честно разметить implemented/planned |
| `.claude/agents/ui-designer.md` (17 214 Б) | ролевой субагент UI (двухфазный) | Claude (авто-докрутка по описанию) | при UI-задачах | поведенческие правила ценны; фаза read-only конфликтует с write-tools | разделить: read-only ревьюер + правила писателя в client-слой |
| `.claude/skills/verify-change/SKILL.md` | skill проверки | Claude | по вызову | неполон против CI (нет lint, data-валидаторов, правила shared→оба) — VERIFIED | REWRITE по матрице CI |
| `.claude/settings.json` | env (ENABLE_TOOL_SEARCH) | Claude | старт | ок | KEEP |
| `.claude/settings.local.json`, `scheduled_tasks.lock` | локальное | Claude | старт | не в git (VERIFIED) | KEEP (не трекать) |
| `.claude/launch.json` | dev-серверы (3000/5173) | Claude Browser | по запросу | порт верен (VERIFIED: `index.ts:15` → 3000) | KEEP |
| `.mcp.json` | MCP Claude (Repowise, Context7) | Claude | старт | абсолютный путь `D:/Pax Historia LOCAL` — непереносим | REWRITE (относительный cwd) |
| `~/.codex/AGENTS.md` («Global Working Agreements») | глобальные правила Codex | Codex | старт | хорош, совместим по духу; markdown с экранированными `\#`/`\*` | KEEP (поправить экранирование) |
| `~/.codex/config.toml` | Codex config | Codex | старт | **нет `mcp_servers`** → Codex без Repowise/Context7 (VERIFIED) | добавить MCP (владелец — пользователь) |
| `~/.codex/skills/` | глобальные skills Codex | Codex | старт | пусто (VERIFIED) | — |
| `~/.claude/settings.json` | user-level Claude | Claude | старт | repowise MCP указывает на **другой проект** `D:/Pax-Automata`; глобальные hooks `repowise-augment`/`repowise-rewrite` | пользователю: убрать чужой MCP из global; hooks см. находку H5 |
| Память Claude (`~/.claude/projects/...MEMORY.md`) | приватные знания | Claude | старт | содержит проектные соглашения, в т.ч. ссылку на EVOLUTION.md, отсутствующий в main | мигрировать проектное в репозиторий |
| `.github/workflows/ci.yml` | CI: client lint+tsc+test, server tsc+test, data-валидация, secret scan | GitHub | push/PR | работает | KEEP |
| `.github/workflows/doc-guardrails.yml` | запрет `.repowise/`, корневых .md; warning на изменение AGENTS.md | GitHub | push/PR | работает | KEEP, расширить agent-config-проверками |
| `.git/hooks/post-commit` | фоновой `repowise update` | локально | commit | не переносится в clone, не является контролем | KEEP как удобство |
| Ветка `codex/global-working-agreements` (12 коммитов) | прототип целевой системы: тонкий AGENTS.md, nested AGENTS.md ×4, `.agent/` (charter/plans/evolution/audits/evals/runs), `.agents/skills/` ×7 с адаптерами openai.yaml, `.codex/config.toml`, ui-reviewer ×2, массовая чистка доков | никто (не смержено) | — | 8 дней; база = вершина main (fast-forward возможен текстуально) | **главный вход для целевой системы** — ревью и интеграция, не переизобретение |
| Ветка `codex/interface-from-scratch` | продолжение ↑ + «reset interface for clean-slate redesign» | никто | — | конфликтует с main-редизайном | решение пользователя (см. C3) |
| Ветка `worktree-orchestrator-kit-test` | эксперимент 2026-06-24: удалён AGENTS.md, корневой CLAUDE.md, шаблоны, 60k строк | никто | — | мёртвый форк | архивировать/удалить ветку |
| Worktree `loving-clarke-e6395e` | старый Claude-worktree | — | — | **dirty**: незакоммиченные правки client (App.css, BudgetPanel, GameView, удалён шрифт) — VERIFIED | инвентаризация владельцем: закоммитить или списать |
| Worktree'ы Codex (`~/.codex/worktrees`, `~/.codex/visualizations/...`) | рабочие деревья Codex-задач | Codex | — | один detached, один на interface-from-scratch | учитывать в координации |

---

## 3. Иерархия загрузки — по инструментам

### Claude Code (VERIFIED в этой сессии + официальная документация)

1. Автозагрузка: `~/.claude/CLAUDE.md` (отсутствует), `./CLAUDE.md` (отсутствует),
   `./.claude/CLAUDE.md` (есть — загружен). Вложенные `CLAUDE.md` — по требованию;
   вложенные `AGENTS.md` — **никогда**.
2. `AGENTS.md` нативно **не читается**. Механизм починки существует: `@AGENTS.md`
   импорт из CLAUDE.md инлайнит файл в контекст. В проекте не используется —
   только прозаическое «read first».
3. Skills: `.claude/skills/*` (1 шт.) + user/plugins. Субагенты: `.claude/agents/*`
   (1 шт.); контекст CLAUDE.md субагентам **не наследуется**.
4. Worktree: `.claude/worktrees/` (native), настройка `worktree.baseRef`;
   флага «всегда в worktree» не существует; принуждение возможно PreToolUse-hook'ом.
5. Плюс личная память (MEMORY.md) — недоступна другим инструментам.

### Codex (VERIFIED по локальной конфигурации; детали загрузки — PRIOR-AUDIT + официальные доки)

1. Автозагрузка: `~/.codex/AGENTS.md` (глобальный) → корневой `AGENTS.md` →
   вложенные `AGENTS.md` по пути к cwd. `CLAUDE.md`, `.claude/**` — не читает.
2. MCP: только `~/.codex/config.toml` — секции `mcp_servers` **нет** → Repowise
   и Context7 недоступны. Поддержка репо-локального `.codex/config.toml` —
   UNKNOWN (прототип на ветке существует, fresh-session проверка не пройдена —
   так честно записано в EVOLUTION.md ветки).
3. Skills: `~/.codex/skills` пуст; репо-локальные `.agents/skills/*` — на ветке
   подтверждён discovery через `$strategy-game-ui` (PRIOR-AUDIT, fresh-session
   Codex). Claude эту директорию не видит — нужен адаптер/зеркало.
4. `codex.exe` из shell недоступен (не в PATH; запуск из пакета — Access denied,
   PRIOR-AUDIT) → **автоматизировать перекрёстное ревью Codex'ом из Claude-сессии
   сейчас нельзя**; только вручную через Desktop.

### Gemini

`.gemini/settings.json` загружает `GEMINI.md` + `AGENTS.md` **вместе** →
каждая Gemini-сессия получает два противоречащих файла (владение client/,
фризы) — конфликт вшит в контекст (VERIFIED).

---

## 4. Находки

Формат: Серьёзность · Уверенность · Место · Проблема → Доказательство →
Последствие → Исправление → Альтернативы → Цена/риск.

### BLOCKER

Отсутствуют. Работа в репозитории возможна; всё ниже — деградация качества,
безопасности координации и автономности, не полная остановка.

### CRITICAL

**C1. Пять конкурирующих слоёв инструкций; один из них объявляет приоритет над боевым.**
Уверенность: высокая (VERIFIED).
Место: `docs/agent/MASTER_PROMPT.md:3-6` («при конфликте побеждает этот файл»),
`AGENTS.md:46` («любая будущая документация в /docs авторитетна»), `.gemini/GEMINI.md`,
ветка `worktree-orchestrator-kit-test` (корневой CLAUDE.md вместо AGENTS.md),
ветка `codex/global-working-agreements` (новая архитектура).
Доказательство: перечисленные строки прочитаны; MASTER_PROMPT ссылается на
несуществующий `shared/src/sim/commands/` (ls → NOT FOUND), при этом претендует
на верховенство.
Последствие: какой набор правил действует — зависит от того, какой файл агент
случайно прочитал; «правило-оборотень» позволяет обычному doc расширять
полномочия.
Исправление: один канонический корневой слой; `docs/` лишить статуса instruction
authority (это спецификации); MASTER_PROMPT/PROTOCOLS — superseded-баннер,
ценное мигрировать.
Альтернативы: формально задекларировать порядок приоритета файлов (хуже: слои
останутся, читать придётся все).
Цена/риск: низкая; риск — потерять ценное содержимое при миграции (лечится
чек-листом миграции).

**C2. Протокол старта мутирует общий checkout и противоречит worktree-реальности.**
Уверенность: высокая (VERIFIED).
Место: `AGENTS.md:345-349`, `docs/agent/PROTOCOLS.md:9`, `.gemini/GEMINI.md:27-33`.
Доказательство: все три требуют `git checkout main && git pull` безусловно.
Фактически: основной checkout `D:/Pax Historia LOCAL` стоит на
`codex/1946-country-borders`, и в нём идёт **активная сессия** (новый commit
`a3e3e68` появился между 08:48 и 08:57 сегодняшнего аудита); Claude-сессии
работают из `.claude/worktrees/*`; Codex — из `~/.codex/worktrees/*`.
Последствие: агент, честно исполнивший протокол, переключит ветку под чужой
живой сессией — потеря/смешение работы; протокол также ломает изолированные
task-ветки (checkout в них невозможен без потери контекста задачи).
Исправление: worktree-first протокол: «изменяешь файлы → отдельный worktree +
ветка задачи; в основном checkout — никогда»; старт с `git status`/`git worktree
list`, без автоматических checkout/pull.
Альтернативы: оставить checkout-протокол только для «одиночного режима» (плохо:
режим ничем не детектируется).
Цена/риск: низкая; требует короткого правила в корне + опционально
PreToolUse-hook (Claude) как страховку.

**C3. Незамерженная целевая система + семантическая развилка UI.**
Уверенность: высокая (VERIFIED по git; интерпретация «развилка не
зафиксирована» — INFERRED: в `docs/DECISIONS.md`/`docs/TODO.md` main записей о
clean-slate reset нет).
Место: ветки `codex/global-working-agreements` (12 коммитов, 2026-07-15..17),
`codex/interface-from-scratch` (+`730cd43` «reset interface for clean-slate
redesign»), `codex/1946-country-borders` (8+ коммитов карты, активна сегодня).
Доказательство: `git log main..<ветка>`; merge-base всех трёх = вершина main
`c347305` (main не двигался с 2026-07-14).
Последствие: (а) все правки инструкций/доков, сделанные в main после 07-15,
делались против уже переписанной в ветке системы; (б) UI развивается в две
несовместимые стороны: main — срезы 0–3 плана 12 (HUD), ветка — сброс интерфейса
«с нуля»; при мерже одна из линий будет выброшена; (в) карта (1946-borders)
может конфликтовать с данными main.
Исправление: решение пользователя по каждой линии: какая UI-линия канонична;
интеграция agent-OS ветки (ревью — затем merge) до любых новых правок
инструкций; фиксация активных линий в видимом обоим агентам месте (task briefs).
Альтернативы: продолжать параллельно (гарантированная потеря одной из работ).
Цена/риск: ревью ветки — часы; риск мержа кода вместе с инфраструктурой
(ветка смешивает и то и другое — мержить осознанно, возможно частями).

**C4. GEMINI.md прямо противоречит AGENTS.md, и Gemini загружает оба.**
Уверенность: высокая (VERIFIED).
Место: `.gemini/GEMINI.md:8-17,87,145-149` vs `AGENTS.md:297-320`;
`.gemini/settings.json` (`fileName: ["GEMINI.md","AGENTS.md"]`).
Доказательство: GEMINI.md — «client/ владеет Gemini», «карта заморожена
(2026-07-03)», «интерфейс заморожен (2026-06-23)», «порт 3001», «Zustand —
целевое решение», «весь дизайн в App.css 30KB»; AGENTS.md — client/ целиком у
Claude с 2026-07-12, фризы сняты; реальный порт 3000 (`server/src/index.ts:15`);
в `client/src/styles/` токены (`tokens-1946.css`) — App.css не единственный.
Последствие: каждая Gemini-сессия стартует с вшитым противоречием; поведение
непредсказуемо (какой файл «победит» — зависит от модели).
Исправление: сжать GEMINI.md до тонкого адаптера (роль: калибровка данных,
контент/иконки; не-домены; ссылки), как в прототипе ветки.
Цена/риск: минимальная.

### HIGH

**H1. Общий слой не доставляется Claude автоматически.**
Уверенность: высокая (VERIFIED: в контексте этой сессии — только
`.claude/CLAUDE.md`; официальные доки: AGENTS.md не поддержан, `@import`
поддержан).
Место: `.claude/CLAUDE.md:3` (прозаическая ссылка вместо импорта).
Последствие: «обязательные» правила AGENTS.md для Claude — opt-in; сессия,
не прочитавшая файл, работает без них (включая правила координации и запреты).
Исправление: `@AGENTS.md` в `.claude/CLAUDE.md` (одна строка) — файл инлайнится
в системный контекст.
Альтернативы: symlink AGENTS.md→CLAUDE.md (на Windows требует прав), дубляж
текста (запрещённый паттерн).
Цена/риск: минимальная; +5.3k токенов постоянной загрузки — аргумент за
предварительное сжатие AGENTS.md (см. прототип ветки: ~130 строк).

**H2. Правила ссылаются на несуществующие пути.**
Уверенность: высокая (VERIFIED: ls/glob).
Место: `AGENTS.md:355-368` и `GEMINI.md` — «`shared/types/`» (нет; есть
`shared/src/types/`); `docs/agent/MASTER_PROMPT.md:43` — «`shared/src/sim/commands/`»
(нет; команды в `server/src/commands/`).
Последствие: самое строгое concurrency-правило проекта (запрет параллельных
правок общих типов) защищает несуществующий каталог; grep агента по буквальному
пути даст ложное «всё чисто».
Исправление: поправить пути; добавить в CI-guardrail проверку упоминаемых путей
(прототип public eval на ветке это уже делает — PRIOR-AUDIT).

**H3. Боевой AGENTS.md содержит ложные статусы.**
Уверенность: высокая (VERIFIED).
Место: `AGENTS.md:5` («Last updated: 2026-06-30» при содержимом от 07-12);
`AGENTS.md:28` — война «currently unbuilt», тогда как план 08 «замкнут
2026-07-11 — все 4 шага» (`docs/plans/README.md:27`), `WarTick`/`WarService` в
коде; `AGENTS.md:29` — торговля «unbuilt», при реализованной v1 (TODO.md
«торговля v1 (мировой рынок ресурсов)» в Готово).
Последствие: агент, доверившийся корню, неправильно оценивает зрелость систем;
подрывается доверие ко всему файлу.
Исправление: статусы систем в корне не хранить вообще (роутинг — да, статус —
в профильном доке); это уже сделано в прототипе ветки.

**H4. Протокол Repowise продублирован в трёх местах без контроля расхождений.**
Уверенность: высокая (VERIFIED).
Место: `.claude/CLAUDE.md` (полный), `.gemini/GEMINI.md:164-179` (сокращённая
копия), `AGENTS.md:112-123` (сжатая копия).
Последствие: ручная синхронизация трёх копий; Gemini-копия уже отстала
(и бесполезна: у Gemini est-ли MCP Repowise — UNKNOWN); контекст ×3.
Исправление: канон в одном месте (Claude-адаптер или условный skill), в корне —
одна строка «используй Repowise, если доступен» (как в прототипе ветки).

**H5. Глобальные user-level hooks искажают команды и невоспроизводимы.**
Уверенность: высокая (VERIFIED, живое воспроизведение в этой сессии).
Место: `~/.claude/settings.json` — PreToolUse `repowise-rewrite`, PostToolUse
`repowise-augment` на все Bash/PowerShell/Read/...; там же MCP repowise на
**другой проект** (`D:/Pax-Automata`).
Доказательство: в этой сессии (а) `git show ветка:путь` был переписан хуком с
искажением `:`→`;` и слэшей → `fatal: ambiguous argument`; (б) префикс
`MSYS_NO_PATHCONV=1 cmd` был исполнен как не-bash («не является внутренней или
внешней командой»); (в) вывод git загрязняется строкой `distill filter failed;
falling back to raw`.
Последствие: недетерминированные сбои инструментов у Claude; поведение сессии
зависит от невидимой в репозитории машинной конфигурации; Codex этих хуков не
имеет → инструментальная асимметрия; MCP на чужой проект — лишний процесс и
риск путаницы индексов.
Исправление (пользователь, вне репо): убрать/сузить matcher хуков (только Bash,
или отключить rewrite); удалить Pax-Automata MCP из user-настроек (проектный
`.mcp.json` уже даёт правильный Repowise).
Цена/риск: низкая; функциональность distill остаётся доступной явным вызовом
`repowise distill`.

**H6. Проектные знания живут в приватной памяти Claude.**
Уверенность: высокая (VERIFIED — моя собственная память).
Место: `~/.claude/projects/D--Pax-Historia-LOCAL/memory/` — 14 записей, среди
них процессные соглашения проекта (цикл «предложи→утверди», «этд = продолжай»,
арт-директорская позиция, разрешение создавать skills/agents «по правилам
EVOLUTION.md» — файла, которого в main **нет**, он только на незамерженной
ветке).
Последствие: Codex и Gemini этих правил не видят; знание невоспроизводимо на
другой машине; память уже ссылается на несуществующий в main артефакт — ровно
тот дрейф, о котором предупреждает сама целевая система.
Исправление: устойчивые процессные соглашения — в version-controlled слой
(корневой AGENTS.md/адаптеры); память — только для личных предпочтений
пользователя и указателей.

**H7. Codex отсутствует в системе координации; фактические домены не совпадают с записанными.**
Уверенность: высокая (VERIFIED).
Место: `AGENTS.md:292-372` («Координация моделей (Claude + Gemini)»).
Доказательство: в таблице доменов и протоколе веток Codex не существует; при
этом ветки `codex/*` — 4 шт., глобальный `~/.codex/AGENTS.md`, активная сессия
в main-checkout; Codex делает UI (interface-from-scratch) — домен, записанный
за Claude.
Последствие: главный второй инструмент работает вне писаных правил; правила
описывают несуществующий мир.
Исправление: нейтральная формулировка «изменяющий агент», без привязки правил к
именам моделей; роли (ведущий/исполнитель/рецензент/аудитор) — функции задачи.

**H8. `verify-change` не совпадает с CI.**
Уверенность: высокая (VERIFIED: сравнение SKILL.md с `.github/workflows/ci.yml`).
Место: `.claude/skills/verify-change/SKILL.md`.
Доказательство: skill — tsc+test двух workspace; CI дополнительно: client
`npm run lint`, data-джоб (`validate_region_economy_1946.py` +
`test_validate_...`), secret scan; правила «изменил shared → проверь оба» в
skill нет.
Последствие: «зелёно по skill» ≠ «зелёно в CI»; ложное чувство завершённости.
Исправление: переписать по матрице CI (расширенная версия уже есть на ветке —
PRIOR-AUDIT: там же добавлен public eval агент-конфигурации).

**H9. Брошенные рабочие деревья и ветки без процедуры закрытия.**
Уверенность: высокая (VERIFIED).
Место: worktree `loving-clarke-e6395e` — dirty (правки client/*.tsx, App.css,
удалён шрифт; ветка без уникальных коммитов); ветка `worktree-orchestrator-kit-test`
(мёртвый эксперимент от 2026-06-24, 60k строк); `gemini/map-data-filling`
(2026-07-04, не смержена); remote `localbackup` с ветками `agents/*`.
Последствие: незакоммиченная работа под риском потери; шум в `git worktree
list`/`branch -a`; старые копии AGENTS.md в worktree активируются при запуске
агента внутри них (PRIOR-AUDIT, подтверждено размером 18 015 Б в loving-clarke).
Исправление: разовая инвентаризация пользователем (сохранить/выбросить) +
правило гигиены в целевой системе: завершение задачи = закрытие worktree.

### MEDIUM

**M1. Обязательный стартовый контекст ~42k токенов.**
VERIFIED: AGENTS.md 21 370 Б + TODO.md 35 118 Б + DECISIONS.md 107 562 Б =
164 КБ ≈ 41k токенов (метод: bytes/4, грубая сравнительная оценка; совпадает с
оценкой PRIOR-AUDIT). Правило «читать всегда» (`AGENTS.md:19-21`) применяется и
к мелким задачам. Исправление: корень требует только релевантные разделы
(«свежие решения», «связанные пункты TODO»), не полные файлы; DECISIONS
продолжает архивироваться помесячно.

**M2. ui-designer: конфликт read-only фазы и write-инструментов; дублирование.**
VERIFIED: `.claude/agents/ui-designer.md` — «Фаза 1 без правок кода» при
`tools: Edit, Write, Bash`; протокол анти-регрессии продублирован с
`PROTOCOLS.md:106-143`. Прототип ветки разделил на read-only `ui-reviewer`
(+19 строк, зеркала для Codex в `.codex/agents/ui-reviewer.toml`), а правила
писателя перенёс в `client/AGENTS.md` — направление правильное; риск: часть
поведенческих правил (арт-директорская позиция, «не исполнитель списка»)
при переносе сжата — при интеграции проверить, что не потеряно то, что
пользователь явно требовал сохранить.

**M3. Fitness-заявления шире фактического покрытия.**
VERIFIED существование `server/src/__tests__/architecture.test.ts`; PRIOR-AUDIT:
покрыты правила 1/5/10 полностью, 2/4/6 частично, 3/7/8/9 — нет, «правило 1
покрывает правило 9» — неверно. Мной детально не перепроверялось (низкая
добавленная ценность: реестр в ARCHITECTURE_GUARDRAILS сам честно помечает часть
как планы). Исправление: в реестре развести implemented/planned.

**M4. Автоматизация перекрёстного ревью невозможна.**
VERIFIED: `codex` не в PATH; PRIOR-AUDIT: запуск из пакета — Access denied.
Последствие: «независимое ревью другой моделью» выполнимо только вручную через
Desktop-сессию Codex. Целевая система должна это честно фиксировать: артефакт
«review brief» вместо ложного «Codex проверил».

**M5. Непереносимость MCP-конфига.**
VERIFIED: `.mcp.json` — абсолютный `D:/Pax Historia LOCAL`. Repowise
поддерживает запуск от cwd (PRIOR-AUDIT: `repowise mcp --help`). Исправление:
убрать абсолютный путь (сделано в прототипе ветки).

### LOW / NOTE

- **L1.** `AGENTS.md:322-331` — зачёркнутый MAS-раздел «оставлен как история»:
  контекст-балласт в always-loaded файле; истории место в git/архиве. VERIFIED.
- **L2.** `~/.codex/AGENTS.md` содержит экранированный markdown (`\#`, `\*`) —
  вероятно артефакт вставки; читается, но заголовки не парсятся как заголовки.
  VERIFIED.
- **L3.** `AGENTS.md:398-408` «Response Format» — вкусовой шаблон ответа в
  постоянной загрузке; кандидат в адаптеры/удаление. VERIFIED.
- **L4.** Доменная таблица называет Sonnet 5 реализатором плана 12
  (`PROTOCOLS.md:137-142` — модельные имена и reasoning-efforts в правилах) —
  инструкции, привязанные к конкретной модели, устаревают при смене модели. VERIFIED.
- **N1.** Положительное: `doc-guardrails.yml` уже флагует изменения AGENTS.md
  warning'ом — зачаток контроля изменений агентной системы. VERIFIED.
- **N2.** Положительное: standing-авторизация локальных коммитов в AGENTS.md
  сформулирована аккуратно (локально — да, push/rewrite — нет). VERIFIED.

---

## 5. Skills и агенты — классификация

| Элемент | Реальная проблема | Используется | Вердикт |
|---|---|---|---|
| `.claude/skills/verify-change` | верификация перед коммитом | да | **исправить** (неполон против CI); канон — общий, зеркала генерировать |
| `.claude/agents/ui-designer` | UI-регрессии, «исполнитель списка» | да (история фиксирует) | **разделить**: read-only ревьюер + правила писателя в client-слой; поведенческое ядро сохранить |
| `~/.claude/skills/skill-creator` (user) | создание skills | вне репо | не трогать |
| Ветка: `.agents/skills/{investigate,plan-challenger,qa,security-audit,token-audit}` | узкие read-only процедуры | discovery подтверждён для Codex (PRIOR-AUDIT) | **принять после ревью**; для Claude нужны зеркала/адаптеры с проверкой идентичности |
| Ветка: `.agents/skills/strategy-game-ui` | scenario-first UI-аудит | fresh-session Codex — да | принять после ревью (крупный: 183 строки + референсы) |
| Ветка: `.agents/skills/verify-change` | канон verify | — | принять; настроить parity-check с `.claude/`-зеркалом |
| Ветка: `.claude/agents/ui-reviewer` + `.codex/agents/ui-reviewer.toml` | независимое UI-ревью | — | принять; проверить сохранение арт-директорских правил |

Новых постоянных агентов «по названиям профессий» не обнаружено — хорошо.
Прототип ветки также сознательно НЕ создал: Codex hooks без потребителя, rules
без возможности `codex execpolicy check`, второй MCP-провайдер — правильная
дисциплина (PRIOR-AUDIT, подтверждаю по дереву ветки).

---

## 6. Экономика контекста (метод: bytes/4, сравнительная)

| Слой | Сейчас | Прототип ветки | Целевое направление |
|---|---:|---:|---|
| Корень AGENTS.md | ~5.3k ток. | ~1.6k | тонкий нейтральный корень |
| Claude-адаптер | ~1.8k | ~0.4k | + `@AGENTS.md` импорт |
| Gemini-адаптер | ~3.6k | (сжат) | тонкий адаптер |
| «Обязательная тройка» (корень+TODO+DECISIONS) | ~41k | — | читать выборочно |
| Теневой комплект docs/agent | ~12.5k (по ссылке) | superseded | мигрировать и заархивировать |

---

## 7. Блокеры автономности (сводно)

1. Стоп при любом doc↔code конфликте (`AGENTS.md:54-56`) — при заведомо
   известных конфликтах в системе это блокирует рутину. → сузить до «конфликт
   меняет продуктовую семантику».
2. Безусловное чтение полных TODO/DECISIONS. → выборочное.
3. Обязательная пауза Фазы 1 ui-designer даже для заказанных мелких правок. →
   разделение ролей ревьюер/писатель.
4. Отсутствие Codex в правилах → любая координация с ним ситуативна.
5. Невозможность автоматического кросс-ревью (M4) → протокол review brief.

---

## 8. Что не удалось проверить

- Фактическая загрузка репо-локальных `.codex/config.toml`/`.agents/skills` в
  свежей Codex-сессии (частично закрыто PRIOR-AUDIT: skills — да; config — нет).
- Поведение Gemini при конфликте GEMINI.md↔AGENTS.md (какой файл «побеждает»).
- Полное покрытие architecture.test.ts (принято по PRIOR-AUDIT).
- Живые прогоны tests/lint в этой сессии не выполнялись (аудит read-only;
  свежие результаты есть в PRIOR-AUDIT от 07-15: server 679 passed, client 92
  passed, client lint — 20 предсуществующих ошибок).

## 9. Снимок живого состояния (2026-07-23, ~09:00)

- `main` = `c347305` (2026-07-14), не двигался 9 дней.
- Основной checkout: ветка `codex/1946-country-borders`, HEAD `a3e3e68`,
  **активная сессия** (commit появился во время аудита), clean.
- Мой worktree: ветка `claude/agent-infrastructure-audit-4335d1` от `c347305`.
- Незамерженные линии: `codex/global-working-agreements` (+12, инфраструктура),
  `codex/interface-from-scratch` (+13, UI reset), `codex/1946-country-borders`
  (+8..9, карта), `gemini/map-data-filling` (+1, 07-04), `claude/loving-clarke`
  (dirty worktree без уникальных коммитов), `worktree-orchestrator-kit-test`
  (мёртвая, 06-24).
