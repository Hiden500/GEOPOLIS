# Флаги — ассеты, а не исходный код: содержимое должно попадать в рабочую копию
# байт в байт. Без этой строки Git на Windows разворачивает LF в CRLF при
# checkout, и scripts/map/fetch_flags.py после свежего клона считает все 145
# файлов изменившимися — сравнение идёт по точному содержимому.
client/public/flags/*.svg -text
