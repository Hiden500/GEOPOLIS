<!-- Claude Code автоматически подхватывает вложенные CLAUDE.md (при чтении файлов
     этого подкаталога), но НЕ вложенные AGENTS.md. Без этого моста правила модуля
     зависели от того, вспомнит ли модель их прочитать: замер по 8 сессиям —
     1 чтение на 8 сессий. Файл держит один импорт и не вводит своих правил.
     HTML-комментарии вырезаются до попадания в контекст и ничего не стоят. -->
@AGENTS.md
