# Журнал архитектурных решений

Last updated: 2026-07-30 (аудит формул и исследование моделей: диагноз «формулы аккуратны, входы мертвы» подтверждён шестью путями, пять P0-дефектов ломают партию и чинятся строками, заведён недостающий класс guard-тестов на свойства, лицензии источников данных проверены дословно и вынесены в гейт; реализация не начиналась; ранее в тот же день — дипломатический слой ЗАГРУЖАЕТСЯ в движок: `diplomacyFileSchema` + `applyDiplomacyLayer` написаны РАНЬШЕ данных — порядок изменён решением пользователя, пара кладётся обеим сторонам, гарантия направленная, слой опциональный и «файла нет» сегодня единственный живой путь, разделение труда с Python-валидатором сохранено; ранее в тот же день — базы легитимности и коррупции сняты со строкового ярлыка идеологии: коррупция ключуется по форме власти, легитимность — U-образная функция политической оси с поправкой на происхождение власти, экономическая ось исключена намеренно и закреплена тестом, обе таблицы стали Record<PowerStructure, number>, 78 стран из 157 перестали падать в дефолт, средняя легитимность мира 58.7 → 47.3; SAVE_VERSION не менялся; ранее в тот же день — обходы мира на полной разметке замерены: три опасения из четырёх не подтвердились (2,7/0,9/16,4 мс), кэш разложения не нужен, зато найдено, что сейв занимает 1,75 МБ до накопления истории при бюджете v1 в 3–5 МБ; ранее в тот же день — разбор Open-Historia: их гео-данные на GADM закрыты явным запретом в гейте релиза, архитектура их AI-геймплея и их коммит-конвенция отвергнуты с причиной, редактор карт оценён и отложен, правило «поле не в схеме — модель его не пришлёт» внесено в контракт примитива, ссылки на чужой код перепроверены по клону; ранее в тот же день — локальный рантайм ВНЕДРЁН провайдером по умолчанию: LocalOpenAIProvider + фабрика выбора, интерфейс переведён на Zod-схему (диалект — забота провайдера), json_schema и лимит 8192 зашиты как условия работоспособности, опечатка в LLM_PROVIDER роняет запрос, стилевой контракт нарратива внесён в промт; ранее в тот же день — замер: локальный LLM-рантайм: LM Studio + Qwen3.5-9B Q5_K_M замерены на реальном промте хода — `json_schema` обязателен (0/10 без него против 10/10 с ним), reasoning рвёт JSON о лимит генерации, MoE в 10 ГБ VRAM проигрывает вшестеро, узкое место — выдуманные идентификаторы, а слабый нарратив оказался дефектом промта, не размера модели; провайдер игры и схемы НЕ менялись; ранее в тот же день — дипломатический слой: формат `diplomacy.json` и сеть проверки написаны РАНЬШЕ данных, договор и политическая реальность разделены, границы читаются из TS без копий формул, загрузка отложена до наполнения; ранее в тот же день — данные 1946: курируемые исторические факты как ERROR-проверка против галлюцинаций разметки, эвристики правдоподобия как WARNING, JSON-отчёт валидатора слоёв; ранее — Милстоун 1, сессия «Структурные глаголы»: алфавит доведён до 18 глаголов — `puppet`/`annex`/`merge_countries`/`create_country`, юридический статус и рантайм-отношение зависимости меняются одним действием и инвариант их согласованности стал рантайм-проверяемым, аннексия последнего региона ведёт к вердикту кампании вместо удаления страны, суммы проверены в обе стороны замкнутым циклом деление→сложение, закрыт предсуществующий дефект палитры раскола, правило исключения сторожа выражено свойством класса; SAVE_VERSION не менялся; ранее — Милстоун 1, сессия «Мягкие глаголы алфавита»: алфавит доведён до 14 глаголов — `send_aid`/`capital_flight`/`condemn`/`support_proxy`, каждый с коридором из ЖИВЫХ полей состояния (константные на данных 1946 поля названы и исключены), помощь покупает влияние и не двигает отношения напрямую, отток капитала бьёт по региону, а не по агрегату страны, ключ капа у бесплатных глаголов не содержит источника, `influence` удалён из старого канала как обход коридора сменой канала, `shift_ideology` отвергнут как дубль `enact_reform`, `shift_mood` разобран и отложен; SAVE_VERSION не менялся; ранее — Милстоун 1, сессия «Формы правления»: введены powerStructure и sovereigntyStatus+overlordIds на 157 стран 1946, зависимость осталась двумя представлениями с разным предикатом и двумя инвариантами вместо равенства множеств, `governmentType` удалён как легаси, SAVE_VERSION не менялся; ранее — Милстоун 1, сессия «Идеология как модификатор союзов»: булев идеологический гейт снят, дистанция вошла в цель дрейфа отношений и в парный порог согласия на союз, отношения возникают у пар с живым каналом вместо полной матрицы, четыре инварианта проверяются на самих константах, право на союз вернулось 103 странам из 157; SAVE_VERSION не менялся; ранее — Милстоун 1, сессия «Жизненный цикл государств»: перенос ссылок одним объявленным обходом с выводимым из состояния сторожем полноты, машина состояний кампании с вычисляемым движком game over, позиционный откат и диф заменены сопоставлением по стабильным id, инварианты дополнены уникальностью id/столицей/нулём висячих ссылок, `split_country` доведён до конца, починены три тихие поломки, исправлен дефект данных 1946 (Италия ссылалась на несуществующие SOM/ERI/LBY), тесты `shared/` подключены к прогону; SAVE_VERSION 9→10; ранее — Милстоун 1, сессия «Контракты ядра»: форма примитива — discriminated union по глаголу, квитанция ответа вместо четырёх полей события, явная транзакция на весь ответ с пост-инвариантами, структурный код отказа с двумя рендерами, idempotency и диагностика распространены на старый канал, сверка результата обобщена на все числовые каналы, runtime-проверка сейва, одноразовые данные промта списываются ответом, `annex`/`puppet` удалены из контракта старого канала, палитра выражает ключи словарей и удаление элементов; SAVE_VERSION 8→9; ранее — Милстоун 0, решение пользователя по итогам внешнего аудита: событие несёт выведенную из фактов степень подтверждённости, летопись строится по применённому, игрок видит пометку; цена названа — текст при частичном применении по-прежнему не переписывается, SAVE_VERSION 7→8; ранее — 2026-07-26, Милстоун 0, повторная верификация внешнего аудита: legacy `annex`/`puppet` отклоняются как нереализованные вместо ложного «применено», откат промта сохраняет источник диагностического факта, область правила «отказ структурного отклоняет весь ответ» ограничена движком с обоснованием и проверкой на обход, три формулировки о правдивости заголовков приведены к факту; Милстоун 0, правки по внешнему аудиту: неприменённый ответ модели не становится каноном и событие несёт машиночитаемый результат, `Event.countries` из применённых примитивов, кап диагностики и кольцо idempotency-ключей разделены по источнику, отказ структурного примитива откатывает весь батч, SAVE_VERSION 6→7; Милстоун 0, вторая доводка по независимому ревью: откат одноразовых данных промта делается слиянием и не стирает чужую диагностику, длина идентификаторов и число диагностических фактов ограничены сверху, ключ быстрой кнопки живёт один ход, два комментария приведены к коду; Милстоун 0, доводка сессии B по независимому ревью: капы примитивов считаются на игровой ход и живут в состоянии партии, бюджет общий у модели и игрока, тратится только применённым примитивом, SAVE_VERSION 5→6; ключ быстрой кнопки переживает сетевой сбой; сбой провайдера возвращает одноразовую диагностику; Милстоун 0 сессия B: петля замкнута — idempotency на границе хода, кризисный кап из латча, локализуемый отклик ключ+параметры, история места, граница агентности режиссёра, путь игрока «текст → распознанное → подтверждение», SAVE_VERSION 4→5; движок примитивов: доводка по независимому ревью закрытия аудита — резюме называет цели с нулевым эффектом, рантайм-сверка расширена на величины в обе стороны + названа её граница, четыре комментария приведены к коду, порог восстания проверяется по максимуму; пять правок по внешнему аудиту — структурированный результат вместо скаляра, палитра видит нулевые поля, предпосылки по виду инцидента, reject недостижимой реформы, кризисный факт называет индекс индексом; демо-срез 1946 наполнен внешне + тест недовольства переведён с расстановки регионов на инвариант механики; Милстоун 0 сессия A: ядро недовольства + движок примитивов, четыре поправки по независимым ревью; capitalRegionId: инвариант вместо позиционного id + 9 исправленных столиц; сценарии-заглушки 1836/2000 удалены как мёртвый код; дизайн-цикл 2026-07-24 — концепт-редизайн, алфавит примитивов)

Append-only журнал. Когда решение полностью спроектировано и реализовано, его суть должна перекочевать в `ARCHITECTURE.md`/`AI_RULES.md`/`LLM_RULES.md`/`TODO.md` как постоянное правило — но запись здесь не удаляется, это история "почему".

Каждая запись: дата, решение, причина, статус. Датированные записи ниже — **только дополняются** (правки оформляются как «Исправлено/Поправка (дата)» внутри записи). Раздел «Открытые вопросы» в конце — это **живой индекс**, его можно переписывать: решённое из него убирается.

**Архив:** записи `2026-06-22`…`2026-06-27` — `docs/decisions/2026-06.md`;
записи `2026-06-28`…`2026-07-26` — `docs/decisions/2026-07.md` (одно
имя-на-месяц, `2026-07.md`, дополняется по мере роста, не дробится на
несколько файлов за один месяц) — та же конвенция "только дополняются"
действует и там. Этот файл держит только свежее + живой индекс открытых
вопросов.

**Когда чистить.** Порог — **1500 строк**, он проверяется public eval
(`.agent/evals/public/run_public_evals.py`), а не памятью сессии. Причина
жёсткая: правило «держать только свежее» существовало с 2026-07-11 и за три
недели не выполнилось ни разу — файл вырос с ~560 до 6724 строк, потому что
проверять его было некому. Чистки: 2026-07-11 (~930→~560), **2026-07-30
(6724→~1600, перенесено 76 записей за 2026-07-10…07-26)**.

## 2026-07-30 — Аудит `docs/`: провенанс отделён от нормативного слоя

**Решение.** Девять файлов происхождения данных (~1 МБ) вынесены в
`docs/provenance/`; `OBJECTIVES.md` внесён в таблицу маршрутизации; статус
`docs/agent/` назван в своде явно.

**Почему.** В корне `docs/` лежало 35 файлов, в которых смешаны три класса:
нормативные документы (читаются постоянно), провенанс данных (читается один раз
при приёмке) и исторический слой. Провенанс демографии занимал 806 КБ в трёх
файлах плюс три вспомогательных; пять из девяти имели ровно одну входящую
ссылку. После переноса в корне 26 файлов.

**Что оказалось не так, как выглядело.** `docs/agent/` (5 файлов, 6–10 ссылок на
каждый) при первом взгляде читался как второй, невидимый своду свод правил.
Проверка содержимого это опровергла: все пять помечены `HISTORICAL / SUPERSEDED
(2026-07-15)` и прямо отсылают к активным правилам. Проблема не в двоевластии, а
в том, что свод про эту папку молчал — исправлено одной строкой, миграция не
потребовалась.

**`OBJECTIVES.md` — 17 входящих ссылок и ни одного упоминания в маршрутизации.**
Самый цитируемый документ вне таблицы: сессия, идущая по таблице, его не нашла бы.

**Побочно.** Индекса у `docs/` нет; не заводился намеренно — таблица
маршрутизации в своде выполняет эту роль, а второй список пришлось бы
синхронизировать вручную.

## 2026-07-30 — Аудит формул и исследование моделей: диагноз и порядок лечения

**Что сделано.** Одиннадцать read-only проходов: три аудита наших расчётных формул
(политика, экономика, дипломатия+война) и восемь исследований внешних моделей
(демография, макроэкономика, конфликт, война, гравитация торговли, таблицы
смертности, динамика долга, лицензии исторических данных). Код не менялся.
Результаты — `.agent/audits/formula-audit-2026-07-30.md` и
`.agent/audits/models-research-2026-07-30.md`; живые задачи вынесены в `TODO`,
лицензионные выводы — в гейт релиза.

**Диагноз — один на все подсистемы: формулы написаны аккуратно, а их входы мертвы.**
Шесть независимых подтверждений: порог соперничества ниже достижимого минимума
тяготения; все пять ветвей равновесия стабильности дают одинаковый ответ 154 странам
из 157; нормировщик идеологической дистанции завышен вдвое против реальной геометрии
данных; расходы стран калибруются от экспортного дохода, который перезаписывается на
первом тике; поля развития региона не пишет ни один тик; стабильность региона
поделена на 100 и влияет в 90 раз слабее задуманного.

**Почему это не поймали 1276 зелёных тестов.** Тесты проверяют формулу на подобранной
фикстуре, а не на распределении живого мира. Найден и физический след этой привычки:
фикстура региона держит `stability: 70` рядом с `development: 0.5` — две шкалы в
одном объекте, и тесты демографии написаны под неё, то есть под баг.

**Отсюда решение о недостающем классе проверок.** Помимо самих правок заводятся
guard-тесты на 12–120 тиков, утверждающие СВОЙСТВА («ВВП не замирает в нуле»,
«население за 50 лет в пределах ×N», «вход различает страны»), а не значения на
примере. Каждый из них падает на текущем коде — негативный контроль обеспечен
бесплатно. Без них следующие правки будут так же невидимы, как эти.

**Порядок работ принят такой, и порядок здесь не косметика:**

1. Реанимация: пять P0-правок (каждая — строки) плюс guard-тесты.
2. Структурные дыры: завершение войны, обратная связь потерь, цена репрессии,
   замкнутая петля инвестиций.
3. Модели: накопление капитала → закон Оукена и долговая динамика поверх него;
   мобилизация отдельно от недовольства; демографический переход.

Демографический переход упирается в калибровку ВВП на душу, а мобилизация — в
починку нормировщика: строить поверх искажённого основания значит калибровать дважды.

**Что решено НЕ брать, хотя выглядело подходящим:** отскок рождаемости при высоком
развитии (гипотеза оспорена; асимметрия ошибки — модель с отскоком при его отсутствии
даёт растущее население развитых стран и ломает позднюю игру); двусторонняя
гравитация торговли (противоречит решению о едином мировом пуле — взята форма,
которая ему не противоречит); полные таблицы смертности и когортно-компонентный
метод (новая подсистема вместо опоры для формулы); ещё двенадцать моделей с
причинами — в исследовании.

**Лицензии проверены дословно и вынесены в гейт релиза.** Главное: единственный
проверенный источник с реальным 1946 годом и чистой лицензией — Clio Infra (CC0),
и в нём ровно годовая добыча ресурсов. Correlates of War запрещён дважды (некоммерческое
использование И редистрибуция) и особо опасен тем, что по переменным подходит идеально.
Свободно лицензированного субнационального экономического датасета на 1946 не
существует вовсе — значит наш путь через собственную аллокацию единственно возможный
и чистый.

**Поправка по замечанию пользователя (2026-07-30).** Формулировка «1946 — пик
колониальной системы, поэтому колониальная связь даёт ×2,5 к торговле» подана
неверно: коэффициент описывает плотность торговых потоков внутри империй, а не
прочность империй, и 1946 — не пик, а НАЧАЛО распада (Филиппины независимы 4 июля
1946, война в Индокитае с декабря 1946, Индия и Пакистан 1947). При этом торговые
связи переживают политическую деколонизацию на десятилетия. Правильная форма — не
константа, а связь с датой разрыва и хвостом затухания.

**Статус.** Аудит и исследование завершены, зафиксированы. Реализация не начиналась.
Три развилки ждут решения пользователя: насколько мир должен искрить на старте
(порог восстания сегодня берут 2 региона из 1399); навязывать ли мир игроку при
военном поражении; форма кривой «режим → устойчивость» (эмпирика говорит, что
опаснее всего промежуточные режимы, — это утверждение о мире и трогает все 157 стран).

## 2026-07-30 — Базы легитимности и коррупции: с ярлыка идеологии на координаты и форму власти

**Решение.** `PoliticsTick` больше не читает строковый ярлык
`politics.ideology`. Коррупция ключуется по `politics.powerStructure`
(`POWER_STRUCTURE_CORRUPTION_BASE`, бывшая `GOVERNMENT_TYPE_CORRUPTION_BASE`),
легитимность — функция координат спектра с поправкой на происхождение власти
(`shared/src/utils/politics.ts`). Разбор формулы — `docs/POLITICS.md`.

**Причина.** Таблицы знали 10 ключей, в данных 1946 встречались 3, и 78 стран
из 157 падали в дефолт (проверено прямым подсчётом по `countries.json`):
Испания Франко, Саудовская Аравия и британская Индия были для движка одним
режимом. Обе таблицы были `Record<string, number>` — именно поэтому расхождение
с данными случилось молча. Теперь обе — `Record<PowerStructure, number>`, и
новая форма власти без записи не компилируется.

**Развилка и почему выбрано так.** Коррупция отдана форме власти, а не
координатам: она про механизм распределения ренты, а не про точку спектра —
конкуренция партий её ограничивает, личный режим на ней держится. Легитимность
отдана координатам, но кривая U-образная, а не линейная: линейная означала бы
«любая диктатура нелегитимна по построению», и СССР-победитель 1946 оказался бы
ниже гибридных режимов. Экономическая ось не участвует намеренно — она отвечает
«чья собственность», а не «по какому праву правят»; это закреплено тестом, а не
только комментарием. Чистых координат, однако, не хватило: у колониальной и
оккупационной администраций мандата нет независимо от точки спектра, и без
поправки временное правительство Франции получало базу почти как Британия, а
оккупированная Япония — как средняя европейская страна.

**Цена, названная явно.** Средняя легитимность мира 58.7 → 47.3: треть мира —
колонии и оккупационные зоны, и теперь движок это видит. Направление намеренное
(низкая легитимность колониальной власти — предпосылка деколонизации), величина
не калибрована, как не были калиброваны и прежние числа. Разброс на 157 странах:
легитимность 4 → 92 различных значения (sd 7.34 → 13.71), коррупция 4 → 11.

**Что НЕ сделано.** Поле `Country.politics.ideology` и фолбэк координат по нему
оставлены: единственный оставшийся читатель — `resolveIdeologyCoordinates`, и
его удаление трогает `Scenario1946.ts`/`scenario1946Schemas.ts`, занятые в этот
день параллельной задачей. Вынесено в `docs/TODO.md` отдельным шагом.

**Статус.** Реализовано. Негативный контроль: на восстановленном чтении по
ярлыку падают 5 новых тестов, на линеаризованной кривой — тест U-образности.
## 2026-07-30 — Обходы мира на полной разметке: три опасения из четырёх не подтвердились

**Решение.** Четыре пункта, отложенные формулой «перемерить при полной разметке
Милстоуна 2», закрыты замером, а не оценкой. Оптимизация не требуется ни в
одном из них; заготовленный кэш разложения на тик НЕ пишется. Инструмент —
`server/scripts/benchWorldPasses.ts`, он остаётся в репозитории, потому что
следующий вопрос о производительности возникнет на длинной партии, а не на
третьем ходу.

**Почему отсрочка вообще была.** Числа брались, когда демографией были размечены
14 регионов из 1399, и каждый пункт нёс честную оговорку «дёшево только потому,
что данных мало». Разметка состоялась 2026-07-29 — оговорка перестала
действовать, но сами числа никто не пересчитал. Это ровно тот случай, когда
отложенное решение продолжает выглядеть принятым.

**Замерено** (1399 регионов с демографией, 157 стран, 186 залатченных кризисов,
третий ход партии, медиана пяти прогонов):

| Обход | Медиана |
|---|---|
| `activeCrises` — кризисная выборка промта | 2,7 мс |
| `enumerateCells` — разложение состояния (3913 ячеек) | 0,9 мс |
| `findStateViolations` — пост-инварианты | 16,4 мс |
| `generatePrompt` целиком | 3,3 мс |

Самый дорогой обход стоит 0,3% времени хода: сам вызов модели занимает 6–60
секунд. Опасение, что `MAX_PROMPT_CRISES` ограничивает рендер, а не выборку, и
разложение по группам будет считаться для тысяч регионов, — верно по
конструкции и ничтожно по цене.

**Чего замер НЕ закрыл, и это названо прямо.** Пик `primitiveTurnBudget` за ход
не измерен: в прогоне не применялось ни одного примитива, а строить валидные
примитивы ради килобайтов не окупается. Пункт снят аргументом о порядке величин,
не измерением.

**Главное нашлось не там, где искали: сейв весит 1,75 МБ на третьем ходу.**
Критерий приёмки v1 требует SAVE < ~3–5 МБ — то есть половина бюджета
израсходована до того, как накопилась история партии. Разбор: `regions` 995 КБ
(54%), `countries` 544 КБ (30%), `mapFeatures` 190 КБ (10%). Внутри регионов
геометрия занимает 5,6 КБ, то есть подозрение на неё снято — вес дают игровые
поля. Следствие: на всю историю остаётся 1,25–3,25 МБ при том, что
`eventHistory` и хроника капов не имеют. Это новый живой вопрос, поставленный в
`TODO`, а не закрытый пункт.

**Статус.** Замер выполнен, три пункта `TODO` закрыты числами, четвёртый —
аргументом, один новый открыт. Кода движка правка не касалась.

## 2026-07-30 — Разбор Open-Historia: что берём, что закрыто лицензией, что не берём

**Решение.** Выводы read-only разбора чужого проекта
([Open-Historia](https://github.com/Open-Historia/open-historia) — открытый
community-аналог Pax Historia, TypeScript/Vite, код MIT) разнесены по
владельцам: лицензионный запрет — в гейт релиза
(`docs/plans/11_MVP_ROADMAP.md`), правило схем — в `docs/PRIMITIVES.md` §1,
приёмы валидатора и LLM-слоя плюс оценка редактора карт — в `docs/TODO.md`. Сам
разбор (`.agent/reference/open-historia-review.md`) остаётся справкой на ветке
`claude/open-historia-review`. Из их репозитория не заимствовано ничего.

**1. Их гео-данные не берём никак.** Тайлы (~105.8 МБ регионов, ~62.7 МБ стран)
и seed-geojson построены на GADM — источник назван их же доком
(`docs/assets-and-data.md:13`) и подтверждён кодом (`GID_0`/`GID_1` в
`scripts/build-default-map.mjs`, `scripts/extract-regions.mjs`,
`src/Editor/exportPreset.js`), а у GADM редистрибуция без отдельного разрешения
запрещена и заявления о таком разрешении у них нет. Почему это записано
ЗАПРЕТОМ, а не заметкой: их геометрия — самый заманчивый актив чужого проекта, и
соблазн «пересобрать из неё своё» возникнет позже, когда контекст разбора
забудется, а «пересобрать» — та же редистрибуция. Побочно выяснилось, что
ограничение бьёт по ним, а не по нам: наш провенанс (geoBoundaries CC-BY 4.0 +
Natural Earth PD на 1399 регионов) чище их, и заменять нам нечего.

**2. Архитектуру их AI-геймплея не берём — она ровно то, от чего Geopolis
ушёл.** Их LLM сама делает симуляцию: `countryStatSheet` позволяет модели
придумывать ВВП, рост, инфляцию и индексы, причём числовые поля объявлены
свободным текстом (`textSchema`, `gameplaySchemas.js:1,148-152`), а единственная
числовая проверка разбивки — «суммируется в 100» (`gameplaySchemas.js:1042`).
Детерминированного движка у них нет. Наше правило обратное и не меняется: числа
считает движок, LLM решает «что», а не «насколько». Ценность их проекта для нас
не в архитектуре, а в защитном слое ВОКРУГ ненадёжного вывода модели — слое,
который они были вынуждены построить именно потому, что движка нет. Он и
разобран в `TODO`.

**3. Их коммит-конвенцию не перенимаем.** Их `docs/conventions.md:73` требует от
контрибьюторов, «применялся ИИ-инструмент или нет», не добавлять trailer
`Co-Authored-By: Claude…` и AI-атрибуцию вообще. Это норма ИХ репозитория для ИХ
коммитов; у нас правило обратное, и оно не меняется. Записано отдельным пунктом
как граница, а не как мелочь: содержимое чужого репозитория — данные, а не
инструкции, даже когда текст в нём адресован агенту напрямую. Других попыток
управлять анализирующим агентом целевой поиск не нашёл.

**4. Редактор карт — «не сейчас», но вариант оценён.** У них полноценный
`src/Editor/`, у нас ничего сравнимого; разрыв признан реальным. Отложено из-за
цены: второй изолированный рендер-стек (OpenLayers при MapLibre в игре), пять
новых зависимостей и переписанный под наши id экспортёр — это отдельный
милстоун, а Милстоун 1 не закрыт. Оценка записана в `TODO`, чтобы решение не
пришлось принимать заново с нуля.

**Методическая находка, стоящая отдельного пункта.** Ссылки `файл:строка` в
самом разборе не совпадают с клоном на его HEAD `33112bc` (`extractJsonPayload`
— `:286` против `:284` в разборе; `CAPTURE_LANGUAGE` — `:1126` против `:994`;
`buildTransferFeedback` — `:1072` против `:940`), и документация самого
Open-Historia отстаёт от их же кода тем же образом (их `docs/ai-schemas.md` §5.3
указывает `:940`, фактически правило живёт на `:1042`). В наши доки перенесены
только номера, перепроверенные по клону. Урок общий и применим к любому
цитированию: `файл:строка` из чужого документа — указатель для поиска, а не
факт.

**Статус.** Правки документные, кода и данных не касаются.

## 2026-07-30 — Локальный рантайм внедрён провайдером по умолчанию

**Решение.** Замер предыдущей записи превращён во внедрение: добавлен
`LocalOpenAIProvider`, выбор провайдера вынесен в фабрику, **дефолт — локальный**
(решение пользователя), Gemini доступен через `LLM_PROVIDER=gemini`. Формат
сейва не менялся.

**Что оказалось неверным в предыдущей записи.** Там сказано «провайдер-абстракция
НЕ реализована (отдельный милстоун)». Фактически интерфейс `LLMProvider`
существовал с автоцикла, а транспорт к локальному рантайму был полностью написан
в самом инструменте замера. Не хватало второй реализации и выбора между ними —
это работа на одну сессию, а не милстоун.

**Интерфейс переведён с готовой схемы провайдера на Zod-схему.** Прежде
конвертацию делал ВЫЗЫВАЮЩИЙ: `routes/primitives.ts` звал `toProviderSchema`,
экспортированный из `GeminiProvider`. Пока реализация была одна, это выглядело
безобидно; со вторым провайдером означало бы, что роутер знает, к кому подключён
движок, и способен отдать локальной модели диалект Gemini. Теперь вызывающий
описывает ФОРМУ ответа, а диалект — забота того, кто знает своё API.

**Три параметра зашиты, а не вынесены в настройки, потому что это условия
работоспособности:**

1. `json_schema` отправляется всегда — без грамматики 0/10 схема-валидных.
2. Лимит генерации по умолчанию 8192 — на 4096 размышление съедает лимит
   целиком (3/10), и мусор в переменной окружения откатывается на рабочий
   дефолт, а не превращается в 0.
3. Размышление включено по умолчанию. Оно втрое дороже по времени (60 с против
   6 с), но снимает выдуманные идентификаторы: 0 против 7. Быстрый режим
   доступен (`LOCAL_LLM_REASONING=none`), и его цена названа прямо — отклонённые
   действия, а не секунды.

**Опечатка в `LLM_PROVIDER` роняет запрос, а не откатывается на дефолт.** Тихий
откат при `gemeni` увёл бы ходы в локальную модель, а игрок увидел бы только
странные ответы — отказ, который невозможно связать с причиной.

**Стилевой контракт нарратива внесён в промт** (закрывает открытый вопрос 10):
запрет цитировать числа из промта, вносить технические id в прозу и упоминать
симуляцию. Замер: 7 нарушений из 10 против 0 из 4, цена ~142 токена. Названная
граница: замер делался русской формулировкой отдельным сообщением, в промт внесён
английский эквивалент, эффект именно английской редакции не перемерен.

**Проверено.** 28 тестов провайдеров (было 18), негативный контроль тремя
поломками — «схема стала необязательной», «опечатка откатывается молча»,
«размышление не вычитается из ответа» — уронил ровно 5 нужных тестов;
после восстановления 28/28. Типы сервера чисты.

**Открыто.** Живого прогона партии на локальном рантайме через игровой цикл (не
через инструмент замера) не было: замер ходил в модель сам, а `POST /llm/auto`
с локальным провайдером не гонялся ни разу. Риски предыдущей записи в силе:
промт мерился на первом ходу (5342 токена), поведение на разросшемся до 8–10k
и на поздней партии с войнами — UNKNOWN.

## 2026-07-30 — Локальный LLM-рантайм: замер на реальном промте хода

**Статус.** Замер, не внедрение. Провайдер игры по умолчанию НЕ менялся,
провайдер-абстракция НЕ реализована (отдельный милстоун), схемы и промты игры
НЕ правились. Цифры и оговорки — `.agent/runs/local-llm-runtime-bench-2026-07-30.json`,
инструмент — `server/scripts/benchLocalModels.ts`.

**Что установлено.** LM Studio 0.4.20 в `D:\ai-tools\LMStudio`, движок
`llama.cpp` CUDA, OpenAI-совместимый сервер на порту 1234. Хранилище моделей —
`D:\ai-models\lmstudio\models` (домашний каталог рантайма перенаправлен с C: на
D: NTFS-junction; на системном диске 7 ГБ, туда не влезла бы и одна модель).
Модели: `Qwen3.5-9B Q5_K_M` (6,62 ГиБ) и `Qwen3.6-35B-A3B Q4_K_M` (19,71 ГиБ).
Имена из задания устарели: dense-8B в линейке не существует (есть 9B), роль
«30B-A3B» закрывает 35B-A3B.

**Замерено на промте, который игра реально шлёт** (`createGame("1946")` →
`generatePrompt()`), с проверкой той же цепочкой, что в
`LLMService.processResponse`. Четыре результата, каждый меняет решение:

1. **`json_schema` обязателен.** Без грамматики 0/10 схема-валидных, с ней
   10/10. Типичные поломки без неё — обёртка ` ```json `, текст вокруг JSON,
   недостающие поля конверта. Это требование к будущему провайдер-слою, а не
   настройка удобства.
2. **Reasoning не помещается в лимит генерации и рвёт JSON.** При
   `max_tokens` 4096 — 3/10, причём провалы не качественные: размышление
   (~16 000 знаков) съедает лимит целиком. При 8192 — 8/8. То есть у локальной
   модели цена хода определяется не ответом, а размышлением перед ним.
3. **MoE в 10 ГБ VRAM не помещается и потому проигрывает вшестеро** (37,9 с
   против 6,1 с на ход) при той же схема-валидности. Попытка отдать ей больше
   GPU делает ХУЖЕ (65,5 с): драйвер вытесняет веса в системную память через
   PCIe, при этом счётчик загрузки GPU показывает 98% — по нему о пользе судить
   нельзя.
4. **Узкое место локальной модели — не JSON, а идентификаторы.** Обе модели
   выдумывают домены технологий (`nuclear_physics`, `civilian_industry`),
   которых у страны нет: схему те проходят, потому что `domain` в контракте —
   свободная строка, а спотыкаются о `validateActionApplicability`. Включённый
   reasoning убирает это полностью (0 против 7).

**Слабый нарратив оказался дефектом промта, а не размера модели.** Первая
оценка пользователя — «не очень»: модель цитировала суммы из промта
(«$118.69B»), вставляла технические id в русскую прозу («США (USA)») и ломала
погружение («в этом симуляции»). Проверено отдельным сообщением, БЕЗ правки
`generatePrompt()`: с явным запретом на эти три вещи нарушений 0/4 против 7/10,
схема-валидность не пострадала, ход подорожал на 2,5 с. 9B знает Трумэна,
Сталина и Восточную Европу — она пересказывала числа, пока её об этом не
попросили.

**Причина писать это сюда.** Замер отвечает на пункт 8 открытых вопросов
(«проверка альтернативных LLM providers на реальном structured-output
контракте») и снимает у него цену: локальные модели не требуют credentials, а
`reasoning_effort`/`json_schema` — стандартные поля OpenAI-протокола, то есть
проверенный режим переносится на провайдер-слой без нового диалекта.

**Что осознанно НЕ сделано.** Схемы под локальную модель не ослаблялись:
неспособность попасть в существующий id — данные о качестве контракта, а не
повод его смягчать. Стилевой запрет в промт игры не внесён — это игровая
семантика, решение пользователя.

**Открыто.** Промт замерен на первом ходу (5342 токена), поведение на
разросшемся до 8–10k не проверено. Фактическая точность дат не решена: модель
отнесла доктрину Трумэна (1947) и устав ООН (1945) к январю 1946 — для игры об
альтернативной истории расхождение с реальностью должно быть следствием решений
игрока, а не незнания модели; возможно, это область существующего
`eventFactuality.ts`.
## 2026-07-30 — Дипломатический слой: сеть раньше данных

**Решение.** Формат стартового слоя `diplomacy.json` задан, валидатор и 11
курируемых дипломатических фактов января 1946 написаны — **до** того, как слой
наполнен. Загрузка слоя в движок сознательно отложена до появления данных.

**Причина порядка.** Ровно этой сессией закрывался обратный случай: демография
1399 регионов была наполнена целиком, а спот-чек появился днём позже, и до его
прогона «воспроизводит статистику» было неотличимо от «правдоподобно
выдумала». Сошлось удачей. Дипломатия дороже демографии по цене ошибки:
втягивание союзников в войну читает `diplomacy.allies` напрямую, поэтому союз,
которого не было, меняет исход партии, а не оттенок.

**Три содержательных решения формата.**

1. **Пара, а не направление.** `relations` в состоянии направленные, но
   `driftRelations` ведёт обе стороны пары к одной цели `structuralAffinity` —
   два разных стартовых числа движок сотрёт за несколько тиков. Хранится одна
   запись на пару, порядок кодов не значим. Гарантии остаются направленными:
   гарант и защищаемый не взаимозаменяемы.
2. **Договор и политика разделены.** `alliances` — юридический факт (договор
   подписан и не расторгнут на 1 января 1946), `relations` — политическая
   реальность того же момента. Англо-советский договор 1942 года действовал до
   1955-го: союз в данных живой, отношения холодные, и союз рассыпается сам
   через `allianceBreakThreshold`. Записать «союза нет, они же враги» значило бы
   подменить данные их последствиями и отнять у движка работу, которую он умеет.
3. **Границы читаются из TS, парные формулы не воспроизводятся.** Шкала и пороги
   берутся из `shared/src/defines/diplomacy.ts` тем же приёмом, что перечни форм
   правления. `allianceThreshold`/`allianceBreakThreshold` зависят от
   идеологической дистанции и в Python не копируются: копия разошлась бы с
   оригиналом при первой правке калибровки. Проверяется только то, что верно при
   ЛЮБЫХ координатах — например, союз с отношениями ниже минимума порога распада
   по всей шкале дистанции (15) распадётся на первом тике независимо от
   идеологии. Точная сверка — дело серверного теста по загруженному сценарию.

**Класс ошибки, вокруг которого построены инварианты:** «данные, которые движок
отменяет на первом тике». Союз без записи отношений, союз ниже порога распада,
соперничество при отношениях выше порога примирения — формально валидны и
бессмысленны: такая разметка описывает не мир, а собственное исчезновение.

**Что проверено.** 72 теста в файле проверок слоёв (было 46), у каждого
предиката и каждого инварианта — тест на срабатывание, у эвристик — на обе
стороны. Отдельно проверена **выполнимость набора фактов**: эталонный слой,
удовлетворяющий всем 11 записям, даёт ноль ошибок — набор является сетью, а не
заблокированной правдой. Пять независимых порч (снятый советско-польский союз,
выдуманный союз СССР — США, тёплая Турция, тёплые англо-советские отношения,
враждебность внутри Арабской лиги) поймано поимённо с объяснением из файла
фактов; тёплая Турция дала два независимых сигнала — структурный инвариант и
спот-чек. Единственное предупреждение на эталоне — задуманное: англо-советский
союз при отношениях 38 «выживет только у идеологически близких».

**Статус.** Сеть реализована, данных нет. Наполнение — домен Gemini по скиллу
`delegate-data-layer`; загрузка в движок (Zod + `applyDiplomacyLayer`) идёт
вместе с данными, потому что схема под ни разу не наполненный формат — код без
потребителя.

**Поправка (2026-07-30, решение пользователя): порядок изменён — загрузчик
написан РАНЬШЕ данных.** Отсрочка выше отменена: `diplomacyFileSchema` и
`applyDiplomacyLayer` реализованы и покрыты тестами до наполнения слоя.
Довод «код без потребителя» оказался слабее своей цены: формат уже задан и
обоснован, поэтому расходиться схеме не с чем, а без загрузчика первое
наполнение проверялось бы только Python-валидатором — то есть повторило бы ту
самую асимметрию «данные раньше проверки», из-за которой запись и написана.

Что реализовано: пара из `relations` кладётся обеим сторонам, `alliances`/
`rivalries` — в списки обеих, гарантия направленная и пишется только гаранту.
Слой опциональный (файла нет — штатный путь; сегодня единственный живой) и
вызывается из `buildScenario1946` напрямую, а не из `applyDemographicLayers` —
тот возвращается рано без `groups.json`. Схема отвергает самопару, дубль пары в
любом порядке, значение вне шкалы и пару, стоящую разом в `alliances` и
`rivalries`; несуществующая страна — `ScenarioDataError` с её кодом.

Разделение труда с Python сохранено намеренно: класс «данные, которые движок
отменяет на первом тике» остался за валидатором, поскольку требует парных
порогов от идеологической дистанции. В схему вошло только то, что верно при
любых координатах.

**Проверено:** `npx tsc --noEmit` чист, `npm test` сервера — 1258 тестов
зелены; 13 новых тестов слоя на фикстурах. Негативный контроль — 11 порч
реализации по одной, каждая поймана своим тестом; в том числе обязательность
файла роняет загрузку боевого сценария 1946 (файла там действительно нет).

## 2026-07-30 — Данные 1946: сеть против правдоподобной выдумки

**Решение.** Валидатор слоёв (`validate_demographics_1946.py`) получил два
уровня — ОШИБКИ (код возврата 1) и ПРЕДУПРЕЖДЕНИЯ (просмотр человеком) — и новую
проверку по курируемым историческим фактам, которая относится к ОШИБКАМ.
Восемь фактов 1946 года лежат данными в
`scripts/map/data/historical_spot_checks_1946.json`.

**Причина.** Слои 1946 (демография 1399 регионов, идеология, формы правления,
влияние) наполнялись внешней моделью. Все прежние проверки — формальные:
сумма долей, ссылка в пустоту, расхождение слоя с составом карты. Правдоподобно
выдуманный состав проходит их ВСЕ: «в Тарту 60% русских» формально безупречно.
Массив, который никто не сверял с публикуемой статистикой, не отличим от
массива, который её воспроизводит, — а на нём стоит вся альтернативная история
партии.

Отсюда три решения:

1. **Курируемые факты — ERROR, не предупреждение.** Если генерация не проходит
   факт, проверяемый по опубликованной переписи, доверия к остальному массиву
   нет: там те же ошибки, только неизвестные. Ослабить до WARNING значит
   разрешить накопление ровно того, против чего проверка ставится.
2. **Эвристики правдоподобия — WARNING, и это не мягкость.** Три эвристики
   («все доли кратны 0.05» — состав назначен, а не оценён; «единственная группа
   на 100%»; «доминант не встречается ни у одного размеченного соседа») ищут
   ПОДОЗРИТЕЛЬНОЕ, а не неверное, и ошибаются по построению. Эвристика,
   способная заблокировать корректные данные, будет отключена при первом ложном
   срабатывании — вместе с настоящими находками. Отброшено намеренно: «группа
   встречается один раз во всём мире» (99 из 460 — вынесено отдельной строкой,
   в потоке утопило бы остальное) и «выброс доли относительно соседей» (без
   порога сотни срабатываний, с порогом — подгонка под текущие данные).
3. **Факты — данные, не код.** Набор расширяется по мере наполнения слоёв, и
   правка факта не должна требовать правки логики. Плата за это — набор может
   разойтись с каталогом групп, поэтому ссылка на исчезнувшую группу тоже ERROR:
   иначе проверка молча перестаёт проверять то, что заявляет. Запись про
   неразмеченный регион пропускается: покрытие слоя — предмет отдельной секции,
   и падать здесь второй раз значит удваивать один сигнал.

**Что показал прогон.** На живых данных — 0 ошибок, все восемь фактов пройдены:
1399 регионов размечены исторично по проверяемым фактам, а не только формально
корректно. 167 предупреждений (115 «все доли круглые», 42 «изолированный
доминант», 10 «единственная группа») — материал для ревизии каталога, не
дефекты. Работоспособность проверена негативным контролем: заведомо ложная
запись (Тарту с русским доминантом) даёт ошибку и код возврата 1; каждый из
пяти предикатов имеет тест на срабатывание, три эвристики — тесты на обе
стороны (срабатывает на подозрительном И молчит на нормальном). Тестов в файле
проверки слоёв: 29 → 46.

**Статус.** Реализовано. Правила уровней и конвенции пополнения набора —
в `scripts/map/AGENTS.md`.

## 2026-07-30 — Мастер-карта: геометрия согласована один раз и
зафиксирована в git; полигоны наконец «прилипают»

Идея пользователя, дословно: «Мы сначала заполняем море по game_map. Потом
режем сушу по морю. Потом что данные с разных источников и не совпадают
полностью. Попробуй упростить задачу, чтобы каждый раз одно и то же не
делать. Сделать, например, новый стартовый json.» Это и оказалось решением
проблемы, которую он ставил с 2026-07-19-e («Пока не будет идеального
прилипания полигонов — не отстану») и которую до сих пор чинили точечно.

**Диагноз, два независимых механизма.** Замер показал, что за жалобой на
белые/чёрные линии стоят разные причины, и ни одна не лечилась тем, чем
занимались раньше:
  1. `unary_union` всех 1577 фич давал полигон с **718 внутренними дырами,
     3312 км²**, медиана ширины 156 м (701 на стыке суша↔море). Их видели и
     сознательно игнорировали: `diagnose_coastline_gaps.py` относит такие
     полосы к `LAND_SEAM` (MRR aspect ≥ 8) и не выводит списком, а порог
     `MIN_AREA_KM2 = 0.5` скрывает всё тоньше полукилометра.
  2. `client/src/map/engine/TopologyBuilder.ts` считает отрезок берегом,
     если он встречается ровно у ОДНОГО региона (ключ — координаты,
     округлённые до 1e-5). При несовпадении узлов ОБА соседа получают
     `isCoast: true`, и на внутренний шов ложатся `coastline-glow` 4 px +
     `coastline-solid` 2.5 px почти чёрного. Замер: ~9200 отрезков рисовали
     берег там, где берега нет. Клиент дважды лечил это в рендере
     (`fill-antialias: false` в 2026-07-01, затем `fill-outline-color` в
     2026-07-18 после того как readPixels нашёл 742 пикселя фона) — симптом,
     не причина.

Проверка на месте жалобы подтвердила разделение: в Пьюджет-Саунд непокрытая
площадь была **81 м² на 49 297 км²** (дыр нет, линии от механизма 2), а в
зоне Панамского канала — реальная дыра 0.76 км² шириной 54.9 м.

**Отвергнуто по замеру, а не по мнению.** `set_precision` не решает задачу:
приведение к сетке 1e-7…1e-4 снижает длину невалидных рёбер лишь с 171° до
135–151° и НИ ПРИ КАКОМ шаге не даёт `coverage_is_valid`. Причина
принципиальная — у соседей РАЗНОЕ ЧИСЛО вершин на общем участке, а
округление вершины не добавляет и не удаляет. Buffer-заливка не
рассматривалась (отвергнута 2026-07-19-e, запрещена правилом №11 README).

**Что сделано.**
  - `build/weld_map_gaps.py`: список дыр берётся из `interior_rings`
    глобального union — исчерпывающий перечень без порогов формы, в отличие
    от `absorb_slivers`, который по построению не может закрыть то, что
    отклоняет. Раздача: 701 береговая дыра → ВОДЕ, 12 межморских → морю, 5
    внутри суши → суше. Направление «берег → воде» выбрано потому, что
    контур суши в проекте уже принят авторитетным и потому что площадь суши
    при этом не меняется — экономика и квантильные тиры
    (`density_tiers.py::generic_tier`, где перескок даёт ×5) не
    затрагиваются. Результат: 718 → 0 дыр, суша +16.7 км² на 146 млн.
  - `build/rebuild_shared_edges.py`: `unary_union` границ всех слоёв сразу
    нодирует их, `polygonize` даёт мозаику ячеек с общими рёбрами по
    построению, каждая ячейка уходит фиче, содержащей её
    `representative_point`. Результат: **`coverage_is_valid=True`,
    невалидных рёбер 0.000000°**, 10 секунд на 973 571 вершину. Ложные
    берега в клиентской топологии **9200 → 783**.
  - `build/freeze_master_map.py`: пишет
    `scripts/map/master/world_1946.master.geojson` (40.6 МБ) + `master.meta.json`
    с контрольными числами, и ОТКАЗЫВАЕТСЯ писать, если дыр не 0 или
    coverage невалиден. Мастер отслеживается git точечным исключением в
    `.gitignore` — тем же приёмом, которым там уже держится
    `client/public/world_1946.geojson` (37.8 МБ), так что вопрос размера в
    проекте был решён давно.
  - `import_to_game.py` и `audit_map_geometry.py` читают мастер;
    `FULL_REBUILD_STEPS` → `MASTER_REBUILD_STEPS` (31 шаг НЕ удалён — в нём
    всё знание о согласовании источников, но прогоняется только вручную
    через `--rebuild-master`). `check_coastline_gaps` переведён на
    геометрию из мастера вместо `load_all_geoms()`, иначе мастер не был бы
    самодостаточным.

**Самодостаточность проверена делом:** при полностью удалённых
`out/*.geojson` пайплайн проходит целиком и все проверки зелёные.

**Побочно исправлен баг измерения площади.** На фигуре, вытянутой вдоль
параллели, геодезическая площадь даёт абсурд: геодезическая линия между
двумя точками одной широты идёт СЕВЕРНЕЕ параллели, и pyproj считает
площадь серпа между ними. British Columbia: полоса вдоль ровно 60-й
параллели (граница BC/Юкон) с планарной площадью 0.0000636 deg² дала
25 070 км² — больше, чем весь diff (25 047), то есть «часть» вышла больше
целого. Новый `safe_area_km2` берёт минимум геодезической и планарной
оценки; SPIKE 77 → 67 записей.

**Разобраны все изменения площади — ни одно не потеря территории.** Gansu
−18.5 км²: перекрывался с Suiyuan на 17.96 км², пересборка отдала спорную
ячейку одному владельцу вместо двойного счёта. Mato Grosso: 18 частей → 17
при неизменной площади (две касающиеся части слились). Beaufort Sea
−2.96 км² моря из 433 965 — те самые мелкие куски, которые аудит помечал
как `WATER_SHATTERED` (теперь 1 → 0). Итого суша −22 км², вода +63 км² на
510 млн.

**Верификация.** `merge_world_1946.py` 1577 фич / 0 невалидных / 0
пересечений; `build_neighbor_graph.py` 0 изолированных; `make_1946.py`
целиком зелёный; 12/12 и 33/33 тестов; покрытие столиц 12/13 как было, все
13 якорей в своих регионах, запасы у BRA/USA/CAN не изменились
(0.45/1.27/2.91 км); регионов с изменением населения даже >1% — **НОЛЬ**;
оба скрипта идемпотентны. COASTLINE_GAP в аудите 318 → 7, и оставшиеся 7 —
краевой артефакт разреза по антимеридиану (точки лежат ровно на ±180°, при
сдвиге на 0.02° внутрь всё покрыто).

**Поправка (2026-07-30, закрытие плана): правку клиента отменили по замеру,
вместо неё — инвариант.** Планировался ещё один этап: в
`TopologyBuilder.ts` определять берег по `region_type` (граница land с
sea/lake), а не по эвристике «отрезок встречается у одного региона». Замер
на готовом мастере показал, что менять нечего и что правка сделала бы хуже.
Из 285 133 отрезков, которые клиент рисует берегом, к ложным относятся
**3, и все три вырожденные** — после клиентского округления до 1e-5 их
концы слипаются, длины нет, `line-cap: butt` не рисует ничего. Остальные
«одиночки» — **66 отрезков настоящего берега, у которого просто нет
морского полигона** (39 Чукотка и 20 Фиджи — оба на антимеридиане, то же
самое, что оставшиеся 7 `COASTLINE_GAP`). Переключение на `region_type`
сняло бы берег ровно с этих 66 в обмен на 3 невидимых: цена больше выгоды,
этап закрыт как ненужный. Цифра «783» из абзаца выше — тот же замер без
исключения воды и края карты, то есть завышенный; корректное число ложных
берегов на мастере — 0 видимых.

Заодно выяснилось, что `COVERAGE_INVALID` этот класс дефекта **не
покрывает**: на мастере GEOS отвечает «рёбра совпадают» (0 невалидных
рёбер), а клиент сравнивает не рёбра, а отрезки по округлённым концам — и
лишняя вершина у соседа разводит ключи при геометрически валидной карте.
Поэтому добавлен 12-й класс аудита **`FALSE_COAST`**, который воспроизводит
клиентскую логику (тот же ключ, то же округление) и находит отрезки
ненулевой длины внутри суши, которые клиент нарисует берегом. Негативный
контроль на историческом снимке `67d017e` (до сшивки): **5217 находок**,
самые длинные — Gansu/Suiyuan 148 км, Arizona/Sonora 51 км, British
Columbia 52 км; на мастере — 0. Первая версия проверки определяла воду
только по `region_type` и на старом формате приняла морские секторы за сушу
(149 858 «дефектов» по 60°E и 125°W) — теперь вода распознаётся по любому
из трёх признаков, встречающихся в наших файлах.

Сравнение до/после снято в самом клиенте (11 точек, зумы 2.6–9): вопрос о
видимости слоёв, который планировалось задать пользователю, снят вместе с
правкой — швы никуда не переезжают, `internal-borders` остаётся как был.

**Что это меняет на будущее.** Позиционные `region_id` перестают ездить:
их порядок больше не пересчитывается при каждой сборке, а именно это весь
предыдущий день ломало `ownership_1946.json`, `names_ru.json` и
`capital_overrides.py` (~33 + 213 + 26 записей). Откат геометрии стал
`git checkout` — необходимость этого подтвердилась в тот же день, когда
бэкап в scratchpad потерялся при перезапуске сессии. Правки границ теперь
коммиты с diff, а не мутации невидимого файла.

---

## Открытые вопросы (актуально на 2026-07-15)

Живой индекс нерешённого — единственный источник статуса "что ещё не решено".
Завершённые пункты убираются отсюда (их история — в датированных записях выше, архив — `docs/decisions/`).

### Нужны дизайн-решения (числа/правила) до кодинга

> 2026-06-26: каждому вопросу ниже завёден профильный дизайн-док в `/docs`
> (заготовка — факты из кода + помеченные открытые вопросы). Список ниже
> остаётся живым индексом статуса; детали дизайна — в самих доках.

1. ~~Бюджетные нудж-правила~~ — доступное подмножество спроектировано (2026-06-23, аустерити по дефициту). Остаток («война→military», «низкая stability→welfare») заблокирован: нет war-системы и динамики `politics.stability` → P3. Теперь отслеживается в `docs/WAR.md` и `docs/POLITICS.md`.
2. ~~Threat/Rivalry Response~~ — спроектирован с полной балансировкой (2026-06-23, числа тюнингуемые). Legitimacy-кризис и war-зависимые части → P3 (нужна war-система + динамика `politics`). Консолидировано в `docs/DIPLOMACY.md`.
3. Явный список стран-тиров для LLM-решений (нужен только для LLM-режима, отложено вместе с LLM). Теперь отслеживается в `docs/DIPLOMACY.md`. — **дизайн решён редизайном (2026-07-24): пять тиров + оси, `CONCEPT.md` §5.8; реализация — план 11.**
4. **Динамика политики** (новое, поднято 2026-06-23): что двигает `politics.stability/legitimacy/governmentSupport` — были полностью статичны. Первый реальный драйвер появился 2026-07-06 (`WarService.makePeace` — исход войны сдвигает `legitimacy`/`governmentSupport`), но это единственный источник; выборы/коррупция/экономика как драйверы `stability` не спроектированы. Теперь отслеживается в `docs/POLITICS.md`. — **дизайн решён редизайном (2026-07-24): `CONCEPT.md` §4/§5.2 (идеология-дрейф + драйверы недовольства); реализация — план 11.**
5. ~~War-система~~ — **Phase 1 реализовано (2026-07-06)**: состояние/объявление/коалиции/упрощённый фронт/мир, см. датированную запись. Phase 2 (честное вооружение) и Phase 3 (логистика/окружение/прокси-сфера/ядерное сдерживание) — не начаты, отслеживается в `docs/WAR.md`.
6. MapFeature: военные объекты (`battalion`) теперь реально создаются войной (2026-07-06, `WarTick.ts`); протесты/восстания, политические объекты, дипломатические оверлеи — по-прежнему не спроектированы. Теперь отслеживается в `docs/MAP_FEATURES.md`. — **дизайн решён редизайном (2026-07-24): `CONCEPT.md` §5.6 (динамические объекты + событийные) + §5.4 (слои блоков); реализация — план 11.**
7. Система мировой торговли/логистики (от неё зависят MapFeature-коннекторы — ж/д/канал/трубопровод). Теперь отслеживается в `docs/TRADE.md`.
8. Петля ресурсов: `stockpile` только копится, ничего не потребляет/продаёт — нужно решить потребление/монетизацию (глубже базовой петли дохода, отдельно). Теперь отслеживается в `docs/ECONOMY.md`. — **дизайн решён редизайном (2026-07-24): `CONCEPT.md` §5.1 (гейт-потребление сырья); реализация — план 11.**
9. ~~Единицы экономики~~ — **решено и реализовано (2026-06-26)**: модель "ВВП-якорь + доли" (`EconomyProfile`, архетипы по `EconomyType`, деривация в `createGame`). Расходы/дефицит теперь влияют на рост/manpower/рождаемость/исследования во всех 8 затронутых местах, подтверждено симуляцией. Попутно исправлен отдельный баг (рост ВВП стирался каждый ход пересчётом `region.gdp`) и id-баг Italy/ITA. Детали — `docs/ECONOMY.md`, архив `docs/decisions/2026-06.md`.

### LLM (отложено пользователем, решения зафиксировать заранее)
6. Формат и механизм нарративной памяти страны между LLM-вызовами.
7. Накопление/вызов LLM для будущего недельного хода; месячный N-step уже
   реализован.
8. ~~Проверка альтернативных LLM providers на реальном structured-output
   контракте~~ — **закрыт 2026-07-30**: замер прогнан на реальном промте хода,
   затем внедрён `LocalOpenAIProvider`, выбор вынесен в фабрику, локальный
   рантайм стал провайдером по умолчанию. Остаток не «слой», а НАБЛЮДЕНИЕ:
   живого прогона партии через `POST /llm/auto` с локальным провайдером не
   было — замер ходил в модель своим инструментом.
9. Расширение каталога исторических развилок после 1953 года
   (`docs/tasks/HISTORICAL_HINGE_POINTS_1946.md`).
10. ~~Стилевой контракт нарратива в промте~~ — **закрыт решением пользователя
    2026-07-30**: запрет внесён в `generatePrompt()` (`docs/LLM_RULES.md`,
    «Стиль нарратива»). Названная граница: замер делался русской формулировкой
    отдельным сообщением, в промт внесён английский эквивалент, и эффект именно
    английской редакции не перемерен.
11. **Фактическая точность дат в нарративе** (поднято 2026-07-30): локальная
    модель относит реальные события к неверным годам (доктрина Трумэна 1947,
    устав ООН 1945 → январь 1946). Для игры об альтернативной истории
    расхождение с реальностью обязано быть следствием решений игрока, а не
    незнания модели. Решать отдельно и, вероятно, в существующем
    `server/src/llm/eventFactuality.ts`, а не новым механизмом рядом.

### Архитектура / долги
12. Окна сравнения не закрываются по Esc. Удержание внутри карты, persistence
    позиции/размера и сброс layout реализованы; Esc требует отдельного UI scope.
13. Human review и внешний enforcement для `.agent/CHARTER.proposed.md`;
    fresh-session проверка загрузки repo skills/MCP/custom agents.

Осознанно НЕ делается (зафиксированный отказ, не "вопрос"): вкладки Армия/Дипломатия/Политика/Население/История/Статистика с HOI4-референса — нет данных/экранов; диаграммы по клику на статы — нет хранения истории значений по ходам; отдельная механическая система разведки/внутренних фракций/культуры (независимый гейм-дизайн разбор, 2026-07-06, подтверждено пользователем) — эти темы остаются чистым нарративом LLM без числового слоя, по уже заявленному в `AGENTS.md` принципу избегать микроменеджмента там, где числа не нужны геймплею.

> Решено и закрыто (детали — в архиве `docs/decisions/2026-06.md`): техдолг `player.ts`/`ProductionTick`/роуты-мимо-сервисов/`resourceBonus` (2026-06-22); flip-flop союзов в `DiplomacyTick` (2026-06-22); `/budget` и `/actions` 404 (2026-06-22); sort-мутация в `LLMService` (2026-06-23); серверные тесты всех сервисов + клиентская тест-инфра + `useWindows` (2026-06-23); кто считает добычу ресурсов — только `ResourceTick` (2026-06-23, бывш. вопрос 4); источник динамического `taxRevenue` = ВВП × ставка (2026-06-23).
