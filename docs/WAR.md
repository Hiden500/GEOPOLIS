# Война

Last updated: 2026-07-31 (потери войны перестали затираться следующим тиком;
замером названы обвал фронта и вечная война без завершения)

> **Phase 1 реализован** (docs/DECISIONS.md, 2026-07-06): состояние войны,
> объявление (LLM для топ-держав / детерминированный порог для остальных),
> коалиции (allies/guarantees/puppets — авто-втягивание; sphereOfInfluence —
> не втягивается, прокси-поддержка отложена в Phase 3), упрощённый фронт по
> контактным границам регионов, мир с последствием для легитимности, видимость
> на карте (`battalion` MapFeature), секция `## Active Wars` в промте LLM.
> **Phase 2 реализован частично** (независимый гейм-дизайн разбор, 2026-07-06):
> производство техники по 8 категориям (`MilitaryState.equipment`, тир домена
> технологий даёт боевой буст), LLM-нудж через `production_shift`. Без понятия
> "доктрина" (детерминированный дефолт — поровну, как research) и без замены
> `activePersonnel`-плейсхолдера — экипировка ДОБАВЛЕНА к боевой формуле, не
> заменила её. Раздел "Предлагаемое направление" ниже соответствует тому, что
> реализовано — числа (пороги/формулы) теперь источник истины в коде
> (`WarService.ts`/`WarTick.ts`/`AiBehaviorTick.ts`/`MilitaryTick.ts`), не в
> этом доке. Явные пробелы Phase 1/2 и план Phase 3 — в конце файла.

## Кто объявляет войну и заключает мир (Милстоун 1, 2026-07-27)

Объявление войны и заключение мира — **структурные примитивы алфавита**
(`war`, `peace`), а не действия старого канала `actions`: типы `war`/`peace`
удалены из `server/src/llm/actionSchemas.ts`. Механика войны при этом НЕ
изменилась — оба глагола вызывают те же `WarService.declareWar` и
`WarService.makePeace`, с тем же авто-втягиванием коалиций, теми же условиями
мира по warScore и теми же значениями сопутствующего сдвига отношений (−100 при
объявлении, +50 при мире). Перенос значений без изменения намеренный: перевод в
алфавит не должен оказаться ещё и тихой рекалибровкой.

Что изменилось по существу:

- **Правила стали предпосылками движка** со структурными кодами отказа: «уже
  воюем», «война с союзником», «нет активной войны». Раньше это были английские
  строки `LLMResponseValidator`, которые игрок читал как есть.
- **Оба глагола структурные**, то есть делят ЕДИНСТВЕННЫЙ структурный слот
  игрового месяца с `enact_reform` и `split_country`, и их отказ движком
  откатывает весь ответ модели.
- **Мир проходит через пост-инварианты**, чего старый канал не делал. Это
  вскрыло предсуществующую ловушку: `transferRegion` не переносит столицу, а
  инвариант требует её среди своих регионов, — значит мир с аннексией столицы
  откатывал бы весь ответ. Столица теперь переезжает той же функцией, которой
  её переносит раскол государства.
- **ПОВОД (casus belli) по-прежнему не проверяется**, и это названо явно:
  претензий и спорных территорий в состоянии не существует, а отношения в
  сценарии 1946 у всех стран нулевые — порог по ним был бы либо пустым, либо
  запирал бы войну во всём мире (`docs/TODO.md`).

Детали контракта — `docs/PRIMITIVES.md` §2.

## Назначение

Дом для дизайна военных конфликтов: как объявляется война, как она хранится
в состоянии игры, как считается бой, и какие последствия она тянет за собой
в других системах (политика, дипломатия). Расширяет 5 строк из
`docs/AI_RULES.md` ("Военная система").

Смежные доки: [`POLITICS.md`](POLITICS.md) (кризис легитимности после
войны), [`DIPLOMACY.md`](DIPLOMACY.md) (война как "зубы" союзов/сферы),
[`MAP_FEATURES.md`](MAP_FEATURES.md) (армии/флоты/авиабазы как визуальные
объекты, тег `battalion`).

---

## Текущее состояние в коде

### Поля `MilitaryState` (`shared/src/types/MilitaryState.ts`)

```
manpower, activePersonnel, reservePersonnel, militaryBudget
armyStrength, navyStrength, airStrength, nuclearWarheads
units: Unit[]
equipment: Record<EquipmentType, number>
```

### Состояние войны (реализовано 2026-07-06, Phase 1)

`GameState.wars: War[]` (`shared/src/types/War.ts`) — пара воюющих сторон
(с коалициями), дата начала, статус (`active`), счётчик флипов территории
для формулы мира. `LLMAction.type: 'war' | 'peace'` теперь реально создаёт/
завершает объект `War` через `WarService` (раньше был ярлыком-заглушкой без
модели состояния).

### Что уже есть и может быть фундаментом

- `MilitaryTick` (реализован, подключён — `AI_RULES.md` "Симуляция")
  считает рутинную военную динамику (manpower из населения региона, см.
  `AI_RULES.md` "Использование регионов"), но без понятия "противник" —
  работает независимо для каждой страны.
- Принцип боя уже зафиксирован в `AI_RULES.md:172-181`: **не**
  посекундная симуляция, а **модель фронтов/операций** — бой считается на
  границах контакта между регионами с разными `ownerCountryId`, находящимися
  в состоянии войны, не "по всей карте". Снабжение — от инфраструктуры
  региона и связности с тылом.
- `MapFeature` уже поддерживает военные типы (`battalion`, `fleet`,
  `airbase`, `naval_base`) и обязательный тег `battalion` для всех наземных
  соединений (`AGENTS.md`) — см. `MAP_FEATURES.md`.
- `Region.ownerCountryId` уже даёт границу контакта "бесплатно" (смена
  владельца — естественный результат боя), не нужна отдельная сущность.

---

## Оккупация ≠ аннексия (реализовано 2026-07-11, план 08 Шаг 1)

> Только Шаг 1 плана `08_WAR_WAVE1.md` — Шаги 2-4 (war score/мирные
> договоры, потери, госдолг) отложены, см. «Отклонения при реализации» в
> файле плана.

Захват региона войной больше не переписывает владение мгновенно и навсегда:

- `Region.occupiedBy?: string` — оккупация поверх `Region.ownerCountryId`.
  Отсутствие = регион не оккупирован, полный контроль у легального
  владельца. `shared/src/utils/regionControl.ts::effectiveController(region)`
  — единая точка «кто сейчас фактически контролирует регион» (оккупант, если
  есть, иначе владелец).
- `WarTick.ts` — фронт (кто с кем граничит, у кого перевес) считается по
  `effectiveController`, не по легальному владению — иначе после первой же
  оккупации граница застревает на довоенной линии вместо боевой. Флип двигает
  только `occupiedBy` (`server/src/simulation/war/occupation.ts::
  setRegionOccupation`) — `ownerCountryId` не меняется войной вообще.
- **Эффекты оккупации:** добыча ресурсов региона (`ResourceTick.ts`) идёт
  оккупанту со штрафом (`OCCUPATION_EXTRACTION_PENALTY=0.5`,
  `shared/src/defines/occupation.ts`) — легальный владелец из оккупированного
  региона больше ничего не получает. `Region.stability` получает
  add-модификатор `OCCUPATION_STABILITY_PENALTY=-20` на весь срок оккупации
  (снимается при освобождении) — первое реальное использование
  region-уровневых модификаторов (план 03, белый список атрибутов не привязан
  к форме сущности — `target.kind` уже disambiguates `Country.politics.
  stability` от `Region.stability`).
- **Конец войны:** `WarService.makePeace` применяет условия по warScore
  (Шаг 2b, ниже) — победитель аннексирует часть оккупированного,
  неаннексированное возвращается легальному владельцу.
- **Осознанно не редиректится оккупанту**: ВВП/население страны
  (`aggregateCountryData.ts` по-прежнему считает по `ownerCountryId`),
  манпауэр/рождаемость/исследовательские центры (`MilitaryTick`/
  `PopulationTick`/`ResearchTick`) — план называет только «доход/добычу».
  Клиентская визуализация (штриховка оккупированной территории) — не
  реализована, интерфейс заморожен.

## Потери → демография (реализовано 2026-07-11, план 08 Шаг 3)

Война теперь стоит людей. `WarTick.ts` каждый месяц начисляет потери сторонам
активной войны и накапливает их в `War.casualties: Record<countryId, number>`
(вход для warScore — Шаг 2a — и мирового факта в промте LLM).

- **Объём потерь.** Общий помесячный урон = `WAR_CASUALTY_BASE_PER_FRONT_REGION`
  (`shared/src/defines/war.ts`, 10 000 — тюнингуемый плейсхолдер) × число
  фронтовых (контактных) регионов обеих сторон. Делится между сторонами по
  доле силы ПРОТИВНИКА: `своя доля = opposingStrength/(own+opposing)` —
  слабейшая сторона теряет больше. Без контакта (нет фронта) потерь нет.
- **Порядок списания** по каждой стране: `military.activePersonnel` в первую
  очередь (кадровые потери) → переполнение сверх активной армии уходит в
  `Region.population` регионов, которые страна фактически контролирует
  (`effectiveController`, пропорционально населению, пол 0) → `military.manpower`
  уменьшается на весь урон страны (мобилизационный пул ужимается и от павших
  солдат, и от погибших гражданских).
- **Списание доезжает только с 2026-07-31.** До этого `MilitaryTick`
  присваивал `activePersonnel = manpower × ACTIVE_PERSONNEL_SHARE` каждый месяц,
  и потери этого месяца стирались следующим тиком: уцелевала доля
  `ACTIVE_PERSONNEL_SHARE`, то есть **из убитого солдата в строй возвращалось
  90%**. Теперь армия — ЗАПАС, пул задаёт только потолок, набор идёт со
  скоростью `ACTIVE_PERSONNEL_MOBILIZATION_RATE` (`shared/src/defines/military.ts`).
  Порядок тиков НЕ менялся и больше не несущий. Замер и разбор —
  `server/scripts/probeWarFeedback.ts`, `docs/DECISIONS.md` (2026-07-31).
- **Нюанс баланса.** Интенсивность привязана к участку фронта, НЕ
  пропорциональна размеру армии. Поэтому маленькая армия быстро исчерпывается и
  потери «переливаются» в гражданское население — тематически это разгром малой
  армии, а не баг. Восстановление демографической ямы идёт естественным ростом
  `PopulationTick` (яма на десятилетия получается сама, отдельной механики
  восстановления нет).
- **Осознанно вне Шага 3:** порог потерь → мировой факт «X потеряла N человек»
  в промте (придёт с warScore, Шаг 2a); эффект потерь на legitimacy/stability
  сверх послевоенного штрафа (Шаг 2/4).

## warScore — кто побеждает (реализовано 2026-07-11, план 08 Шаг 2a)

`server/src/simulation/war/warScore.ts::computeWarScore(war)` — **чистая
функция**, не хранимое поле War (производное состояние — флипы + потери уже в
War — не дублируется в GameState, считается на месте, не рискует рассинхроном).
Диапазон −100..100 от лица атакующих (+ = атакующие впереди).

- **Территориальная компонента:** `(toAttackers − toDefenders) ×
  WARSCORE_PER_NET_FLIP` (15). ~7 чистых флипов = решительные 100.
- **Компонента потерь:** `(defenderCas − attackerCas)/(сумма) ×
  WARSCORE_CASUALTY_WEIGHT` (40). Кто пролил меньше крови относительно врага —
  в плюсе. Максимум ±40 от потерь, территория добивает до ±100 (капается).
- **Использование:** промт LLM (секция Active Wars теперь показывает war score
  + ярлык `warScoreLabel` + потери сторон) — модель предлагает соразмерный мир,
  не выдумывая по цифре. Ценообразование мирного договора против warScore —
  Шаг 2b (мир + границы, отдельный заход).
- **Нюанс баланса / отклонение от буквы плана:** план называл третьим входом
  «длительность (истощение тянет к миру)». warScore здесь — измеритель
  ПРЕИМУЩЕСТВА (кто побеждает), а не готовности к миру; истощение от
  длительности отыгрывается отдельной военной усталостью в `makePeace`
  (`STALEMATE_WEARINESS_*`), не смешивается с сигналом преимущества — иначе
  решительная, но долгая победа занижалась бы неверно.

## Мирное разрешение и аннексия (реализовано 2026-07-11, план 08 Шаг 2b)

`WarService.makePeace` → `applyPeaceTerms(war)` — детерминированные условия по
warScore. LLM/игрок решает, КОГДА заключить мир (действие `peace`); движок —
СКОЛЬКО территории переходит.

- **Белый мир.** `|warScore| < WARSCORE_ANNEXATION_THRESHOLD(30)` — граница не
  меняется, вся оккупация войны снимается (`revertOccupationForWar`).
- **Аннексия по warScore.** Иначе победитель (сторона по знаку warScore)
  аннексирует оккупированные ИМ регионы проигравшего — но не больше «бюджета
  очков»: `maxAnnex = floor(|warScore| / WARSCORE_COST_PER_ANNEXED_REGION(25))`.
  Выбор регионов детерминирован (по возрастанию id). Остальная оккупация
  снимается. Проигравший-оккупант ничего не аннексирует.
- **`transferRegion`** (`simulation/war/occupation.ts`) — единственная
  прод-мутация `ownerCountryId` вне сценарной загрузки: меняет владельца и
  снимает оккупацию (occupiedBy + стабильность-модификатор). Командная обёртка
  `commands/war.ts::transferRegion` — для будущего LLM-действия с условиями.
- Числа — тюнингуемые плейсхолдеры (`shared/src/defines/war.ts`). Легитимность/
  репарации проигравшему (ниже) применяются поверх, без изменений.
- **Отложено (обогащение):** LLM предлагает конкретный `PeaceTerms` (составной
  `data`-объект контракта), валидируемый против того же бюджета warScore — нужен
  живой прогон Gemini, отдельный заход. Сейчас условия чисто детерминированы.

## Легитимность после войны (реализовано 2026-07-06)

`docs/DECISIONS.md` (2026-06-22, "Threat/Rivalry Response"; уточнено
2026-07-06): исход войны → **детерминированное** изменение
`PoliticsState.legitimacy/governmentSupport` (`WarService.makePeace`) —
проигравший получает штраф, агрессор-проигравший — жёстче, затянувшаяся
ничья бьёт по обеим сторонам (военная усталость). Форма кризиса (протесты/
импичмент/переворот/ничего явного) — нарратив LLM, само срабатывание и
магнитуда — движок, не LLM.

---

## ⚠️ Открытые вопросы (Q5) — статус на 2026-07-06

1. ~~Где живёт состояние войны~~ — **реализовано**: `GameState.wars: War[]`
   (`shared/src/types/War.ts`) — `attackers`/`defenders` (коалиции, [0] —
   инициатор/цель), `supporters` (Phase 3, пусто сейчас), `startDate`,
   `warGoal?`, `active`, `territoryFlips`.
2. ~~Объявление войны~~ — **реализовано, область сужена 2026-07-31**: войну
   начинают только игрок (`POST /primitives/apply`) и режиссёр-LLM — оба через
   примитив `war` (`PrimitiveEngine.ts`). Детерминированного пути больше нет:
   Правило D (`AiBehaviorTick.ts`, порог для non-major) удалено решением
   пользователя, потому что за всё время не объявило ни одной войны — у него не
   было ни одного кандидата (все соперничества мира возникают с участием
   великой державы, а правило смотрело только пары non-major↔non-major) и
   вдобавок недостижимый порог. Замер — `server/scripts/probeWarReach.ts`,
   разбор — `docs/DECISIONS.md` (2026-07-31). **Цена:** без LLM мир остаётся
   вечно мирным, и вся военная механика ниже не запускается ничем.
3. **Флип фронта решается ЛОКАЛЬНО (2026-07-31).** Сила стороны делится по её
   участкам фронта; давление на регион пропорционально числу соседей под
   контролем противника, оборона — доля армии плюс инфраструктура региона
   (снабжение и укреплённость). До этого сравнивались глобальные силы сторон, и
   решение было одинаковым для всего фронта: он не двигался, а падал целиком.
   Стартовые армии при этом сеются из населения (`seedStartingArmies`) —
   сценарий военных полей не размечает, и без сева война была сравнением нулей.
   Числа и разбор — `docs/DECISIONS.md` (2026-07-31).
4. **Расчёт боя на границах — Phase 2 частично реализован (2026-07-06).**
   `WarTick.ts::sideStrength()` теперь = `activePersonnel × combined-arms
   мультипликатор` (широта вложений в военные домены) `+ getEquipmentPower ×
   EQUIPMENT_STRENGTH_WEIGHT` (`shared/src/utils/equipment.ts` — количество
   техники по 8 категориям × эффективность по тиру релевантного домена).
   `activePersonnel` остаётся частью формулы, не заменена — экипировка
   добавляет вклад поверх неё. `FLIP_THRESHOLD_RATIO` — тот же детерминированный
   порог перевеса.
4. **Снабжение — не реализовано, Phase 3.** Нет штрафа за отрезанность от
   тыла (BFS-связность с столицей/портом) — заведено в TODO.md.
5. ~~Условия мира~~ — **реализовано**: движок считает исход по
   `territoryFlips` + длительность + роль агрессора → детерминированный сдвиг
   `politics.legitimacy`/`governmentSupport` (`WarService.makePeace`), LLM
   даёт только `warGoal`/нарратив, не числа.
6. **"Зубы" союзов — реализовано; "зубы" сферы (прокси) — Phase 3.**
   `allies`/`guarantees`/`puppets` автоматически втягиваются в войну (один
   уровень, без транзитивного замыкания — `WarService.declareWar`).
   `sphereOfInfluence` осознанно НЕ втягивается (решение пользователя,
   2026-07-06: "сферу — необязательно... нужна механика прокси-войн") —
   `War.supporters` заведено в типе, сам механизм (детерминированный триггер
   + боевой бонус, без превращения в участника) — Phase 3.

---

## Реализовано (Phase 1, docs/DECISIONS.md 2026-07-06)

- `shared/src/types/War.ts` + `GameState.wars`.
- `server/src/services/WarService.ts` — `declareWar` (коалиции, идемпотентно),
  `makePeace` (формула легитимности), `getActiveWarBetween`.
- `server/src/simulation/war/WarTick.ts` — глобальный тик, контакт по
  `neighboringRegionIds`, флип `ownerCountryId`, `battalion` MapFeature на
  контактных регионах (убираются при `makePeace`).
- `LLMService.ts` — реальные `applyWarAction`/`applyPeaceAction`, секция
  `## Active Wars` в промте, инструкция избегать прямой войны между
  ядерными державами (не механика — только текст промта, полноценное
  сдерживание — Phase 3).
- `LLMResponseValidator.ts` — применимость `war`/`peace` (не дублирует
  идущую войну, нельзя объявить войну союзнику, `peace` требует активной
  войны).
- Живая проверка пройдена (объявление войны, коалиции, продвижение фронта,
  мир с легитимностью, очистка маркеров) — детали прогона и числа:
  `docs/DECISIONS.md`, 2026-07-06 ("War-система Phase 1").

## Реализовано (Phase 2, независимый гейм-дизайн разбор, 2026-07-06)

- `shared/src/types/military/EquipmentType.ts` (8 категорий:
  rifles/trucks/tanks/artillery/fighters/bombers/destroyers/submarines) —
  существовало как мёртвый скелет (нигде не читалось/писалось), теперь
  реально используется.
- `MilitaryTick.ts` — производство: доля `militarySpending` на категорию →
  прирост `equipment[type]` (`EQUIPMENT_SPENDING_SCALE`), категории без явной
  доли в `productionAllocation` делят остаток поровну (тот же паттерн, что
  `researchAllocation`/`ResearchTick.ts`). Без отдельного понятия "доктрина" —
  дефолт "поровну" и есть доктрина по умолчанию. С 2026-08-02 выпуск
  умножается на покрытие сырьевого спроса (гейт сырья, вариант А): спрос
  `militarySpending / EQUIPMENT_RESOURCE_DEMAND_SCALE` сверяется с запасом
  военного сырья (`WAR_MATERIAL_RESOURCE_IDS`, `shared/src/defines/military.ts`)
  и физически его потребляет; непокрытая доля пропорционально режет выпуск
  всех категорий (матрица «ресурс × тип техники» — после v1, `docs/IDEAS.md`
  §11). Замеры и калибровка — `docs/DECISIONS.md` 2026-08-02.
- `shared/src/utils/equipment.ts` — `getEquipmentPower()`: количество техники
  по категории × эффективность по тиру соответствующего домена
  (`EQUIPMENT_DOMAIN_MAP`, жёстко на домены эры 1946).
- `WarTick.ts::sideStrength()` — экипировка добавлена к боевой силе поверх
  `activePersonnel × combined-arms`, не заменяет.
- `LLMAction 'production_shift'` — доступен и игроку, и топ-державам через
  LLM (`sourceCountryId` — любая страна, тот же паттерн, что `research_shift`).
  Потолок доли `MAX_PRODUCTION_SHARE=0.7`, **без** admin-capacity снижения за
  войну (в отличие от `research_shift`) — война логически повышает фокус на
  производстве, не распыляет его.
- Декоративные названия оружия по эре ("ППШ→АК как тир внутри одной
  категории") — не нужна новая работа: детекция пересечения тира в
  `SimulationEngine.ts` (2026-07-06, тех-вехи) уже работает по всем доменам,
  включая военные (`armor`/`infantry`/`aviation`/`naval`).

**Не реализовано в Phase 2:** честная замена `activePersonnel`-плейсхолдера
(экипировка — дополнительное слагаемое, не основа формулы); "доктрина" как
отдельная механика (сведена к дефолту "поровну").

## Явные пробелы (не реализовано сейчас)

- **Фронт обваливается, а не движется** (замер 2026-07-31,
  `server/scripts/probeWarFeedback.ts`). `FLIP_THRESHOLD_RATIO` сравнивает
  ГЛОБАЛЬНУЮ силу стороны с глобальной силой противника, поэтому решение
  одинаково для всех контактных регионов сразу: ITA→FRA — 18 регионов из 20 за
  6 месяцев, ROU→YUG (равные соседи) — все 6 за 6 месяцев. Усугубляется тем,
  что помесячные потери фронтового региона (`WAR_CASUALTY_BASE_PER_FRONT_REGION`
  = 10 000) заведомо больше месячного набора манпауэра (0,1% населения):
  слабейшая сторона упирается в ноль, отношение сил уходит в бесконечность.
  Замена — локальная боевая мощь по регионам + обобщённый Ланчестер,
  `.agent/audits/models-research-2026-07-30.md` §1.
- **Война не кончается ничем, кроме глагола `peace`.** После обвала фронта
  контакта нет, потерь нет — в замере 114 из 120 месяцев не происходит ничего, а
  война остаётся активной с warScore ±100. Реализация условия отложена решением
  пользователя (2026-07-31); там же заранее решено, что мир от движка
  применяется ко всем одинаково, включая войны игрока.
- **Оккупированная страна продолжает призывать.** `MilitaryTick` набирает
  манпауэр по `ownerCountryId`: у полностью оккупированной ROU 96% той армии,
  что была бы без войны. Раздел «Оккупация ≠ аннексия» выше фиксирует, что
  манпауэр не редиректится ОККУПАНТУ; вопрос «остаётся ли он законному
  владельцу» отдельный и не решён.
- **Морские десанты (высадки)** — контакт ищется только через
  `neighboringRegionIds` (сухопутное соседство). Войны против заморских/
  островных целей без общей границы не находят точку контакта вообще, фронт
  не двигается. Отложено до починки `isCoastalRegion()`
  (`generateMapFeatures.ts`, сейчас всегда `false`) — готовая к внедрению
  механика (навал-превосходство открывает контакт) описана в TODO.md, не
  реализовывать плейсхолдером на `capitalRegionId`.
- **Phase 3** — логистика/окружение (BFS-связность), `War.supporters`
  (прокси от sphereOfInfluence), полноценное ядерное сдерживание в формуле
  порога ИИ (сейчас — только текстовая инструкция LLM, не механика).
- **Позже** — именные передвигаемые армии/флоты с приказами (Option 2 из
  обсуждения дизайна) — рост поверх Phase 1, не начинать без явного запроса.
