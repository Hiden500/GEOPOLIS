Last updated: 2026-08-03 — история изменений и решений: `docs/DECISIONS.md`; штамп — одна строка не длиннее 300 символов.

# Geopolis — План работ

Живой план: **что делать дальше и в каком порядке**. История "почему" и завершённые
решения — в `docs/DECISIONS.md` (здесь не дублируются). Конкретные числа/дизайн —
в профильных доках (`WORLD.md`/`TECH_TREE.md`/`AI_RULES.md`/`LLM_RULES.md`).

Постоянные рамки (не меняются от сессии к сессии):
- **Обе UI-линии заморожены** (решение пользователя 2026-07-23, см.
  `docs/DECISIONS.md`): линия A — HUD плана 12 (срезы 0-3 в `main`; срез 4 —
  онбординг/NationalBriefing — реализован на bootstrap-линии и присутствует в
  этой ветке, в `main` не влит); линия B — clean-slate редизайн
  (`codex/interface-from-scratch`, материалы `docs/UI_LAYOUT_PROPOSAL.md`,
  `docs/UI_VISUALIZATION_PROMPT.md`). Никто не меняет UI-код и не развивает
  ни одну из линий до отдельного решения пользователя, какая каноническая.
  Игровые расчёты/геометрия карты — по-прежнему вне UI-плана.
- **Геометрия/топология/рендер карты** (`client/src/map/**`) не переписывать
  без причины (инкремент вместо переписывания, AGENTS.md) — карта рабочая
  система, не фриз. UI-слой поверх карты (mapmodes, попапы MapLibre, легенда,
  контролы зума) — в скоупе редизайна (`docs/plans/12_UI_REDESIGN.md`).
  Задачи по содержанию карты (план 06, новые фичи) — отдельно, не брать без
  явного запроса.
- **Охват до релиза — только 1946** (независимый гейм-дизайн разбор, 2026-07-06):
  1836/2000 не реализованы (заглушки-сценарии удалены как мёртвый код
  2026-07-26, `docs/DECISIONS.md`) — эпохи вне v1 (`docs/CONCEPT.md` §8), не
  параллельная цель разработки.
- **Работа моделей координируется по task-веткам и scope**, а не через
  дублирующиеся provider-specific ownership rules. Рекомендуемая роль Gemini:
  историческая калибровка/фактчекинг + тексты/контент/визуальные ассеты.

---

# Текущий фокус

Каждая система строится с расчётом на LLM как главный двигатель событий — канонические правила
и приоритет см. `docs/LLM_RULES.md` §Роль LLM в архитектуре.

Планы 01–05 и 08 замкнуты; валютные зоны плана 10 имеют MVP. План 06
(Map Features) не начат, план 07 (1836) вне охвата до релиза. Текущие
product-направления определяет `docs/plans/11_MVP_ROADMAP.md`. UI-план 12: в
`main` реализованы срезы 0-3; код среза 4 (онбординг + NationalBriefing)
живёт на bootstrap-линии (ветки `codex/global-working-agreements` →
`codex/1946-country-borders`, Срез 5 не запускается) и в `main` не влит;
`codex/interface-from-scratch` (линия B) интерфейс, наоборот, сбросила под
clean-slate. Весь UI-трек стоит до решения пользователя о канонической линии
(см. «Постоянные рамки»); ближайший самостоятельный P1-план вне этого трека
— 06 (Map Features).

---

# Открытые вопросы калибровки/дизайна

**Калибровка ВВП/чел бедных стран 1946** — **формульная сторона решена
2026-07-11** (по решению пользователя «углубить фикс формулы»): корень был в
том, что формула рождаемости привязана положительно к богатству/образованию/
welfare (обратно демографическому переходу — бедные аграрные общества 1946
имели ВЫСОКУЮ фертильность + высокую смертность). Бедность тройно подавляла
рождаемость. Фикс: пол уровня жизни в рождаемости
(`STANDARD_OF_LIVING_BIRTH_FLOOR=0.5`) + базы education/welfare подняты к
нейтрали (1.0) — бедность не штрафуется ниже аграрной нормы, траты дают лишь
мягкий бонус, преимущество развитых в приросте идёт через низкую смертность.
`campaignSmoke` зелёный, регрессионный тест в `PopulationTick.test.ts`.
**Осталась сторона данных (домен Gemini):** сами стартовые ВВП/чел 157 стран
(уточнено прямым подсчётом 2026-07-26, было устаревшее «128») не сверялись
с источниками (`docs/SCENARIOS.md`) — историческая калибровка
чисел остаётся кандидатом на проход Gemini, но население больше не убывает.

**Национальные цели ИИ-стран (major, для флейвора LLM) — приостановлено
пользователем 2026-07-06.** **[закрыто редизайном 2026-07-24: разблокировано §5.8 (тир пересобран) + цели ИИ = те же контейнеры целей §5.8/§5.9; реализация — план 11.]** Черновик (только major, read-only, ~10 целей, план
по СССР как иллюстрация формата) остановлен — пользователь усомнился в составе
тира major прежде, чем авторить цели под него. Не возвращаться, пока состав
стран не пересмотрен (следующий пункт). **Отдельно:** цели ИГРОКА
(самопоставленные, оцениваемые движком) реализованы 2026-07-11 —
`docs/OBJECTIVES.md`, это другая задача (не про флейвор ИИ).

**Состав тиров major/regional и пробелы ростера — ждёт ответа пользователя.** **[закрыто редизайном 2026-07-24: пять тиров + разгрузка на оси, CHN/TWN через display/history mapping — CONCEPT §5.8; реализация — план 11.]**
`TierTick.ts::HISTORICAL_TIERS_1946` придуман Claude-сессией 2026-06-30
единолично, без утверждения пользователем по конкретным странам. Конкретные
претензии: `CHN` в списке major, а `TWN` — нет, хотя в датасете `TWN`
(Гоминьдан) держит 34 региона материка и место в Совбезе ООН 1946, а `CHN`
(коммунистические районы) — только 8 регионов; `ARG`/`BRA` — под вопросом как
великие державы 1946 года; Индия (устарело: раньше считалась не отдельной
сущностью под `QCG`, British Colonies — на деле `IND`/"British Raj" уже
реальная страна, 41 регион, см. `docs/tasks/SOUTH_ASIA_1946_HINGE_
POINTS.md`) — под вопросом статус great power (Британская Индия крупнее
многих majors по населению, но без суверенитета до 1947). Та же группа
пробелов остаётся для Индонезии/Индокитая/Палестины — см. "Заблокировано" в
`docs/tasks/HISTORICAL_HINGE_POINTS_1946.md`. Область работы (пересмотр
списка, или ещё добавление недостающих сущностей) — не решена.

---

# Открытые направления (не блокированы, можно брать)

## Реанимация симуляции — по аудиту формул 2026-07-30

Три независимых read-only аудита (политика, экономика, дипломатия+война) нашли один
класс дефекта, повторённый шестью путями: **формулы написаны аккуратно, а их входы
мертвы**. Доказательная база с числами и файлами-строками —
`.agent/audits/formula-audit-2026-07-30.md`; здесь только задачи.

Ни один из этих дефектов не виден в 1276 зелёных тестах, потому что тесты проверяют
формулу на подобранной фикстуре, а не на распределении живого мира.

**P0 — все пять закрыты 2026-07-31** (бюджетная петля, тех-бонусы, шкала
`region.stability`, стартовая политика, пороги соперничества; разбор —
`docs/decisions/2026-08.md`).

- **Guard-тесты на длинную дистанцию — частично закрыто.** Класс «вход
  различает страны» заведён (`liveInputGuards.test.ts`), достижимость порогов
  недовольства закреплена (`thresholdReachability.test.ts`). Остаётся: горизонт
  600 месяцев в наборе (сейчас 50 лет гоняются вручную `probe*.ts`) и верхние
  границы величин — их по-прежнему нет ни у одной.

**P1 — структурные дыры.**
- **Отказ `schemaInvalid` не называет поле** — только глагол и позицию в
  массиве, поэтому модель починить его не может. Найдено 2026-08-02 разбором
  сырых ответов (`.agent/runs/director-prompt-regions-2026-08-02`): на форме
  `target` терялся целый класс ходов. Промтовая часть вылечена, диагностика —
  правка движка.
- **ISO-дата просачивается в прозу:** «В течение 1946-08-01 — августа 1946
  года…». `## Current Date` подаёт обе формы через тире, и модель копирует
  строку целиком. Замеры 2026-08-02: 0/5/0/7 заголовков из 72 — с правками
  промта не связано. Лечится подачей даты, а не ещё одним запретом.
- **Фактические ошибки эпохи на текущей модели НЕ воспроизводятся** (разметка
  `.agent/runs/integration-director-prompt-2026-08-02/p5-factuality-markup.md`:
  28 утверждений, 1 неточность, 1 опережение на два месяца) — правку промта не
  делать без нового замера; запрет на реальные события вне секции подсказок
  сейчас ВРЕДЕН.
- **Нарратив — сплошной текст, хотя события внутри уже датируемые.** Замер
  2026-08-02: модель сама называет «Длинную телеграмму» (февраль 1946), Фултон
  (март), Бикини (июль), закон Макмагона (август), провал Лондонской сессии
  СМИД и отречение короля Румынии (декабрь 1947) — всё датировано верно, но
  лежит абзацами прозы. Событий с датами как СТРУКТУРЫ (по образцу
  `eventSchema` Open-Historia: `date`, `title`, `description`, `impacts`) нет —
  один `Event` на ход. Отдельная задача: меняет схему ответа, `Event`,
  `SAVE_VERSION` и ленту событий.
- **Замеры промта на локальной 9B к текущей модели не применимы.** «34 отказа
  из 49» (Qwen3.5-9B, 2026-08-01) против **0** агентных отказов на
  `gemini-3.6-flash-high` (2026-08-02, 72 хода) — подтверждай дефект на
  текущей модели, иначе чинится слабость снятой.

- **Бюджетный профицит растёт без предела** — ТА ЖЕ ПРИЧИНА, что у мёртвого
  Правила C выше: расходы ИИ не следуют за доходом. Медиана
  `budgetBalance/gdp` идёт +6,0% на первом месяце → +13,3% к 120-му.
  Чинится тем же переводом расходов ИИ на доли, отдельной работы не требует.
- **Фронт не двигается — он обваливается. ЗАКРЫТО ПОЛНОСТЬЮ 2026-07-31**
  (две ветки; разбор и числа — `docs/decisions/2026-08.md`).
- **Мир ветшает сам по себе — НЕ ВОСПРОИЗВОДИТСЯ, перемерено 2026-08-03.**
  Прогон `server/scripts/runCampaign.ts --years 15` на текущем коде дал
  `medianInfrastructure` 0,321 → 0,3555 за 15 лет (180 месяцев) — рост, а не
  убыль. Прежний замер 2026-07-31 (0,321 → 0,207) был снят ДО обратного хода
  Правила A (2026-08-02); промежуточный замер 2026-08-01 (0,321 → 0,353) уже
  показывал перелом тренда, и E4 пометил «перемерить перед решением» —
  подтверждено: после обратного хода аустерити мир на текущей калибровке
  сам по себе не беднеет. Закрыто числом; при следующей правке бюджетных
  правил перемерить заново.
- **Оккупированная страна продолжает призывать.** `MilitaryTick` считает
  манпауэр по `ownerCountryId`, а не по фактическому контролю: ROU с 0 из 6
  регионов имеет 96% той армии, что была бы у неё без войны. `docs/WAR.md`
  фиксирует, что манпауэр осознанно не редиректится ОККУПАНТУ, — но «не отдавать
  оккупанту» и «оставлять законному владельцу» разные решения, второе явно не
  принималось. Продуктовая развилка.
- **Завершения войны нет вовсе.** `makePeace` зовёт только глагол `peace`; ни один
  тик. У обычной страны очередь на внимание LLM — раз в ~30 циклов. Дорогая часть
  (`applyPeaceTerms`, легитимность, репарации) уже написана — не хватает ~30 строк
  условия. Модель и ловушки — `.agent/audits/models-research-2026-07-30.md` §1.
  **Замер 2026-07-31:** после обвала фронта контакта нет, потерь нет — 114 из 120
  месяцев не происходит ничего, а война висит активной с warScore ±100.
  **Отложено решением пользователя 2026-07-31**; попутно решено заранее, что мир
  от движка применяется ко всем одинаково, включая войны игрока
  (`docs/DECISIONS.md`). Вопрос к реализации: что именно завершает войну —
  капитуляция (доля удержанных регионов), распад контакта, потолок длительности
  или полная торговая модель аудита.
- **У влияния нет механизма ПОДДЕРЖАНИЯ — только рост через один гейт и
  безусловное затухание.** Вскрыто 2026-07-31 при закрытии «влияние без
  насыщения» (`server/scripts/probeInfluence.ts`): вверх влияние двигают лишь Правило B
  (`AiBehaviorTick`, только для страны игрока и только там, где доминирование
  выше `THREAT_LEVEL`) и мягкие глаголы (`send_aid`, `support_proxy`); вниз —
  `min(0.5, influence × 0.02)` каждый тик у всех. Затухание с капом линейно, то
  есть доводит связь до нуля за конечное число месяцев, а не асимптотически.
  Следствие на живых данных: авторский слой `influence.json` (300 связей, сумма
  13 896, 107 связей выше порога сферы) растворяется за 10 лет у ВСЕХ 46
  ИИ-источников, а теперь и у игрока вне гейта — среднее влияние игрока за SUN
  падает 68 → 22, сфера 13 → 11. Прежняя формула это скрывала: она прижимала
  влияние игрока к сотне, и убыль была не видна. Нужно продуктовое решение,
  а не калибровка: что именно поддерживает присутствие — деньги (содержание
  сферы статьёй бюджета), формальная связь, торговля? До него дипломатический
  слой мира к концу партии беднеет.
- **`Country.aiTraits` осиротели** (`aggressiveness`, `riskTolerance`). Читало их
  только удалённое Правило D; значения сеются и продвигают `rngState`, но ни на
  что не влияют. Решить: найти нового потребителя (разные характеры ИИ в
  экономических правилах A/C — дешёвое применение) или удалить вместе с ростом
  `SAVE_VERSION`. Удаление сдвинет всю последующую случайность партии — это не
  косметика.
- **Петля «инвестиции → развитие → рост» — ИНФРАСТРУКТУРА ЗАМКНУТА 2026-07-31**
  (`EconomyTick.updateInfrastructure`, модель капитала с амортизацией). Остаётся:
  - `development` и `urbanization` по-прежнему не пишет никто. Это отдельные
    петли со своей семантикой (от чего растёт развитость — от образования? от
    урбанизации? от накопленной инфраструктуры?), и они требуют продуктового
    решения, а не калибровки;
  - **перевес потока над запасом.** `INFRASTRUCTURE_SPENDING_GROWTH_COEFFICIENT`
    (стройка) против `BASE_GROWTH_INFRASTRUCTURE_COEFFICIENT` (построенное):
    процесс в ~двадцать раз важнее результата (при медианных значениях до
    калибровки 2026-08-03 — 0,0060 против 0,0003/мес; калибровка уменьшила оба
    одним множителем ≈0,43, ПРОПОРЦИЯ сохранена намеренно). Из-за этого
    наследие пятилетней стройки — всего 3,3% ВВП за следующие пять лет при
    вдвое с половиной большей инфраструктуре. Перенос веса пробовался и
    откачен: сумма сохранялась для медианы, а мировой ВВП за 120 месяцев вырос
    1,393 → 1,974 трлн (на старых коэффициентах). Калибровать надо по
    распределению, отдельной задачей; при переносе перекалибровать коридор
    `growthCorridor.test.ts`. Числа — у самих коэффициентов.
- **Рост ВВП односторонний** (`Math.max(0, …)`): рецессии нет, единственный путь
  вниз — примитив бегства капитала. **Пробовано и снято 2026-07-31, причина
  измерена:** пол −1,25%/мес не «включает рецессию», а запускает спираль —
  падение ВВП режет налоговую базу, дефицит становится долгом, долг даёт
  `debtPenalty`, тот углубляет падение, и так без предела, потому что штраф не
  ограничен сверху, а дефолта в игре нет. Одна страна за 120 месяцев: с полом
  −1,25% долг/ВВП 18,8 и ВВП ×0,238, с полом 0 — долг 0,00 и ВВП ×1,500; по миру
  спираль забирала 22 страны из 157 в коллапс ×0,236 (все minor, долг 11–32×
  ВВП). **Условие включения:** сначала ограничитель долговой петли — кап
  `debtPenalty` или механизм дефолта (`default` из плана 08 так и не сделан).
  После него рецессия — одна строка в `computeGrowthRate`.

**P2 — искажения, видимые в партии** (детали и числа в аудите). Закрыты
2026-07-31/08-01/08-02: равновесие стабильности, влияние без насыщения, счёт
тира, `regionWelfare`, эмбарго, `build_extraction`, достижимость порогов,
гейт сырья (вариант А). Остаются: недовольство одномерно (дефект данных
каталога, см. ниже); союз между воюющими достижим.

**Закрыто 2026-08-01** (эмбарго больше не уничтожает товар, `build_extraction`
на потолке; разбор — `docs/decisions/2026-08.md`). Открытый остаток: **все
2055 пар (регион, ресурс) с депозитом стоят ровно на `MAX_EXTRACTION_LEVEL`**
— оживление требует решения о данных или о потолке (`docs/IDEAS.md` §12).

**Гейт сырья закрыт 2026-08-02** (вариант А, решение пользователя; числа —
`docs/DECISIONS.md` 2026-08-02). Вариант Б — после v1 (`docs/IDEAS.md` §11);
импорт эмбарго по-прежнему не гейтится — блокада бьёт по производству через
доход, не через физический голод склада.

**Нормировщик дистанции и пороги — измерено 2026-08-01, правка не требуется**
(разбор — `docs/decisions/2026-08.md`): нормировка математически верна, сжатие
шкалы — следствие данных (`desiredIdeology.political > 0` у 93,7% групп), все
пороги достижимы; достижимость закреплена `thresholdReachability.test.ts`.
Настоящая задача — одномерность каталога групп (`groups.json`), записана
отдельно.

**Замечено попутно (product, не дефект):** половина мира живёт ниже
`STABILITY_LOW` постоянно (75 / 81 / 68 / 48 стран на 0 / 12 / 60 / 120-м
месяце), то есть Правило C для них — не реакция на кризис, а фон. Это вопрос
калибровки порога или равновесия стабильности, решение продуктовое.

**ОСТАЁТСЯ ОТКРЫТЫМ: импорт не входит в бюджетную роспись.** Пять статей 84,9%
дохода + прочие расходы 7,9% + импорт 17,5% = 110,9%, поэтому на первом году в
долгу 68% мира (было 31%, когда суммы были заморожены). Яма переходная — 6% к
60-му месяцу (было 3% до обратного хода аустерити 2026-08-02: восстановленные
расходы гасят долг медленнее), 0% к 120-му, стран с долгом свыше 60% ВВП к пятому году нет, — но
структурный разрыв реален: либо импорт учитывается в росписи, либо стартовые
доли нормируются под него. Развилка по объёму, решение за пользователем.

**Мёртвый код и docs drift, найденные попутно:** `developSectors`/`investInSector`
названы в `CONCEPT.md:228`/`11_MVP_ROADMAP.md:42,133` «мёртвым кодом» — их
**никогда не существовало** (`git log -S` пуст); `DIPLOMACY.md` называет сферу
влияния «по-прежнему ярлыком», хотя она входит в расчёт зависимости;
`calculateDiplomaticTension` и три вида санкций из четырёх не имеют потребителя;
`aggregateCountryFromRegions` не присваивает результат никуда.

## Милстоун 0 — хвосты (сессия A + сессия B, план 13)

Закрытые доводки (idempotency-ключ, капы примитивов в состоянии хода,
диагностика отказов, `Event.factuality`, структурный код отказа) — история в
`docs/decisions/2026-08.md`. Ниже — то, что осталось.

- **Старый канал `actions` структурных кодов отказа не получил — но канал
  ужался.** Причины примитивов с 2026-07-27 — код + параметры
  (`server/src/primitives/rejections.ts`), игрок видит русский текст без величин
  несостоявшегося действия. У `actions` причина по-прежнему английская строка
  `LLMResponseValidator`. Сессия дипломатии (2026-07-27) забрала оттуда
  `diplomacy`/`war`/`peace`/`sanction` вместе с их правилами, поэтому английские
  строки остались у пяти действий: `guarantee`, `influence`, `research_shift`,
  `production_shift`, `build_extraction`. Чинится их миграцией, а не отдельным
  кодированием legacy-канала.
- **Числа последствий в нарративе модели.** Модель пишет текст ДО применения,
  поэтому ей запрещено называть величины (инструкция промта), а фактические
  числа даёт движок отдельным откликом и следующим промтом. `PRIMITIVES.md` §4
  описывает более сильную схему — «после commit движок возвращает LLM
  фактические величины, и она вплетает их в нарратив», то есть второй вызов
  модели в том же ходу. Не реализовано: бюджет COST (`CONCEPT.md` §7). Проверить
  на живых прогонах, достаточно ли отложенного варианта.
- **Хроника (`game.chronicle`) по-прежнему идёт в промт целиком, без капа.**
  Сессия B добавила две секции промта (кризисы — с капом, отказы — одноразовые),
  но рекурсивную суммаризацию летописи не трогала. Замер 2026-08-02 на живых
  прогонах: `promptTokens` медиана 8,6–8,8 тыс. при бюджете ~20k — запас есть,
  растёт хроника. **Ловушка для следующего замера:** `logs/token-usage.jsonl`
  пишет только автоматический цикл через сервер и на 2026-08-02 пуст — вся
  живая работа шла прогонами `runCampaignWithLLM.ts`, которые с 2026-08-02
  пишут расход в СВОЙ журнал (`llm.jsonl`, поле `usage`).
- **Значения `MAX_PROMPT_CRISES = 5` и `PLACE_HISTORY_MAX_ENTRIES = 12` —
  плейсхолдеры** той же калибровки, что коридоры магнитуды.
- **Визуальный QA панели приказов и разделов кризиса не проводился.** Причина
  инструментальная: preview-тулинг поднимает dev-сервер из ОСНОВНОГО checkout
  (`.claude/launch.json` читается оттуда, `--prefix client` резолвится
  относительно него), поэтому в браузере открывается `main` без этой ветки, а
  редактировать основной checkout и поднимать dev-сервер руками нельзя.
  Проверено статически: typecheck, 15 компонентных тестов, `npm run build`,
  lint без новых ошибок. Чтобы посмотреть живьём после влития в `main`:
  `npm run dev --prefix server` и `npm run dev --prefix client`. Что смотреть:
  переполнение `OrdersBox` длинными RU-строками отклика, узкий viewport,
  focus-порядок кнопок подтверждения.
- **Калибровка коридоров магнитуды** (`shared/src/defines/discontent.ts`,
  секция «магнитуды эффектов») — все `MIN`/`MAX` и веса факторов состояния
  выбраны так, чтобы ветки ответов расходились на тестовой фикстуре, и не
  проверялись живым прогоном. После сессии B — калибровать на реальных
  партиях, а не на синтетике. **Не путать со структурной правкой 2026-07-26**
  (`DECISIONS.md`, «Поправка 2»): она вводила легитимность вторым входом в
  каналы отчуждения и уступки, чтобы схлопывание коридора стало достижимо, а
  `stateFactor` перестал быть константой на титульных доминантах. Балансовая
  калибровка от этого не выполнена: на сидовых данных легитимность у всех трёх
  размеченных стран равна 50, поэтому наблюдаемый разброс `stateFactor` по
  регионам по-прежнему даёт только доля группы (`alienation` 0.31…0.49,
  `concession` 0.30…0.49) — второй вход заработает на данных, где легитимность
  различается.
- **Значение капа «один verb на цель за ход» — плейсхолдер.**
  `MAX_PRIMITIVES_PER_TARGET_PER_TURN = 1` (реализация — 2026-07-26). Сам кап
  требуется `PRIMITIVES.md` §4, а вот число — предмет той же калибровки на
  живых прогонах: возможно, крупный ход должен допускать два удара по одной
  цели. Открытый вопрос §«Точные капы» в `PRIMITIVES.md` остаётся открытым.
- **Сейв занимает 1,75 МБ ещё до накопления истории (замер 2026-07-30).**
  Критерий приёмки v1 требует SAVE < ~3–5 МБ, и половина бюджета израсходована
  на третьем ходу партии. Разбор: `regions` 995 КБ (54%), `countries` 544 КБ
  (30%), `mapFeatures` 190 КБ (10%), `ethnicGroups` 62 КБ, `pendingWorldFacts`
  40 КБ. Внутри регионов геометрия занимает всего 5,6 КБ — то есть подозрение
  на неё снято, вес дают сами игровые поля (демография 209 КБ, имена 69 КБ,
  остальное — экономика и инфраструктура). Следствие: на всю историю партии
  остаётся 1,25–3,25 МБ, а `eventHistory` и хроника капов не имеют (пункты
  ниже). Мерить рост на длинной партии и решать про кап — до Милстоуна 3, а не
  после.
- **Легитимность усиливает репрессию и удешевляет её — доминирование по обеим
  осям снято 2026-07-31** введением третьей оси, цены акта
  (`REPRESS_LEGITIMACY_COST_*`): легитимный режим давит сильнее и мягче, но
  каждый разгон тратит то, что даёт ему оба преимущества (числа —
  `docs/decisions/2026-08.md`). **Калибровка второй оси открыта:** мандат
  по-прежнему опускает отчуждение до самого ПОЛА коридора
  (`REPRESS_ALIENATION_LEGITIMACY_RELIEF = 1`) — полностью легитимный режим не
  добавляет отчуждения вовсе. Решить: оставить как точку схлопывания канала
  или поднять пол.
- **Чтения фактических дельт вида `?? 0` не отличают «ноль» от «забыли»
  (сузилось 2026-07-26, но не закрыто).** Тип `applied?: TApplied`
  необязательный, поэтому команда, которая в будущем забудет его заполнить,
  отчитается нулём молча — а по этому числу пишется нарратив
  (`PRIMITIVES.md` §4, «цифры правдивые»). Каналы ПАМЯТИ воздействий с тех пор
  прикрыты: `impactEffects` не выдумывает запись для незаполненного поля, и
  рантайм-сверка отчёта с фактическим дифом отклонит примитив, чей эффект в
  результат не попал. Остались два места без такой страховки:
  `enact_reform` читает `shift.applied?.[axis] ?? 0` (диф координат идеологии
  не сверяется), а отклик соседей — `grants[i]?.applied?.concession ?? 0`
  (забытое `applied` даст нулевой отклик, а не отказ). Кандидаты прежние:
  сделать `applied` обязательным в `CommandResult<TApplied>` при
  `success: true` (discriminated union) либо валить примитив на отсутствующем
  `applied`.
- **Стартовая дипломатия 1946 пуста — фильтр союзника в `border_dispute` на
  единственном поставляемом сценарии не срабатывает никогда.** Из **157** стран
  сценария ноль имеют непустой `diplomacy.allies`/`relations` на старте;
  заполнены только `puppets`/`sphereOfInfluence` (13 стран), а их `alliedWith`
  не читает. Следствие: пограничный кризис СССР — Польша в январе 1946 проходит
  без реальной проверки союзников. Код корректен — недостаёт данных.
  **Инфраструктура готова с 2026-07-30:** формат слоя `diplomacy.json`
  (`docs/DIPLOMACY.md`), валидатор с 11 фактами января 1946, Zod-схема и
  `applyDiplomacyLayer` (`server/src/scenarios/`), покрыты тестами. Осталось
  само наполнение (домен Gemini) — после него слой заработает без правок кода.
- **Демо-срез 1946 наполнен внешне (2026-07-26); остаточные шаткие места —
  не блокеры, но и не проверенные числа.** Прежний seed заменён внешним
  историческим наполнением: 9 групп, 14 регионов. Пообъектный провенанс —
  `docs/provenance/DEMOGRAPHICS_1946_PROVENANCE.md`. Координаты идеологии с 2026-07-27
  покрывают все 157 стран (`docs/provenance/IDEOLOGY_1946_PROVENANCE.md`), 8 строк из
  них — низкой уверенности. Что сам источник признаёт слабым и что стоит
  уточнить, когда дойдут руки:
  **Калининград** (доли сильно зависят от конкретного месяца 1946 — принят
  срез второй половины года, 65 % немцев), **Рига** (нет надёжного среза
  именно конца 1946), **литовские регионы** (игровые границы не совпадают с
  уездами переписей 1923), **Гродно** (польская доля 34 % зависит от границ
  полигона и темпа репатриации 1944-47), **желаемые идеологии русских,
  белорусов, украинцев, евреев и немцев** (игровые агрегаты, а не результат
  опросов), **экономическая координата Китая** (разумный диапазон −0.10…0.15).
  Правки вносить в таблицы `scripts/map/generate_demographics_1946.py`
  (демография) и `scripts/map/config/ideology_1946.json` (координаты), затем
  перегенерировать — JSON-файлы это выход, а не источник. Тесты движка на
  конкретные доли и номера регионов не опираются, поэтому уточнение любой
  строки — правка данных, а не кода.
- **Каталог именованных зон беден, и после раздачи координат это стало
  видно (2026-07-27).** Пять ярлыков на 157 стран, из них три
  (`Traditionalism`/`Nationalism`/`Authoritarianism`) описывают разные
  источники легитимности внутри одной области спектра и двумя осями честно
  не разделяются; центра спектра в каталоге нет вовсе, «фашизма» из
  `docs/CONCEPT.md` §4.2 — тоже. Следствия:
  - **Экономическая шкала распределена неравномерно.** 87 стран из 157 стоят
    в ступени «Регулируемый рынок» (0.15…0.40), а в «Свободный рынок» — одна.
    Дробление осей это не лечит: колониям массово розданы координаты 0.2–0.35,
    то есть перекос в самих данных, а не в разбиении. Лечится уточнением
    координат зависимых территорий, не границами ступеней.
  - **`alliedWith` в `border_dispute` сравнивает отношения с ПЛОСКИМ порогом
    70, которого больше нет.** `server/src/primitives/PrimitiveEngine.ts`
    читает «отношения не хуже союзнических» как `relations >= 70`, а с
    2026-07-28 союзнический уровень свой у каждой пары
    (`allianceThreshold`): для родственных режимов он 60, для антиподов 80.
    Расхождение маленькое и одностороннее (фильтр строже, чем нужно, для
    родственных и мягче для антиподов), но это предпосылка примитива, а не
    дипломатический тик, — правка меняет семантику чужой области и вынесена
    отдельным пунктом.
  - **Поле `Country.politics.ideology` осталось, и удалить его можно одним
    шагом.** Игроку не показывается (ярлык вычисляется), дипломатия его не
    читает (гейт снят 2026-07-28), `PoliticsTick` — тоже (базы переведены
    2026-07-30). Содержательный читатель остался ровно один:
    `resolveIdeologyCoordinates` берёт по ярлыку фолбэк координат для стран
    без них — а в сценарии 1946 таких стран ноль. Удаление трогает
    `Scenario1946.ts`/`scenario1946Schemas.ts` и потому вынесено отдельным
    шагом: на 2026-07-30 они были заняты параллельной задачей.

## Перечень статусов суверенитета не знает автономии

Аландские острова размечены как `protectorate` Финляндии — исторически это
автономная демилитаризованная территория под финским суверенитетом, а не
протекторат. Замечено внешним рецензентом при разметке влияния (2026-07-29).

Перечень `SOVEREIGNTY_STATUSES` подходящего значения не содержит: `sovereign`
неверен (Аланды не субъект международного права), `protectorate` неточен.
Лечится добавлением `autonomous_territory` в перечень — но значение без
потребителя в механике заводить рано, поэтому пока оставлено как есть с этой
записью. Влияние `FIN → ALA = 95` от статуса не зависит и верно в любом случае.

## Недовольство не отличает «чуждое население» от «готового воевать»

Модель считает недовольство как дистанцию между позицией власти и желаемой
позицией группы. Дистанция идеологий — не то же самое, что организация, оружие
и внешняя поддержка.

**Частично снято полной разметкой (2026-07-29).** Пока мир был размечен на
треть, пиком был Южный Сахалин в одиночку — Индокитай и Индонезия, где реально
шли бои, разметки не имели вовсе. Теперь в топе Голландская Ост-Индия, Папуа и
Вьетнам рядом с Сахалином, то есть модель указывает примерно туда, куда надо.
Остаётся исходная претензия: недовольное население и население, готовое
воевать, по-прежнему одно и то же число.

**Побочное свойство данных, которое стоит учесть при калибровке:** у всех 190
групп последнего захода `political` положителен, тогда как у прежних 270
десятая часть отрицательна. Группы, поддерживавшие авторитарный порядок, в
наборе не представлены — недовольство авторитарной властью систематически
завышено относительно демократической.

Возможное лечение (не начато): восстание требует не только порога, но и
второго условия — например, региона в кризисе несколько ходов подряд. Тогда
высокое недовольство перестаёт означать мгновенную готовность к восстанию.
Это новая механика, а не калибровка; до неё горячие точки ограничены долей
(решение 2026-07-27, `docs/DECISIONS.md`).

**Межстрановая одномерность локализована в ДАННЫХ (замер 2026-08-01, не
начато).** Экономический вход недовольства сделан двусторонним, и корреляция ПО
РЕГИОНАМ упала −0,742 → −0,693, но корреляция ПО СТРАНАМ (R = −0,89) не
сдвинулась вовсе — и не могла: относительное благосостояние сравнивает регион с
его же страной, поэтому усредняется по ней в ноль. Причина замерена на текущем
каталоге: `desiredIdeology.political > 0` у **431 группы из 460 (93,7 %)**,
разброс политической оси [−0,55; +0,75], среднее по экономической −0,009 при
разбросе [−0,70; +0,55]. Двумерная геометрия одномерна на входе.

Формулой это не лечится, и это ПРОВЕРЕНО, а не предположено. Свип кандидатов во
второй вход (`server/scripts/probeDiscontentHealth.ts`):
- `politics.legitimacy` — R с политической осью +0,52; добавление её членом с
  весом 0,10 корреляцию не снижает, а ПОДНИМАЕТ (−0,888 → −0,907), потому что
  вход частично коллинеарен оси. Вес 0,40 её опускает (−0,858), но кризисных
  регионов становится 328 из 1399 вместо 201 — порог кризиса обесценивается;
- `politics.corruption` — R = −0,77, переодетая та же ось;
- `politics.stability` / `governmentSupport` — на момент свипа были константами
  у всех стран (оба дефекта закрыты: стабильность 2026-08-01, рейтинг
  2026-08-03); кандидатами во вход недовольства всё равно не годятся —
  `governmentSupport` теперь сам ЧИТАЕТ недовольство, обратное ребро дало бы
  петлю;
- `region.stability` — разброс есть (0,184…0,809), но поле статично (писателя в
  симуляции нет) и семантически дублирует само недовольство.

Лечение — в слое данных: `server/data/scenarios/1946/groups.json` и
`scripts/map/generate_demographics_1946.py`. Нужны группы, поддерживающие
авторитарный порядок, и непустые экономические желания. Отмечено и в разборе
выше: у прежних 270 групп десятая часть `political` была отрицательна, у
нынешних 460 — 6,3 %.

**Хвосты правки экономического входа (2026-08-01, не начато).**
- `polityLifecycle.separatistGroupOf` берёт ориентир благосостояния с
  ФАКТИЧЕСКОГО контролёра (`regionAuthority`), тогда как `regionGroupDiscontent`
  перешёл на легального владельца. Расхождение проявляется только на
  оккупированном регионе раскалывающейся страны, но оно есть: две функции
  считают одно и то же разными знаменателями. Не правилось здесь — `server/src/primitives/**`
  чужой scope в задаче правки; правится одной строкой (`regionWelfareReference`).
- У 41 страны из одного региона экономический вход мёртв ПО ПОСТРОЕНИЮ: регион
  сравнивается сам с собой, паритет тождественен. Это верно для меры
  «относительно своей страны», а не дефект её реализации; чтобы экономика
  влияла и там, нужен второй ориентир (мир/соседи/абсолютный уровень) — то есть
  другая мера, а не другая константа. Оценивать после v1.
- **Координация:** правка нормировщика `IDEOLOGY_MAX_DISTANCE` (тот же список
  P2, чужая ветка) увеличит дистанции примерно в 1,46 раза и поднимет число
  регионов над каждым порогом. Пороги здесь СОЗНАТЕЛЬНО не двигались, чтобы не
  считать этот эффект дважды; верхние границы guard-тестов
  (`discontentLiveGuards.test.ts`) сработают на объединённой базе и покажут,
  какие пороги пора поднимать.

## Подтверждение необратимого от самого игрока (решение пользователя)

`CONCEPT.md` §7.2 требует двух вещей, и вторая закрыта лишь наполовину:
«LLM за страну игрока — всё предложением» ✅ (граница агентности покрывает весь
алфавит с 2026-07-29) и «необратимое — подтверждение **даже от игрока**» —
частично.

**Что подтверждение уже есть.** Свободный приказ текстом идёт путём
`POST /primitives/translate` → предпросмотр (`buildPrimitivePreview`) →
отдельный `POST /primitives/apply`. Игрок видит, что распознано и что
изменится, и применяет вторым действием. Распад собственной страны требует
выбора осколка (`succession_choice_pending`) — это подтверждение необратимого в
чистом виде.

**Чего нет.** Быстрые кнопки панели приказов
(`PrimitiveOrdersPanel.quickOrder`) вызывают `apply` напрямую с `preview: []` —
предпросмотра нет вовсе. И ни один путь не различает обратимое и необратимое:
`war` применяется тем же одним кликом, что `condemn`, хотя §7.2 называет войну
среди необратимого прямо.

**Вопрос продуктовый, не инженерный.** Механически различить легко —
`STRUCTURAL_VERBS` уже перечисляет структурные, а `war`/`peace` в нём есть.
Решать нужно другое: нужен ли отдельный шаг подтверждения для необратимых
действий самого игрока, или предпросмотр свободного приказа этого достаточно, а
быстрая кнопка — сознательное удобство за счёт защиты. Второе защитимо: игрок
жмёт её сам, и «вы уверены?» на каждый ход превращается в шум.

Обе UI-линии заморожены, поэтому до решения пользователя ничего не делается.

## Милстоун 1 — хвосты контрактов ядра (независимое ревью 2026-07-27)

Ревью сессии «Контракты ядра» (read-only) нашло шесть дефектов/расхождений —
все закрыты той же сессией (детали `docs/decisions/2026-08.md`). Ниже — то,
что решено НЕ чинить сейчас.

- **Кап диагностической записи обходится ИМЕНАМИ ключей.** `MAX_PRIMITIVE_ID_LENGTH`
  ограничивает длину ЗНАЧЕНИЯ трёх известных идентификаторов, но `.strict()` в
  схеме глагола кладёт в причину отказа имя нераспознанного ключа целиком, а
  имена ключей в теле запроса не ограничены ничем. Оценка ревью: одиннадцать
  примитивов с пятитысячесимвольным ключом дают секцию отказов ~67 000 символов
  против бюджета `CONCEPT.md` §7 (~20k токенов). Предсуществующее, не регрессия
  Милстоуна 1. Лечение — кап на длину САМОЙ записи в `pushRejectionFact`.
- **Удаление записи словаря, все числовые поля которой нули, диф не видит.**
  `leavesEqual` считает отсутствие листа и нулевой лист одним и тем же (это
  осознанно — так палитра остаётся точной при создании записи памяти), но у
  СЛОВАРЕЙ то же правило делает удаление нулевой записи невидимым. Переход на
  сопоставление по id (2026-07-27) массивы вылечил, словарей не касается: у их
  записей идентичность — сам ключ, и «ключа не стало» отличимо от «значение
  ноль» только на уровне листа. Сегодня недостижимо ни одним глаголом.
- **«Память страны» выдаёт сырой заголовок независимо от подтверждённости.**
  `LLMService.getRecentTitlesLine` и `## Recent Events` берут `Event.title`
  дословно, включая `partial`, — в отличие от летописи. Это НАЗВАННАЯ граница
  решения 2026-07-27 (окно короткое и самоочищается, летопись копится всю
  партию), но записана она пока только в `PRIMITIVES.md` §3, без теста. Решать
  вместе со вторым вызовом модели «перепиши по фактам».
- **Интерфейс не читает признак дубля из квитанции.** `receipt.duplicate = true`
  клиент показывает как «режиссёр предложил невозможное» (повтор попадает в ту же
  ветку «ничего не применилось»), хотя это ретрай уже применённого ответа, а не
  отказ. Правка клиентская: отдельная ветка в панели цикла по `duplicate`.

## Хвосты сессии «Жизненный цикл государств» (Милстоун 1, 2026-07-27)

- **Осколок носит имя ГРУППЫ, а не государства.** `split_country` даёт новой
  стране `LocalizedText` этнической группы («Литовцы»), потому что имён
  гипотетических государств в состоянии нет вовсе. Игроку это читается как
  «страна Литовцы». Лечение — словарь «группа → имя государства» в данных
  сценария либо отдельное поле `EthnicGroupDefinition.statehoodNames`;
  и то и другое — наполнение данных, не механика.
- **Именные соединения и ядерный арсенал при расколе не делятся.**
  `military.units` и `nuclearWarheads` остаются у метрополии (либо у
  правопреемника): штучные объекты не являются величиной, делимой по населению.
  Запасы ресурсов (`stockpile`) копируются осколку целиком — то есть при расколе
  РАСТУТ в сумме по миру. Сегодня безобидно (запасы малы и быстро
  пересчитываются торговым тиком), но это нарушение того же принципа, что держат
  казна и живая сила. Решать вместе с `merge_countries`, где встанет обратная
  задача — сложение.
- **Столица и объекты карты — отдельный реестр ссылок.** `countryRefs.ts`
  объявляет ссылки на СТРАНУ; ссылки на РЕГИОН (`capitalRegionId`,
  `MapFeature.regionId`, `groupImpactMemory.regionId`, `Modifier.target` вида
  region) живут по местам и проверяются `invariants.ts` поимённо. Пока регионы
  не создаются и не удаляются, этого достаточно; глагол, меняющий состав
  регионов, потребует второго такого же обхода.
- **Составной ключ бюджета хода держится соглашением о формате строки.**
  `primitiveTurnBudget.targetUses` ключуется `"<verb> -> country <id>"`, и
  `countryRefs.ts` разбирает его сегментами. Это единственное место реестра, где
  связь не выражена типом: изменение формата ключа в `targetUseKey` молча
  сломает перенос. Лечение — структурированный ключ в `PrimitiveTurnBudget`
  (формат сейва, поэтому вместе со следующим `SAVE_VERSION`).
- **Правило «раскол не заявляет числовых ячеек» — исключение в сторожевом
  тесте.** Сторож «палитра и сверка описывают одни и те же каналы» получил
  исключение, ключуемое ПАРОЙ (глагол, путь), и `split_country` исключён
  целиком. Это осознанно: его правдивость держат сходимость сумм и ноль висячих
  ссылок — механизмы сильнее поячеечной сверки. Но каждый следующий структурный
  глагол придётся так же решать вручную; если их станет много, правило стоит
  выразить свойством класса (структурные не заявляют ячеек), а не списком.
- **След «здесь сменился флаг» не пишется в историю места ни одним глаголом.**
  `peace` с аннексией по договору, `annex` и `merge_countries` двигают границы, но
  записи в `regions[*].placeHistory` не оставляют: строка отклика у всех троих
  называет страны, а не место, и записи потребовалась бы своя. Решать одной
  работой на все три — иначе у одинаковых случаев окажутся разные ответы.
- **`create_country` не требует смежности отделяемых регионов.** Уходят все
  регионы владельца с той же группой-большинством, независимо от географии — то
  же правило, что у раскола. Связность территории в состоянии не выражена ничем,
  кроме списка соседей; требовать её значило бы изобрести правило, которого у
  раскола нет. Решать вместе с механикой, которая первой прочитает связность.
- **`puppet` не снимается.** Обратного глагола («освободить клиента», разрыв
  вассалитета) в алфавите нет: спецификация §2 его не заявляет, а выводить
  отдельный глагол под снятие одной связи — расширение алфавита числом, которое
  §1 прямо не рекомендует. Кандидат — параметр направления у существующего
  глагола, вместе с механикой деколонизации.
- **Порог `VASSALAGE_MIN_HELD_SHARE` (0.5) — решение, а не вывод из данных.**
  Оккупированных регионов в поставляемом сценарии ноль из 1399, поэтому
  распределения, из которого его можно было бы вывести, не существует.
  Перепроверить на первой партии с настоящей войной.

- **Клиентский минимум состояния кампании.** `CampaignStatePanel` — простая
  разметка без визуального дизайна: выбор осколка списком кнопок и текст
  поражения. Дизайн ведёт пользователь отдельно.

## Хвосты сессии «Формы правления» (Милстоун 1, 2026-07-29)

Введены `politics.powerStructure` и `politics.sovereigntyStatus`/`overlordIds`
на 157 стран 1946 (`docs/POLITICS.md`, `docs/provenance/GOVERNMENT_1946_PROVENANCE.md`).
Ниже — то, что осталось.

- **У `powerStructure` появился первый расчётный читатель (2026-07-30):**
  `PoliticsTick` берёт по нему базис коррупции и поправку легитимности на
  происхождение власти (`docs/POLITICS.md`). У `sovereigntyStatus` расчётного
  читателя по-прежнему нет — он заведён под деколонизацию, механики которой
  ещё нет; это осознанная отсрочка, а не забытый провод. Перевороты
  (`powerStructure` определяет, кто способен захватить власть и во что режим
  превращается) тоже ждут своей механики.
- **`overlordIds` в статусе `disputed_administration` двузначен.** Для
  советских клиентов (`QAZ`, `QMH`) названная держава — патрон, для ДРВ и
  Республики Индонезия — претендент, против которого они восстали. Различить по
  паре (статус, форма власти) нельзя: `QMH` и `QDV` совпадают. Либо отдельный
  статус для непризнанной власти, либо явный признак «претензия vs контроль» —
  решать вместе с механикой, которая первой это прочитает.
- **Показ у зависимых стран занимает до четырёх строк на узкой панели**
  (самая длинная — 82 символа, высота подзаголовка растёт с 72 до 144 px на
  360/280/220; переполнения и обрезки нет, но вёрстка тяжёлая). Обе UI-линии
  заморожены («Постоянные рамки»), поэтому правка — в общий UI-трек.
- **`leased_territory` — статус без экземпляра в 1946.** В перечне движка есть,
  в данных не встречается (Гонконг размечен иначе). Либо появится с уточнением
  разметки, либо статус лишний — проверить при следующем наполнении.

## Хвосты сессии «Дипломатический блок алфавита» (Милстоун 1, 2026-07-27)

Реализованы `diplomacy`/`sanction`/`war`/`peace`, переведённые типы удалены из
старого канала, у `influence` снято числовое поле. Ниже — то, что осталось.

- **Три вида санкций из четырёх — репутационные.** Торговлю режет только
  `trade_embargo` (`TradeTick.getEmbargoPenalty`). `economic_sanctions`,
  `military_sanctions`, `diplomatic_sanctions` меняют отношения и лежат в
  состоянии, но их не читает ни одна подсистема. Примитив это НЕ скрывает
  (`AppliedSanction.cutsTrade`), поэтому нарратив не врёт, — но механики у них
  нет. Чем они должны отличаться, решается в `TRADE.md` и военной модели, а не
  в алфавите.
- **`guarantee` остаётся в старом канале** — единственное оставшееся там
  двустороннее действие. Он не несёт от модели чисел и правило алфавита не
  нарушает; уходит позже, вместе с семейством `form_bloc`/`join_bloc`.
  (`influence` из старого канала УДАЛЁН 2026-07-29 вместе с `send_aid` — см.
  «Хвосты сессии „Мягкие глаголы алфавита“».)
- **Повод войны (casus belli) не проверяется.** Претензий, спорных территорий
  и обид в состоянии не существует; отношения — единственный кандидат — в
  сценарии 1946 нулевые у всех 157 стран, поэтому порог по ним был бы либо
  пустым, либо запирал бы войну во всём мире. Закрывается наполнением стартовой
  дипломатии 1946 (тот же пункт, что держит фильтр союзника в `border_dispute`),
  а не правкой движка.
- **Мир не оставляет записи в истории места.** Аннексия по договору меняет
  владельца региона, и «здесь сменился флаг по мирному договору» истории места
  было бы к лицу, но записи потребовалась бы своя строка отклика (заголовок
  `peace` называет страны, а не регион). Отложено осознанно.
- **Отчёт `peace` строится из дифа состояния**, поэтому сверка результата на
  ячейках `legitimacy:`/`treasury:`/`relation:` этого глагола становится
  тождеством и лжи поймать не может. Граница там — палитра. Альтернатива —
  перечислять эффекты мира по памяти — хуже: это второе мнение о том, что
  сделал `WarService`, и оно разойдётся с ним при первой правке условий мира.
- **Общий слот пары запрещает два мягких дипломатических акта по одной паре за
  месяц.** Это защита от обхода коридора сменой глагола, и цена у неё есть:
  «объявить санкции и одновременно предложить переговоры» в один месяц по одной
  паре невозможно. Смягчается калибровкой
  (`MAX_PRIMITIVES_PER_TARGET_PER_TURN`), а не отменой слота.
- **Потолок накопления по величине покрывает только память воздействий.**
  `IMPACT_FIELD_TURN_CEILING` считает дельты троек (регион, группа, поле);
  у каналов `relation:`/`legitimacy:`/`treasury:` такого потолка нет. Сегодня
  их держит слот пары (спилловера в чужие ячейки у домена нет по построению),
  но глагол, который начнёт писать в отношения ТРЕТЬИХ стран, потребует
  обобщения потолка на ячейки сверки — с изменением формата ключей
  `primitiveTurnBudget.impactAccrued` и, следовательно, `SAVE_VERSION`.
- **Сторож «промт не упоминает `annex`/`puppet`» переписан 2026-07-29** —
  проверка подстроки была не просто хрупкой, а неверной (оба глагола законно
  вернулись в алфавит); история причины — `docs/decisions/2026-08.md`.

## Хвосты сессии «Мягкие глаголы алфавита» (Милстоун 1, 2026-07-29)

Реализованы `send_aid`, `capital_flight`, `condemn`, `support_proxy`; `influence`
удалён из старого канала. Ниже — то, что осталось.

- **`shift_mood` — глагол без поля состояния; нужен ПРОДУКТОВЫЙ ответ, а не
  реализация.** Спецификация §2 описывает его как «толчок настроения группы»,
  но поля «настроение» в состоянии нет вовсе. Есть четыре следа воздействий
  (`GroupImpactMemory`: `suppression`, `alienation`, `concession`,
  `emboldenment`), и каждый уже принадлежит своему глаголу: подавление —
  `repress`, отчуждение — `repress`, уступки — `grant_autonomy`, решимость —
  `incite_unrest`/`spawn_incident`/`grant_autonomy`. Разбор трёх вариантов:
  - **выразить через существующие следы** — значит сделать `shift_mood`
    синонимом одного из четырёх глаголов с другим именем. Отвергнуто по тому
    же основанию, что и `shift_ideology`: два способа сделать одно и то же
    хуже, чем один;
  - **завести пятое поле** (`mood`/`morale`) — единственный честный путь, но он
    требует ответа на вопрос, которого спецификация не даёт: чем настроение
    отличается от НЕДОВОЛЬСТВА, которое движок и так выводит из дистанции,
    благосостояния и памяти воздействий (`CONCEPT.md` §4.1). Без этого ответа
    поле окажется вторым мнением о том же самом, а недовольство — центральная
    величина среза;
  - **признать глагол лишним в этой форме** — вероятный исход, и он ничего не
    отнимает: «настроение группы» уже двигают четыре существующих канала.

  Решение отложено до наполнения Милстоуна 2, где появится второй потребитель
  «настроения» (кризисная выборка). Заводить поле состояния раньше него значило
  бы калибровать вслепую.
- **«Влияние без денег» стало недоступно.** `send_aid` покрывает влияние,
  КУПЛЕННОЕ помощью; пропаганда, ухаживание и дипломатическое давление без
  бюджета не выражаются ничем. Это цена удаления `influence` из старого канала,
  названная сознательно: плоский шаг мимо коридора хуже, чем временно
  отсутствующая способность. Возвращается отдельным глаголом алфавита, когда
  для него найдётся предпосылка, отличная от «есть деньги».
- **`shift_ideology` отвергнут, а не отложен.** Честного отличия от
  `enact_reform` не нашлось (`docs/PRIMITIVES.md` §2). Если он понадобится, у
  него должна появиться СВОЯ природа — например дрейф координат под давлением
  общества, — но это механика тика, а не примитива с источником-государством.
- **`send_aid` не усиливает получателя военно.** Спецификация §2 говорит
  «эконом/воен. усиление»; реализовано экономическое (деньги в казну).
  Военное отдано `support_proxy`, у которого для этого есть предпосылка
  (идущая война). Помощь оружием в мирное время не выражается — если
  понадобится, это параметр вида помощи, а не новый глагол.
- **Три вида санкций из четырёх по-прежнему репутационные** (пункт прошлой
  сессии, не закрыт). Теперь рядом с ними стоит `condemn`, который
  репутационным является ПО ЗАМЫСЛУ и честно бьёт по легитимности, — то есть
  разрыв между «объявили режим» и «ничего не произошло» стал заметнее.
- **Ключи капа `condemn`/`capital_flight` запрещают двум государствам
  действовать по одной цели в один месяц.** Это защита от сложения коридоров, и
  цена у неё есть: «весь мир осудил X» в один месяц невыразимо. Смягчается
  калибровкой `MAX_PRIMITIVES_PER_TARGET_PER_TURN` либо обобщением потолка
  накопления на ячейки сверки (последнее меняет `SAVE_VERSION`).
- **Паритет клиентских словарей не проверяется тестом.** Новые ключи
  `primitiveOutcome`/`primitiveRejection` заведены в обеих локалях вручную;
  автоматической проверки «ru и en описывают один набор ключей» и «сервер не
  эмитит ключа, которого нет в словаре» нет ни одной. Отдельная работа — она
  требует либо чтения клиентских JSON из серверного теста, либо экспорта
  списка ключей в `shared/`.

## Что придётся менять в Милстоуне 1 (по внешнему аудиту 2026-07-26)

Внешний независимый аудит (Codex, read-only) дал приёмке Милстоуна 0 вердикт
FAIL по одной критичной находке; она и две средние закрыты в этой ветке
(`DECISIONS.md`, запись 2026-07-26 «правки по внешнему аудиту»). Ниже — то, что
аудит отнёс к Милстоуну 1 сознательно.

**Общее свойство блока: это изменения КОНТРАКТОВ, а не добавление веток.** Ни
один пункт нельзя закрыть новым `if` внутри существующей функции — каждый меняет
форму типа, порядок фаз или формат сейва, то есть требует согласованной правки
движка, схем, клиента и тестов одним заходом.

**Закрыто сессией «Контракты ядра» (2026-07-27, `SAVE_VERSION` 8→9)** —
per-verb discriminated union, транзакция `plan → validate → apply →
post-invariants → commit`, runtime-проверка сейва и другое (детали
`docs/decisions/2026-08.md`). Ниже — что осталось открытым.

- **Настоящая миграция сейва вместо честного отказа по `SAVE_VERSION`.**
  Содержимое с 2026-07-27 проверяется (`SaveService.validateLoadedGame`), но
  версия по-прежнему несовместима без миграции — сейв v8 отклоняется целиком.
  Осознанно: контракт ещё не устоялся (алфавит расширяется с 5 глаголов до 18),
  и миграцию пришлось бы переписывать вместе с ним.
- **Инварианты состояния — не полная схема мира.** Проверяются поля, на которых
  держатся защиты алфавита, ссылочная целостность того, что создают примитивы, и
  (с 2026-07-27) состав стран: уникальность id, столица среди своих регионов,
  ноль висячих ссылок на страну. Геометрия регионов, экономика и военный контур
  по-прежнему не проверяются — их порча ломает свои подсистемы, а не защиты
  алфавита. Расширять вместе с подсистемой, а не «за компанию».
- **Полная правдивость ТЕКСТА при частичном применении — почти закрыто**
  (неприменённый ответ не становится каноном, `Event.factuality` помечает
  неподтверждённое, решение пользователя 2026-07-27). Не закрыто ровно одно:
  сама проза модели по-прежнему написана ДО применения и может описывать
  отклонённую половину — в ленте игрок читает её как есть, только с пометкой.
  Лечится вторым вызовом модели («перепиши по фактам») — упирается в бюджет
  COST (`CONCEPT.md` §7).
- **Фитнес-правила против класса «утверждение сильнее гарантии».** За один
  Милстоун 0 этот класс всплывал не меньше пяти раз, и каждый раз его находил
  только адресный вопрос независимому рецензенту — дорогой ручной проход.
  Случаи: константа с `_PER_TURN` в имени считалась за вызов движка, а не за
  игровой ход; комментарий обещал «скрытых эффектов не бывает», тогда как
  рантайм-сверка покрывала один канал памяти из четырёх; шапка модуля магнитуды
  ссылалась на несуществующий тестовый файл; документ утверждал «летопись читает
  только подтверждённые заголовки», хотя канонизировался сырой текст модели;
  комментарий уверял, что ветка отказа платежа недостижима и потому не покрыта
  тестом, — она была достижима, и тест на неё существовал. Значительная часть
  детектируется машинно, рядом с существующими правилами
  `server/src/__tests__/architecture.test.ts` (там уже живут запрет магических
  чисел в симуляции и запрет мутаций в обход слоя команд). Кандидаты:
  (1) ссылка в комментарии на тестовый файл или тест по имени — файл обязан
  существовать (ловится одной строкой, а стоило целого раунда ревью);
  (2) константа, чьё имя обещает границу («per turn», «max», «never»), обязана
  иметь тест, эту границу пересекающий; (3) слова «никогда»/«всегда» в
  комментарии или профильном документе — только вместе с негативным тестом
  рядом. Почему именно Милстоун 1: там алфавит расширяется с пяти глаголов до
  восемнадцати, и цена расхождения растёт вместе с числом мест, где оно может
  возникнуть.
- **UI определяет доступность приказа по владельцу региона, движок — по
  фактическому контролёру.** `PrimitiveOrdersPanel` показывает быстрые кнопки по
  `ownerCountryId`, `PrimitiveEngine` проверяет право через `effectiveController`
  (`occupiedBy ?? ownerCountryId`). Пока войны нет, эти два всегда совпадают; при
  подключении оккупации разойдутся — игрок увидит кнопку, которую движок
  отклонит, и наоборот. Мелкая находка того же аудита; чинится вместе с военным
  контуром, потому что «кто контролирует регион» должно приходить на клиент
  одним полем, а не выводиться там второй раз.

- **Два интеграционных теста гонятся за один файл `data/saves/autosave.json`.**
  `persistence.integration.test.ts` и `routes.integration.test.ts` чистят слот
  парой `existsSync` + `unlinkSync`, а vitest гоняет файлы параллельно — изредка
  второй падает `ENOENT` на `unlink`. Наблюдалось 1 раз на 4 полных прогонах
  2026-07-27; поодиночке оба файла зелёные. Структурный TOCTOU в тестовой
  обвязке, к коду сейвов отношения не имеет: чинится изоляцией каталога сейвов
  по worker'у либо `try/catch` вокруг очистки.

## LLM-цикл
- Рост `eventHistory` без границ — не реализовано, отложенное улучшение.
- Смена названия страны на карте + создание Map Feature от LLM — отдельная
  client/map задача; геометрические алгоритмы для неё переписывать не нужно.
- Каталог исторических развилок нужно расширять — текущие 6 записей покрывают
  только 1946–1953, эра Cold War идёт до 1999. Кандидаты и требование сверки
  реальных id — `docs/tasks/HISTORICAL_HINGE_POINTS_1946.md` §"Требуется расширение каталога".
- **Южная Азия (2026-08-02): 5 записей интегрированы** — `fall_of_
  portuguese_india`, `french_india_transfer`, `sikkim_merger`,
  `sino_indian_border_war`, `naga_insurgency` в
  `shared/src/data/historicalHingePoints1946.ts`, id сверены с живым
  `countries.json`. Осталось нерешённым (`docs/tasks/
  SOUTH_ASIA_1946_HINGE_POINTS.md`): выделить ли Хайдарабад отдельной страной
  (продуктовое решение, не принято) и Раздел Индии/Пакистан, заблокированный
  отсутствием механики "создать страну в рантайме" (не связано с
  отсутствием `IND`).
- Мультиязычность данных (`Country.name`/`shortName` → `LocalizedText`, как
  уже сделано для `Region.names`) — мигрировать вместе с 12 рукописными
  файлами стран, когда дойдут руки. Детали — `docs/LOCALIZATION.md`.

## Приёмы из разбора Open-Historia (2026-07-30)

Источник — read-only разбор чужого проекта
([Open-Historia](https://github.com/Open-Historia/open-historia), код MIT; клон
`.reference/open-historia`, вне git, HEAD `33112bc`). Полный разбор —
`.agent/reference/open-historia-review.md` (пока живёт на ветке
`claude/open-historia-review`, коммит `fe0ded8`). Ссылки `файл:строка` ниже
сверены с клоном напрямую: **их собственная документация местами отстаёт от их
же кода**, поэтому номера из их доков перепроверять, а не переносить. Их
гео-данные брать нельзя (гейт релиза в `docs/plans/11_MVP_ROADMAP.md`) — ниже
только код и приёмы. Архитектуру их AI-геймплея мы отвергли осознанно
(`docs/DECISIONS.md`, 2026-07-30).

**Валидатор примитивов — три улучшения:**
- **Отказ возвращает СЛОВАРЬ, а не голый reject.** Модель назвала
  несуществующую цель — ей в фидбек уходит список реальных кандидатов
  (`resolveRegionTransfers` `gameplay.js:904` → `buildTransferFeedback`
  `gameplay.js:1072`, применяется в `:1140-1142`). У нас причина отказа уже
  структурный код с отдельным английским рендером в промт (`PRIMITIVES.md` §3);
  не хватает именно перечня допустимых целей. Точечный corrective-retry со
  словарём дешевле потери хода.
- **Дуальность strict/salvage у ОДНОЙ проверки.** Попытка 1 возвращает модели
  корректирующую строку; финальная не выбрасывает готовую генерацию из-за
  косметики, а спасает на месте, отбрасывая неразрешимые элементы
  (`validateGeneratedWorldChanges` `gameplay.js:1128-1165`). Ортогонально нашей
  градации ERROR/WARNING в валидаторе слоёв данных (коммит `b93d23e`): та делит
  проверки по тяжести, эта — одну проверку по признаку «остался ли ещё повтор».
  Совмещать, не заменять.
- **Их зафиксированный баг, который не надо повторять:** флаг «финальная
  попытка» обязан приходить ОТ РАННЕРА, а не из подсчёта вызовов валидатора
  (`gameplay.js:538-547`). Провал схемы на первой попытке означает, что
  валидатор не звался вовсе — счётчик вызовов сделает вторую попытку «первой»,
  вернёт strict-фидбек, адресованный модели, и тот утечёт игроку как причина
  отказа. У них это полевой отчёт, а не гипотеза.

**LLM-слой — что взять:**
- **Страж «нарратив против структуры».** Событие с языком захвата при НУЛЕВЫХ
  структурных эффектах отклоняется ДО применения (`CAPTURE_LANGUAGE`
  `gameplay.js:1126`, сама проверка `:1147-1165`, только на strict-попытке).
  Это буквально наша находка F1 (нарративная аннексия — «успех» при полном
  отказе команд); закрывается одной проверкой и дополняет правило «нарратив
  только после commit».
- **Терпимый разбор ответа** (`extractJsonPayload` `gameplay.js:286`): срез
  `<think>…</think>`, починка умных кавычек и висячих запятых, fenced-блоки,
  скан сбалансированных скобок — и только ПОСЛЕ провала строгого парсинга
  (`gameplay.js:230`), корректный вывод не трогается. Нужно для заявленной нами
  поддержки локальных моделей.
- **Мультипровайдерный транспорт** (`providerConfig.js` — пять провайдеров:
  Gemini / OpenAI / Anthropic / OpenAI Compatible / Anthropic Compatible),
  принудительный structured output на каждого и машина деградации
  `tool → json_schema → json_object → text_json` по ответам 400/422
  (`main.jsx:732,743,751,756`). Грабли собраны за нас: `tool_choice` передаётся
  СТРОКОЙ `"required"`, а не объектом `{type:"function",…}` — llama.cpp-серверы
  (LM Studio, Jan) объектную форму не парсят, логируют «Wrong type supplied for
  parameter 'tool_choice'» и молча откатываются в `auto`, теряя принуждение
  (`main.jsx:711-718`). Наш «гибрид провайдеров» — ровно эта задача.
- **Стриминг только для локальных эндпоинтов** (`main.jsx:347-352`, флаг
  `streamLocalEndpoint` `:672`). Локальный сервер замечает мёртвый сокет лишь
  при записи следующего токена: без стрима отменённый запрос догенерирует ответ
  целиком, и Cancel перестаёт быть физическим.
- **Замороженный промт в сейве + директивы на вызове** (их `docs/ai-prompts.md`
  §2 «Frozen-prompt caveat», §6). Кампания несёт свою копию промтов, поэтому
  правка шаблона старые кампании НЕ меняет; правила, обязанные действовать
  ретроактивно, дописываются в промт в момент вызова, из кода. Нам это нужно
  для воспроизводимости и replay: иначе правка шаблона тихо меняет контракт
  старых кампаний.
- **Консолидация истории** (`compactHistoryIfNeeded` `gameplay.js:627`, задача
  `eventConsolidator` `:613`) — сжатие старых событий и чатов в
  сводку-непрерывность. Наш кап летописи F20/F49.
- **Rollback-снапшоты и busy-lock** (`beginSimulation` `gameplay.js:702`,
  `loadRollbackSnapshots` `:1410`, `rollBackToSnapshot` `:1419`) — посмотреть ДО
  того, как писать свой атомарный commit (F45, атомарная запись сейва).

**Редактор карт — оценённый вариант, решение «не сейчас».** У них полноценный
`src/Editor/` (рисование, слияние, carve, покраска владельца, undo/redo, города,
флаги, базовые карты), у нас нет ничего сравнимого — реальный незакрытый разрыв.
Брать надо архитектуру, а не код: их экспортёр прибит к GADM-id и их структуре
мира. Цена: новые зависимости (`ol` ^10.9.0, `ol-pmtiles` ^2.0.2,
`polygon-clipping` ^0.15.7, `shpjs` ^6.2.0, `geotiff` ^3.0.5 — по их
`package.json`), второй изолированный рендер-стек OpenLayers при MapLibre в
игре, плюс переписать экспортёр под наши id и алфавит примитивов. Это отдельный
милстоун — реализацию не начинать без решения пользователя. Сами приёмы
(разделение лёгкого сериализуемого документа и тяжёлой геометрии,
`VectorImageLayer` + `wrapX:false` на тысячах полигонов, экспорт по строгому
whitelist) перечислены в §4 разбора и **по их коду отдельно не
перепроверялись** — при заходе в задачу проверять заново.

## UI-редизайн (весь трек заморожен — см. «Постоянные рамки»; пункты ниже — для будущего решения)
- **Новая композиция Header и плоских информационных панелей** (линия B) —
  зафиксирована в `docs/UI_LAYOUT_PROPOSAL.md`; не утверждена и не
  реализуется до выбора канонической UI-линии пользователем. Отдельный
  prompt для concept board — `docs/UI_VISUALIZATION_PROMPT.md`.
- **1946 `countries.json` не содержит стартовый `stockpile`** — найдено при
  живом QA-фидбеке 2026-07-13 (Header, ряд ресурсов пуст у всех стран). Схема
  (`scenario1946Schemas.ts`) поле поддерживает (`optional()`), но
  MAP-пайплайн (`scripts/map/generate_country_registry.py` и соседние) его не
  заполняет — казна/ВВП растут с хода 0, а `stockpile` остаётся нулевым до
  первого `ResourceTick`. Рукописные `server/src/data/countries/{USSR,USA,
  UnitedKingdom}.ts` содержали авторские значения `stockpile`, но были
  **мёртвым кодом** для сценария 1946 (использовались только сценариями
  1836/2000) — удалены 2026-07-26 вместе с этими сценариями-заглушками
  (`docs/DECISIONS.md`), примера для копирования больше нет. Решить осознанно: либо пайплайн сам считает стартовый
  stockpile (например, от `deposits`/инфраструктуры региона), либо это
  намеренно пустое состояние — не чинить умолчанием. Клиент уже обрабатывает
  пустой список корректно (ряд ресурсов в шапке скрывается, а не показывает
  пустую полосу).
- **Леджер уведомлений** — отложен целиком (решение пользователя 2026-07-13):
  в `GameState` нет сущности «уведомление», только `eventHistory`. Колокольчик
  в `Header` — честная disabled-заглушка. Когда возьмётся: список последних
  N событий из `eventHistory` с уровнями crit/warn/info по типу события,
  бейдж непросмотренных за последний ход.
- **Визуальный QA перекраски карты по mapmode не закрыт** — Browser pane в
  сессии Среза 2г держал вкладку `document.hidden`, requestAnimationFrame-луп
  MapLibre не тикал, пиксельно подтвердить `regionModeColors` не удалось
  (см. docs/plans/12_UI_REDESIGN.md, Срез 2, «Отклонения»). Логика проверена
  независимо (тиринг на живом `GameState`), но нужен обычный браузер или
  видимая вкладка, чтобы визуально сверить с эталоном.
- **Mobile Header overflow (<600px)** — найдено Chrome/CDP QA 2026-07-16
  сессией онбординга (bootstrap-линия): RU/EN на 1440×900 и 760×900 без
  overlap/clipping, но на 390×844 Header делает документ шире viewport —
  stats-якорь почти уезжает за экран, кнопка следующего хода недоступна.
  Coach-card сам помещается. Композиция Header общая с `main`, так что
  находка вероятно применима к обеим линиям, но повторная проверка на самом
  `main` не выполнялась. Нужен отдельный responsive scope для композиции
  Header (после разморозки UI), а не локальный фикс онбординга.

## Крупные системы

### Экономика эпохи: смена веса ресурсов и цена деколонизации (поднято 2026-07-30)

Оба пункта — Милстоун 3. Гейт сырья, за которым они стояли, сделан 2026-08-02
(вариант А, `MilitaryTick.ts`): военное сырьё потребляется производством.
Порядок остатка прежний: цена как функция спроса и технологий, потом
эпохальные сдвиги.

- **Веса ресурсов меняются с эпохой, а не заданы константой.** Сегодня
  `WORLD_PRICE_BY_CATEGORY` — четыре числа (`energy: 50`, `metal: 80`,
  `agricultural: 20`, `strategic: 60`) плюс два переопределения (`gold`, `uranium`).
  **Нефть и уголь оба попадают в `energy` и стоят ровно одинаково**, то есть
  главный энергетический переход XX века модель не выражает вообще. Нужна цена как
  функция спроса и технологического уровня мира: тогда сдвиг «уголь → нефть»
  возникает как СЛЕДСТВИЕ освоения технологий, которым нужна нефть, а не как
  скрипт «в 1955 нефть подорожала». Историчность появляется сама, а расхождение с
  реальностью становится следствием решений игрока — то, чего требует
  альтернативная история.
- **Деколонизация должна иметь экономическую цену для обеих сторон.**
  `politics.sovereigntyStatus` и `overlordIds` размечены на все 157 стран и **не
  читаются ни одной механикой** (`docs/POLITICS.md` это признаёт). Экономический
  смысл потери колоний — доступ к дешёвому сырью и преференциальный рынок; для
  колонии — потеря субсидий и инфраструктуры. Как только ресурсы начнут
  потребляться, а торговля получит множители, деколонизация станет экономическим
  событием без отдельной механики.
  Форма связи — **не константа, а связь с датой разрыва и хвостом затухания**
  (поправка пользователя 2026-07-30): политическая рвётся в момент независимости,
  торговая инерция уходит десятилетиями. Метрополия получает выбор — удерживать
  силой или конвертировать в торговое влияние. Коэффициент колониальной связи в
  мета-анализе (×2,5 к объёму торговли) описывает плотность потоков внутри империй,
  а НЕ прочность империй: 1946 — начало распада, а не пик (Филиппины независимы
  4 июля 1946, война в Индокитае с декабря 1946, Индия и Пакистан 1947).
  Числа и источники — `.agent/audits/models-research-2026-07-30.md` §1.

- **Торговля**: коннекторы (ж/д/канал/трубопровод — вообще не генерируются),
  индивидуальные цены/резервы по ресурсу — детали `docs/TRADE.md`
  §"Явные пробелы v1/v2". (Импорт дефицита — реализовано 2026-07-11.)
- **War Phase 3** (логистика/окружение/прокси-«зубы» сферы/ядерное
  сдерживание) — не начата. Также не в Phase 2: честная замена
  `activePersonnel`-плейсхолдера, "доктрина" как отдельная механика.
- Расширить влияние `Country.aiTraits` с порога войны на military ramp/cap при
  угрозе — отдельная балансировочная задача.
- **MapFeature как unit экономики** **[частично закрыто редизайном 2026-07-24: мощность = агрегат регион-вкладов CONCEPT §5.1 (не MapFeature); объекты карты §5.6. Фабрики — честный открытый хвост.]** (черновик `docs/decisions/2026-06.md`,
  22-23.06) — найдено при разборе 2026-07-11: черновик ссылается на
  `ProductionTick`, удалённый как мёртвый код (см. "Готово"). Мины уже
  функционально покрыты моделью deposits/extraction (план 04) — не хватает
  только визуальной связки с MapFeature `mine` (план 06 не начат).
  Реальный остаток — фабрики (никак не моделируются экономически) — требует
  проектирования production/input-output системы с нуля, крупнее одного захода.
- Расширить MVP валютных зон за пределы 7 авторенных стран: нужна широкая
  историческая калибровка массива 1946 (`docs/plans/10_CURRENCY_ZONES.md`).
- **Упростить создание и подключение стран** (нужно для масштаба 200+) —
  найдено при разборе 2026-07-11: мотивация пункта — сценарии 2000/1836,
  которые сейчас вне охвата до релиза (только 1946, см. "Постоянные рамки").
  Авторский формат для 1946 уже упрощён (план 05, Срез 2, JSON без нулевых
  блоков) — пайплайн для ДРУГИХ эпох целенаправленно не строится, пока они
  не станут параллельной целью разработки.
- Возврат территории ("status quo ante") после войны — не реализован, требует хранить довоенных владельцев регионов.
- Исторические развилки → `legitimacy`/`stability` напрямую — не реализовано,
  требует сначала разобрать открытые вопросы `docs/tasks/HISTORICAL_HINGE_POINTS_1946.md`. `docs/POLITICS.md`.

## P4 — Инженерный долг (низкий приоритет, оппортунистически)
- **Предсуществующий red lint клиента** (`npm run lint` в `client/`): 20
  ошибок в 4 файлах (`PlayerIntentPanel.tsx`, `map/MapView.tsx`,
  `map/engine/GeometryEngine.ts`, `map/engine/TopologyBuilder.ts`, в основном
  `no-explicit-any`) — подтверждено по `git log`, что не регрессия текущей
  задачи. Чинить точечно при следующем касании каждого файла (map/** — не
  переписывать без причины), не отдельным проходом.
- **Редирект ВВП/населения оккупанту** (`shared/src/utils/aggregateCountryData.ts`) —
  сейчас редиректится оккупанту только добыча ресурсов (`ResourceTick.ts`);
  ВВП/население оккупированного региона по-прежнему считаются легальному
  владельцу. Потребовало бы разделения `aggregateCountryFromRegions` на два
  фильтра — кандидат на будущий заход.
- **Region-уровневый читатель `effectiveValue()`** — модификатор оккупации на
  `Region.stability` ставится/снимается корректно, но ни один тик не читает
  `region.stability` через `effectiveValue()` (нужно протащить `game`/`modifiers`
  в `PopulationTick.ts`). Кандидат на заход, когда появится второй потребитель.
- **Запись команд в `eventHistory`/журнал хода** (`source: llm|player|engine`) —
  `Event` не имеет поля `source` сегодня. Отдельная задача аудита/трассировки.
- **`apply*Action` игнорируют `CommandResult`** (`LLMService.ts:362-454`) —
  действие попадает в `appliedActions` даже если команда вернула `success: false`,
  то есть LLM получает подтверждение применения того, что не применилось.
  Новый движок примитивов (`server/src/primitives/`) этот разрыв не наследует:
  отказ команды откатывает примитив целиком. Мигрировать старые 11 действий —
  отдельная задача (пересекается с подключением примитивов к LLM-пути, план 13, сессия B).
- **`MapFeature.expiresAt` сравнивается с wall-clock** (`MapFeatureService.removeExpiredFeatures`,
  уже в белом списке `architecture.test.ts`) — из-за этого игровая дата истечения (1946)
  считается просроченной немедленно. `spawn_incident` поэтому создаёт объект без
  `expiresAt`; временные объекты карты появятся только после перевода очистки на
  `game.currentDate`.
- **Полный перевод читателей `politics.stability` на `effectiveValue()`** —
  переведён только `AiBehaviorTick.applyStabilityWelfareNudge`. Мигрировать по
  мере того, как на конкретное поле реально вешаются модификаторы.
- **План 04, отложенные пункты**: связь `buildExtraction` с MapFeature `mine`
  (план 06 не начат); автоматический триггер `damageExtraction` от
  войны/событий (команда существует и тестируется напрямую, ничего не вызывает
  её сама); детальная видимость `deposits`/`extraction` по регионам в промте LLM.
- **План 05, отложенные пункты**: клиентская часть (TopoJSON-конвертация
  `world_1946.geojson`, критерий «≤5МБ по сети») — отдельная map-data задача
  (инцидент ошибочного удаления `game_map.json` исправлен 2026-07-23, детали
  `docs/decisions/2026-08.md`); 12 рукописных TS-стран 1836/2000 удалены как
  мёртвый код 2026-07-26 (`docs/DECISIONS.md`) — вопрос миграции в авторский
  JSON-формат закрыт удалением, не отложен; штампы `_meta` в промежуточных
  `scripts/map/out/*.json` — формат промежуточных артефактов, не поведение.
- тесты `Window.tsx` (drag/resize через DOM-события — инфра уже есть, `happy-dom` даёт `innerWidth`).
- `pendingLlmActions`/`savePendingActions`/`getPendingActions`/`clearPendingActions`
  (`GameState.ts`/`LLMService.ts`) — подтверждённая мёртвая scaffolding-механика,
  не вызывается в проде. Кандидат на уборку мёртвого кода.
- Live gated-тест `GeminiProvider` (`RUN_LIVE_LLM_TESTS=1 npx vitest run
  src/llm/providers`) — прогнать повторно, когда сбросится free-tier дневная
  квота Gemini. Детали — `docs/plans/02_LLM_CONTRACT.md`, «Отклонения при реализации».
- **Воспроизводимость Python map pipeline:** добавить dependency manifest с
  PyShp/pyproj/Shapely и точную provenance-таблицу входов. Порядок China→Asia
  и Windows-safe `--help` исправлены global audit; validators проходят, но
  full rebuild без manifest/sources всё равно не считается воспроизводимым.
- **Runtime/licensing hygiene:** решить Node pin (`CI=22`, локально проверено
  на 24), согласовать лицензию repository/server manifest и добавить readiness
  check вместо fixed delay в `start.ps1`.

---

# Отложено / не в фокусе сейчас

## Интерфейс — унаследованный техдолг (фриз снят 2026-07-12, учитывать в `docs/plans/12_UI_REDESIGN.md`)
- **Исправлено в P0 (2026-07-16, детали `docs/decisions/2026-08.md`):**
  нормализация position/size, drag/resize, reset layout. `useWindows` остаётся
  сужен до сценария «сравнить два объекта» (план 12, Срез 2в).
- Esc-закрытие окна; persistence позиции/размера уже реализован в
  `useWindows.ts` через `localStorage`.
- `ResearchPanel`: хардкод списка технологий — упирается в tech tree.
- Минорные оговорки: `BudgetPanel` без жёсткой блокировки при дефиците
  (осознанно — дефицит допустим); `ErrorBoundary` не проверен живым крашем;
  accessibility покрыта частично (панели — да, мировой рейтинг/табы — нет).

## Карта/регионы (не заморожено; геометрия меняется только по доказанной причине)

- **Названия стран — исправлено и подтверждено browser QA (2026-07-16)**
  (глифы, resize карты, `RangeError` в `computeCountryAxis`; детали
  `docs/decisions/2026-08.md`).
- **`MapFeature` типы, кроме `capital`, визуально неотличимы друг от друга.**
  Слой `map-features-icons` в `MapView.tsx` проверяет только `type === 'capital'`
  → спрайт столицы, иначе всегда generic `city-icon`. Функции
  `getIconForType`/`getColorForType` в файле есть, но ни один слой их не
  читает — мёртвый код. Пользователь решил (2026-07-06) не чинить сейчас —
  война подтверждена через цвет региона, не через иконку.

### Следующий геометрический проход (отдельный блок, дороже)
- **Турция/Сирия/Иордания — пере-нарезать тоньше (не приоритет).** Проверка
  плотности 2026-07-19 показала: Сирия (~18.5k км²/регион) и Иордания
  (~14.8k км²/регион) уже достаточно детальны (тоньше Ирака, который никто
  грубым не называл) — реально выбивается только азиатская зона Турции
  (~108k км²/регион, вчетверо грубее соседей). Хатай — отдельным регионом
  (сейчас поглощён "Sanliurfa"). Источник тонкой нарезки — `game_map.json`
  (4596 провинций), живой файл `client/src/assets/game_map.json` (см.
  восстановление 2026-07-23 выше, план 05).
- ARE (Trucial States) уже 1 страна (2026-07-19, docs/DECISIONS.md) —
  схлопнуть переподробных миноров (DOM 19 регионов на 48k км², OMN,
  центральноамериканский кластер) остаётся отдельной задачей, лучше через
  новое поле `tier`, не вручную страна за страной (осознанно отложено
  2026-07-19 — конфликтует по имени с двумя уже существующими понятиями
  `tier` в коде, см. `shared/src/defines/tier.ts` и
  `scripts/map/economy_1946/density_tiers.py`).
- Прочие спецзоны на будущее: Триест, Танжер, Саар, Кёнигсберг, Шпицберген.

### Визуал карты
- **Динамическая смена цвета в рантайме и Режимы карты (крупная фича).** Цвет
  как `f(состояние страны, режим карты) → цвет` на клиенте. Режимы:
  Политический (есть), Идеологический (коммунизм/либдем/национализм — цвета),
  Дипломатический/Блоки (относительно игрока), Экономический (интенсивность
  по ВВП/ресурсам).
- **Поле `tier`** (важность страны) — бюджет регионов при пере-нарезке
  геометрии, насыщенность цвета, стартовый шортлист держав. Состав
  major/regional согласовать с пользователем (см. "Открытые вопросы" выше).

### Прочее
- настоящая `isCoastalRegion()` в `generateMapFeatures.ts` (сейчас всегда
  false → порты не генерируются). **Зависимость:** блокирует морские десанты
  в war-системе (`docs/WAR.md`, Phase 1 явно пропускает — навал-превосходство
  `navyStrength` открывает контакт в `WarTick.ts`, цель — ближайший
  прибрежный регион, не реализовывать плейсхолдером на `capitalRegionId`).
- `scripts/map/config/country_merge.json` — дефолтная группировка колоний по
  сюзерену (один блок на сюзерена), не финал — ручная доводка по географии ожидаема.
- `scripts/map/README.md` "Внешние источники" — нет прямых ссылок на загрузку
  geoBoundaries/китайских исторических шейпов, только имена файлов.
- **Береговые линии не стыкуются** — полигоны суши/моря из MAP местами не
  совпадают по границе, заметно при приближении. Геометрическая проблема
  исходных данных MAP, не тривиальная правка. Конкретизировано 2026-07-23
  двумя независимыми находками (`docs/DECISIONS.md` 2026-07-23):
  - **`fix_sea_coastline_gaps.py` печатает отрицательные "+X км2" для части
    морей при повторном прогоне** (найдено 2026-07-23) — проверено, что это
    транзиентный артефакт измерения (`unary_union` float-шум GEOS), а не
    реальная потеря площади: сохранённая геометрия (`area_km2`) бит-в-бит
    стабильна между прогонами. Мелкое, не блокирует; точный механизм не
    докопан — кандидат на разбор, если понадобится точная площадь морей.
  - **Разрывы того же типа (gate 1 `absorb_slivers`, компактность) — гораздо
    шире, чем Гренландия (~854 км²)/Шпицберген (~143 км²), НЕ исправлены.**
    Постоянный диагностический скрипт `scripts/map/build/diagnose_
    coastline_gaps.py` (2026-07-24, `--scan`/`--render`, см. `docs/
    DECISIONS.md`) нашёл **COASTLINE: 991 ячеек, 4701.1 км²** — тот же
    подкласс (компактная, не лента — отклоняется первой, общей проверкой),
    сильно сконцентрирован на изрезанном берегу Аляски (сотни мелких
    fjord-разрывов, подтверждено рендером — Prince William Sound 75 км²,
    реальный видимый разрыв), плюс единичные в Европе/Африке/Азии/
    Антарктиде/Океании/С.Америке. Дальнейшее ослабление общего порога
    компактности рискованно (общая функция, 6+ вызывающих скриптов) без
    более узкого решения — кандидат: `minimum_rotated_rectangle` aspect
    ratio + абсолютная ширина вместо compactness (этот приём уже
    подтверждён на самом диагностическом скрипте — compactness ложно
    показывает "ленту" на компактных блобах с зубчатой границей). Масштаб
    сопоставим с уже исправленным (Great Lakes+global), не на порядок
    меньше, как считалось раньше — решение о приоритете за пользователем.
  - **Новая находка, другой класс: LAND_HOLE ~2732.5 км² на границе
    Brazil/Paraguay** (24 ячейки, тот же скрипт, не у воды). 23 из 24 —
    один кластер (-56,-21): блоб 1464.2 км² + мелкие вдоль существующих
    административных границ — вероятный побочный эффект `resolve_brazil_
    paraguay_overlap.py` (2026-07-23, не подтверждено перечитыванием
    кода). Плюс 1 отдельная находка южнее (-57.6,-30.2), не исследована.
    НЕ исправлено.
- **Колониальные блоки — не реальная историческая структура.** Сейчас "1
  блок на сюзерена" (всё британское — единый QCG). Реально были признанные
  административные единицы (Французская Западная Африка/AOF, Британская
  Вест-Индия и т.д.) — переделать `country_merge.json`/`generate_country_registry.py`
  на курируемую конфигурацию, не авто-мердж по сюзерену.
- **Выбор страны** — список на старте (`ScenarioSelector.tsx`) уезжает за
  нижнюю границу экрана при 126 странах, только 3-буквенные коды без
  иерархии важности. Нужно: (а) curated-список "крупных держав" + кнопка
  "показать все"/переход на полную карту, (б) выбор страны кликом по карте.
  Требует поля `tier` (см. выше).
- **Границы регионов — один уровень, не зависят от зума.** Нужно два слоя
  (внутренние тоньше/скрыты при малом zoom, внешние толще, всегда видны) +
  зум определяет выбор региона vs всей страны.
- **Флаги стран** — 145 из 157 скачаны и нормализованы
  (`client/public/flags/<КОД>.svg` + `index.json`, 8,4 МБ; собираются
  `scripts/map/fetch_flags.py` из манифеста `config/flags_1946.json`,
  2026-08-03). Все 145 проверены рендером в браузере. Осталось:
  (а) поле флага в типах и вывод в HUD, (б) 12 кастомных сущностей (зоны
  оккупации Германии/Австрии/Кореи, советская Маньчжурия, Договорный Оман) —
  своих флагов не существовало, (в) 26 файлов тяжелее 100 KB (Венгрия 1 МБ):
  герб нечитаем в HUD ~88 px, нужна облегчённая версия, (г) 15 файлов под
  CC BY-SA — атрибуция или замена на PD, список в `index.json`,
  (д) `SDN` (Англо-Египетский Судан) — кондоминиум нарисован двумя флагами
  рядом, пропорция 3,46 даёт в общем боксе большие поля: нужен свой ассет,
  (е) 40 стран делят флаг метрополии (французский триколор — у десяти) и в
  HUD неразличимы: различение служебным маркером, не подменой флага.
  **Пакеты вида `flag-icons` для 1946 непригодны** (проверено): они дают
  современные флаги и врут на Италии, Испании, Венгрии, Египте, Канаде, ЮАР,
  Иране, а по коду `CHN` отдают флаг КНР вместо Китайской Республики.
  Генеративная модель (Nano Banana) как источник флагов отклонена: флаг —
  точная геометрия и госсимвол, диффузия плывёт в пропорциях и гербе. Её
  область — только вымышленные страны из `create_country`.
- **Текстурная подложка морей** — сейчас плоская заливка. Нужен растровый
  тайлсет или процедурная текстура — источник/лицензия не исследованы.
  Кандидат на Nano Banana (Gemini), не выбрано формально.

---

# Готово (архив, кратко — детали в git и `DECISIONS.md`)

- **Движок:** тики Economy/Resource/Population/Military/Research/Diplomacy/War/Trade/Chronicle/Tier;
  агрегация регион→страна; deposit/extraction/output (план 04); модификаторы
  и слой команд (план 03); оккупация ≠ аннексия (план 08 Шаг 1).
- **Сервисы:** GameService, SimulationService, CountryService, RegionService,
  DiplomacyService, RegionEconomyService, MapFeatureService, ResearchService,
  LLMService, WarService, SaveService; zod-валидация везде на входе; кастомные
  error types; роуты только валидируют и зовут сервис.
- **LLM-цикл:** ручной (copy-paste) + автоматизированный (`GeminiProvider`,
  `POST /llm/auto`); типизированный контракт действий (`actionSchemas.ts`,
  план 02); гейт хода "только после ответа LLM"; переменная длина хода (N месяцев);
  память страны между вызовами; исторические развилки в промте; летопись кампании.
- **Данные:** расслоение regions/countries JSON, Zod-валидация на загрузке,
  единый источник каталога ресурсов TS↔Python, CI-джоб валидации данных,
  оркестратор пайплайна `make_1946.py` (план 05); сейвы/persist (план 01).
- **Игрок (backend + панели):** намерение свободным текстом, бюджет,
  исследования; BudgetPanel/ResearchPanel/PlayerIntentPanel.
- **Интерфейс:** HUD (`Header`, `SidePanel`, `ContextPanel`, `OrdersBox`),
  сравнение через окна, ErrorBoundary, единый API-клиент и `react-i18next`.
- **Тесты:** server/client Vitest, route tests через Supertest, data validators;
  точные counts не хранятся в живом TODO.
- **Прочее:** переименование в Geopolis; `DECISIONS.md` (журнал); TS strict
  на клиенте; Tech tree v2 (домен-инвестиции, без каталога именных технологий);
  торговля v1 (мировой рынок ресурсов).
