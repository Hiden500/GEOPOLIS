Last updated: 2026-07-10 (Мультиязычность интерфейса реализована: react-i18next)

# Geopolis — План работ

Живой план: **что делать дальше и в каком порядке**. История "почему" и завершённые
решения — в `docs/DECISIONS.md` (здесь не дублируются). Конкретные числа/дизайн —
в профильных доках (`WORLD.md`/`TECH_TREE.md`/`AI_RULES.md`/`LLM_RULES.md`).

Постоянные рамки (не меняются от сессии к сессии):
- **Карта заморожена** (решение пользователя 2026-07-03, заменяет «общую
  ответственность» от 2026-06-28): к карте возвращаемся «сильно позже» —
  сначала полностью рабочая симуляция. Задачи по карте не брать без явного
  запроса пользователя.
- **Интерфейс заморожен** (решение пользователя 2026-06-23) — пока есть более важная работа.
- **Охват до релиза — только 1946** (независимый гейм-дизайн разбор, 2026-07-06):
  1836/2000 остаются сценариями-заглушками (`testRegions` + 3 страны), не параллельная
  цель разработки. Довести 1946 до играбельного состояния, прежде чем расширять на
  другие эпохи.

---

# Текущий фокус

Каждая система строится с расчётом на LLM как главный двигатель событий — канонические правила
и приоритет см. `docs/LLM_RULES.md` §Роль LLM в архитектуре (не дублируется здесь).

Трек тестов и интерфейса закрыт/заморожен. Карта заморожена (см. «Постоянные рамки» выше).
Следующий крупный блок — **игровые системы + LLM-цикл** (порядок приоритетов пересмотрен
2026-06-30, см. ниже).

---

# Приоритеты

~~БАГ: население тихо вымирает~~ — **починено 2026-07-06** (`PopulationTick.ts`/
`ResourceTick.ts`): `standardOfLiving` теперь считается через `getGdpPerCapita()`/
`GDP_PER_CAPITA_REFERENCE` (не хардкод `/10000`), домены `biology`/`industry`
(нижний регистр, реальные ключи эры 1946, не `Biotechnology`/`Industry`).
Подтверждено новым smoke-тестом кампании (`campaignSmoke.test.ts`) — после
фикса USA/SUN/GBR/FRA/JPN/CAN растут нормально за 5 лет мирного времени.

**Новая находка того же прогона (не баг — открытый вопрос калибровки/дизайна):**
страны с очень низким стартовым ВВП/чел в датасете 1946 (`CHN` ~$83/чел против
референса $850, также `ARG`/`BRA`/`ITA`) продолжают терять население и после
фикса — формула `standardOfLiving = min(gdpPerCapita/850, 2)` корректно даёт им
множитель у нижнего пола 0.5, это не баг в смысле "неверный ключ/константа".
Два возможных источника: (а) формула рождаемости слишком резко давит на нижнем
конце шкалы ВВП/чел (историческая рождаемость у бедных аграрных стран в 1946
обычно была ВЫШЕ, не ниже, чем у богатых — см. `docs/HISTORICAL_ACCURACY.md`),
(б) стартовые региональные экономические данные для этих стран занижены
относительно источников (`docs/SCENARIOS.md` уже отмечает, что числа 128 стран
не калибровались по надёжным источникам). Решать перед кодом — не понижать
`MAX_PEACETIME_ANNUAL_POPULATION_DECLINE` в тесте, чтобы скрыть.

## P1 — Замкнуть экономические петли (фундамент для всего остального)
**РЕАЛИЗОВАНО.** Детали — `docs/ECONOMY.md`, `docs/DECISIONS.md`. `exportIncome` теперь
динамический (`docs/TRADE.md`, 2026-07-06); `stateEnterprise`/`other` остаются статичными.

## P2 — Оживить ИИ-страны (детерминированно, без полноценного utility-AI)
**РЕАЛИЗОВАНО** доступное подмножество — `server/src/simulation/ai/AiBehaviorTick.ts`. Детали —
`docs/DECISIONS.md`. Союзы/гарантии/пуппеты теперь дают реальные «зубы» — автоматически втягиваются
в войну (`WarService.declareWar`, `docs/WAR.md` Phase 1, 2026-07-06). Правило D (порог войны для
non-major) там же.

**Ещё не сделано (не блокирует P2, отдельные заходы):**
- Вариативность характера (`aggressiveness`/`riskTolerance`) — после, не блокер.
- War-система: Phase 1 (состояние/объявление/коалиции/упрощённый фронт/мир) и Phase 2
  (производство техники по 8 категориям, `production_shift`) реализованы — детали `docs/WAR.md`.
  Не в Phase 2: честная замена `activePersonnel`-плейсхолдера (экипировка — доп. слагаемое, не
  основа формулы), "доктрина" как отдельная механика. Phase 3 (логистика/окружение/прокси-«зубы»
  сферы/ядерное сдерживание) не начата.

## P2.5 — LLM-цикл (поднят из отложенных, 2026-06-30)

Ручной цикл, действия игрока свободным текстом, качество/историческая конкретика нарратива,
локаль LLM-нарратива, таймлайн-интерфейс — **реализовано**, детали и даты — `docs/DECISIONS.md`.

~~Механизм "памяти страны" между вызовами LLM~~ — **реализовано 2026-07-05**. Окно последних
  заголовков (`Event.title`) из `eventHistory` по стране, в `## Player Country`/`## Major Powers`/
  `## Spotlight Countries` строкой "Recent: <title> (<дата>)" — без нового поля в ответе LLM, без
  лишних выходных токенов. Подход и отклонённые альтернативы — `docs/DECISIONS.md`, 2026-07-05.
  tsc/тесты зелёные (276 сервер), живая проверка через preview подтверждена.
- **Рост `eventHistory` без границ** — не реализовано, отложенное улучшение (не сейчас). Подход —
  `docs/DECISIONS.md`, 2026-07-05.
- **Смена названия страны на карте + создание Map Feature от LLM** — отложено до снятия заморозки
  карты, не начинать без нового явного запроса. Детали — `docs/DECISIONS.md`.
~~Мультиязычность интерфейса (UI-слой, `react-i18next`)~~ — **реализовано 2026-07-10**: все
  15 клиентских компонентов с текстом мигрированы, живой переключатель языка на
  `ScenarioSelector` управляет и интерфейсом, и `GameState.locale`. Детали — `docs/LOCALIZATION.md`,
  `docs/DECISIONS.md`.
- **Мультиязычность данных** (`Country.name`/`shortName` → `LocalizedText`, как уже сделано для
  `Region.names`) — остаток "полной мультиязычности", отдельная задача, не начинать без нового
  запроса. Детали — `docs/LOCALIZATION.md`.
~~Переменная длина хода — N месяцев (1/3/6)~~ — **реализовано 2026-07-06**: `POST /game/next-turn`
  валидирует `{months}` через `advanceTurnSchema` (1-12, дефолт 1), `GameService.advanceMonth(months)`
  прогоняет `simulateMonth` N раз подряд за один гейт-чек (один ответ LLM разблокирует весь
  мульти-месячный ход). Без UI-селектора (интерфейс заморожен) — API готов к использованию.
~~Недельный ход (1нед)~~ — **отклонено пользователем 2026-07-06** (независимый гейм-дизайн
  разбор): нет жанровых прецедентов, где еженедельная каденция нужна без юнит-микроменеджмента,
  которого в игре сознательно нет; N-месячный ход (реализован) достаточен. Вопрос закрыт, не
  возвращаться без нового явного запроса.
~~`LLMProvider`-абстракция~~ — **`GeminiProvider` реализован и подтверждён живым вызовом
  2026-07-06**. `LLMProvider` (интерфейс `generateResponse(prompt): Promise<string>`) +
  `GeminiProvider` (`generateContent`, модель `gemini-3.1-flash-lite` по умолчанию, `GEMINI_MODEL`
  переопределяет), `responseSchema` под `{title, descriptions, actions[]}`,
  `thinkingConfig.thinkingLevel: "high"`. Роут `POST /llm/auto`: промт → провайдер → та же
  `LLMService.processResponse`, что и у ручного `/response` — без дублирования валидации. Ключ —
  только `server/.env` (`GEMINI_API_KEY`, см. `server/.env.example`), не уходит на клиент. Кнопка
  "Автоматически" в `LLMPanel`, рядом с ручным способом, не вместо него. Ошибки провайдера — 502
  (`LLMProviderError`), ход не продвигается. `GigaChat`/`OpenRouter`/`Ollama`/Groq — не реализованы,
  не блокеры (см. `docs/DECISIONS.md` про сравнение Groq/Gemini). tsc/тесты зелёные (282 сервер,
  83 клиент, +6 тестов `GeminiProvider`), живая проверка: реальный вызов `/llm/auto` вернул чистый
  JSON на русском (локаль игры), 2 применённых действия, память страны (`Recent: ...`) подхватила
  созданное событие в следующем промте.
~~Гейт хода "только после ответа LLM"~~ — **реализовано 2026-07-06**: `GameState.llmRespondedThisTurn`,
  `GameService.advanceMonth()` отказывает (`LLMGateError`, 409) если LLM ещё не ответила в этом
  цикле, флаг сбрасывается после успешного хода. Работает и для ручного, и для авто-цикла (оба идут
  через `LLMService.processResponse`). Попутно исправлен баг: `GameView.tsx` двигал календарь через
  `nextTurn()` при старте/остановке исследования вместо простого обновления состояния — гейт сделал
  бы это неработающим. Детали — `docs/DECISIONS.md`, 2026-07-06.
~~наполнение промта реальным контекстом: активные войны~~ — **реализовано вместе с War Phase 1**
  (2026-07-06): секция `## Active Wars` в `LLMService.generatePrompt()` (стороны, статус фронта,
  цель войны) — было забыто вычеркнуть при закрытии war-задачи.
~~Исторические развилки в промте~~ — **реализовано 2026-07-06**
  (`docs/tasks/HISTORICAL_HINGE_POINTS_1946.md`, каталог 6 записей на 1946 —
  `shared/src/data/historicalHingePoints1946.ts`, отбор по предусловиям —
  `shared/src/utils/hingePoints.ts`, промт §"Historical Context"). Подсказка
  для LLM, не рельсы: движок только решает, показывать ли, LLM решает,
  отразить или проигнорировать.
- **Каталог исторических развилок нужно расширять** (отмечено 2026-07-06) — текущие 6 записей
  покрывают только 1946–1953, эра Cold War идёт до 1999, весь период 1954–1999 не покрыт вообще.
  Кандидаты и требование сверки реальных id — `docs/tasks/HISTORICAL_HINGE_POINTS_1946.md`
  §"Требуется расширение каталога".
- ~~ВВП на душу населения + "благосостояние/уровень жизни"~~ — **реализовано 2026-07-06**
  (`shared/src/utils/countryMetrics.ts`, `docs/DECISIONS.md`) — производные метрики поверх уже
  трекаемых `economy.gdp`/`population`/`welfareSpending`/урбанизации регионов, показаны в промте.

**Зависимости:** политическая динамика (P3) и базовая war-модель нужны для полноценного
LLM-контекста, но интеграцию цикла можно начинать без них на текущем состоянии.

## P3 — Крупные системы (большой объём, после P1–P2)
~~Мировой рынок, торговля, логистика~~ — **v1 реализовано 2026-07-06** (`docs/TRADE.md`,
  `TradeTick.ts`): один мировой пул цены на категорию ресурса, продажа излишка сверх резерва
  списывает stockpile, `trade_embargo` снижает доход. Не реализовано: коннекторы (не
  генерируются вообще), импорт дефицита, индивидуальные цены/резервы по ресурсу — детали
  `docs/TRADE.md` §"Явные пробелы v1". Часть национальных целей ("экспорт сложной продукции"/
  Севморпуть) и колониальная экономика по-прежнему не начаты, теперь разблокированы.
- MapFeature как unit экономики: шаблоны по типу (factory/mine/port) с input/output,
  числа считает регион — черновик в `DECISIONS.md`, зависит от вопроса 4 (см. P1).
- Международные финансы/валютные зоны (рублёвая зона/петрорубль из обсуждения 2026-07-06) —
  **целиком не спроектировано**, не начинать без отдельного захода.
~~Военная система Phase 2~~ — **реализовано 2026-07-06** (`docs/WAR.md`) — производство техники
  по 8 категориям, `production_shift`. Phase 3 — отдельно, не начата.
- ~~Tech tree: спроектировать~~ — **реализовано 2026-07-06** (`docs/TECH_TREE.md`,
  `docs/DECISIONS.md`): без каталога именных технологий (не рельсы) — тир домена как
  непрерывная функция вложений через `PlayerIntent`/LLM `research_shift` экшн, не дропдаун.
  Старая проводка (`ResearchPanel.tsx`/`ResearchService.ts`/`coldWarTechTree.ts`/
  `TechnologyDefinition`/`ResearchProject`/`canResearch`) удалена целиком (была разорвана
  насквозь — прогресс никогда не зачислялся в реальный домен, см. разбор в `DECISIONS.md`),
  не чинилась точечно. tsc/тесты зелёные (347 сервер, 83 клиент), живая проверка через `curl`
  подтверждена (домены растут пропорционально распределению, тир пересекается, промт
  показывает тиры/ВВП-на-душу/благосостояние).
- **Национальные цели стран** (`docs/DECISIONS.md`, 2026-07-06; уточнено 2026-07-06) — у
  каждой страны свой короткий текущий курс/набор целей, исторически достоверный сид на старте
  эпохи; план по СССР был только иллюстративным примером формата, не образцом для копирования
  на все державы буквально. Мягкий нарративный ориентир для LLM, не механический гейт/рельсы —
  LLM пересматривает курс страны при значимом расхождении с реальной историей (например,
  победитель наследует амбиции разгромленного соперника). Разбор по разделам плана СССР (что уже
  покрыто, что требует новых механик, что чисто нарратив без чисел) — там же, как пример разбора,
  не как итоговый список для всех стран. **Приостановлено пользователем 2026-07-06** после черновика
  (только major, read-only, ~10 целей) — пользователь усомнился в самом составе тира major
  (см. пункт про состав стран ниже) прежде чем авторить цели под него. Не возвращаться, пока
  состав стран не пересмотрен.
- **Пересмотреть состав тиров major/regional и известные пробелы ростера стран** (поднято
  пользователем 2026-07-06). `TierTick.ts::HISTORICAL_TIERS_1946` придуман Claude-сессией
  2026-06-30 единолично, без утверждения пользователем по конкретным странам — ровно то, что уже
  висело нерешённым вопросом ("состав major/regional согласовать с пользователем, не выдумывать").
  Конкретные претензии пользователя: `CHN` в списке major, а `TWN` — нет, хотя в этом датасете
  `TWN` (Гоминьдан) держит 34 региона материка и реальное место в Совбезе ООН 1946 года, а `CHN`
  (коммунистические районы) — только 8 регионов, малый анклав; `ARG`/`BRA` — под вопросом как
  великие державы геополитического уровня 1946 года; Индия вообще не отдельная сущность (под
  `QCG`, British Colonies) — та же группа пробелов, что "Заблокировано" в
  `docs/tasks/HISTORICAL_HINGE_POINTS_1946.md` (Индия, Индонезия, Индокитай, Палестина/Израиль).
  Область работы (только пересмотр списка major/regional, или ещё добавление недостающих
  сущностей вроде Индии) — не решена, ждёт ответа пользователя.
~~Промт: "мир адаптируется под фантастическое намерение игрока"~~ — **реализовано 2026-07-06**:
  новый пункт в `## Instructions` (`LLMService.generatePrompt()`) — явное намерение игрока
  отменяет историческую достоверность для всего, что после.
- **Упростить создание и подключение стран в игру** (нужно для масштаба 200+ —
  все современные страны для сценария 2000, ещё больше для Викторианской эпохи).
  Архетипы экономики (`economyArchetypes.ts`, 2026-06-26) — первый шаг (страна почти
  не авторит экономику руками). Остаётся спроектировать: упрощённый ввод для
  военных/политических/дипломатических полей (сейчас каждая страна — полный
  ручной литерал, см. `server/src/data/countries/templates/CreateCountry.ts`),
  и сам пайплайн подключения новой страны в сценарий/реестр без правки кода вручную.

### Предложения по механикам (независимый гейм-дизайн разбор, 2026-07-06)

~~Детерминированный «бросок» мировых событий~~ — **реализовано частично, 2026-07-06**:
  только технологические вехи (пересечение тира домена → `GameState.pendingWorldFacts`,
  детекция в `SimulationEngine.ts`, рендер+очистка в `LLMService.generatePrompt()` §"Notable
  Developments This Month"). Экономический кризис/переворот как отдельные триггеры того же
  паттерна — не реализованы, нужны калиброванные пороги по реальным диапазонам
  `inflation`/`stability` — отдельный заход, не выдумывать числа без данных.

~~Градуированный мир~~ — **реализовано частично, 2026-07-06**: только денежное измерение —
  репарации 10% казны проигравшего победителю при решительной победе (`toAttackers`/
  `toDefenders` ≥2× друг друга, `WarService.makePeace`, константы `DECISIVE_FLIP_RATIO`/
  `REPARATIONS_SHARE`). Возврат территории ("status quo ante") — не реализован, требует
  хранить довоенных владельцев регионов, отдельная более крупная задача.

~~Combined-arms бонус~~ — **реализовано, 2026-07-06**: `getCombinedArmsMultiplier`
  (`shared/src/utils/technology.ts`) — минимальный тир среди `armor`/`infantry`/`aviation`/
  `naval` (эра 1946), не среднее — награда за широту, не за суммарный объём. Применяется в
  `WarTick.ts::sideStrength()`.

~~Мягкий потолок «на сколько фронтов сразу»~~ (admin capacity) — **реализовано частично,
  2026-07-06**: только потолок доли `research_shift` (`LLMResponseValidator.ts`,
  `getResearchShareCap` — минус 0.1 за каждую активную войну страны, пол 0.3). Ограничение
  количества `PlayerIntent`-действий за цикл — не реализовано (что делать с действиями сверх
  лимита — отдельное решение, не «быстрая» правка).

**Исторические развилки → legitimacy/stability напрямую** — не реализовано, требует сначала
  разобрать открытые вопросы черновика `docs/tasks/HISTORICAL_HINGE_POINTS_1946.md` (какие
  вехи, откуда факты) — не выдумывать без этого. `docs/POLITICS.md`.

## P4 — Инженерный долг (низкий приоритет, оппортунистически)
- route-слой тесты (нужен `supertest`, integration-style) — единственный незакрытый кусок тест-трека.
- тесты `Window.tsx` (drag/resize через DOM-события — инфра уже есть, `happy-dom` даёт `innerWidth`).
- Zustand: внедрить (заявлен обязательным в `AI_RULES.md`, в коде нет) или снять требование.
- **Ревизия старых доков `/docs`** (поднято 2026-07-04 при разборе `ECONOMY.md`): часть
  профильных доков писалась на раннем, поверхностном понимании проекта (до модели
  "ВВП-якорь + доли", до LLM-цикла, до многих решений в `DECISIONS.md`) и местами не
  отражает текущее устройство/приоритеты. Пройти по каждому файлу `/docs` и почистить
  от устаревшего/противоречащего коду, привести к текущему виду проекта — не переписывать
  вслепую, сверяться с кодом и `DECISIONS.md` по каждому спорному месту.

---

# Заморожено / отложено

## Интерфейс (заморожен пользователем 2026-06-23)
- **БАГ (открыт):** окна утаскиваются за край вьюпорта и теряются; кламп-попытка
  2026-06-23 не сработала (см. `DECISIONS.md`). При возврате — начинать с диагностики, почему.
- Esc-закрытие окна; сохранение позиций окон между перезапуском.
- `ResearchPanel`: хардкод списка технологий — упирается в tech tree (см. P3).
- Минорные оговорки по готовому интерфейсу: `BudgetPanel` без жёсткой блокировки при дефиците
  (осознанно — дефицит допустим); `ErrorBoundary` не проверен живым крашем; accessibility
  покрыта частично (панели — да, мировой рейтинг/табы — нет).


## Карта/регионы (заморожено пользователем 2026-07-03 — сначала симуляция)

- **БАГ (открыт, 2026-07-03): названия стран не отображаются вообще.** После
  мержа `gemini/ui-map-improvements` (движок подписей `GeometryEngine`,
  60 E2E-тестов зелёные) в живом приложении ни одной подписи страны. Тесты
  проверяют математику `buildCountryLabels`, не рендер — интеграция со слоем
  MapLibre не покрыта. При возврате начинать с диагностики слоя
  `country-labels` в `MapView.tsx`: доходит ли source до слоя, фильтр
  `appearZoom`, формула `text-size` (последний фикс c2363bf), загрузка глифов
  шрифта (CARTO CDN, `Open Sans Semibold`).
  **Уточнение (2026-07-06, обнаружено при живой проверке war-системы):** тот
  же движок подписей падает с `RangeError: Maximum call stack size exceeded`
  в `computeCountryAxis`/`buildCountryLabels` (`GeometryEngine.ts`), когда
  много регионов одной страны меняют `ownerCountryId` за один тик (война
  двигает фронт резче, чем движок ожидал) — ErrorBoundary ловит, приложение
  не падает насмерть, но карта перерисовывается с нуля. Не чинить попутно с
  war-задачами (карта заморожена) — фиксируется здесь как усиление
  существующего бага, не новый отдельный.
- **Найдено при войне (2026-07-06): `MapFeature` типы, кроме `capital`, визуально
  неотличимы друг от друга.** Слой `map-features-icons` в `MapView.tsx`
  проверяет только `type === 'capital'` → спрайт столицы, иначе всегда
  `city-icon` — `battalion`/`factory`/`port`/... рендерятся одинаковой
  дженерик-точкой. Функции `getIconForType`/`getColorForType` (эмодзи/цвет по
  типу) в файле есть, но ни один слой их не читает — мёртвый код, не
  подключён к рендеру. Не было заметно раньше (единственный часто создаваемый
  тип — `capital`/`city`). Пользователь решил (2026-07-06) не чинить сейчас —
  карта заморожена, война подтверждена через цвет региона на карте/API, не
  через иконку.

Ближний Восток 1946 — историческая точность владения (Иранский кризис →
`QAZ`/`QMH`, Иордания → брит. мандат, Аден → `QCG`, Сирия/Ливан/Суэц →
сфера влияния, `SOM_GBR` → `QSO`) **реализовано 2026-06-28**, см.
`docs/DECISIONS.md`.

### Следующий геометрический проход (отдельный блок, дороже)
- **Ближний Восток — пере-нарезать тоньше.** Источник `client/src/assets/
  game_map.json` (4596 провинций) на месте — текущий мердж слишком грубый
  (Турция 81→9, Сирия 15→10, Иордания 12→6). Турция/Сирия политически верно
  (Хатай у Турции), но имена/нарезка обманчивы («Sanliurfa» поглощает Хатай).
  Хатай — отдельным регионом.
- ARE (Trucial States) → 1 полигон вместо 7; схлопнуть переподробных миноров
  (DOM 19 регионов на 48k км², ARE, OMN, центральноамериканский кластер) —
  лучше через новое поле `tier` (см. ниже), не вручную страна за страной.
- **Баги данных:** Руанда (`RWA`) и Урунди (`BDI`) — одна страна 1946 под
  двумя кодами с одинаковым именем "Ruanda-Urundi", надо объединить.
  (Реализовано 2026-07-04: Курируемая привязка столиц к реальным городам и координатам для крупнейших держав: SUN, USA, GBR, FRA, DNK, CAN, BRA, ITA, JPN, CHN, TWN, AFG, EGY). Прочие
  спецзоны на будущее: Триест, Танжер, Саар, Кёнигсберг, Шпицберген.

### Визуал карты
Палитра держав (EU5/HOI-стиль), прозрачность границ, удаление устаревшей
кнопки "Карта 1946" — **реализовано 2026-06-28/29**, см. `docs/DECISIONS.md`.
Блокер выбора сценария (128 стран обрезали кнопку «Начать игру») —
**реализовано 2026-06-29** (прокрутка `.country-list`/`.scenario-selector`,
см. `docs/DECISIONS.md`). Курируемый шортлист держав вместо 128 кодов —
отдельно, после поля `tier` (см. ниже).

- **Динамическая смена цвета в рантайме и Режимы карты (крупная фича) — перенесено в бэклог**. Цвет как `f(состояние страны, режим карты) → цвет` на клиенте. Необходимые режимы:
  - *Политический* (по умолчанию): раскраска стран по их государственным цветам (уже есть, вассалы тонируются).
  - *Идеологический*: цвета по политическому режиму/идеологии (коммунизм — красный, либеральная демократия — синий, национализм — коричневый/желтый и т.д.).
  - *Дипломатический / Блоки*: цвета относительно игрока (союзники/вассалы — зеленые, соперники/враги — красные, нейтралы — серые).
  - *Экономический*: интенсивность цвета в зависимости от ВВП региона или ресурсов.
- **Поле `tier`** (важность страны) — даёт сразу три выигрыша: бюджет
  регионов при пере-нарезке геометрии, насыщенность цвета, стартовый
  шортлист держав (см. ниже). Состав major/regional согласовать с
  пользователем, не выдумывать.

### Прочее
- настоящая `isCoastalRegion()` в `generateMapFeatures.ts` (сейчас всегда false → порты не генерируются).
  **Зависимость:** блокирует морские десанты в war-системе (`docs/WAR.md`, Phase 1 явно пропускает —
  войны против заморских/островных целей без сухопутной границы не находят точку контакта). Механика
  готова к внедрению (навал-превосходство `navyStrength` открывает контакт в `WarTick.ts`, цель —
  ближайший прибрежный регион), не реализовывать плейсхолдером на `capitalRegionId` — ждать этой
  починки.
- `scripts/map/config/country_merge.json` — дефолтная группировка колоний по сюзерену
  (один блок на сюзерена), не финал — ручная доводка по географии ожидаема.
- `scripts/map/README.md` "Внешние источники" — нет прямых ссылок на загрузку
  geoBoundaries/китайских исторических шейпов, только имена файлов.
- `Country.name`/`shortName` ещё `string`, не `LocalizedText` (см. AGENTS.md
  "Design for Multi-Locale From the Start") — мигрировать вместе с 12
  рукописными файлами стран, когда дойдут руки.
- **Береговые линии не стыкуются** — полигоны суши/моря из MAP местами не
  совпадают по границе (видны рваные тёмные клинья на стыке суши и воды),
  заметно при приближении. Геометрическая проблема исходных данных MAP, не
  тривиальная правка — отдельная задача, не делать походом.
- **Австрия не поделена на зоны оккупации** — как Германия, реально была
  поделена на 4 зоны (+ Вена на 4 сектора) до 1955, но ни MAP, ни текущий
  импорт это не отражают (9 регионов под единым `AUT`). Нужен ручной оверлей
  по `region_id`, аналогичный `occupation_overlay.json` для Германии —
  распределение зон уточнить перед кодом, не угадывать.
- **Колониальные блоки — не реальная историческая структура.** Сейчас "1
  блок на сюзерена" (всё британское — единый QCG, от Карибов до Гонконга).
  Реально были признанные административные единицы (Французская Западная
  Африка/AOF, Французская Экваториальная Африка/AEF, Британская Вест-Индия
  и т.д.) — переделать `country_merge.json`/`generate_country_registry.py`
  на курируемую конфигурацию по реальным историческим федерациям, не
  авто-мердж по сюзерену.
- **Выбор страны** — список стран на старте (`ScenarioSelector.tsx`) уезжает
  за нижнюю границу экрана при 126 странах, только 3-буквенные коды без
  иерархии важности. Нужно: (а) curated-список "крупных держав" на стартовом
  экране + кнопка "показать все"/переход на полную карту, (б) выбор страны
  кликом по карте до старта игры. Требует нового поля типа `tier` в
  `Country` — не выдумывать набор держав без проверки с пользователем.
- **Границы регионов — один уровень, не зависят от зума.** Все границы
  (внутри страны и между странами) одной толщины/цвета всегда. Нужно: два
  слоя (внутренние тоньше/скрыты при малом zoom, внешние толще, всегда
  видны) + при выборе региона зум определяет, выбирается ли регион или вся
  страна (ниже порога — страна, выше — конкретный регион).
- **Флаги стран** — нет ассетов вообще. Нужен набор SVG по ISO/private-use
  кодам (готовые наборы типа `flag-icons` закрывают суверенов; для
  кастомных кодов — `QGS`/`QCG`/... — свои плейсхолдеры).
- **Текстурная подложка морей** — сейчас плоская заливка (границы между
  морями уже есть, см. `oceans-fill`/`oceans-outline` в `MapView.tsx`), но
  не "красивая". Нужен источник растрового тайлсета или процедурная текстура
  — отдельное исследование источника/лицензии.

---

# Готово (архив, кратко — детали в git и `DECISIONS.md`)

- **Движок:** тики Economy/Resource/Population/Military/Research/Diplomacy; агрегация регион→страна.
- **Сервисы:** GameService, SimulationService, CountryService, RegionService, DiplomacyService,
  RegionEconomyService, MapFeatureService, ResearchService, LLMService;
  zod-валидация, кастомные error types; роуты только валидируют и зовут сервис.
- **Игрок (backend + панели):** намерение свободным текстом, бюджет, исследования;
  BudgetPanel/ResearchPanel/PlayerIntentPanel (заменила декоративную ActionPanel, 2026-07-05).
- **Интерфейс:** оконный редизайн (2 итерации) — TopStatBar, InspectorPanel, Window, useWindows,
  ResourceTicker, WorldRankingPanel, TerritoriesPanel; ErrorBoundary; единый API-клиент.
- **Тесты:** сервер ~187 (тики + все сервисы + Diplomacy + LLM-слой); клиентская инфра
  (Vitest + RTL + happy-dom) + `useWindows` (14). Фикстуры в `server/src/test-utils`.
- **Техдолг закрыт:** удалены `player.ts`/`ProductionTick`; flip-flop союзов; `/budget`+`/actions` 404;
  sort-мутация в `LLMService`; `resourceBonus`.
- **Прочее:** переименование в Geopolis; `DECISIONS.md` (журнал); TS strict на клиенте.
