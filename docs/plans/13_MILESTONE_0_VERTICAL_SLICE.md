# 13 — Милстоун 0: вертикальный срез (ТЗ для сессии разработки)

Приоритет: P0 (первый милстоун реализации редизайна).
Статус: **сессия A (ядро) закрыта 2026-07-26**; **сессия B (LLM-путь + путь игрока + отклик)
закрыта 2026-07-26** — петля замкнута, см. «Итог сессии B» ниже. Доводки по внешнему аудиту и
последняя правка по решению пользователя (2026-07-27, пометка события + летопись по факту) —
`.agent/plans/milestone-0-loop.md`, разделы «Доводка B9»…«Доводка B11»; открытый хвост Милстоуна 1
названный там же — `docs/TODO.md`.
Источники истины: [`../CONCEPT.md`](../CONCEPT.md) §9 (срез), §7 (контракты), §4 (фундамент), §5.2 (политика);
[`../PRIMITIVES.md`](../PRIMITIVES.md) (алфавит); [`11_MVP_ROADMAP.md`](11_MVP_ROADMAP.md) (Милстоун 0).

> Это kickoff для отдельной **сессии разработки** (код). Дизайн принят; здесь — что реализовать,
> что доопределить первым, критерии готовности. Дизайн-цикл не влит в main — разработку стартовать
> от ветки `claude/design-partner-audit` (доки видны), либо после влития в main.

## Цель среза

Доказать **ядро** редизайна на одном кризисе через полный стек: LLM-режиссура из примитивов под
**движковым валидатором** работает на нетривиальной предпосылке (демо-дистанция «власть ↔ группа»,
а не голое число). Это самое рискованное место дизайна (§2 «новое сердце сложности»), поэтому срез
бьёт в него, а не в войну.

**Одна полная вертикаль, играбельная, узкая.** НЕ «все системы понемногу».

## Сценарий

Внутренний политический кризис в национальном регионе СССР, игрок за СССР. Глубина — **только цикл
недовольства** (без отделения/рождения государства — lifecycle-контракт §7.1 в срез НЕ тянем).

## Что доопределить ПЕРВЫМ (оставлено на реализацию — тестовый v0)

Дизайн намеренно не детерминирует эти числа; определить в начале разработки (мои стартовые
кандидаты — плейсхолдеры, калибровка на симуляции):

1. **Регион и демо-состав** (вручную, 1–2 региона):
   - Кандидат: **Литовская ССР** (`SUN_LT` или реальный id из `regions.core.json`) — литовцы ~80 %
     (доминант), русские/поляки — меньшинства. Исторический якорь: «лесные братья» 1944–53.
   - Опц. второй: Западная Украина (украинцы/поляки) — для проверки «соседи осмелели».
2. **Идеология-координаты** (§4.2, две оси экономическая × политическая):
   - СССР — крайне лево + авторитаризм.
   - Группа «литовцы» — «желаемая» позиция ближе к центру/национализму → **большая дистанция** →
     высокое недовольство.
3. **Формула недовольства** (§4.2 геометрия): `discontent(регион) = f(идеол_дистанция «власть↔группа»,
   благосостояние региона, история репрессий/уступок)`. Стартовый вид:
   `base + k1·distance + k2·(1 − welfare) − эффекты_примитивов`; `k1/k2/base/порог` — плейсхолдеры.
4. **Предпосылки ~5 примитивов** (численно): порог дистанции для `incite_unrest`; порог discontent
   для `spawn_incident`; требование контроля региона для `repress`/`grant_autonomy`; политическая
   цена для `enact_reform`.
5. **`incite_unrest` vs `stoke_discontent`** — выбрать по фактическому имени переменной состояния.

## Петля (реализовать)

1. Тики живут; движок растит `discontent` региона от демо-дистанции (§4.2) + экономики.
2. Порог → факт «кризис в регионе X» (механизм `pendingWorldFacts`).
3. LLM режиссирует событие из примитивов (`incite_unrest` + `spawn_incident` + нарратив); движок
   **валидирует предпосылку** (§3 PRIMITIVES: validate → compute → apply; reject целиком).
4. Игрок решает примитивом: `repress` / `grant_autonomy` / `enact_reform` (через текст → перевод, с
   показом распознанного, или кнопкой — гибрид-интерфейс §PRIMITIVES.1).
5. Движок детерминированно: `repress` → discontent ↓ кратко, отчуждение/дистанция ↑; `grant_autonomy`
   → ↓, но соседние группы осмелели; `enact_reform` → медленный сдвиг координат. → новое состояние →
   новое давление (петля). Отклик: хроника + история места (§5.6) + объект на карте.

## Реализовать НОВО

| Задача | Ссылка | Объём |
|---|---|---|
| Мини-демо-состав СССР + 1–2 нацрегиона (структура: доминант + меньшинства с долями) | §4.1 | S/M |
| Идеология-координаты (2 оси) для СССР + групп | §4.2 | S |
| Расчёт недовольства (геометрия дистанции + экономика) | §4.2 | M |
| Детекция порога → `pendingWorldFact` «кризис» | §5.2 | S |
| Мини-алфавит ~5 примитивов: `incite_unrest` / `repress` / `grant_autonomy` / `enact_reform` / `spawn_incident` | PRIMITIVES §2 | M |
| **Валидатор предпосылок + атомарное применение** (зачаток контракта §7.2) | §7, PRIMITIVES §3 | M/L |
| Путь игрок → примитив (намерение-текст → перевод → показ распознанного → подтверждение) | PRIMITIVES §1 | M |
| Отклик: локализованная хроника + история места + объект на карте | §5.6 | S/M |

## Переиспользовать (не строить с нуля)

Тики (`simulateMonth`), `PoliticsState`, `pendingWorldFacts`, `chronicle`, LLM-цикл (промт/ответ/гейт),
`MapFeatureService` CRUD. Многое в состоянии уже есть мёртвым кодом (§5.1/§5.2 концепта).

## Инженерные контракты (соблюдать с самого начала)

- **§7.2 атомарный pipeline (зачаток):** примитив = validate → compute на клоне → apply; нарратив
  пишется ТОЛЬКО после успешного применения; reject → диагностический факт (не ложный канон);
  idempotency на ход.
- **Агентность (§7):** LLM за игрока — предложением; игрок подтверждает (гибрид-интерфейс).
- **§7.1 lifecycle — НЕ в срезе** (отделение/распад не реализуем; страна не создаётся/не делится).
- **Числа — движок:** LLM даёт качественные params, движок считает magnitude (PRIMITIVES §1/§3).

## Вне среза (осознанно)

Война, море, дипломатия, глубокое производство, полный демо-состав (1366), полный алфавит (~18),
красивый UI, отделение-lifecycle, советник, проекты. Это последующие милстоуны (план 11).

## Итог сессии A (2026-07-26) — что закрыто и где стык

Закрыто (детали механики — `docs/POLITICS.md` §«Региональное недовольство»,
развилки — `docs/DECISIONS.md` 2026-07-26):

| Строка «Реализовать НОВО» | Где |
|---|---|
| Мини-демо-состав + идеология-координаты | `scripts/map/generate_demographics_1946.py` → `server/data/scenarios/1946/{groups,demographics,ideology}.json`; загрузка — `Scenario1946.ts` |
| Расчёт недовольства | `shared/src/utils/discontent.ts` + `shared/src/defines/discontent.ts` |
| Детекция порога → `pendingWorldFact` | `server/src/simulation/politics/DiscontentTick.ts` (типизированный факт `kind: "region_crisis"`) |
| Мини-алфавит 5 примитивов | `server/src/primitives/types.ts` |
| Валидатор предпосылок + атомарное применение | `server/src/primitives/PrimitiveEngine.ts`, палитра — `palette.ts` |

НЕ закрыто на момент конца сессии A (закрыто сессией B, см. раздел ниже): путь игрока
(намерение-текст → перевод → подтверждение), подключение примитивов к LLM-контракту,
отклик в UI (хроника, история места, объект карты на экране).

**Стык для сессии B** (исторический раздел — описывает состояние на конец сессии A;
что из этого сделано и как, см. «Итог сессии B»). Единственная точка входа —
`applyPrimitiveBatch(game, primitives): PrimitiveBatchResult` из
`server/src/primitives/PrimitiveEngine.ts`. Подключение:

1. Разобрать ответ модели `parsePrimitives(raw)` (`primitiveSchemas.ts`) — вернёт
   структурно валидные примитивы и список причин по битым. Схема `.strict()`, поэтому
   попытка модели задать величину числом (`"magnitude": 0.8`) — ошибка схемы, а не молча
   отброшенное поле.
2. Позвать `applyPrimitiveBatch`. Ничего не мутируется до финального commit'а; функция не
   бросает исключений.
3. Нарратив писать **только** по `result.applied` — там фактические `magnitude` и `summary`
   по каждому применённому примитиву. Это и есть «числа в событии правдивые»
   (`PRIMITIVES.md` §4).
4. `result.rejected` уже уложен в `game.pendingWorldFacts` с `kind: "primitive_rejected"` —
   отдельно дублировать не нужно, достаточно отрендерить факты в следующий промт.
5. Для промта: `regionDiscontent(game, region)` и `regionGroupDiscontent(...)`
   (`shared/src/utils/discontent.ts`) дают число и разложение по группам;
   `game.regionCrisisLatch` — список регионов в активном кризисе.

Что сессия B обязана взять с собой (детали — `docs/TODO.md`, «Милстоун 0 — хвосты
сессии A»): idempotency-key на ход (§7.2, движок применит батч столько раз, сколько его
позвали); кризисный кап ≤ ~5 (на первом же тике порог пересекают 9 регионов сразу);
калибровка коридоров магнитуды на живых прогонах.

Известное ограничение на стыке, которое сессии B придётся решить: сегодня
`LLMService.getNotableDevelopmentsInfo()` фильтрует `pendingWorldFacts` по странам, уже
видимым в промте, и **очищает весь массив** — факты по невидимым странам теряются.
Региональные кризисы наследуют это поведение; для среза (игрок за СССР, кризис в регионе
СССР) оно не мешает, но фильтр по `kind`/`regionId` — задача сессии B.

> Решено сессией B: факты `region_crisis` и `primitive_rejected` потребляются СВОИМИ
> секциями, которые считаются раньше «Notable Developments», поэтому фильтр видимости их
> больше не выбрасывает. Само поведение `getNotableDevelopmentsInfo` для остальных видов
> фактов не менялось — оно вне scope сессии B.

## Итог сессии B (2026-07-26) — что закрыто

Ветка `claude/milestone-0-loop`. Развилки и мотивировка — `docs/DECISIONS.md`
(запись «Милстоун 0, сессия B»); контракт промта — `docs/LLM_RULES.md`;
граница хода и путь игрока — `docs/PRIMITIVES.md` §1/§3.

| Строка «Реализовать НОВО» | Где |
|---|---|
| Путь игрок → примитив (текст → перевод → показ распознанного → подтверждение) | `server/src/llm/primitiveTranslation.ts`, `server/src/routes/primitives.ts`, `client/src/components/PrimitiveOrdersPanel.tsx` |
| Отклик: локализованная хроника + история места + объект на карте | `shared/src/types/politics/PrimitiveOutcome.ts`, `server/src/primitives/outcomes.ts`, `Region.placeHistory`, `client/src/hud/ContextPanel/ContextPanel.tsx`; объект карты — существующий слой `mapFeatures` |
| Подключение примитивов к LLM-контракту | `server/src/llm/{primitiveContract,primitiveAgency,crisisDigest}.ts`, `actionSchemas.ts`, `LLMService.ts` |
| Idempotency-ключ на ход (§7.2) | `server/src/primitives/turnBatch.ts`, `GameState.primitiveBatchKeys`, `SAVE_VERSION` 4→5 |
| Кризисный кап ≤5 (§7) | `MAX_PROMPT_CRISES`, `LLMService.getRegionalCrisesInfo` (выводится из латча) |

Ядро сессии A не изменялось: весь новый код — слой поверх `applyPrimitiveBatch`.

## Критерии готовности

Легенда: `[x]` — выполнено полностью; `[~]` — выполнено частично, невыполненный
остаток назван в тексте пункта.

- [x] Тики растят недовольство нацрегиона от демо-дистанции + экономики; guard-тест: разные
      координаты → разное недовольство. — `DiscontentTick.test.ts`, `campaignSmoke.test.ts`
- [x] Порог → `pendingWorldFact` «кризис в регионе X» срабатывает детерминированно. —
      типизированный `kind: "region_crisis"`, латч с гистерезисом
- [~] Ответ с примитивами проходит валидатор: валидные применяются, невалидные → reject целиком
      + диагностический факт; нарратив только после commit. — `PrimitiveEngine.test.ts`;
      подключение к LLM-ответу закрыто сессией B (`LLMService.primitives.test.ts`:
      применение из ответа модели, отказ схемы на числовой величине, диагностика в
      следующем промте и её потребление).
      **Поправка 2026-07-26 (внешний аудит):** половина «нарратив только после commit»
      была отмечена закрытой ошибочно — движок эту защиту держал (reject целиком, откат),
      а `LLMService` писал `title`/`descriptions` в `eventHistory` БЕЗУСЛОВНО, то есть
      отклонённое предложение становилось каноном. Закрыто по-настоящему: событие
      создаётся только если ответ ничего не предлагал (чистый нарратив) либо хоть что-то
      применилось, и несёт машиночитаемый результат (`Event.primitiveOutcomes` /
      `rejectedPrimitives`). Проверка — `milestone0Loop.test.ts` (полный отказ, устаревший
      ответ, чистый нарратив), `LLMPanel.test.tsx`.
      **Статус после решения пользователя 2026-07-27 — закрыто ЧАСТИЧНО, и часть названа.**
      Закрыто: событие несёт выведенную из фактов степень подтверждённости
      (`Event.factuality`), в летопись идёт только подтверждённое (у частично применённого —
      фактические следы вместо заголовка), игрок видит пометку. НЕ закрыто: сам ТЕКСТ при
      частичном применении по-прежнему пишется ДО применения и не переписывается по фактам —
      требует второго вызова модели, Милстоун 1 (`docs/TODO.md`). Формулировку «нарратив
      только после commit» считать выполненной по КАНОНИЗАЦИИ и по ДОЛГОЙ ПАМЯТИ, но не по
      содержанию прозы. Проверка — `ChronicleTick.test.ts`, `eventFactuality.test.ts`,
      `milestone0Loop.test.ts` («частично применённый ответ помечен», «летопись доносит
      применённое, а не заявленное»), `EventTimelinePanel.test.tsx`
- [x] Игрок отвечает примитивом; три ответа (`repress`/`grant_autonomy`/`enact_reform`) ведут в
      **различимо разное** состояние. — расхождение состояний доказано
      (`branchDivergence.test.ts`); интерфейс ответа — `PrimitiveOrdersPanel`
      (`PrimitiveOrdersPanel.test.tsx`), различимость веток НА ЭКРАНЕ —
      `ContextPanel.test.tsx` («три ветки дают три разных текста»: отчуждение против
      уступки против сдвинутой координаты курса)
- [x] Петля замыкается: новое состояние → новое давление; отклик виден (хроника + история места +
      объект карты). — сквозной тест на данных 1946 `server/src/__tests__/milestone0Loop.test.ts`:
      тик → 11 кризисов → кап 5 в промте → режиссура (0.632 → 0.728) → ответ игрока
      (0.728 → 0.575) → через 12 месяцев 0.702 (подавление выдохлось до 0.013,
      отчуждение 0.183 осталось). Отклик: объект карты `protest`, три записи истории
      места, локализуемый результат
- [x] Числа — только движок (LLM не задаёт magnitude). — три независимых замка:
      (1) `params` без числовых полей + Zod `.strict()` (тест «схема не принимает `magnitude`»);
      (2) величина выводится из состояния — `magnitude = MIN + (MAX − MIN) × stateFactor ×
      intensityPosition` (`server/src/primitives/magnitude.ts`), тесты «одинаковые `params`,
      разное состояние → разные числа» на каждый из пяти глаголов;
      (3) качественный хинт клампится коридором: при нулевом `stateFactor` `severe` равен
      `mild` (тест «без способности применить силу хинт перестаёт что-либо значить»).
      **Осталось:** сами коэффициенты коридоров — плейсхолдеры, живая калибровка после
      сессии B (`docs/TODO.md`, «Милстоун 0 — хвосты сессии A»)
- [x] Верификация зелёная по затронутому scope. — сессия A: server tsc + 760 тестов, client tsc +
      102 теста, python-валидаторы, public eval. Сессия B: server tsc + 57 файлов/913 тестов,
      client tsc + 12 файлов/117 тестов, `npm run build`, lint без новых ошибок
      (предсуществующий red в четырёх файлах — `docs/TODO.md`, P4). После доводок по внешнему
      аудиту и решения пользователя 2026-07-27: server tsc + 59 файлов/970 тестов, client tsc +
      14 файлов/130 тестов, `npm run build`, тот же предсуществующий lint-red

## Протокол разработки

- Отдельный worktree/ветка (`claude/milestone-0-slice` или аналог) — изменяющий агент = свой
  worktree, основной checkout не трогать.
- Стартовать от `claude/design-partner-audit` (доки редизайна) либо от main после влития.
- Проверки — `verify-change` по затронутому scope; docs footprint (обновить §статус милстоуна в
  плане 11 при завершении).
- Абсолютные пути worktree для файловых операций; `git -C <worktree>` для git (cwd между ходами
  соскальзывает в main checkout).
