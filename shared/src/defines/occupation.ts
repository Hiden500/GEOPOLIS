/**
 * Баланс-константы военной оккупации (docs/plans/08_WAR_WAVE1.md, Шаг 1).
 * Тюнингуемые стартовые ориентиры, не откалиброванный баланс.
 */

/** Доля обычной добычи региона, которую получает оккупант (штраф за оккупацию, не полная отдача). */
export const OCCUPATION_EXTRACTION_PENALTY = 0.5;

/**
 * Значение add-модификатора на Region.stability, пока регион оккупирован.
 *
 * ШКАЛА 0..1, как у самого поля (`shared/src/types/map/Region.ts`, фактический
 * разброс 0,184…0,809) — НЕ 0..100, как `country.politics.stability`. Прежнее
 * `-20` было записью в чужой конвенции и давало оккупированному региону
 * стабильность −19,57 (`.agent/audits/formula-audit-2026-07-30.md`, третий
 * экземпляр той же путаницы шкал). 0,2 — примерно четверть наблюдаемого
 * диапазона: тяжёлый, но не выводящий значение за пределы шкалы штраф.
 */
export const OCCUPATION_STABILITY_PENALTY = -0.2;
