/**
 * Баланс-константы PopulationTick.ts (docs/plans/03_MODIFIERS_COMMANDS.md,
 * Шаг 3) — план не называл их явно, но файл физически в периметре критерия
 * приёмки (server/src/simulation/**).
 */

export const BASE_BIRTH_RATE_PER_MONTH = 0.01;
export const BASE_DEATH_RATE_PER_MONTH = 0.005;

/** Потолок standardOfLiving (относительно GDP_PER_CAPITA_REFERENCE, см. countryMetrics.ts) — вдвое богаче ориентира не даёт больше бонуса. */
export const STANDARD_OF_LIVING_CAP = 2;

/**
 * Пол уровня жизни ТОЛЬКО в формуле рождаемости (демографический переход,
 * docs/TODO.md "Калибровка ВВП/чел бедных стран"). Формула привязывает
 * рождаемость положительно к богатству — это пост-трансформационное
 * отношение, обратное реальности 1946 года: бедные аграрные общества имели
 * ВЫСОКУЮ рождаемость (высокая фертильность + высокая смертность), а не
 * низкую. Без пола крайняя бедность (CHN ~$83/чел против ориентира $850)
 * искусственно загоняла рождаемость ниже смертности — страна теряла
 * население в мирное время. Пол = множитель, соответствующий 0.5×ориентира:
 * беднее этого фертильность не падает (аграрная норма). Смертность пол НЕ
 * трогает — она управляется медициной/стабильностью отдельно. Тюнингуемый
 * плейсхолдер; калибровка самих данных ВВП/чел бедных стран — отдельный
 * открытый вопрос (кандидат на историческую сверку Gemini).
 */
export const STANDARD_OF_LIVING_BIRTH_FLOOR = 0.5;

/** Бонус медицины (домен "biology") на единицу уровня технологии — снижает смертность. */
export const MEDICINE_TECH_BONUS_RATE = 0.1;

// --- множители формулы рождаемости региона ---
// Демографический переход (docs/TODO.md, калибровка бедных стран): базы подняты
// к нейтрали (≈1), чтобы БЕДНОСТЬ не подавляла рождаемость ниже аграрной нормы.
// Формула привязывает рождаемость положительно к богатству/образованию/welfare
// — обратно реальности 1946: бедные аграрные общества имели ВЫСОКУЮ фертильность
// (+ высокую смертность). Раньше низкие траты на образование/welfare (у бедной
// страны) множили рождаемость на 0.8/0.9 — тройное подавление вместе с низким
// уровнем жизни, из-за чего CHN терял население в мире. Теперь базы = 1.0:
// нищая страна не штрафуется по этим осям, а траты дают лишь МЯГКИЙ бонус
// (coeff × доля дохода, доли малы). Преимущество развитых стран в приросте идёт
// через НИЗКУЮ СМЕРТНОСТЬ (медицина/стабильность), а не через рождаемость.
// Все числа — тюнингуемые плейсхолдеры; калибровка данных ВВП/чел — домен Gemini.
export const BIRTH_RATE_LIVING_STANDARD_BASE = 0.5;
export const BIRTH_RATE_LIVING_STANDARD_COEFFICIENT = 0.3;
export const BIRTH_RATE_EDUCATION_BASE = 1.0;
export const BIRTH_RATE_EDUCATION_COEFFICIENT = 2;
export const BIRTH_RATE_WELFARE_BASE = 1.0;
export const BIRTH_RATE_WELFARE_COEFFICIENT = 2;
export const BIRTH_RATE_STABILITY_BASE = 0.8;
export const BIRTH_RATE_STABILITY_COEFFICIENT = 0.4;

// --- знаменатель формулы смертности региона ---
export const DEATH_RATE_STABILITY_BASE = 0.9;
export const DEATH_RATE_STABILITY_COEFFICIENT = 0.3;

/** Минимум населения региона — не даёт региону обнулиться. */
export const MIN_REGION_POPULATION = 1000;
