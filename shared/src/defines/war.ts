/**
 * Баланс-константы WarTick.ts (docs/plans/03_MODIFIERS_COMMANDS.md, Шаг 3).
 * Не путать с константами WarService.ts (services/, вне периметра этого
 * шага) — тот же класс баланса, разные файлы истории.
 */

/**
 * Сколько единиц эквивалентной стоимости даёт 1 единица боевой мощи
 * экипировки (shared/src/utils/equipment.ts) в масштабе, сопоставимом с
 * activePersonnel — War Phase 2, независимый гейм-дизайн разбор, 2026-07-06.
 * При нулевой технике (дефолт всех стран сейчас) вклад равен 0, не меняет
 * поведение Phase 1.
 */
export const EQUIPMENT_STRENGTH_WEIGHT = 500;

/**
 * Перевес силы над обороной, нужный для флипа контактного региона
 * (AI_RULES.md, "модель фронтов и операций", не 1:1 — оборона держится при
 * небольшом перевесе).
 *
 * С 2026-07-31 сравниваются ЛОКАЛЬНЫЕ мощности участка, а не глобальные силы
 * сторон (разбор — `WarTick.ts`), поэтому порог перестал быть переключателем
 * «весь фронт или ничего» и стал условием прорыва на конкретном участке.
 */
export const FLIP_THRESHOLD_RATIO = 1.5;

/**
 * ЛОКАЛЬНАЯ БОЕВАЯ МОЩЬ (2026-07-31): вклад самого региона в оборону участка,
 * помимо доли армии.
 *
 * ЗАЧЕМ. До этой правки условие флипа сравнивало глобальную силу стороны с
 * глобальной силой противника — выражение, в котором регион не участвовал
 * вовсе. Поэтому решение было одинаковым для ВСЕХ контактных регионов сразу:
 * фронт не двигался, он обваливался целиком. Замер на живом сценарии
 * (`server/scripts/probeWarFeedback.ts`): Италия против Франции теряла 18
 * регионов из 20 за шесть месяцев, Румыния против Югославии — все шесть, и
 * решала это разница в тысячу манпауэра на первом месяце. После обвала
 * контакта не остаётся, и 114 месяцев из 120 в войне не происходит ничего.
 *
 * ОТКУДА БЕРЁТСЯ НЕОДНОРОДНОСТЬ. Разделить силу поровну между фронтовыми
 * регионами мало: доля у всех участков стороны одинакова, и условие снова
 * становится общим. Различие даёт ГЕОМЕТРИЯ: давление на регион пропорционально
 * числу соседей, с которых по нему бьют, а оборона — доле армии плюс тому, что
 * даёт сам регион. Отсюда выступ, окружённый с трёх сторон, падает раньше
 * участка с одним контактом — фронт получает форму и движется постепенно.
 *
 * Вклад региона — его инфраструктура (снабжение и укрепления, шкала 0..1),
 * взвешенная этой константой относительно доли армии. 0,5 означает: полностью
 * развитый регион добавляет к обороне половину той силы, что даёт стоящая на
 * участке армия. Число тюнингуемое, как весь файл.
 */
export const REGION_DEFENSE_INFRASTRUCTURE_WEIGHT = 0.5;

/**
 * Потери войны (docs/plans/08_WAR_WAVE1.md, Шаг 3) — тюнингуемый плейсхолдер,
 * не финальная калибровка. Базовая месячная «интенсивность» одного
 * контактного (фронтового) региона: сколько человек уносит война на этом
 * участке за месяц. Общий помесячный урон = база × (число фронтовых регионов
 * обеих сторон), распределяется между сторонами по доле силы ПРОТИВНИКА
 * (слабейшая сторона теряет больше — facing a stronger enemy).
 *
 * Нюанс баланса: интенсивность привязана к участку фронта, НЕ пропорциональна
 * размеру армии. Поэтому маленькая армия быстро исчерпывается и потери
 * «переливаются» в гражданское население (Region.population) — тематически
 * это разгром малой армии с мобилизацией/жертвами среди гражданских, а не
 * баг. 10 000/мес на регион при типичном фронте в 2 региона даёт ~120 000/год
 * на страну — исторически правдоподобный порядок для крупной державы 1946.
 */
export const WAR_CASUALTY_BASE_PER_FRONT_REGION = 10_000;

/**
 * Потолок месячных ВОЕННЫХ потерь стороны — доля её активной армии.
 *
 * ЗАЧЕМ. Базовая интенсивность выше задана абсолютным числом на участок фронта,
 * и это осознанное допущение — но оно калибровалось «при типичном фронте в 2
 * региона», а фактическая геометрия сценария 1946 даёт 6–10 участков у пары
 * соседей. Замер (`server/scripts/probeWarFeedback.ts`, ROU против YUG): при
 * армиях 237 тыс. потери шли 60 тыс./мес, мобилизация даёт максимум 21 тыс./мес,
 * и обе армии обнулялись к шестому месяцу. Дальше воевали нули против нулей —
 * то самое состояние, из-за которого фронт и обваливался.
 *
 * Поднять стартовые армии не помогает: потери от их размера не зависят, поэтому
 * обнуляется любой масштаб — вопрос лишь в том, за сколько месяцев.
 *
 * ЧТО ПОТОЛОК НЕ ДЕЛАЕТ. Он ограничивает убыль АРМИИ, а не общий урон войны:
 * превышение по-прежнему уходит в `manpower` и население фронтовых регионов
 * (`distributeSideCasualties`). Тотальная война остаётся тотальной — просто
 * армия перестаёт исчезать быстрее, чем страна успевает мобилизовать.
 */
export const MAX_MONTHLY_ARMY_LOSS_SHARE = 0.15;

/**
 * warScore (docs/plans/08_WAR_WAVE1.md, Шаг 2a) — детерминированная оценка
 * «насколько решительно ведёт лидирующая сторона», −100..100 от лица
 * атакующих (+ = атакующие впереди). Тюнингуемые плейсхолдеры.
 *
 * Территориальная компонента: каждый нетто-флип оккупации (toAttackers −
 * toDefenders) даёт столько очков. ~7 чистых флипов = решительные 100.
 */
export const WARSCORE_PER_NET_FLIP = 15;

/**
 * Компонента потерь: относительное доминирование по потерям
 * (defenderCas − attackerCas)/(сумма) ∈ [−1,1], умноженное на этот вес.
 * Кто пролил меньше крови относительно врага — в плюсе. Максимум ±40 очков
 * от одних потерь, территория может добить до ±100 (итог капается).
 *
 * Нюанс баланса / отклонение от буквы плана: план называл третьим входом
 * «длительность (истощение тянет к миру)». warScore здесь — измеритель
 * ПРЕИМУЩЕСТВА (кто побеждает), а не готовности к миру; истощение от
 * длительности уже отыгрывается отдельной военной усталостью в
 * WarService.makePeace (STALEMATE_WEARINESS_*), не смешивается с сигналом
 * преимущества, иначе решительная, но долгая победа занижалась бы неверно.
 */
export const WARSCORE_CASUALTY_WEIGHT = 40;

/**
 * Пороги ярлыка исхода для промта LLM (warScoreLabel): |score| ≥
 * DECISIVE → «решительно побеждает», ≥ WINNING → «побеждает», иначе
 * «примерно равно». Тюнингуемые плейсхолдеры — грубая шкала, чтобы модель
 * предлагала соразмерный мир.
 */
export const WARSCORE_DECISIVE_THRESHOLD = 60;
export const WARSCORE_WINNING_THRESHOLD = 20;

/**
 * Мирное разрешение по warScore (docs/plans/08_WAR_WAVE1.md, Шаг 2b) —
 * тюнингуемые плейсхолдеры. Ниже порога аннексии |warScore| — белый мир: вся
 * оккупация снимается, границы не меняются. Выше — победитель аннексирует
 * оккупированные им регионы проигравшего, но не больше «бюджета очков»:
 * maxAnnex = floor(|warScore| / COST_PER_ANNEXED_REGION). Остальная оккупация
 * снимается. Так граница меняется только при решительном исходе и соразмерно
 * ему; LLM/игрок решает, КОГДА заключить мир, движок — СКОЛЬКО переходит.
 */
export const WARSCORE_ANNEXATION_THRESHOLD = 30;
export const WARSCORE_COST_PER_ANNEXED_REGION = 25;
