import { type PowerStructure } from "../types/politics/Government";

/**
 * Баланс-константы PoliticsTick.ts (docs/plans/03_MODIFIERS_COMMANDS.md,
 * Шаг 3) — план не называл их явно, но файл физически в периметре критерия
 * приёмки (server/src/simulation/**). politics.stability/legitimacy/
 * corruption/governmentSupport дрейфуют к структурному равновесию
 * (equilibrium), которое зависит от режима/экономики/друг друга — числа
 * ниже калибруют и равновесия, и скорость дрейфа к ним.
 *
 * Функции, читающие эти числа, — `shared/src/utils/politics.ts`.
 */

/**
 * Структурный базис коррупции по МЕХАНИЗМУ удержания власти — нижний потолок
 * без институциональных изменений.
 *
 * Ключуется по `politics.powerStructure`, а не по ярлыку идеологии (переведено
 * 2026-07-30, прежнее имя — `GOVERNMENT_TYPE_CORRUPTION_BASE`). Коррупция —
 * вопрос не о точке спектра, а о том, кто и через что распределяет ренту:
 * конкуренция партий и сменяемость власти её ограничивают, личный режим на ней
 * держится. Прежняя таблица знала 10 ярлыков, из которых в данных 1946
 * встречались 3, и 78 стран из 157 падали в дефолт.
 *
 * Тип — `Record<PowerStructure, number>`, а не `Record<string, number>`: новая
 * форма власти в `POWER_STRUCTURES` без записи здесь не скомпилируется. Именно
 * это отсутствовало у прежней таблицы и позволило ей молча разойтись с данными.
 */
export const POWER_STRUCTURE_CORRUPTION_BASE: Record<PowerStructure, number> = {
  /** Сменяемость власти и свободная пресса — сильнейший из доступных 1946 ограничителей. */
  competitive_multiparty: 20,
  /** Конкуренция есть, но ценз и цензура сужают круг спрашивающих. */
  limited_multiparty: 30,
  /** Оппозиция допущена, но не выигрывает — проверка власти формальна. */
  dominant_party: 38,
  /** Партия монопольна, зато дисциплинирует аппарат сверху. */
  one_party: 42,
  /** Мандат на человеке: лояльность покупается доступом к ренте — это и есть механизм. */
  personalist: 60,
  /** Армия как корпорация распределяет ресурсы вне гражданского контроля. */
  military: 55,
  /** Двор и патронаж: должность — часть личного пожалования. */
  dynastic_court: 50,
  /** Иерархия дисциплинирует, но подотчётности мирянам нет. */
  clerical: 40,
  /** Администрация извлекает ресурс и подотчётна метрополии, а не населению. */
  colonial_administration: 45,
  /** Временность плюс чёрный рынок разрушенной экономики. */
  occupation_administration: 50,
  /** Институты ещё не сложились, дележ идёт до правил. */
  provisional_coalition: 48,
  /** Единого центра нет — нет и того, кто спрашивает. */
  fragmented: 70,
};

/**
 * Легитимность как база: центр политической оси (см. ниже) без поправок.
 *
 * Заменила `IDEOLOGY_LEGITIMACY_BASE`, ключевавшуюся по строке
 * `politics.ideology` (2026-07-30). Модель базы — три слагаемых:
 *
 *   `LEGITIMACY_SPECTRUM_BASE`
 *     + `LEGITIMACY_DEMOCRATIC_MANDATE_WEIGHT` × political
 *     + `LEGITIMACY_CONSOLIDATION_WEIGHT` × |political|
 *     + `POWER_STRUCTURE_LEGITIMACY_MANDATE[powerStructure]`
 *
 * КРИВАЯ U-ОБРАЗНАЯ, А НЕ ЛИНЕЙНАЯ. Линейная («чем демократичнее, тем
 * легитимнее») означала бы, что любая диктатура нелегитимна по построению, и
 * СССР-победитель 1946 оказался бы ниже гибридных режимов. У консолидированного
 * полюса — любого — источник мандата ясен (выборы либо идеология и аппарат), у
 * режима посередине не работает ни один: это известная неустойчивость гибридов.
 * Наклон в пользу демократии остаётся, но он меньше вклада консолидации.
 *
 * ЭКОНОМИЧЕСКАЯ ОСЬ В БАЗУ НЕ ВХОДИТ НАМЕРЕННО. Она отвечает на вопрос «чья
 * собственность», а не «по какому праву правят»; включив её, движок заявил бы,
 * что плановая экономика сама по себе делает власть менее правомочной.
 */
export const LEGITIMACY_SPECTRUM_BASE = 45;

/** Наклон в пользу демократии: процедура обновляет мандат, автократия его донашивает. */
export const LEGITIMACY_DEMOCRATIC_MANDATE_WEIGHT = 8;

/** Вклад консолидации (|political|): у полюса мандат есть, у гибрида нет ни одного. */
export const LEGITIMACY_CONSOLIDATION_WEIGHT = 22;

/**
 * Поправка на ПРОИСХОЖДЕНИЕ власти. Координаты говорят, где режим стоит, но не
 * откуда взялось его право править, — а колониальная и оккупационная
 * администрации не имеют его у населения вовсе, каким бы либеральным ни был их
 * метрополийный оригинал. Без этой поправки временное правительство Франции
 * получало базу почти как Британия, а оккупированная Япония — как средняя
 * европейская страна.
 *
 * Ноль у форм, чью легитимность координаты уже описывают: их мандат — вопрос
 * положения на спектре, а не способа прихода к власти. Тип — полная
 * `Record<PowerStructure, number>` по той же причине, что и таблица коррупции.
 */
export const POWER_STRUCTURE_LEGITIMACY_MANDATE: Record<PowerStructure, number> = {
  /** Мандат описан координатами — выборы и есть источник права. */
  competitive_multiparty: 0,
  limited_multiparty: 0,
  dominant_party: 0,
  one_party: 0,
  /** Наследственное право — самостоятельный источник, не производный от спектра. */
  dynastic_court: 5,
  /** Религиозный мандат — тоже собственный источник. */
  clerical: 3,
  /** Право держится на человеке, а не на институте: уходит вместе с ним. */
  personalist: -5,
  /** Пришли силой, гражданского мандата не получали. */
  military: -8,
  /** Мандат заведомо временный — до учредительных выборов. */
  provisional_coalition: -10,
  /** Легитимировать нечего: единой власти нет. */
  fragmented: -12,
  /** Власть метрополии над чужим населением. */
  colonial_administration: -18,
  /** Власть победителя — минимум мандата из возможных. */
  occupation_administration: -22,
};

/** Утечка казны из-за коррупции: доля ВВП в месяц на единицу коррупции. При corruption=50, GDP=1 трлн → 2.5 млрд/мес утечки. */
export const CORRUPTION_TREASURY_DRAIN = 0.00005;

// --- corruptionEquilibrium ---
/**
 * Коррупция страны без размеченной формы власти (сценарии без `government.json`:
 * `powerStructure` опционален). Именно фолбэк, а не «средний режим»: страна с
 * разметкой берёт свой базис из `POWER_STRUCTURE_CORRUPTION_BASE`.
 */
export const CORRUPTION_EQUILIBRIUM_DEFAULT = 35;
export const CORRUPTION_LOW_STABILITY_THRESHOLD = 40;
export const CORRUPTION_LOW_STABILITY_PENALTY = 15;
export const CORRUPTION_HIGH_STABILITY_THRESHOLD = 70;
export const CORRUPTION_HIGH_STABILITY_ADJUSTMENT = -5;
/** Низкие расходы на образование (доля дохода) → слабые институты → рост коррупции. */
export const CORRUPTION_LOW_EDUCATION_SHARE_THRESHOLD = 0.05;
export const CORRUPTION_LOW_EDUCATION_PENALTY = 10;

// --- stabilityEquilibrium ---
/**
 * ПОЧЕМУ ЗДЕСЬ НЕТ ПОРОГОВ (переведено 2026-08-01).
 *
 * Прежняя модель складывала равновесие из четырёх пороговых ступеней над
 * экономикой: безработица <5 → +15, бюджет ≥0 → +5, инфляция >20 → −5,
 * коррупция >60 → −10. Замер живого сценария (`server/scripts/
 * probeStabilityEquilibrium.ts`) показал, что мир лежит ЦЕЛИКОМ по одну сторону
 * трёх порогов из четырёх: безработица 0,92…4,92 при пороге 5, инфляция
 * 1,82…3,15 при пороге 20, бюджет в профиците у всех 157 стран. Итог —
 * 153 страны из 157 на старте и 156 из 157 через два года получали ровно одно
 * и то же число 70; различных значений во всём мире 3 и 2 соответственно.
 *
 * Ступень над узким диапазоном — это константа. Порог заявляет «здесь
 * качественный скачок» и обязан попадать в реальный разброс; иначе он выдаёт
 * один ответ всем и молчит об этом, потому что фикстурному тесту видно только
 * «функция вернула ожидаемое на подготовленном примере».
 *
 * Поэтому каждый экономический вход отвечает НЕПРЕРЫВНО: отклонение от опорной
 * точки, поделённое на масштаб насыщения и зажатое в [-1, +1]. Такая форма
 * различает страны при ЛЮБОМ наполнении данных — в том числе при узком, — а
 * прежние пороги живут дальше как точки насыщения, сохранив смысл «дальше уже
 * некуда хуже».
 *
 * КАЛИБРОВКА ВЕСОВ — ОТ ДВУХ ТРЕБОВАНИЙ СРАЗУ, а не от вида кривой.
 *
 * 1. На ЖИВЫХ данных различение задаёт якорь. Мир 1946 однороден по экономике
 *    (безработица укладывается в 4 пункта, инфляция в 1,3), поэтому отклики
 *    держатся далеко от насыщения и вносят sd порядка 7 пунктов против sd 12 у
 *    якоря: партия начинается со сценария, а не с экономической ряби.
 *
 * 2. В ПРЕДЕЛЕ экономика обязана обрушить любую страну. Сумма весов ниже — 72
 *    из 100, то есть даже самый крепкий якорь сценария (80,9 у микрогосударств)
 *    при полном крахе — безработица за 15%, инфляция за 20, дефицит в половину
 *    дохода, нулевой мандат, предельная коррупция — уходит ниже кризисного
 *    порога `POLITICAL_CRISIS_STABILITY_THRESHOLD`. С весами вдвое меньшими
 *    этого не происходило: стабильная страна получала вечный пол по сценарию и
 *    не могла рухнуть НИ ОТ ЧЕГО, потому что `politicsTick` — единственный
 *    писатель `politics.stability` во всём сервере (ни война, ни восстание, ни
 *    оккупация её не двигают, `.agent/audits/formula-audit-2026-07-30.md`).
 */

/**
 * Нейтральная точка шкалы и фолбэк структурного якоря для страны без регионов
 * (`stabilityBase`). Именно «не размечено», а не «средняя страна»: страна с
 * регионами берёт якорь из сценарных `region.stability`.
 */
export const STABILITY_EQUILIBRIUM_DEFAULT = 50;

/**
 * Вес структурного якоря — авторской стабильности сценария (население-взвешенная
 * `region.stability`, шкала 0..1) в шкале политики 0..100.
 *
 * ЗАЧЕМ ЯКОРЬ. `region.stability` данных 1946 генерируется из СТРАНОВОЙ таблицы
 * «политическая ситуация на январь 1946» (`scripts/map/
 * fill_region_economy_1946.py`, `_DEV_TIERS`: США 0,62, СССР 0,55, Британия и
 * Япония 0,42, Китай 0,32, Афганистан 0,20) с поправкой по тиру региона и
 * детерминированным джиттером ±8%. То есть авторская оценка страновой
 * стабильности в данных уже есть — 155 различных значений на 157 стран, разброс
 * 0,193…0,809 — и страновая политика её не читала вовсе, а `docs/MAP_FEATURES.md`
 * называет эту связь «естественной, не реализованной». Тот же дефект, что был у
 * легитимности и коррупции до перевода на структурные базисы: живой авторский
 * вход существует, механика вместо него берёт литерал.
 *
 * Множитель 100, а не подогнанный масштаб: шкалы 0..1 и 0..100 — одна и та же
 * шкала, и сохранение стартового состояния сценария означает буквальное
 * соответствие, а не сжатие к середине.
 */
export const STABILITY_REGION_ANCHOR_SCALE = 100;

/**
 * «Естественный» уровень безработицы: точка, в которой она не двигает равновесие
 * ни вверх, ни вниз. Бывший `STABILITY_LOW_UNEMPLOYMENT_THRESHOLD` — то же число
 * и тот же смысл «ниже этого хорошо», но теперь граница знака, а не ступень.
 */
export const STABILITY_UNEMPLOYMENT_REFERENCE = 5;

/**
 * Насколько пунктов безработицы сверх опорной точки отклик доходит до предела.
 * Насыщение ровно на прежнем `STABILITY_HIGH_UNEMPLOYMENT_THRESHOLD` = 15:
 * «дальше уже некуда хуже» — сохранённый смысл старого порога.
 */
export const STABILITY_UNEMPLOYMENT_SATURATION = 10;

/** Полный вес безработицы в равновесии при насыщении отклика. */
export const STABILITY_UNEMPLOYMENT_WEIGHT = 20;

/**
 * Доля ДОХОДА, на которой бюджетный отклик достигает предела в обе стороны.
 *
 * ПОЧЕМУ ОТ ДОХОДА, А НЕ ОТ ВВП. Прежний `STABILITY_SEVERE_DEFICIT_GDP_SHARE`
 * = 0,05 нормировал баланс к ВВП, и живой мир упирался в предел ВЕСЬ: замер
 * даёт баланс 4,6…6,7% ВВП на старте и 1,3…9,2% через два года — то есть
 * отклик, посчитанный от ВВП, выдавал бы всем один и тот же максимум, повторяя
 * в мелком масштабе ровно тот дефект, который эта правка закрывает.
 *
 * Доля дохода — величина безразмерная и устойчивая: политическое значение имеет
 * не размер бюджета относительно экономики, а сводит ли правительство концы с
 * концами. Порог в долях ВВП вдобавок означал бы разную строгость к разным
 * режимам — плановая экономика проводит через бюджет несравнимо большую долю
 * ВВП, чем рыночная. Живой разброс баланса к доходу — 0,13…0,35 на старте и
 * −0,03…0,43 через два года, то есть при насыщении 0,5 отклик различает страны
 * и не упирается.
 */
export const STABILITY_BUDGET_SATURATION_INCOME_SHARE = 0.5;

/** Полный вес бюджетного баланса при насыщении отклика. */
export const STABILITY_BUDGET_WEIGHT = 12;

/** Инфляция, при которой она ещё не давит на равновесие. */
export const STABILITY_INFLATION_REFERENCE = 2;

/**
 * Порог «высокой инфляции». Больше не ступень равновесия, а точка насыщения
 * инфляционного отклика И порог факта «экономический кризис»
 * (`SimulationEngine.ts::pendingWorldFacts`) — одно число на оба смысла, как и
 * было задумано, когда факт кризиса вводился.
 */
export const STABILITY_HIGH_INFLATION_THRESHOLD = 20;

/** Полный вес инфляции при насыщении отклика (односторонний: вниз). */
export const STABILITY_INFLATION_WEIGHT = 18;

/**
 * Вес коррупции: отклонение от середины шкалы политики, полный вклад на её
 * краях. Бывший порог 60 со ступенью −10 срабатывал ровно у ОДНОЙ страны мира
 * (`fragmented`, базис 70) — остальные 156 лежали ниже.
 */
export const STABILITY_CORRUPTION_WEIGHT = 10;

/**
 * Вес легитимности: мандат власти удерживает порядок дешевле принуждения.
 * Новый вход равновесия — до 2026-08-01 стабильность не зависела от
 * легитимности вовсе, хотя обратная связь есть (`coerciveCapacity` в
 * `server/src/primitives/magnitude.ts` читает обе), и получалась односторонняя
 * стрелка. Живой разброс легитимности после посева из структурных базисов —
 * 23,0…72,6 при 92 различных значениях на 157 стран.
 */
export const STABILITY_LEGITIMACY_WEIGHT = 12;

// --- governmentSupportEquilibrium ---
/**
 * ПОЧЕМУ ЗДЕСЬ НЕТ ПОРОГОВ (переведено 2026-08-03, решение пользователя
 * 2026-08-02: оживить, не удалять). Тот же класс дефекта, что закрыли у
 * равновесия стабильности 2026-08-01, и лечение той же формой.
 *
 * Прежняя модель складывала равновесие из ступеней: безработица <5 → +10,
 * >15 → −10, дефицит → −5, «щедрый welfare» → +5. Замер живого сценария
 * (`server/scripts/probeGovernmentSupport.ts`) после двустороннего Правила A
 * (2026-08-02): на старте 2 различных значения на 157 стран (модальное 65 у
 * 154), к 60-му месяцу — РОВНО ОДНО (65,00 у всех 157). Вдобавок welfare-вход
 * сравнивал ДЕНЬГИ с ДОЛЕЙ: `spendingFloor` со сменой шкалы 2026-08-01 стал
 * долей дохода, а `welfareSpending` остался суммой — «щедрость» была истинной
 * у любой страны с ненулевым полом.
 *
 * Теперь каждый вход отвечает непрерывно (`response()`), а веса выбраны от
 * живого распределения входов (тот же probe, 0/12/60/120 месяцев):
 *
 * - недовольство регионов: 157 различных из 157, sd 0,13 при шкале 0..1,
 *   разброс устойчив на всём горизонте — главный статический различитель;
 * - бюджет/доход: 157 различных, sd 0,03…0,10 — после двустороннего Правила A
 *   бывает и дефицитным (мин −0,33 на 12-м месяце);
 * - welfare/пол: 122…141 различных, разброс 1,5…5,0 пола;
 * - безработица: 157 различных, но узко (sd 0,11…1,24) — вклад мал в мирном
 *   мире и полон при крахе;
 * - зазор равновесия стабильности: 157 различных, затухает к покою (sd 6,2 на
 *   12-м месяце → 0,16 на 120-м) — это ДИНАМИЧЕСКИЙ вход «население винит /
 *   хвалит власть, пока стабильность движется», в покое он и должен гаснуть;
 * - оккупация своей территории: в мирном прогоне ноль у всех — вход войны,
 *   оправдан дизайном (see WAR.md), не распределением мирного времени.
 *
 * В пределе краха (безработица, дефицит, welfare у пола, недовольство 1,
 * рушащаяся стабильность, половина населения под оккупацией) сумма откликов
 * −75 уводит равновесие с якоря 50 в 0: правительство в катастрофе теряет
 * рейтинг ПОЛНОСТЬЮ, а не до «65 минус пара ступеней».
 *
 * Референсы безработицы и бюджета переиспользованы из блока стабильности
 * (`STABILITY_UNEMPLOYMENT_REFERENCE/SATURATION`,
 * `STABILITY_BUDGET_SATURATION_INCOME_SHARE`): «естественная безработица» и
 * «сводит ли бюджет концы с концами» — факты мира, не ручки одной формулы.
 */
export const GOV_SUPPORT_EQUILIBRIUM_DEFAULT = 50;

/** Полный вес безработицы в равновесии рейтинга при насыщении отклика. */
export const GOV_SUPPORT_UNEMPLOYMENT_WEIGHT = 10;

/** Полный вес бюджетного баланса (доля дохода, симметрично) при насыщении. */
export const GOV_SUPPORT_BUDGET_WEIGHT = 10;

/**
 * Нейтральная точка welfare-щедрости: доля welfare в доходе, делённая на пол
 * Правила A. Пол — ПОЛОВИНА стартовой авторской доли, поэтому 2 = «страна
 * тратит свою стартовую долю»: урезание к полу штрафуется, щедрость сверх
 * старта вознаграждается.
 */
export const GOV_SUPPORT_WELFARE_REFERENCE_FLOOR_RATIO = 2;

/**
 * Ширина насыщения welfare-отклика в единицах пола: на самом полу (ratio 1)
 * отклик достигает предела вниз, на 1,5 стартовой доли (ratio 3) — вверх.
 */
export const GOV_SUPPORT_WELFARE_SATURATION_FLOOR_RATIO = 1;

/** Полный вес welfare-щедрости при насыщении отклика. */
export const GOV_SUPPORT_WELFARE_WEIGHT = 8;

/**
 * Нейтральная точка и насыщение недовольства регионов (шкала 0..1): отклонение
 * от середины шкалы, полный вклад на её краях — та же трактовка середины, что
 * у легитимности и коррупции в равновесии стабильности.
 */
export const GOV_SUPPORT_DISCONTENT_REFERENCE = 0.5;
export const GOV_SUPPORT_DISCONTENT_SATURATION = 0.5;

/** Полный вес недовольства регионов — главный статический различитель. */
export const GOV_SUPPORT_DISCONTENT_WEIGHT = 20;

/**
 * Насыщение отклика на ДИНАМИКУ стабильности: зазор «равновесие − текущая» в
 * пунктах шкалы, при котором население полностью винит (падение) или хвалит
 * (рост) правительство. Живой зазор на старте −18…+18, в покое → 0.
 */
export const GOV_SUPPORT_STABILITY_TREND_SATURATION = 10;

/** Полный вес динамики стабильности при насыщении отклика. */
export const GOV_SUPPORT_STABILITY_TREND_WEIGHT = 12;

/**
 * Доля населения под чужой оккупацией, при которой военный отклик насыщается:
 * половина страны под оккупантом — «дальше винить уже некуда». Односторонний
 * (только вниз): отсутствие войны на своей земле — норма, не заслуга.
 */
export const GOV_SUPPORT_OCCUPATION_SATURATION_POP_SHARE = 0.5;

/** Полный вес войны на своей территории при насыщении отклика. */
export const GOV_SUPPORT_OCCUPATION_WEIGHT = 15;

// --- legitimacyBase ---
// Отдельного фолбэка у легитимности нет — и не нужен: координаты резолвятся
// всегда (страна без явных получает их по ярлыку либо центр спектра,
// `resolveIdeologyCoordinates`), а центр спектра и есть база
// `LEGITIMACY_SPECTRUM_BASE`. Прежняя `LEGITIMACY_DEFAULT = 55` удалена вместе
// с таблицей по ярлыку: она означала «ярлык неизвестен», а неизвестных ярлыков
// больше нет — есть известная точка спектра.

// --- скорость дрейфа к равновесию (politicsTick) ---
export const CORRUPTION_DRIFT_RATE = 0.003;
export const STABILITY_DRIFT_RATE = 0.05;
export const GOV_SUPPORT_DRIFT_RATE = 0.08;
export const LEGITIMACY_DRIFT_RATE = 0.005;

/**
 * Порог кризисного (не просто "низкого") stability — детекция
 * политического кризиса/переворота (SimulationEngine.ts::pendingWorldFacts,
 * "того же паттерна", что пересечение тира технологий, 2026-07-06). Ниже
 * уже существующего "низкого" порога (STABILITY_LOW=40, shared/defines/ai.ts,
 * управляет рутинным AI-нуджем Правила C) — кризис-факт реже рутинной
 * подстройки бюджета, не дублирует её частоту.
 */
export const POLITICAL_CRISIS_STABILITY_THRESHOLD = 20;
