/**
 * Пределы магнитуды последствий LLM-действий (docs/plans/02_LLM_CONTRACT.md).
 * Тюнингуемые константы — балансировать на симуляции, как THREAT-пороги в
 * AiBehaviorTick. Движок выставляет пределы, LLM работает внутри них
 * (docs/LLM_RULES.md): ответ с выходом за предел — ошибка данных LLM, а не
 * «сильное событие». Единый источник для валидации (actionSchemas.ts) и
 * текста промта (LLMService.ts) — раньше жили в server/src/llm/
 * LLMResponseValidator.ts, перенесены сюда правилом 4 конституции
 * (MASTER_PROMPT.md).
 */
export const MAX_ACTIONS_PER_RESPONSE = 20;

/**
 * Шаг влияния действия `influence` — величина ДВИЖКА, а не модели
 * (Милстоун 1, дипломатический блок алфавита).
 *
 * `MAX_RELATION_CHANGE` и `MAX_INFLUENCE_CHANGE` удалены вместе со своими
 * действиями. Они были капами на число, которое присылала МОДЕЛЬ, — то есть
 * описывали контракт, прямо противоречащий правилу «модель решает что, движок
 * решает насколько» (`docs/PRIMITIVES.md` §1). Отношения переехали в примитив
 * `diplomacy` с коридором от состояния пары; у влияния поле снято, а прежнее
 * значение по умолчанию (10) стало единственным.
 */
export const INFLUENCE_STEP = 10;

/**
 * Потолок доли researchSpending на один домен за один сдвиг фокуса
 * (docs/DECISIONS.md, 2026-07-06) — остаток делят поровну домены без явной
 * доли (ResearchTick.ts). Не 1.0 — сфокусированность не бесплатна, остальные
 * направления всё равно получают что-то. Это потолок мирного времени —
 * см. WAR_RESEARCH_SHARE_PENALTY ниже про снижение на активную войну.
 */
export const MAX_RESEARCH_SHARE = 0.7;

/**
 * Admin capacity (независимый гейм-дизайн разбор, 2026-07-06): правительство,
 * воюющее на нескольких фронтах, отвлечено — потолок доли research_shift
 * снижается на эту величину за каждую активную войну страны (не только
 * инициированную ею — оборона тоже отвлекает). Применяется одинаково ко
 * всем странам, включая игрока — не спец-правило для ИИ. Это динамическая
 * поправка к MAX_RESEARCH_SHARE — не выражается в статической Zod-схеме
 * (та не знает о game.wars конкретной партии), остаётся проверкой в
 * server/src/llm/LLMResponseValidator.ts::getResearchShareCap.
 */
export const WAR_RESEARCH_SHARE_PENALTY = 0.1;

/** Пол потолка research_shift — даже страна на нескольких фронтах не теряет фокус целиком. */
export const MIN_RESEARCH_SHARE_CAP = 0.3;

/**
 * Потолок доли militarySpending на одну категорию техники за один сдвиг
 * фокуса (War Phase 2, независимый гейм-дизайн разбор, 2026-07-06) — тот же
 * принцип, что MAX_RESEARCH_SHARE. Плоский, БЕЗ снижения за активную войну
 * (в отличие от research_shift) — война логически повышает фокус на
 * производстве, а не распыляет его, поэтому admin-capacity штраф сюда не
 * переносится.
 */
export const MAX_PRODUCTION_SHARE = 0.7;
