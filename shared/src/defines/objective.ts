/**
 * Баланс-константы целевого слоя (docs/OBJECTIVES.md, план 11 категория B) —
 * тюнингуемые плейсхолдеры, не финальная калибровка.
 *
 * Индекс национальной силы `getNationalPower` (shared/src/utils/nationalPower.ts)
 * — взвешенная сумма нормализованных компонент. Масштабы приводят разнородные
 * величины (ВВП в миллиардах, армия в сотнях тысяч, население в миллионах) к
 * сопоставимому вкладу; веса задают их относительную важность. Абсолютное
 * значение не важно — важен порядок стран (ранг).
 */

/** Делители нормализации компонент (приводят к единицам ~одного порядка). */
export const POWER_GDP_SCALE = 1_000_000_000; // ВВП → миллиарды
export const POWER_MILITARY_SCALE = 100_000; // сила армии → сотни тысяч
export const POWER_POPULATION_SCALE = 1_000_000; // население → миллионы

/** Веса компонент силы (ВВП доминирует — как реальная мера мощи державы). */
export const POWER_WEIGHT_GDP = 1.0;
export const POWER_WEIGHT_MILITARY = 0.5;
export const POWER_WEIGHT_POPULATION = 0.3;
export const POWER_WEIGHT_TECHNOLOGY = 2.0; // за суммарный тир технологий (число малое)
