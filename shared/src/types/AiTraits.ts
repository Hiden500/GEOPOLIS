/**
 * Вариативность характера ИИ-страны (docs/AI_RULES.md §"Искусственный
 * интеллект стран") — посеянные один раз при createGame случайные константы
 * (shared/src/utils/rng.ts, seeded, не Math.random), смещающие пороги
 * детерминированных правил AiBehaviorTick.ts. Не реальный ИИ — дешёвая
 * иллюзия индивидуальности поверх одних и тех же формул.
 *
 * ЧИТАТЕЛЕЙ НЕ ОСТАЛОСЬ (2026-07-31). Оба поля читало только Правило D, и
 * вместе с ним они осиротели: значения по-прежнему сеются, но ни на что не
 * влияют. Структура НЕ удалена намеренно, и причина не в лени: посев
 * продвигает состояние `game.rngState`, поэтому его снятие сдвинуло бы все
 * последующие случайные величины партии — тихое изменение поведения, которое
 * нечем проверить в рамках задачи об удалении войны. Решение (найти нового
 * потребителя или убрать вместе с ростом SAVE_VERSION) — в docs/TODO.md.
 */
export interface AiTraits {
  /** Был множителем порога объявления войны (Правило D, удалено). Сейчас не читается. */
  aggressiveness: number;

  /** Был множителем требуемого манпауэр-перевеса (Правило D, удалено). Сейчас не читается. */
  riskTolerance: number;
}
