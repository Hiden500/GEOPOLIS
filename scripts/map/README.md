# `scripts/map` — pipeline сценария 1946

Pipeline формирует геометрию, ownership, соседство, имена, страновой registry
и региональную экономику для сценария 1946. Фактические шаги определяет
`make_1946.py`; пути — `build/paths.py`.

## Чек-лист перед/после любой правки геометрии

Собрано из двух раундов hard-won багов (изначальная сборка карты + правка
Палестины/Ливана/ОАЭ 2026-07-19, `docs/DECISIONS.md`) — не переоткрывать
одни и те же грабли:

1. **Визуальный/числовой рендер обязателен** после любого изменения
   прибрежных или крупных внутренних границ. `invalid + overlaps` НЕ ловит
   "раздутие в океан"/"перекрытие через воду" — сравнивай площадь
   результата с реальной (аномалия >2-3× от ожидаемого — сигнал раньше
   визуального осмотра).
2. **Проверка разрывности** — после любой правки, затрагивающей регион,
   проверяй `geom_type == MultiPolygon` с крупным вторым куском (>5% от
   первого по площади). Не доверяй совпадению имён при группировке —
   особенно пустым/дублирующимся ключам.
3. **`distance == 0` не гарантирует настоящего касания** после
   difference/buffer/union — после любой такой цепочки операций явно
   проверяй `touches()`/`intersects()` со смежными регионами, не просто
   малую дистанцию.
4. **Один авторитетный файл** — не работать с двумя копиями одного пути.
5. **Любой постпроцессинг встраивать в сам pipeline** (вызов внутри
   `build_*.py::main()`, не разовый скрипт "запустить один раз") — то, что
   не часть воспроизводимой цепочки, гарантированно теряется при следующей
   пересборке. Пример: `clip_palestine_to_neighbors()` в
   `build_asia_1946.py` — вызывается из `main()`, не отдельно.
6. **`ownership_1946.json`/`names_ru.json` — позиционно хрупкие, не
   регенерируются репозиторием.** Изменение числа выходных регионов ЛЮБОЙ
   страны ЛЮБОГО континента сдвигает нумерацию всего, что идёт после неё в
   порядке сборки (спец-блоки вроде Китая/Палестины стоят в начале — сдвиг
   не ограничен алфавитно-более-поздними странами). Не ловится тестами
   (population-по-anchor сверяет только сумму, не распределение). Уже
   ловило Азию дважды (2026-07-19-c, потом снова -i) И Европу дважды
   (2026-07-19-h, потом снова -i, второй раз из-за Кипра) — не считай
   проблему закрытой после одной починки одного континента, она повторится
   при следующем изменении числа регионов там же. После любой такой правки — обязательно
   `python scripts/map/build/remap_region_ids.py --old <снимок
   world_1946.geojson до правки> --prefix <XXX-> --apply` (см. докстринг
   скрипта), затем вручную добавить полностью новые регионы (0 кандидатов
   в dry-run), которых в старом снимке не было вовсе. Третья, отдельная
   позиционно-хрупкая точка — `CAPITAL_REGION_OVERRIDES` (хардкод-словарь в
   `generate_country_registry.py`, не файл) — уже известная/отложенная
   категория `validate_region_economy_1946.py` ("~31-33 отложенных
   capitalRegionId"), не чинить попутно с несвязанной правкой геометрии.
7. **Явный override-механизм для county/ADM2-based spatial join** — когда
   геометрия исходника расходится с документированным историческим фактом
   (пример: `COUNTY_OVERRIDES` в `build_china_1946_v2.py`), не полагаться
   на автоматику молча.
8. **Числовые проверки (invalid/overlaps/distance) не заменяют визуальный
   осмотр** (2026-07-19, второй раунд — пользователь поймал разрыв
   Газа-Синай и границы, заходящие на Мёртвое море, ПОСЛЕ того, как
   числовые проверки уже "прошли чисто"). После любой правки рендери
   ТОЛЬКО конкретный регион под правкой + непосредственных соседей (не весь
   мир — потеряешь деталь) через отдельный matplotlib-скрипт
   (`scripts/map/build/paths.py` + `shapely`/`matplotlib`, PNG в scratchpad,
   смотреть через Read) — Browser pane ненадёжен для этого. Числовые
   проверки — фильтр ДО рендера, не замена ему.
9. **Разрыв между странами из РАЗНЫХ continent-скриптов не ловится
   `clip_*_to_neighbors()` внутри одного скрипта** — `build_asia_1946.py`
   строится ДО `build_africa_1946.py` (см. `make_1946.py`,
   `FULL_REBUILD_STEPS`), поэтому на момент клипа Палестины по соседям
   Египет физически ещё не существует. Межконтинентальные швы (пример:
   Газа/Беэр-Шева ↔ Синай) чинятся ОТДЕЛЬНЫМ шагом ПОСЛЕ обоих continent-
   скриптов (`fill_palestine_egypt_gap.py`, свой пункт в
   `FULL_REBUILD_STEPS` после `build_africa_1946.py`), читающим уже
   записанные выходные файлы обеих сторон.
10. **"Наложение/разрыв между СУЩЕСТВУЮЩИМИ фичами" не ловит буквальную
    ДЫРУ** (территория, не покрытая НИКЕМ) — если явно исключить юнит из
    одной страны (пример: Golan Heights, исключён из Палестины как
    исторически сирийская территория) и не проверить, что он ДЕЙСТВИТЕЛЬНО
    попал в выход другой страны, дыра не всплывёт ни в одной попарной
    проверке overlap/distance (там просто не с чем сравнивать). Проверяй
    `Point(...).within(...)`/`contains()` для координат каждой явно
    исключённой территории против ФИНАЛЬНОГО `world_1946.geojson`.
11. **Разрывы между полигонами закрывать ТОЛЬКО gap-first методом
    (`build/geometry_cleanup.py::absorb_slivers`), НЕ буфером.**
    Buffer-based дозаполнение (наращивание полигонов навстречу друг другу)
    испробовано и заменено 2026-07-19-e — у него врождённые дефекты, каждый
    из которых был реальным багом этой ветки: форма шва зависит от
    join_style ("пипка" на Газе), пороговый радиус подбирается руками под
    конкретный стык, сходимость требует нескольких проходов без гарантии,
    и буфер раздувает полигон ВО ВСЕ стороны — попутно наезжая на третьих
    соседей и моря, что требовало каскада пост-клипов (Иордания наехала на
    Ирак/Сирию, Беэр-Шева на Гулф-оф-Акаба). Gap-first свободен от всего
    этого по построению: polygonize всех границ -> ячейки, не покрытые ни
    землёй, ни водой = настоящая пустота -> каждая отдаётся фиче с самой
    длинной общей границей (длинные ленты предварительно режутся, чтобы
    сегменты ушли своим ближайшим регионам). Слайвер ни с кем не
    пересекается, поэтому новые наложения невозможны; шов идёт ровно по
    существующей границе соседа. Один проход сходится; второй запускается
    как проверка инварианта (должен дать 0).
12. **Клип наложений всё равно нужен ДО absorb_slivers** — gap-first чинит
    только разрывы, наложение двух источников (Палестина заходит на
    Иорданию) убирается односторонним `difference()` по авторитетному
    соседу (`clip_palestine_to_neighbors`), И только потом поглощаются
    слайверы. Порядок: клипы наложений -> клип водоёмов -> absorb.
13. **У absorb_slivers есть защиты, которые надо знать при расширении на
    новые регионы**: (а) компактный блоб крупнее MAX_COMPACT_AREA не
    поглощается — это может быть настоящее озеро-дыра, отсутствующее в
    lakes_1946.geojson; порог компактности ненадёжен (изрезанная береговая
    линия даёт ленточный Polsby-Popper) — известные озёра заносить в
    PROTECTED_HOLE_POINTS по точке (лучше — реальной фичей в
    lakes_1946.geojson, см. `extract_kinneret.py`); (б) прибрежные полосы
    (1 земля + вода) ТОЖЕ поглощаются в единственную касающуюся сушу, не
    только чистые "пинхолы" — раньше пропускались в предположении, что
    маскируются фоном карты, предположение было НЕВЕРНО (2026-07-19-g: фон
    темнее моря, разрыв виден); единственный предохранитель —
    MAX_COMPACT_AREA/ribbon-исключение; (в) контекст (авторитетные соседи,
    которых нельзя двигать) и вода режутся по clip_box для скорости —
    ячейки, касающиеся рамки, отбрасываются как артефакт обрезки.
14. **При добавлении новой фичи из чужого источника — клип по СЫРЫМ юнитам
    ВСЕХ соседей, не одного.** Голан (geoBoundaries) обрезали по сырой
    Иордании, но не по Ливану — вылезло наложение South Lebanon/Rif Dimashq
    в районе Шебаа/Хермон (2026-07-19-e), которое absorb не чинит (это
    наложение, не разрыв) и попарная проверка "только чиненных пар" не
    поймала. Проверяй наложения против ВСЕХ территориально смежных стран.
15. **Перед выводом "территории X нет в источнике" — искать НЕ только по
    `iso_a2` целевой страны.** Вся история с Киренией (2026-07-19-g) была
    построена на неверной посылке: проверка шла только по `iso_a2=="CY"`
    (5 округов), заключение "Кирении нет вовсе" — а полный `Northern
    Cyprus` лежал в том же `game_map.json` под `iso_a2=="-1"` (тот же тег
    не-страны, что у Кашмира/Спратли), стыкуясь с существующими округами
    БЕЗ разрыва (2026-07-19-i). Обязателен протокол скилла
    `find-existing-solutions` (мультиключевой поиск, включая `iso_a2=="-1"`)
    до любого вывода об отсутствии и до внешнего источника. Если после
    поиска разрыв всё же реальный — минимальный мост
    `LineString([p1, p2]).buffer(gap/2 + 0.002, cap_style=2, join_style=2)`
    (плоские торцы, без "пипок"), НЕ `convex_hull` (добавляет на порядок
    больше лишней площади). Но проверь, не "разрыв" ли это на самом деле
    третья фича, которую ты просто не загрузил — `fix_cyprus_famagusta_gap`
    (2026-07-19-g) построил мост Famagusta↔Larnaca через 100+ км² самой
    настоящей территории Northern Cyprus (нашлось при добавлении последней,
    2026-07-19-i) — мост убран, не разрыв.
16. **Git LFS на raw GitHub URL молча отдаёт pointer-заглушку, не файл.**
    `raw.githubusercontent.com/<repo>/<commit>/<path>` для LFS-файла — это
    ~130 байт текста `version https://git-lfs.github.com/...`, без ошибки.
    Нужен `media.githubusercontent.com/media/<repo>/<commit>/<path>`.
17. **Оверлап между двумя независимыми источниками для соседних/
    накладывающихся сущностей — реальный дефект, не фон.** UNDOF (Natural
    Earth, современный анахронизм) и Golan (geoBoundaries ISR, специально
    построенная историческая граница) независимо оцифровали одну и ту же
    территорию — 42.6 км², ~16% площади UNDOF (2026-07-19-i). Специально
    построенный исторический полигон авторитетен; сырой/общий источник
    обрезается по нему перед склейкой.
18. **Живая проверка `/game/start` может отвечать зомби-процессом.**
    В Git Bash на Windows `ps aux` показывает wrapper-PID, не настоящий
    Windows PID процесса — `kill`/`taskkill` по нему не трогает реальный
    node-процесс, слушающий порт. Перед тем как доверять ответу, сверь
    `netstat -ano | grep :3000` (или свой порт) с PID только что
    запущенного сервера (2026-07-19-i: старый процесс с начала сессии
    отдавал заведомо устаревшие данные, выглядело как настоящий баг).
19. **`A.difference(B)` между разными источниками может оставить несколько
    мелких оторванных кусков A на границе B, не одну чистую линию.**
    HaZafon минус Golan (2026-07-19-j) оставил 4 обрезка (22.8 км²),
    отрендерившихся отдельными подписями поверх Golan. После любого такого
    difference/union между фичами из разных источников проверяй
    `geom_type == "MultiPolygon"` — если да, оставляй только наибольший
    кусок, обрезки отдавай тому соседу, кого они реально касаются
    (`distance == 0`). За сессию сработало дважды (HaZafon→Golan, потом
    PSE-Иерусалим→Bethlehem) — проверяй каждый раз, не только один.
20. **`remap_region_ids.py --apply` НЕЛЬЗЯ запускать дважды с одним и тем
    же `--old`-снимком после промежуточной правки.** Инструмент сравнивает
    снимок с ТЕКУЩИМ world, не текущее состояние позиционных файлов со
    снимком — если файлы уже частично сдвинуты первым прогоном, второй
    прогон с тем же снимком не узнает уже-сдвинутые id и тихо дропнет их
    как "не резолвлено" (2026-07-19-j: потеряно ~20 корректных записей).
    Копи все геометрические/код-правки и прогоняй remap ровно один раз, в
    самом конце, после финальной пересборки — не после каждого шага.
21. **Новая суша рядом с существующей водой не вычитается из воды сама.**
    `out/seas_1946.geojson`/`lakes_1946.geojson` — статичные вручную
    поддерживаемые входы, ни один шаг пайплайна их не пересчитывает.
    Northern Cyprus/Dhekelia (2026-07-19-j) целиком лежали внутри полигона
    Средиземного моря, пока это не обрезали явно (2026-07-19-k) — тот же
    приём, что `clip_against_dead_sea`, только в обратную сторону (вода
    режется по новой суше). Делай это в ТОМ ЖЕ шаге, что добавляет сушу, не
    отдельным напоминанием на потом.
22. **НИКОГДА не запускай один `build_*.py`-шаг "просто проверить, работает
    ли" если его выход — gitignored `out/*.geojson`.** Эти файлы копят
    состояние ЦЕЛОЙ цепочки шагов (`build_namerica_1946.py` →
    `build_us_states_split_1946.py` → `fill_us_border_gaps.py`, 51 штат →
    112 county-кластера) плюс минимум один невоспроизводимый ни одним
    скриптом enrichment (Зона Панамского канала — тот же класс потери, что
    немецкие оккупационные зоны и клип Ладоги/Байкала в записи -l, все три
    уничтожены собственным сегодняшним прогоном build-скрипта). Одиночный
    прогон `build_namerica_1946.py` "проверить pyshp" (2026-07-19-k) стёр 62
    региона (268→206) без возможности восстановить их пересборкой — только
    из последнего чистого git-коммита. Если нужно проверить исполняемость
    скрипта — смотри только код возврата/трейсбек, не давай дописать выход,
    либо сразу сверь количество фич с `git show <коммит>:client/public/
    world_1946.geojson` и восстанови оттуда при уменьшении.
23. **"Это было до этой сессии" проверяется ПОСЛЕДНИМ коммитом перед
    сессией, не самым старым коммитом в истории.** Запись -h сравнила
    состояние Германии с САМЫМ ПЕРВЫМ импортом (`146933a`) и заключила
    "корень не в этой сессии" — неверно: `git show <последний коммит перед
    сессией>:client/public/world_1946.geojson` показал все 7 немецких зон
    на месте на старте сессии, схлопнула их МОЯ СОБСТВЕННАЯ более ранняя
    правка `build_europe_1946.py` сегодня (см. запись -l). Сравнение с
    самым старым коммитом доказывает только "это когда-то существовало",
    не "сегодняшние правки этого не трогали".
24. **Не списывай остаточные numeric-diagnostic пересечения на "фоновый
    шум" без проверки реальной площади.** Запись -k назвала 18 оставшихся
    пересечений "тем же фоновым шумом, что и всегда, включая Байкал/Ладогу"
    — не измерив их. На деле это было 20013.8/8572.6/11536.4/9093.0 км²
    (озёра не вырезаны из суши после того же build-прогона, что уничтожил
    немецкие зоны). Печатай `.area` каждого пересечения перед тем, как
    называть его шумом — прибрежный шум — доли км², пропущенный клип —
    тысячи.
25. **Отсутствующий клип воды может быть багом, который НИКОГДА не был
    починен на этом континенте, а не только регрессией этой сессии.**
    Виктория/Танганьика лежали поверх Tabora/Uganda/Katanga/Maniema/Rift
    Valley/Muchinga/Burundi с самого добавления этих озёр в
    `lakes_1946.geojson` — `build_africa_1946.py` вообще никогда не имел
    шага клипа (в отличие от Европы, где Мёртвое море/Кинерет уже
    вырезались). Не считай каждый пропущенный клип "сломала эта сессия" —
    проверь, был ли шаг в build-скрипте континента вообще когда-либо.
26. **Докстринг и код под ним могут молча расходиться — сверяй с сырыми
    данными источника, не с любым из двух.** Докстринг `build_africa_
    1946.py` утверждал "Уганда - ровно 4 британские провинции", а
    `game_map.json` действительно содержит эти 4 значения в поле `region`
    у всех 112 районов — но код держал Уганду в `SINGLE_COUNTRY` (1 сплошной
    регион), полностью игнорируя это поле (запись -m). Если комментарий
    описывает механизм, который код не реализует — проверяй сырые поля
    источника напрямую, не доверяй ни комментарию, ни своему предположению.
27. **`remap_region_ids.py` сравнивает `--old` с `client/public/
    world_1946.geojson`, НЕ с только что пересобранным `scripts/map/out/
    world_1946.geojson`.** Если `import_to_game.py` ещё не запускался после
    пересборки, `client/public` устарел, и remap-скрипт отрапортует "0
    реальных сдвигов" — не потому что ничего не сдвинулось, а потому что
    сравнивает старый файл сам с собой. Порядок: build-цепочка ->
    `import_to_game.py` (обновляет `client/public`, предупредит о
    безвладельческих регионах по ещё старым позиционным файлам — это
    ожидаемо) -> remap (теперь видит реальный сдвиг) -> ручные добавления
    новых регионов -> `import_to_game.py` снова -> `generate_country_
    registry.py` -> `fill_region_economy_1946.py` -> валидаторы/тесты.
28. **У remap-скрипта нет флага ручной дизамбигуации; при блокировке
    `--apply` на дублирующихся именах — резолвь по точному совпадению
    площади/центроида и вызывай `apply_*_remap` напрямую.** Два источника
    могут законно иметь одинаковое имя (Алжир: 2× "Territoires du Sud",
    Малави: 2× "Chitipa" — оба уже задокументированы в коде как намеренно
    неслитые дубли), и ключ `(name, iso_a2)` инструмента не может их
    различить. Импортируй `remap_region_ids` как модуль, вызови
    `build_remap()`, добавь вручную проверенный `{old_id: new_id}` словарь
    для неоднозначных пар (проверка через `shapely` — идентичные `.area`
    и `.centroid` старого и нового кандидата это доказательство, не
    догадка), затем вызови все 4 функции `apply_*_remap` с объединённым
    словарём сам.
29. **Инверсия `absorb_slivers` (mutable=море, а не суша) требует ДВУХ
    проходов, не одного.** Предохранитель "не поглощай крупные компактные
    ячейки" защищает настоящие озёра-дыры, когда mutable — СУША; для
    mutable-МОРЯ он лишний и вредный (все настоящие водоёмы уже в
    `water_geoms`) — в архипелагах/фьордах оставляет видимые белые пятна
    (Аляска-Панхандл: +11603 км² первым проходом, ЕЩЁ +23947 км² вторым,
    без порога компактности). Пиши второй проход отдельной функцией, не
    трогай порог в общей.
30. **"Снимок один раз в начале" для соседних mutable-фич в одном скрипте
    устаревает, как только скрипт мутирует ЛЮБУЮ из них.** Починка 113
    морей по одному в цикле, где каждое читало ВСЕ ОСТАЛЬНЫЕ моря из
    списка, построенного ДО цикла — море Б, обработанное после уже
    выросшего моря А, видело СТАРУЮ форму А и могло независимо
    претендовать на ту же спорную ячейку. Итог: 433 пересечения море-море
    вместо ~6 фоновых (`merge_world_1946.py` diagnostic). Читай мутирующих
    соседей ЖИВЫМ (`shape(other_ft["geometry"])`), не из списка снятого до
    цикла.
31. **Даже с живым чтением независимые попарные проходы не гарантируют
    partition — нужен один финальный детерминированный проход.** После
    основного цикла: пройти список ещё раз, каждую фичу обрезать по
    объединению уже финализированных (порядок списка = приоритет) — дёшево
    и гарантирует 0 наложений внутри группы независимо от пути, которым
    основной цикл туда пришёл.
32. **`client/public/world_1946.geojson` и `scripts/map/out/world_
    1946.geojson` — РАЗНЫЕ схемы свойств.** `out/`-файл (прямо из
    `merge_world_1946.py`) хранит explicit `region_type: "land"/"sea"/
    "lake"`. `client/public/`-файл (после `import_to_game.py`) хранит
    `type: "region"`/`continent`, БЕЗ `region_type` вовсе. Фильтр по
    `region_type` на клиентском файле молча возвращает пустой список ->
    `STRtree` на пустом списке молча не находит кандидатов ни для одного
    запроса -> любой последующий `.difference()` — бесшумный no-op. Нужен
    `region_type` — читай `out/`-файл, даже если по смыслу тебе нужна та
    же самая суша, что есть и в клиентском.
33. **"Локальный" clip_box из `.bounds()` многочастной фичи ломается на
    антимеридиане.** Море, чьи части лежат по разные стороны линии перемены
    дат (Берингово/Чукотское, тихоокеанские "секторы"), даёт `.bounds() ==
    (-180, ..., 180, ...)` для ВСЕЙ MultiPolygon-фичи целиком — "локальный"
    clip_box (±0.5°) оказывается почти всем земным шаром по долготе, gap-
    fill подхватывает разрывы у случайных далёких побережий (Берингово море
    получило кусочки у Норвегии/Исландии/Гренландии/Финляндии). Считай
    clip_box ПО КАЖДОЙ ЧАСТИ (`geom.geoms`) отдельно, не по bounds() всей
    фичи. Легитимно одночастная глобальная фича (Southern Ocean, кольцо
    вокруг Антарктиды) законно получит большой bbox — это не баг.
34. **Чистка уже испорченных фрагментов по СУММАРНОЙ площади кластера
    недостаточна — нужен якорь.** Кластеризация частей по близости и
    порог по суммарной площади можно обойти: мелкие оторванные фрагменты
    у ОДНОГО чужого побережья (напр. Гренландии) кластеризуются между
    собой (просто близко друг к другу) и всё равно проходят порог по
    сумме, даже если ни один не легитимен. Требуй, чтобы кластер содержал
    хотя бы одну часть с площадью на порядок больше любого случайного
    фрагмента (настоящий "ствол" моря) — рой мелких кусков без такого
    якоря отбрасывается целиком, независимо от суммы.
35. **`unary_union`/`buffer(0)`/`.intersection()` может выродить Polygon/
    MultiPolygon в `GeometryCollection` с нулевыми `LineString`/`Point`-
    артефактами.** Найдено на White Sea: `AttributeError: 'NoneType' object
    has no attribute 'length'` глубоко внутри `absorb_slivers` — геометрия
    оказалась `GeometryCollection` из 12 вырожденных `LineString` + 1
    настоящий `Polygon`. Пиши `to_polygonal()` (отбрасывает не-полигональные
    компоненты) и применяй его ПОСЛЕ каждого union И после каждого
    `.intersection(clip_box)` (касательные пересечения дают тот же
    вырожденный микс) — не только там, где баг впервые проявился.
36. **Добавление ранее отсутствовавшей суши всё равно требует и клипа
    моря, и проверки эвристик экономики.** Фарерские острова существовали
    целиком в `game_map.json`, но не входили ни в один список стран
    `build_europe_1946.py` — молча пропускались с момента написания файла.
    Починка "суши нет" всё равно требует: (1) `clip_seas_against_land` для
    моря, которое раньше рисовало это место открытой водой; (2) проверки
    generic-эвристики плотности (`economy_1946/density_tiers.py::
    generic_tier` трактует самый маленький по площади регион страны как
    "столица/город", x5.0 множитель) — маленькая ОСТРОВНАЯ территория,
    добавленная в список регионов страны, может словить эту эвристику и
    получить абсурдное население (Фареры получили 1.2М вместо реальных
    ~23-30 тыс., пока не добавлен явный per-country классификатор).
37. **Если пользователь просит перестать подгонять море под уже
    искривлённую кураторскую сушу — не клипай новый (авторитетный)
    источник под старый, оставь несовпадение как известный, отложенный
    пробел.** Ранняя версия финализации клипала свежеприклеенное море по
    ТЕКУЩЕЙ кураторской суше, чтобы убрать наложения — это напрямую
    отменяло смысл использования сырого источника там, где кураторская
    суша (Газа/Западный берег, немецкие зоны, US-county-кластеры)
    расходится с ним. Прямое указание пользователя: сырое побережье верно
    уже сейчас; несовпадающая суша обрезается ПОЗЖЕ отдельным проходом.
    Удаление "явно полезного" клипа, когда он противоречит заявленному
    намерению — иногда правильный фикс, а не регресс.
38. **Быстрый matplotlib-рендер для проверки может соврать о покрытии,
    если игнорирует internal holes полигона.** `ax.fill(*p.exterior.xy)`
    красит ВЕСЬ внешний контур сплошным, молча игнорируя `p.interiors` —
    море, корректно исключающее маленький остров (настоящая дыра вокруг
    него), всё равно рисуется одним сплошным пятном поверх этого острова,
    из-за чего верные данные выглядят как баг ("Фарерские острова
    пропали" — на деле были на месте, просто рендер-скрипт не мог
    показать дыру). Используй `matplotlib.path.Path`/`PathPatch` с
    вершинами/кодами И exterior, И каждого interior-кольца, когда проверка
    зависит от видимости дыр, а не голый `ax.fill` по одному exterior.
39. **Проверка "легитимен ли кластер" целиком позволяет чистому машинному
    шуму спрятаться за легитимным соседом.** После фикса антимеридиана
    diagnostic заявил "0 сиротских фрагментов" по всем 113 морям —
    неверно (пользователь опроверг: "Ionian Sea, Inner Seas off the West
    Coast of Scotland, Norwegian Sea"). Десятки частей площадью
    1e-18..1e-6 deg2 (чистая вычислительная погрешность от повторных
    union/intersection/buffer(0), не гео-фичи) выжили, потому что проверка
    "легитимность кластера" (порог по площади ИЛИ близость к суше)
    применялась к кластеру ЦЕЛИКОМ — шумовая частица рядом (< 2°) с
    настоящим маленьким островом наследовала легитимность через соседа.
    Фильтруй отдельные части ниже абсолютного эпсилон (порядка 1e-4 deg2)
    ДО кластеризации/якорной логики, не как часть той же проверки.
    Проверяй выбор порога гистограммой площадей всех оставшихся частей по
    всему датасету — настоящий эпсилон попадает в чистый разрыв между
    "явный шум" и "явная маленькая настоящая фича", а не в середину
    плавного распределения.
40. **Порог расстояния "легитимен ли далёкий фрагмент" не может отличить
    "далеко от своего моря, но законно" от "далеко и на самом деле
    чужое".** Урезание `CLUSTER_DIST_DEG` (2.0°→0.3°) починило Baltic
    Sea/Norway, но тем же движением стало отбрасывать законный залив
    Патраикос (Ionian Sea) как будто это тот же мусор. Замена абсолютного
    порога на относительный ("своё море vs ближайшее ДРУГОЕ") тоже не
    сработала: расстояние до СОСЕДНЕГО зарегистрированного моря почти
    всегда ≈0 (соседние моря по построению касаются друг друга на границе
    пролива — это норма, не признак чужеродности). Настоящая причина —
    ниже.
41. **Корень обеих находок (40) — не пост-обработка, а последовательная
    обработка 113 морей ПО ОЧЕРЕДИ с остальными как авторитетным
    контекстом.** Спорная непокрытая ячейка суши в буфере СРАЗУ НЕСКОЛЬКИХ
    морей (Патраикос — и Ionian, и Aegean; побережье Норвегии — и Baltic,
    и Skagerrak/North Sea; Bristol Channel/Thames Estuary/Moray Firth — и
    гигантский "Сев. Атлантика — Европейский сектор", и Celtic Sea/North
    Sea) доставалась ПЕРВОМУ по списку, не географически верному морю.
    Исправлено: `absorb_compact_gaps_multi()` — несколько mutable-морей
    ОДНОВРЕМЕННО, спорная ячейка отдаётся тому, с кем у неё САМАЯ ДЛИННАЯ
    общая граница (как уже делает `absorb_slivers` для суши), не первому
    по списку. Полный прогон (`fix_sea_coastline_gaps.py` без `--only`)
    теперь обрабатывает моря ГРУППАМИ по 7 континентальным тайлам, а не
    по одному. Подтверждено: суммарно добавлено к морям 36,560 km2 (было
    24,100 при старой per-sea схеме), после основного цикла чистка убрала
    только 1 крошечный фрагмент (-2.8 km2) вместо десятков морей, терявших
    сотни-тысячи km2 каждый.
42. **Разовый глобальный gap-diagnostic доверять можно, только если обе
    стороны сравнения используют ОДИН И ТОТ ЖЕ слой суши.** Смешивание
    кураторской (`client/public/world_1946.geojson`) земли с морем,
    приклеенным к СЫРОЙ `game_map.json`, превращает уже известное/принятое
    расхождение кураторской суши (детальные US county-сплиты — Chesapeake
    Bay; county-полигон, включающий целый пролив как "сушу" — Washington —
    San Juan, кураторские bounds до 49.71°с.ш. против сырых 48.99°с.ш.) в
    ложные "разрывы"/"наложения". Раздутый черновой diagnostic насчитал
    2772 разрыва/6555 km2; тот же diagnostic на СЫРОЙ земле (том же слое,
    что реально авторитетен для `fix_sea_coastline_gaps.py`) — 316/531.9
    km2, почти все в удалённых зонах (Канадская Арктика, атоллы
    Французской Полинезии — суши-острова там нет в датасете вообще, не
    пропуск поглощения). Перед тем как считать грубый счётчик разрывов/
    наложений мерой оставшейся работы — проверь, что обе стороны сверки
    читают ОДИН источник суши.
43. **"Неправильно приписана чужому морю" — вопрос КАСАНИЯ границы, не
    расстояния, и нужен отдельный проход ДО чистки шума.** После
    tile-batched правки (41) отдельные моря всё ещё могли нести часть,
    которая ВИЗУАЛЬНО принадлежит соседу (Арголидский залив у Ionian Sea
    касается Aegean Sea вплотную, но не касается собственного тела Ionian;
    слайверы Andaman Sea касаются Bay of Bengal, не своего же "ствола").
    Дистанционная чистка это не ловит — часть БЛИЗКО к чему-то (просто не к
    своему стволу), а относительный порог "своё vs ближайшее другое" уже не
    сработал по той же причине (40): соседние моря касаются друг друга по
    построению. Реальный фикс — отдельный проход ДО чистки: часть, НЕ
    касающаяся остальных частей своей же фичи, но касающаяся ДРУГОГО моря —
    переносится туда, с кем у неё самая длинная общая граница
    (`transfer_misassigned_parts()`). Прогон по всем 113 морям (не только
    2 заявленных) нашёл ~100 таких случаев — выборочно проверяй рендером
    образцы из разных частей света, не только два указанных пользователем,
    и подтверждай сходимость (повторный прогон — 0 новых переносов).
44. **`python -c` с импортом модуля и печатью значения доказывает, что
    ЧТЕНИЕ и строковый литерал верны — НЕ доказывает, что уже записанный на
    диск файл корректен, если предыдущий баговый прогон уже успел его
    испортить.** Починка отсутствующего `encoding="utf-8"` в `open()`
    (2026-07-22, `build_us_states_split_1946.py` читал `namerica_1946.
    geojson`/counties-источник без него — на машине с не-UTF-8 кодировкой
    по умолчанию это молча декодировало уже-корректные UTF-8-байты не той
    кодовой страницей, а затем ПРАВИЛЬНО записывало уже испорченные
    строки — итог выглядел валидным UTF-8, просто с неверным содержимым)
    необходима, но недостаточна, если скрипт уже успел прогнаться ДО
    фикса: файл на диске уже мойбайк, который валиден как UTF-8 навсегда.
    Повторный прогон исправленного чтения просто консистентно читает уже
    испорченный текст. Нужно ЗАНОВО пересобрать источник с нуля (более
    ранним шагом, не задетым багом), и только потом прогонять исправленный
    скрипт поверх чистых данных. Диагностика "мойбайк или нет":
    `bad_string.encode('cp1251').decode('utf-8')` — если восстанавливает
    читаемый текст, это чужая кодировка при чтении, не пропуск перевода.
45. **`build_*.py`-шаг, упавший на отсутствующем внешнем источнике ДО
    записи вывода — безопасно пропустить, если континент/страна не в
    scope задачи.** `build_china_1946_v2.py`/`build_brazil_1946.py` упали
    на отсутствующих shapefile/geoBoundaries файлах (2026-07-22) — оба до
    `json.dump`, их старые `out/*.geojson` (по mtime — недели до этой
    сессии) остались нетронуты, нижестоящие шаги (`build_asia_1946.py`/
    `build_southamerica_1946.py`) читали их как обычно — не регрессия.
    Контраст: `build_us_states_split_1946.py` упал на отсутствующем
    `geoBoundaries-USA-ADM2.geojson`, но БЕЗ него нельзя было выполнить
    саму задачу (нужен county-сплит США) — там правильным решением было
    СКАЧАТЬ файл (с разрешения пользователя, тот же закреплённый коммит
    geoBoundaries `9469f09`, что уже у ISR/PSE/CYP, через
    `media.githubusercontent.com`, не `raw.` — та же LFS-ловушка, что и
    у CYP-ADM1), а не пропускать шаг.
46. **Целые мелкие территории тихо выпадают при country-filtered сборке —
    ручной обход мира их не найдёт, нужна диагностика покрытия.** Akrotiri
    (никогда не было в карте), Мальдивы (21 фича), Нормандские о-ва, Мэн и
    др. — 11 территорий рендерятся фоном. Класс `MISSING_LAND` в
    `scripts/map/build/audit_map_geometry.py` (бывший отдельный
    `diagnose_missing_land.py`, схлопнут туда 2026-07-29) сравнивает
    сырой `game_map.json` с выходом по покрытию, 3 слоя: целые страны
    (iso в источнике, нет в выходе — с проверкой покрытия, отделяет реальную
    пропажу от влитого-в-соседа), спорные iso="-1" по имени, пофичевые
    пропажи с пометкой [LAKE] для приозёрных ложных срабатываний. Запускай
    после любой сборки, меняющей состав регионов. Сценарио-независим.
47. **Разбросанные острова с политической значимостью ≈0 — игровой
    "архипелаг", а не баг: сливать в 1 полигон, не детализировать.** Правило
    пользователя (2026-07-22): такие острова консолидируются в один регион
    (отдельно не обрабатываются — лишняя глубина), но должны СУЩЕСТВОВАТЬ
    для флота (базы, дальность). Механизм уже есть: `ARCHIPELAGO_BUFFER_
    DEG=3.0`+`reduce_clusters` в `build_oceania_1946.py` сливают острова
    одной страны в MultiPolygon БЕЗ заливки воды. Несоединённый регион
    материк+острова (Филиппины MIMAROPA: 24 части, 340 км) — тот же класс.
48. **Разрывы Китая — отдельный самонанесённый класс: `host.difference(city.
    buffer(δ))` оставляет δ-широкое кольцо вокруг каждого вырезанного
    спецмуниципалитета.** `build_china_1946_v2.py` режет город как
    `city = уезд ∩ host_провинция`, затем `host.difference(city.buffer(0.001))`
    — раздутие на ~95 м даёт ничейное кольцо вокруг 6 городов (Nanjing/
    Qingdao/Guangzhou/Hankou/Harbin/Dalian, 19-47 км² каждое). Прохода
    закрытия разрывов для Китая НЕТ (v2 отключил `fill_china_gaps`/`clip_
    china_to_neighbors`). Регенерация невозможна (Virtual Shanghai shapefile
    `china_hist/1947-49` не коммитили, нигде нет) — правки Китая идут
    ПОСТ-ОБРАБОТКОЙ готового `china_1946_historical.json` (47 фич), не
    перезапуском build_china_1946_v2.py.
49. **Баг "мелкий регион = столица" ловит ЛЮБУЮ страну, включая ту, у
    которой уже ЕСТЬ явный классификатор.** Добавление 11 островов
    (2026-07-22) повторило класс Фарер/Akrotiri дважды: у GBR/FRA явные
    `gbr_tier`/`fra_tier` уже были, но новые имена не совпали ни с одним
    паттерном → упали в безусловный `return 3` (Isle of Man: 136К вместо
    реальных ~52К); у AUS классификатора не было вовсе →
    `generic_tier`'s "нижние 20% площади страны" поймала все 4 новые
    крошечные территории на фоне материковых штатов (Christmas Island:
    43,791 вместо ~2-3К; необитаемый Heard Island: 21,973). Фикс для
    страны БЕЗ классификатора: не переписывай все её регионы явно (риск
    сломать уже верное поведение) — дай классификатору возвращать `None`
    для нераспознанного, `compute_region_tier` (`fill_region_economy_
    1946.py`) откатывается на `generic_tier`. Даже тир 1 не даёт настоящий
    ноль для больших необитаемых территорий (French Southern Territories,
    7244 км², 43,658 при тире 1 — `area^0.55` всё равно даёт заметный вес)
    — принятое, известное ограничение формулы (тот же паттерн уже был у
    Clipperton Island, 636 при 3.1 км²), не блокирует.
50. **Зомби-сервер на порту 3000 (2026-07-19-i) повторился в ЭТОЙ ЖЕ
    сессии два раунда спустя.** `nohup npm run dev &` после ребилда — первый
    ответ `/game/start` выглядел правдоподобно, но был неверным незаметно
    (`regions: 1388` вместо ожидаемых 1399, перепутанные владельцы) — старый
    сервер с прошлого раунда сессии остался слушать порт, новый процесс
    "Server started on port 3000" в логе НЕ доказывает, что это ТВОЙ новый
    процесс держит порт. `netstat -ano | grep :3000` перед КАЖДОЙ живой
    проверкой — единственное, что ловит это; `taskkill //F //PID <тот, что
    в netstat>` + новый запуск решает.
51. **`build_oceania_1946.py::SINGLE_ORPHANS` не завязан на `ALL_COUNTRIES`
    — фильтрует сырые фичи по iso_a2 заново на каждой итерации.** Страна с
    реальным iso (не "-1"), даже с несколькими разбросанными фичами,
    сливаемыми в один регион-"архипелаг" (Французские Южные территории — 4
    фичи: Кергелен/Крозе/Амстердам/Эпарсе), заходит в `SINGLE_ORPHANS` так
    же дёшево, как `SINGLE_REGION` в других континентах. Только iso_a2="-1"
    (общий тег Natural Earth для спорных/безISOшных фич) требует более
    тяжёлого `EXTRA_SINGLE_FEATURES` по `adm1_code` — добавлен в oceania
    впервые (2026-07-22, для Кокосовых/Рождества/Кораллового моря), тот же
    паттерн, что уже был в asia/namerica.
52. **Проверять закрытие China-колец по РОСТУ площади самого города — не
    работает.** `absorb_slivers` отдаёт спорную ячейку той из двух сторон
    (город/host), у кого длиннее общая граница — для тонкого кольца это
    может оказаться host, а не сам город (площадь города тогда не растёт
    вообще, хотя разрыв полностью закрыт — обе стороны Китай, видимой дыры
    больше нет). Верный чек — прямой остаток: `city.buffer(δ).difference(
    city).difference(вся_суша ∪ вся_вода)` должен быть пуст. По росту
    площади 2 из 6 спецгородов (Guangzhou/Harbin, 2026-07-22) ложно
    выглядели "не починенными".
53. **Часть MultiPolygon дальше по площади не отличить "реальный далёкий
    кусок" от "вырожденный слайвер-артефакт" — нужен зумированный рендер.**
    У Циндао (2026-07-22) 4 части: 2 крупные (930+131 км²) — настоящая
    география (полуостров + западный берег залива, разделены морем), 2
    мелкие (0.66+0.07 км²) — вырожденные иглы-треугольники у горловины
    залива, артефакт `city = уезд ∩ host` при вычислении спецмуниципалитета.
    Порог отбрасывания — точечный словарь `{имя_фичи: порог_км2}`, НЕ общая
    чистка континента/страны (у соседних Shandong/Liaoning десятки мелких
    частей — настоящие прибрежные острова).
54. **Переименование СУЩЕСТВУЮЩЕГО региона (тот же `region_id`, число
    регионов не меняется) — отдельный триггер устаревания `names_ru.json`,
    не только сдвиг числа регионов (см. пункт 6).** `import_to_game.py`
    берёт `name_en`/`name_ru` из `names_ru.json` по `region_id`, падая на
    geojson-имя ТОЛЬКО если записи для этого id вообще нет. Чистое
    переименование в `build_<continent>_1946.py` (Тайвань, 2026-07-22) не
    трогает число регионов → `remap_region_ids.py` не нужен и не ловит эту
    правку → старая запись в `names_ru.json` остаётся и выигрывает у
    свежего geojson-имени. Симптом: `client/public/world_1946.geojson` уже
    верный, а `server/data/scenarios/1946/names.en.json`/`names.ru.json`
    (и живая игра) — ещё нет. После ЛЮБОГО переименования проверяй оба
    файла, не только geojson.
55. **Хардкоженный `region_id` в тесте может протухнуть от ЧУЖОГО remap'а
    в несвязанной части карты и молча пролежать сломанным до следующего
    прогона тестов.** `test_country_entities_1946.py` держал 2 литеральных
    `"OCE-0008"` (Norfolk Island); remap Океании из пакета A (2026-07-22)
    сдвинул реальный id на `OCE-0011` в конфигах, но не в тесте — заявленный
    "12/12" в ExecPlan пакета A был неверен уже на момент коммита. Тот же
    класс, что уже описан в пункте 6 про тест Gulf/Tonga (переписан на
    сравнение по `(name, iso_a2)`) — при следующем протухании ЭТИХ ЖЕ двух
    ассертов делать такую же переписку, не третий раз патчить строку.
56. **Дыра-остров в полигоне моря/озера без покрывающей суши — класс,
    который класс `MISSING_LAND` НЕ ловит вообще** (тот сверяется с
    ADM1-записями `game_map.json`; часть дыр — острова, у которых ADM1-
    записи никогда не было). Новый `diagnose_sea_holes.py` (2026-07-23):
    `hole.difference(вся_суша_рядом)` — не representative-точка (даёт
    ложное "покрыто", когда сырая фича лишь частично перекрывает дыру).
    115 дыр по всему миру, 95 совпали с сырым источником (>30% площади —
    остров реален, просто не дошёл до выхода), 20 — без источника
    (синтез суши из формы дыры, владелец только по решению пользователя).
57. **Восстанавливая потерянный остров — сливай геометрию ДЫРЫ, не сырой
    фичи.** Дыра бесшовно стыкуется с морем по построению (это и есть
    вырезка в море); сырая ADM1-фича могла быть оцифрована по другому
    источнику береговой линии и не совпасть день-в-день. Сырая фича нужна
    только чтобы определить страну/штат (для выбора, в какую СУЩЕСТВУЮЩУЮ
    выходную фичу сливать), не для формы.
58. **Слияние "в ближайшую фичу той же страны" по чистому расстоянию может
    молча наложиться на ДРУГУЮ фичу той же страны**, если geometric-
    кластеры сложные/далеко тянутся. У Вашингтона (2026-07-23) дыра у
    Сан-Хуанских островов слилась с "Washington — Adams" (ближе по
    расстоянию), хотя "Washington — San Juan" — очевидный тематический
    владелец; оба — geometric-мульти-county-кластеры, дало реальное
    наложение 0.002°. Поймано штатной проверкой `merge_world_1946.py`
    (98→99) — не геометрической спецификой, обычным regression-счётчиком.
    Чинится `A.difference(B)` в пользу более тематически подходящего
    кластера. Вывод: пересчитывать overlap-diagnostic после ЛЮБОЙ заливки
    дыр, не только после правок, которые ВЫГЛЯДЯТ как трогающие границу.
59. **Рендер уже исправленного места может случайно вскрыть НЕСВЯЗАННЫЙ,
    более крупный баг** — просто потому что до этого его не рендерили с
    водой сразу. Проверка Isle Royale (оз. Верхнее) после заливки дыры
    показала ~448 км² настоящих разрывов суша↔озеро по южному берегу — тот
    же класс есть у всех 4 Великих озёр (~2086 км² суммарно). Вероятная
    причина — побочный эффект более раннего `refresh_lakes_from_ne10m.py`
    (пакет A, 2026-07-22): новый детальный контур `ne_10m_lakes` не
    сверялся с существующей сушей проходом "сшить сушу с водой" (какой уже
    есть у всех морей). НЕ починено в этом же заходе — слишком объёмная
    отдельная задача, передана пользователю как находка.
60. **Постпроцессинг, патчащий `out/<continent>_1946.geojson` напрямую,
    безопасен ТОЛЬКО если этот файл больше НИЧЕМ не перезаписывается —
    проверяй `.gitignore` перед тем как копировать паттерн `fix_china_
    geometry.py`.** Тот скрипт безопасен вне `FULL_REBUILD_STEPS`, потому
    что патчит `china_1946_historical.json` — git-ОТСЛЕЖИВАЕМЫЙ файл,
    который больше НИКТО не пересобирает (регенерация Китая невозможна).
    Первая версия `fill_sea_holes.py` скопировала форму "отдельный
    постпроцессинг-скрипт", но патчила `out/namerica_1946.geojson`/`out/
    europe_1946.geojson` — оба под `*.geojson` в `.gitignore` и
    ПОЛНОСТЬЮ перезаписываются `build_namerica_1946.py`/`build_europe_
    1946.py` при любом запуске по ЛЮБОЙ несвязанной причине. Фикс тихо
    исчез бы при следующей пересборке континента. Поймано ДО коммита по
    `git status` — модифицированные continent-геоджейсоны просто не
    появились в diff. Починка: читать/писать continent-файлы напрямую (не
    смёрженный `client/public/world_1946.geojson`) + добавить в
    `FULL_REBUILD_STEPS` (`make_1946.py`), сразу после `fix_sea_
    coastline_gaps.py`, до `merge_world_1946.py`.
61. **Диагностика без приёмки не работает: четыре независимых
    `diagnose_*` не мешали дефектам расти годами.** До 2026-07-29
    проверки жили россыпью — свой запуск, свой формат, своя метрика у
    каждой, и ни одна не падала. В итоге пользователь находил глазами то,
    чего не показывал ни один скрипт: `diagnose_scattered_regions.py`
    явно пропускал `SEA-`/`LAK-` (моря не проверялись вообще) и искал
    только "часть далеко от своего тела", но не "часть приросла к ЧУЖОЙ
    стране"; никто не сверял площадь региона с сырым `game_map.json` —
    поэтому `Washington — San Juan` жил с 9706 км² при 412 км² настоящей
    суши (96% — акватория пролива и 2475 км² канадской земли), а
    `clip_sea_by_land.py` покорно резал МОРЕ по этой ложной суше, разбив
    `North Pacific — American Sector` с 1 куска до 17 (1 в `628a3bd` → 15
    в `main` → 17 к 2026-07-29 — дефект молча РОС с каждой серией фиксов
    берега). Замена: `build/audit_map_geometry.py` — один вход, двенадцать
    классов (`INFLATED`, `ORPHAN_FOREIGN`, `ORPHAN_UNSOURCED`,
    `WATER_SHATTERED`, `SPIKE`, `SCATTERED`, `MISSING_LAND`,
    `COASTLINE_GAP`, `SEA_HOLE`, `UNION_HOLE`, `COVERAGE_INVALID`,
    `FALSE_COAST`), общий отчёт и — главное — baseline
    (`config/geometry_audit_baseline.json`): находка, которой в нём нет,
    даёт ненулевой exit. Стоит в `STANDARD_STEPS` перед
    `import_to_game.py`, то есть валит пайплайн сразу, а не через три
    сессии по чужому скриншоту. `diagnose_missing_land.py` и
    `diagnose_scattered_regions.py` удалены (логика внутри аудита);
    `diagnose_coastline_gaps.py` и `diagnose_sea_holes.py` ОСТАВЛЕНЫ —
    их `_gap_cells`/`find_holes` импортируют `fix_*`-скрипты как
    библиотеки, а рендер `--render` нужен для глазной проверки.
    Обновлять baseline — только `--update-baseline` вместе с датированной
    записью в `docs/DECISIONS.md`, никогда молча.
62. **Критерий "часть приросла к чужой стране" должен опираться на СЫРОЙ
    грунт, и сравнивать надо с грунтом ГЛАВНОЙ части, а не с номинальным
    `iso_a2`.** Первая версия чека (`оторвана от тела` + `касается чужой
    фичи`) дала 74 находки, почти все ложные: остров Ванкувер законно
    оторван от материковой British Columbia, Огненная Земля поделена
    Чили/Аргентиной, Бруней физически разрезан Сараваком, Тимор — между
    Индонезией и португальской частью. Касание соседа на общем острове —
    норма. Вторая версия (сравнение с номинальным `iso_a2`) дала 62: у
    Ньюфаундленда номинальный код `NF` (доминион 1946), а сырьё
    современное и знает там только `CA`, поэтому каждый его остров
    выглядел чужим — та же ловушка, что и с историческими SUN/YUG/CSK.
    Рабочий критерий: доминирующий сырой `iso_a2` под частью ≠
    доминирующий сырой `iso_a2` под главной частью того же региона.
    Историческая переразметка сдвигает код у ВСЕГО региона сразу, поэтому
    грунт тела — устойчивый эталон. 25 находок, все настоящие. Негативный
    контроль обязателен: чек прогнан на историческом снимке (`--world` на
    `git show 6a4bfd5:client/public/world_1946.geojson`), где дефект ещё
    был, — нашёл ровно 3 куска San Juan и ноль ложных.

## Обычный путь

Из корня репозитория:

```powershell
python scripts/map/make_1946.py
```

Геометрию команда берёт из **мастера**
`scripts/map/master/world_1946.master.geojson` (2026-07-30) — не из
`out/*.geojson`. Мастер отслеживается git и является единственным стартовым
источником: вся геометрия согласована один раз (`build/weld_map_gaps.py`
закрыл 718 внутренних дыр, `build/rebuild_shared_edges.py` сделал границы
соседей общими рёбрами), поэтому обычная сборка больше не прогоняет 31 шаг
согласования несовпадающих источников. Проверено: пайплайн проходит целиком
при полностью удалённых `out/*.geojson`.

Остальное берётся как раньше: `out/ownership_1946.json`,
`out/names_ru.json`, `out/neighbor_graph.json` (все отслеживаются git, кроме
neighbor_graph, который регенерируется), каталог ресурсов из server. Команда
изменяет generated scenario files — сначала проверь `git status` и ожидаемый
scope.

Пересобирать геометрию нужно ТОЛЬКО при правке самих границ:

```powershell
python scripts/map/make_1946.py --rebuild-master
```

Это прогоняет все 31 шаг из `MASTER_REBUILD_STEPS`, затем сшивку и
`build/freeze_master_map.py`, который проверяет «0 внутренних дыр +
coverage_is_valid» и лишь потом перезаписывает мастер. Нужны внешние
источники из `scripts/map/sources/` — в linked worktree их нет (каталог
gitignored), путь к основному checkout задаётся через `PAXMAP_SOURCES`, см.
`build/paths.py`.

Минимальные non-generating checks:

```powershell
python scripts/map/validate_region_economy_1946.py
python scripts/map/test_validate_region_economy_1946.py
```

## Входы и выходы

Вход `scripts/map/out/`:

- `world_1946.geojson`;
- `ownership_1946.json`;
- `neighbor_graph.json`;
- `names_ru.json`;
- `countries_1946.json`.

Основной output:

- `client/public/world_1946.geojson`;
- `server/data/scenarios/1946/regions.core.json`;
- `server/data/scenarios/1946/regions.state.json`;
- `server/data/scenarios/1946/names.en.json`;
- `server/data/scenarios/1946/names.ru.json`;
- `server/data/scenarios/1946/countries.json`.

`import_to_game.py` создаёт структурный skeleton; экономические поля затем
заполняет `fill_region_economy_1946.py`. Устаревший монолитный `regions.json`
не используется.

## Исторические страновые сущности

`config/country_entities_1946.json` — исполняемый курированный слой снимка
`1946-01-01`. По континентам он фиксирует:

- `preserve` — колония/зависимая территория остаётся отдельной Country и
  связывается с управляющей державой через существующий `subject_of`;
- `ownerOverrides` — современный код территории переводится реальному
  историческому владельцу/администрации 1946;
- `regionOwnerOverrides` — точечная граница исправляется, когда один source
  owner смешивает несколько администраций или вообще отсутствует;
- `subjectOverrides` — историческая зависимость задаётся для каталожной
  страны, ошибочно помеченной суверенной;
- официальные или архивные sources и confidence для каждого решения.

`country_merge.json` — generated-результат этой конфигурации, не ручной
источник истины. Targeted-инварианты проверяет `test_country_entities_1946.py`,
который входит в стандартный pipeline.

На текущем срезе завершены все шесть континентов (Южная Америка, Северная
Америка/Карибы, Европа, Африка, Азия, Океания); легаси колониальные блоки
(`QCG/QCF/QCP/QCN/QCU/QCZ/QCS`) больше нигде не используются.
В Карибах современные островные ISO-коды объединяются по администрациям 1946
(Leeward, Windward, Jamaica dependencies, Curaçao and Dependencies), а не
механически превращаются в отдельные страны. В Европе Cyprus, Gibraltar и
Malta выведены из мирового британского блока как отдельные зависимости.
Для территорий, которые upstream ошибочно пишет прямо на метрополию,
`regionOwnerOverrides` в том же континентальном разделе задаёт точечный owner
(например, Martinique/Guadeloupe и острова Curaçao and Dependencies).
Африка сохраняет AOF/AEF как реальные федерации 1946, объединяет современные
RWA/BDI в Ruanda-Urundi и исправляет анахроничные BIOT и отдельные Comoros на
их администрации Mauritius/Madagascar.
Азия отделяет British Raj, Burma, Ceylon, Hong Kong, Malaya/Singapore и
британские территории Borneo; восстанавливает Tibet, Sikkim, Jammu and
Kashmir, Portuguese/French India и семь Trucial Sheikhdoms. Переходные
послевоенные зоны Republic of Indonesia/Netherlands Indies и северного
DRV/южной French administration выражены только там, где текущие ADM1-полигоны
дают воспроизводимую границу; невидимые городские bridgeheads не дорисовываются.
Океания задокументирует Papua and New Guinea (мандат AUS), Solomon Islands
(протекторат GBR), Tonga (собственная монархия под британским протекторатом),
New Hebrides (англо-французский кондоминиум) и Western Samoa (мандат NZL,
отдельно от American Samoa) — все пять уже существовали в исходном каталоге
корректно, просто не были заявлены в курируемой конфигурации.

## Полная пересборка геометрии

Интерфейс существует:

```powershell
python scripts/map/make_1946.py --full-rebuild
```

Но на 2026-07-15 full rebuild **не воспроизводим из чистого clone** и не был
выполнен в global audit:

- `scripts/map/sources/` не хранится в repository;
- точная provenance/URL-таблица inputs отсутствует;
- нет Python dependency manifest/версий;
- требуются Shapely, pyproj и PyShp (`shapefile`), а PyShp отсутствовал в
  проверенной локальной среде.

Не заявляй full rebuild успешным по одному validator pass. Перед следующим
геометрическим проходом нужно добавить provenance и dependency manifest, затем
выполнить pipeline end-to-end и проверить geometry/output diff.

Ожидаемые external inputs (имена, не достаточная provenance):

- `geoBoundaries-BRA-ADM2.geojson`;
- `geoBoundaries-CHN-ADM2.geojson`;
- `china_hist/1947-49/1947_1949`;
- Antarctica/seas/lakes overlays.
- `palestine_hist/geoBoundaries-ISR-ADM2.geojson` (15 израильских округов,
  CC0 1.0 Public Domain) и `palestine_hist/geoBoundaries-PSE-ADM2.geojson`
  (16 палестинских губернаторств, CC BY 4.0) — точная provenance ЕСТЬ
  (2026-07-19, единственные 2 входа в этом списке с полным URL): скачаны с
  `https://github.com/wmgeolab/geoBoundaries/raw/9469f09/releaseData/gbOpen/ISR/ADM2/geoBoundaries-ISR-ADM2.geojson`
  и `.../gbOpen/PSE/ADM2/geoBoundaries-PSE-ADM2.geojson` соответственно
  (geoBoundaries, коммит `9469f09`). Используются `build_palestine_1946.py`
  для реконструкции 1946 подрайонов Подмандатной Палестины — см. докстринг
  скрипта и `docs/HISTORICAL_ACCURACY.md`.
- `naturalearth/ne_10m_lakes.geojson` (Natural Earth 10m lakes, public
  domain, ~5 МБ) — скачан с
  `https://raw.githubusercontent.com/nvkelso/natural-earth-vector/master/geojson/ne_10m_lakes.geojson`
  (2026-07-19-f). Используется `build/extract_kinneret.py` для извлечения
  озера Кинерет ("Sea of Galilee") в `out/lakes_1946.geojson`, и
  `build/refresh_lakes_from_ne10m.py` (2026-07-22) для полных контуров
  Великих озёр (Superior/Michigan-Huron/Erie/Ontario, взамен урезанных по
  границе США-Канада версий) и добавления Lake Malawi. Мёртвое море/Арал/
  Каспий — из того же Natural Earth, добавлены раньше без сохранённого URL.
- `geoBoundaries-USA-ADM2.geojson` (3233 округа США, тот же коммит
  geoBoundaries `9469f09`, что и ISR/PSE/CYP ниже) — скачан 2026-07-22 с
  `https://media.githubusercontent.com/media/wmgeolab/geoBoundaries/9469f09/releaseData/gbOpen/USA/ADM2/geoBoundaries-USA-ADM2.geojson`
  (media.githubusercontent.com, не raw. — та же Git LFS ловушка, что и
  CYP-ADM1 ниже: raw отдаёт только pointer-заглушку). Используется
  `build/build_us_states_split_1946.py` для county-сплита 51 штата в 112
  регионов.
- `geoBoundaries-CHN-ADM2.geojson` (2391 округ Китая, тот же коммит и
  media.githubusercontent.com-URL, что и USA-ADM2 выше) — скачан
  2026-07-22 для возможной точечной доводки китайских разрывов. По факту
  НЕ понадобился: контекста сырых `game_map.json`-соседей хватило, чтобы
  `fix_china_geometry.py` закрыл все 6 колец спецмуниципалитетов без
  обращения к ADM2-геометрии Китая. Файл остаётся в `sources/` про запас
  (Циндао/Далянь — известные отложенные неточности площади, могут
  потребовать ADM2-детализацию в будущем), сейчас не используется НИ ОДНИМ
  скриптом.
- `cyprus_hist/geoBoundaries-CYP-ADM1.geojson` (6 округов Кипра, CC0,
  ~584 КБ) — скачан с
  `https://media.githubusercontent.com/media/wmgeolab/geoBoundaries/9469f09/releaseData/gbOpen/CYP/ADM1/geoBoundaries-CYP-ADM1.geojson`
  (тот же коммит geoBoundaries `9469f09`, что и Израиль/Палестина; **не**
  raw.githubusercontent.com — файл через Git LFS, raw отдаёт только
  pointer-заглушку, см. чек-лист выше). **SUPERSEDED (2026-07-19-i)**:
  диагноз "Kyrenia отсутствует во всех 5 CY-юнитах `game_map.json`"
  (2026-07-19-g), из-за которого понадобился этот внешний источник, был
  неверным — полный `Northern Cyprus` нашёлся в самом `game_map.json` под
  `iso_a2="-1"`. `build/extract_kyrenia.py` (потребитель этого файла)
  больше не вызывается пайплайном. Файл остаётся на диске/в записи как
  история находки, не как активный источник.

`out/lakes_1946.geojson` и `out/ownership_1946.json` — внешние, вручную
поддерживаемые входы (не перегенерируются пайплайном целиком, gitignored
как прочие `out/*.geojson`); их изменения попадают в игру через
уже-закоммиченные `client/public/world_1946.geojson` и `server/data/...`.

## QA геометрии

Любая правка геометрии/границ/числа регионов/озёр — под скилл
`map-geometry-qa` (`.claude/skills/map-geometry-qa/SKILL.md`): он
энфорсит чек-лист выше, требует gap-first `absorb_slivers` (не буферы) и
пофичевый рендер каждого стыка. История каждого правила — `docs/DECISIONS.md`
(записи 2026-07-19-a…i).

Environment overrides `PAXMAP_GAME_MAP`, `PAXMAP_SOURCES` и `PAXMAP_OUT`
описаны в `build/paths.py`.
